---
phase: 05-onboarding-polish
plan: 05
type: execute
wave: 5
depends_on: [05-04]
files_modified:
  - VitaminG/VitaminG/VitaminG/Views/TierPickerView.swift
autonomous: true
requirements: [UI-05, UI-06]
gap_closure: true
gap_source: 05-VERIFICATION.md

must_haves:
  truths:
    - "TierCardView displayName text uses Dynamic Type semantic font, not fixed .system(size: 14)"
    - "TierCardView description text uses Dynamic Type semantic font, not fixed .system(size: 12)"
    - "TierCardView unselected background uses semantic color that adapts in Dark Mode, not Color.white"
    - "TierCardView icon size 28 is documented as a layout-fixed exception (decorative SF Symbol in constrained grid cell)"
  artifacts:
    - path: "VitaminG/VitaminG/VitaminG/Views/TierPickerView.swift"
      provides: "TierCardView with Dynamic Type fonts and Dark Mode-compatible colors"
      contains: ".font(.subheadline.weight(.semibold)).fontDesign(.rounded)"
  key_links:
    - from: "TierPickerView.swift"
      to: "GoalTier enum"
      via: "tier.displayName, tier.description, tier.color, tier.icon"
      pattern: "tier\\.(displayName|description|color|icon)"
---

<objective>
Close two blocking verification gaps in TierCardView (private struct inside TierPickerView.swift):
1. Dynamic Type: Migrate hardcoded .system(size:) fonts on displayName and description text to semantic Dynamic Type fonts
2. Dark Mode: Replace hardcoded Color.white background with semantic color that adapts automatically

Purpose: TierPickerView was excluded from Plan 03's font/color migration scope. These are the only two remaining blockers preventing Phase 05 verification from passing (score 10/12 -> 12/12).

Output: Updated TierPickerView.swift with all gaps closed
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-onboarding-polish/05-VERIFICATION.md
@.planning/phases/05-onboarding-polish/05-03-SUMMARY.md

<interfaces>
<!-- TierCardView is a private struct — all changes are self-contained within TierPickerView.swift -->
<!-- No external interface changes. The file's public API (TierPickerView with @Binding var selectedTier: GoalTier) is unchanged. -->

Current TierCardView lines requiring change (from TierPickerView.swift):

Line 36: .font(.system(size: 28))                                          — icon (document as exception)
Line 41: .font(.system(size: 14, weight: .semibold, design: .rounded))     — displayName (MUST migrate)
Line 45: .font(.system(size: 12, weight: .regular, design: .rounded))      — description (MUST migrate)
Line 55: .fill(isSelected ? tier.color.opacity(0.12) : Color.white)        — background (MUST migrate)

Semantic font pattern established in Phase 05 Plan 03 (GoalDetailView, GoalListView, ProfileView, ProfileEditSheet):
- Text labels: .font(.subheadline.weight(.semibold)).fontDesign(.rounded)
- Caption text: .font(.caption).fontDesign(.rounded)
- D-09 exception comment format: // D-09 exception: layout-fixed [description]

Semantic color pattern established in Phase 05 Plan 03:
- Backgrounds: Color(.secondarySystemGroupedBackground) for card/cell backgrounds
- Already used throughout app for Dark Mode adaptation
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate TierCardView fonts to Dynamic Type and fix Dark Mode background</name>
  <read_first>
    - VitaminG/VitaminG/VitaminG/Views/TierPickerView.swift
  </read_first>
  <files>VitaminG/VitaminG/VitaminG/Views/TierPickerView.swift</files>
  <action>
In TierPickerView.swift, modify only the TierCardView private struct. Make exactly these changes:

1. LINE 36 — Icon font (document as exception, do NOT change the size):
   BEFORE: `.font(.system(size: 28))`
   AFTER:  `.font(.system(size: 28)) // D-09 exception: layout-fixed decorative SF Symbol in constrained 2x2 grid cell`

2. LINE 41 — displayName font (migrate to Dynamic Type):
   BEFORE: `.font(.system(size: 14, weight: .semibold, design: .rounded))`
   AFTER:  `.font(.subheadline.weight(.semibold)).fontDesign(.rounded)`

3. LINE 45 — description font (migrate to Dynamic Type):
   BEFORE: `.font(.system(size: 12, weight: .regular, design: .rounded))`
   AFTER:  `.font(.caption).fontDesign(.rounded)`

4. LINE 55 — Background fill (fix Dark Mode):
   BEFORE: `.fill(isSelected ? tier.color.opacity(0.12) : Color.white)`
   AFTER:  `.fill(isSelected ? tier.color.opacity(0.12) : Color(.secondarySystemGroupedBackground))`

Do NOT modify any other lines. The TierPickerView struct (lines 6-25) and all other TierCardView lines remain unchanged.
  </action>
  <verify>
    <automated>cd "/Users/kyleharrington/Desktop/AI/Vitamin G" && grep -n "\.system(size:" VitaminG/VitaminG/VitaminG/Views/TierPickerView.swift | grep -v "D-09 exception" | wc -l | tr -d ' ' | grep -q "^0$" && echo "PASS: No undocumented .system(size:) calls" || echo "FAIL: Undocumented .system(size:) calls remain"</automated>
  </verify>
  <acceptance_criteria>
    - TierPickerView.swift line with displayName text contains `.font(.subheadline.weight(.semibold)).fontDesign(.rounded)` (grep-verifiable)
    - TierPickerView.swift line with description text contains `.font(.caption).fontDesign(.rounded)` (grep-verifiable)
    - TierPickerView.swift does NOT contain `Color.white` anywhere (grep-verifiable)
    - TierPickerView.swift contains `Color(.secondarySystemGroupedBackground)` (grep-verifiable)
    - TierPickerView.swift does NOT contain `.system(size: 14` (grep-verifiable)
    - TierPickerView.swift does NOT contain `.system(size: 12` (grep-verifiable)
    - TierPickerView.swift contains exactly one `.system(size: 28)` with a `D-09 exception` comment on the same line (grep-verifiable)
    - The file still compiles: `xcodebuild build -project VitaminG/VitaminG/VitaminG.xcodeproj -scheme VitaminG -destination 'platform=iOS Simulator,name=iPhone 16' 2>&1 | tail -1` shows "BUILD SUCCEEDED"
  </acceptance_criteria>
  <done>
    TierCardView displayName uses .subheadline.weight(.semibold) with .fontDesign(.rounded).
    TierCardView description uses .caption with .fontDesign(.rounded).
    TierCardView icon has D-09 exception comment documenting why .system(size: 28) is retained.
    TierCardView unselected background uses Color(.secondarySystemGroupedBackground) instead of Color.white.
    No undocumented .system(size:) calls remain in TierPickerView.swift.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries introduced. This plan modifies only presentation-layer font and color values in a private view struct.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05-01 | Information Disclosure | TierCardView | accept | No data handling changes — font/color only. No new inputs or outputs. |
</threat_model>

<verification>
After task completion, run these checks:

```bash
cd "/Users/kyleharrington/Desktop/AI/Vitamin G"

# 1. No undocumented .system(size:) in TierPickerView
echo "=== Check 1: No undocumented fixed fonts ==="
grep -n "\.system(size:" VitaminG/VitaminG/VitaminG/Views/TierPickerView.swift | grep -v "D-09 exception"

# 2. Dynamic Type fonts present
echo "=== Check 2: Semantic fonts present ==="
grep -c "\.subheadline\.weight(\.semibold)" VitaminG/VitaminG/VitaminG/Views/TierPickerView.swift
grep -c "\.font(\.caption)" VitaminG/VitaminG/VitaminG/Views/TierPickerView.swift

# 3. No Color.white
echo "=== Check 3: No hardcoded Color.white ==="
grep -c "Color\.white" VitaminG/VitaminG/VitaminG/Views/TierPickerView.swift

# 4. Semantic background color present
echo "=== Check 4: Semantic background color ==="
grep -c "secondarySystemGroupedBackground" VitaminG/VitaminG/VitaminG/Views/TierPickerView.swift

# 5. D-09 exception documented
echo "=== Check 5: Icon exception documented ==="
grep -c "D-09 exception" VitaminG/VitaminG/VitaminG/Views/TierPickerView.swift
```

Expected: Check 1 produces no output (0 undocumented lines). Checks 2-5 each produce "1".
</verification>

<success_criteria>
- Both verification gaps from 05-VERIFICATION.md are closed
- Truth 9 (Dark Mode) passes: no Color.white in TierPickerView.swift
- Truth 10 (Dynamic Type) passes: no undocumented .system(size:) in TierPickerView.swift
- Phase 05 verification score moves from 10/12 to 12/12
</success_criteria>

<output>
After completion, create `.planning/phases/05-onboarding-polish/05-05-SUMMARY.md`
</output>
