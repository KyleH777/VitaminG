---
phase: 05-onboarding-polish
plan: 03
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - VitaminG/VitaminG/VitaminG/Views/GoalDetailView.swift
  - VitaminG/VitaminG/VitaminG/Views/GoalListView.swift
  - VitaminG/VitaminG/VitaminG/Views/ProfileView.swift
  - VitaminG/VitaminG/VitaminG/Views/ProfileEditSheet.swift
  - VitaminG/VitaminG/VitaminG/Views/StatsView.swift
autonomous: true
requirements:
  - UI-04
  - UI-05
  - UI-06

must_haves:
  truths:
    - "App renders correctly in both Light and Dark Mode with no hardcoded light-only backgrounds"
    - "All semantic text uses Dynamic Type fonts with .fontDesign(.rounded) — no .system(size: N) except display-proportional exceptions"
    - "StatsView streak numerals (size: 28, size: 48) are the only remaining fixed sizes — documented exceptions"
    - "GoalRowView completion toggle animations respect accessibilityReduceMotion (already present — verified unchanged)"
  artifacts:
    - path: "VitaminG/VitaminG/VitaminG/Views/GoalDetailView.swift"
      provides: "Dynamic Type fonts + semantic background color"
      contains: ".fontDesign(.rounded)"
    - path: "VitaminG/VitaminG/VitaminG/Views/ProfileView.swift"
      provides: "Dynamic Type fonts"
      contains: ".fontDesign(.rounded)"
    - path: "VitaminG/VitaminG/VitaminG/Views/GoalListView.swift"
      provides: "Semantic background + Dynamic Type for GoalRowView"
      contains: "Color(.systemGroupedBackground)"
  key_links:
    - from: "GoalDetailView.swift"
      to: "system colors"
      via: "Color(.systemGroupedBackground) background"
      pattern: "Color\\(\\.systemGroupedBackground\\)"
    - from: "GoalListView.swift"
      to: "EmptyStateView background"
      via: "semantic color replacement"
      pattern: "Color\\(\\.systemGroupedBackground\\)"
---

<objective>
Migrate all hardcoded colors and fixed font sizes to semantic system values across five view files so the app renders correctly in Light/Dark Mode and scales with Dynamic Type.

Purpose: D-08 (semantic colors), D-09 (Dynamic Type), and D-10 (reduced motion) deliver UI-05 and UI-06 accessibility requirements. This is a mechanical migration with exact replacements specified per the RESEARCH.md audit.

Output: All five view files use semantic Dynamic Type fonts with `.fontDesign(.rounded)` and semantic system colors. No `.system(size: N)` remains except documented exceptions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-onboarding-polish/05-CONTEXT.md
@.planning/phases/05-onboarding-polish/05-RESEARCH.md

@VitaminG/VitaminG/VitaminG/Views/GoalDetailView.swift
@VitaminG/VitaminG/VitaminG/Views/GoalListView.swift
@VitaminG/VitaminG/VitaminG/Views/ProfileView.swift
@VitaminG/VitaminG/VitaminG/Views/ProfileEditSheet.swift
@VitaminG/VitaminG/VitaminG/Views/StatsView.swift

<interfaces>
<!-- Dynamic Type migration map: exact before/after for each view -->

GoalDetailView.swift fixed font occurrences:
- Line 66: `.font(.system(size: 16, weight: .regular, design: .rounded))` — share toggle label
- Line 77: `.font(.system(size: 12, weight: .regular, design: .rounded))` — share toggle subtext
- Line 95: `.font(.system(size: 12, weight: .semibold, design: .rounded))` — tier pill badge
- Line 103: `.font(.system(size: 22, weight: .semibold, design: .rounded))` — goal title
- Line 109: `.font(.system(size: 12, weight: .regular, design: .rounded))` — creation date
- Line 120: `.font(.system(size: 12, weight: .regular, design: .rounded))` — completion date
- Line 143: `.font(.system(size: 18, weight: .regular, design: .rounded).italic())` — quote/inspiration
- Line 170: `.font(.system(size: 13, weight: .semibold, design: .rounded))` — "Notes" header
- Line 173: `.font(.system(size: 16, weight: .regular, design: .rounded))` — notes body
- Line 30: `.background(Color(red: 0.949, green: 0.949, blue: 0.969))` — hardcoded light background

GoalListView.swift fixed font occurrences:
- Line 203: `.font(.system(size: 16, weight: goal.tier.typographicWeight, design: .rounded))` — GoalRowView title
- Line 325: `.background(Color(red: 0.949, green: 0.949, blue: 0.969))` — EmptyStateView background

ProfileView.swift fixed font occurrences:
- Line 62/66: `.font(.system(size: 22, weight: .semibold, design: .rounded))` — display name (x2)
- Line 95: `.font(.system(size: 16, weight: .regular, design: .rounded))` — toggle label
- Line 108: `.font(.system(size: 12, weight: .regular, design: .rounded))` — privacy footer
- Line 122: `.font(.system(size: 12, weight: .semibold, design: .rounded))` — "Public Goals" header
- Line 131: `.font(.system(size: 16, weight: .semibold, design: .rounded))` — empty goals title
- Line 134: `.font(.system(size: 14, weight: .regular, design: .rounded))` — empty goals subtitle
- Line 152: `.font(.system(size: 16, weight: .regular, design: .rounded))` — goal row title
- Line 159: `.font(.system(size: 12))` — tier icon size (Image, not text)
- Line 184/196: `.font(.system(size: 16, weight: .semibold, design: .rounded))` — share button (x2)

ProfileEditSheet.swift fixed font occurrences:
- Line 34: `.font(.system(size: 16, weight: .regular, design: .rounded))` — text field
- Line 47: `.font(.system(size: 12, weight: .regular, design: .rounded))` — char count
- Line 55: `.font(.system(size: 12, weight: .regular, design: .rounded))` — footer text

StatsView.swift — streak numerals:
- Line 68: `.font(.system(size: 48, weight: .bold, design: .rounded))` — global streak number (EXCEPTION)
- Line 169: `.font(.system(size: 28, weight: .bold, design: .rounded))` — tier streak number (EXCEPTION)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate GoalDetailView and GoalListView — semantic colors + Dynamic Type</name>
  <files>
    VitaminG/VitaminG/VitaminG/Views/GoalDetailView.swift,
    VitaminG/VitaminG/VitaminG/Views/GoalListView.swift
  </files>
  <read_first>
    VitaminG/VitaminG/VitaminG/Views/GoalDetailView.swift,
    VitaminG/VitaminG/VitaminG/Views/GoalListView.swift
  </read_first>
  <action>
**GoalDetailView.swift — Dark Mode color fix (D-08):**

Locate line 30: `.background(Color(red: 0.949, green: 0.949, blue: 0.969))`
Replace with: `.background(Color(.systemGroupedBackground))`

**GoalDetailView.swift — Dynamic Type migration (D-09):**

Make the following exact replacements. Each replacement swaps `.font(.system(size: N, weight: W, design: .rounded))` with a semantic font + `.fontDesign(.rounded)`.

1. `publicToggleSection` — "Share this goal" label:
   - Find: `.font(.system(size: 16, weight: .regular, design: .rounded))`
   - Replace: `.font(.body).fontDesign(.rounded)`

2. `publicToggleSection` — subtext:
   - Find: `.font(.system(size: 12, weight: .regular, design: .rounded))`
   - Replace: `.font(.caption).fontDesign(.rounded)`

3. `headerSection` — tier pill badge:
   - Find: `.font(.system(size: 12, weight: .semibold, design: .rounded))`
   - Replace: `.font(.caption.weight(.semibold)).fontDesign(.rounded)`

4. `headerSection` — goal title:
   - Find: `.font(.system(size: 22, weight: .semibold, design: .rounded))`
   - Replace: `.font(.title2.weight(.semibold)).fontDesign(.rounded)`

5. `headerSection` — creation date:
   - Find: `.font(.system(size: 12, weight: .regular, design: .rounded))`
   - Replace: `.font(.caption).fontDesign(.rounded)`

6. `headerSection` — completion date:
   - Find: `.font(.system(size: 12, weight: .regular, design: .rounded))`
   - Replace: `.font(.caption).fontDesign(.rounded)`

7. `quoteCardSection` — inspiration text:
   - Find: `.font(.system(size: 18, weight: .regular, design: .rounded).italic())`
   - Replace: `.font(.body.italic()).fontDesign(.rounded)`

8. `quoteCardSection` — quote icon:
   - Find: `.font(.system(size: 20))`
   - Replace: `.font(.title3)` (this is an Image, not text — `.fontDesign` not needed)

9. `notesSection` — "Notes" header:
   - Find: `.font(.system(size: 13, weight: .semibold, design: .rounded))`
   - Replace: `.font(.footnote.weight(.semibold)).fontDesign(.rounded)`

10. `notesSection` — notes body:
    - Find: `.font(.system(size: 16, weight: .regular, design: .rounded))`
    - Replace: `.font(.body).fontDesign(.rounded)`

**GoalListView.swift — Dark Mode color fix (D-08):**

In `EmptyStateView`, locate line 325: `.background(Color(red: 0.949, green: 0.949, blue: 0.969))`
Replace with: `.background(Color(.systemGroupedBackground))`

**GoalListView.swift — Dynamic Type migration (D-09):**

In `GoalRowView` (private struct inside GoalListView.swift), locate line 203:
`.font(.system(size: 16, weight: goal.tier.typographicWeight, design: .rounded))`
Replace with: `.font(.body.weight(goal.tier.typographicWeight)).fontDesign(.rounded)`

Do NOT modify any other fonts in GoalListView — the section headers already use semantic `.font(.subheadline.weight(...))` and the EmptyStateView uses `.font(.title2.bold())` and `.font(.body)` which are already correct.
  </action>
  <verify>
    <automated>cd /Users/kyleharrington/Desktop/AI/Vitamin\ G && xcodebuild build -scheme VitaminG -destination 'platform=iOS Simulator,name=iPhone 16' -quiet 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - GoalDetailView.swift does NOT contain `Color(red: 0.949`
    - GoalDetailView.swift contains `Color(.systemGroupedBackground)`
    - GoalDetailView.swift does NOT contain `.system(size:` (zero fixed-size fonts)
    - GoalDetailView.swift contains `.font(.title2.weight(.semibold)).fontDesign(.rounded)` for the goal title
    - GoalDetailView.swift contains `.font(.caption.weight(.semibold)).fontDesign(.rounded)` for the tier pill
    - GoalDetailView.swift contains `.font(.body.italic()).fontDesign(.rounded)` for the quote
    - GoalDetailView.swift contains `.font(.footnote.weight(.semibold)).fontDesign(.rounded)` for the Notes header
    - GoalListView.swift EmptyStateView does NOT contain `Color(red: 0.949`
    - GoalListView.swift GoalRowView contains `.font(.body.weight(goal.tier.typographicWeight)).fontDesign(.rounded)`
    - GoalListView.swift GoalRowView does NOT contain `.system(size: 16`
    - Build succeeds with zero errors
  </acceptance_criteria>
  <done>GoalDetailView uses semantic system background and all Dynamic Type fonts. GoalListView EmptyStateView uses semantic background. GoalRowView title uses .font(.body.weight(...)).fontDesign(.rounded).</done>
</task>

<task type="auto">
  <name>Task 2: Migrate ProfileView, ProfileEditSheet, and StatsView — Dynamic Type</name>
  <files>
    VitaminG/VitaminG/VitaminG/Views/ProfileView.swift,
    VitaminG/VitaminG/VitaminG/Views/ProfileEditSheet.swift,
    VitaminG/VitaminG/VitaminG/Views/StatsView.swift
  </files>
  <read_first>
    VitaminG/VitaminG/VitaminG/Views/ProfileView.swift,
    VitaminG/VitaminG/VitaminG/Views/ProfileEditSheet.swift,
    VitaminG/VitaminG/VitaminG/Views/StatsView.swift
  </read_first>
  <action>
**ProfileView.swift — Dynamic Type migration (D-09):**

Make the following exact replacements:

1. `displayNameRow` — display name text (appears TWICE — both the `if let name` branch and the "Add your name" branch):
   - Find (both occurrences): `.font(.system(size: 22, weight: .semibold, design: .rounded))`
   - Replace: `.font(.title2.weight(.semibold)).fontDesign(.rounded)`

2. `displayNameRow` — pencil button icon:
   - Find: `.font(.system(size: 22))`
   - Replace: `.font(.title2)` (Image, no `.fontDesign` needed)

3. `privacyToggleSection` — "Public Profile" toggle:
   - Find: `.font(.system(size: 16, weight: .regular, design: .rounded))`
   - Replace: `.font(.body).fontDesign(.rounded)`

4. `privacyToggleSection` — privacy description text:
   - Find: `.font(.system(size: 12, weight: .regular, design: .rounded))`
   - Replace: `.font(.caption).fontDesign(.rounded)`

5. `publicGoalsSection` — "Public Goals" header:
   - Find: `.font(.system(size: 12, weight: .semibold, design: .rounded))`
   - Replace: `.font(.caption.weight(.semibold)).fontDesign(.rounded)`

6. `publicGoalsSection` — "Your goals are private." empty state title:
   - Find: `.font(.system(size: 16, weight: .semibold, design: .rounded))`
   - Replace: `.font(.body.weight(.semibold)).fontDesign(.rounded)`

7. `publicGoalsSection` — empty state subtitle:
   - Find: `.font(.system(size: 14, weight: .regular, design: .rounded))`
   - Replace: `.font(.subheadline).fontDesign(.rounded)`

8. `publicGoalsSection` — goal row title:
   - Find: `.font(.system(size: 16, weight: .regular, design: .rounded))`
   - Replace: `.font(.body).fontDesign(.rounded)`

9. `publicGoalsSection` — tier icon in goal row:
   - Find: `.font(.system(size: 12))`
   - Replace: `.font(.caption)` (Image — no `.fontDesign` needed)

10. `shareProfileButton` — "Share Profile" label (appears TWICE — in both the ShareLink and fallback Button):
    - Find (both occurrences): `.font(.system(size: 16, weight: .semibold, design: .rounded))`
    - Replace: `.font(.body.weight(.semibold)).fontDesign(.rounded)`

**ProfileEditSheet.swift — Dynamic Type migration (D-09):**

1. Text field font:
   - Find: `.font(.system(size: 16, weight: .regular, design: .rounded))`
   - Replace: `.font(.body).fontDesign(.rounded)`

2. Character count:
   - Find: `.font(.system(size: 12, weight: .regular, design: .rounded))`
   - Replace: `.font(.caption).fontDesign(.rounded)`

3. Footer text:
   - Find: `.font(.system(size: 12, weight: .regular, design: .rounded))`
   - Replace: `.font(.caption).fontDesign(.rounded)`

**StatsView.swift — Exception documentation (D-09):**

StatsView has TWO fixed-size fonts that are EXCEPTIONS per D-09 and RESEARCH.md Open Question 3:
- Line 68: `.font(.system(size: 48, weight: .bold, design: .rounded))` — global streak number
- Line 169: `.font(.system(size: 28, weight: .bold, design: .rounded))` — tier streak number

Do NOT migrate these. They are display-proportional numerals in fixed-size cards, analogous to AvatarView initials. Instead, add a comment above each one:

Above line 68, add:
```swift
// D-09 exception: display-proportional numeral in fixed-size card (analogous to AvatarView initials)
```

Above line 169, add:
```swift
// D-09 exception: display-proportional numeral in fixed-size card (analogous to AvatarView initials)
```

StatsView has no other `.system(size:` occurrences for text — all other fonts already use semantic styles (`.font(.headline)`, `.font(.title3.weight(.semibold))`, `.font(.subheadline.weight(.medium))`, `.font(.caption)`, `.font(.caption.weight(.semibold))`, `.font(.caption2)`).

StatsView background already uses `Color(UIColor.systemGroupedBackground)` — no Dark Mode fix needed.
  </action>
  <verify>
    <automated>cd /Users/kyleharrington/Desktop/AI/Vitamin\ G && xcodebuild build -scheme VitaminG -destination 'platform=iOS Simulator,name=iPhone 16' -quiet 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - ProfileView.swift does NOT contain `.system(size:` anywhere
    - ProfileView.swift contains `.font(.title2.weight(.semibold)).fontDesign(.rounded)` for display name
    - ProfileView.swift contains `.font(.body).fontDesign(.rounded)` for toggle label
    - ProfileView.swift contains `.font(.caption).fontDesign(.rounded)` for privacy description
    - ProfileView.swift contains `.font(.subheadline).fontDesign(.rounded)` for empty state subtitle
    - ProfileEditSheet.swift does NOT contain `.system(size:` anywhere
    - ProfileEditSheet.swift contains `.font(.body).fontDesign(.rounded)` for the text field
    - ProfileEditSheet.swift contains `.font(.caption).fontDesign(.rounded)` for char count and footer
    - StatsView.swift contains exactly TWO `.system(size:` occurrences (the documented exceptions at sizes 48 and 28)
    - StatsView.swift contains `D-09 exception` comment above both fixed-size fonts
    - Build succeeds with zero errors
  </acceptance_criteria>
  <done>ProfileView and ProfileEditSheet fully migrated to Dynamic Type. StatsView streak numerals documented as D-09 exceptions. Zero .system(size:) remains in ProfileView and ProfileEditSheet.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | No new trust boundaries — this plan only modifies visual presentation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Information Disclosure | Dark Mode color contrast | accept | Completion green (0.063, 0.725, 0.506) is high-saturation — reads in both modes. Manual WCAG check recommended at phase verification. |
</threat_model>

<verification>
1. Build succeeds: `xcodebuild build -scheme VitaminG -destination 'platform=iOS Simulator,name=iPhone 16'`
2. `grep -r "Color(red: 0.949" VitaminG/VitaminG/VitaminG/Views/` returns zero results
3. `grep -c ".system(size:" VitaminG/VitaminG/VitaminG/Views/GoalDetailView.swift` returns 0
4. `grep -c ".system(size:" VitaminG/VitaminG/VitaminG/Views/ProfileView.swift` returns 0
5. `grep -c ".system(size:" VitaminG/VitaminG/VitaminG/Views/ProfileEditSheet.swift` returns 0
6. `grep -c ".system(size:" VitaminG/VitaminG/VitaminG/Views/StatsView.swift` returns 2 (exceptions only)
7. `grep -c ".system(size:" VitaminG/VitaminG/VitaminG/Views/GoalListView.swift` returns 0
</verification>

<success_criteria>
- No `Color(red: 0.949, green: 0.949, blue: 0.969)` remains in any view file
- All text fonts use semantic Dynamic Type styles with `.fontDesign(.rounded)`
- StatsView streak numerals are the only remaining `.system(size:)` calls, with D-09 exception comments
- App builds with zero errors
- GoalRowView reduced motion pattern (already present) is unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/05-onboarding-polish/05-03-SUMMARY.md`
</output>
