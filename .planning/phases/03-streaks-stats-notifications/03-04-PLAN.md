---
phase: 03-streaks-stats-notifications
plan: 04
type: execute
wave: 3
depends_on: ["03-02", "03-03"]
files_modified: []
autonomous: false
requirements:
  - STATS-04
  - STATS-05
  - NOTIF-02
  - NOTIF-06
  - NOTIF-07

must_haves:
  truths:
    - "Stats tab displays streak values, completion rates, and heatmap"
    - "Settings tab allows notification time change"
    - "All three tabs are navigable"
    - "Full test suite passes"
  artifacts: []
  key_links: []
---

<objective>
Final verification of Phase 3 — run full test suite and visually verify Stats screen, Settings screen, and TabView navigation.

Purpose: Ensures all Phase 3 features work together before moving to Phase 4.

Output: Human-verified Phase 3 completion.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-streaks-stats-notifications/03-01-SUMMARY.md
@.planning/phases/03-streaks-stats-notifications/03-02-SUMMARY.md
@.planning/phases/03-streaks-stats-notifications/03-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite</name>
  <files></files>
  <read_first>
    - VitaminG/VitaminG/VitaminGTests/StreakEngineTests.swift (verify test file exists)
    - VitaminG/VitaminG/VitaminGTests/NotificationSchedulerTests.swift (verify test file exists)
  </read_first>
  <action>
    Run the complete test suite to verify all Phase 1, 2, and 3 tests pass together:

    ```bash
    cd VitaminG/VitaminG && xcodebuild test -scheme VitaminG -destination 'platform=iOS Simulator,name=iPhone 16'
    ```

    All tests must pass. If any test fails, investigate and fix the issue. Common causes:
    - Import errors from new files not added to the correct target
    - StreakEngine or NotificationScheduler not in the VitaminG target membership
    - Test files not in VitaminGTests target membership

    After tests pass, build the app for simulator to confirm it launches:
    ```bash
    xcodebuild build -scheme VitaminG -destination 'platform=iOS Simulator,name=iPhone 16'
    ```
  </action>
  <verify>
    <automated>cd /Users/kyleharrington/Desktop/AI/Vitamin\ G/VitaminG/VitaminG && xcodebuild test -scheme VitaminG -destination 'platform=iOS Simulator,name=iPhone 16' 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - xcodebuild test exits with code 0
    - Output contains "Test Suite 'All tests' passed"
    - Output contains "StreakEngineTests" with no failures
    - Output contains "NotificationSchedulerTests" with no failures
    - Output contains "GoalViewModelTests" with no failures
    - Output contains "GoalSortTests" with no failures
  </acceptance_criteria>
  <done>Full test suite passes including all Phase 3 tests alongside existing Phase 1 and 2 tests.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of Phase 3 features</name>
  <files></files>
  <action>
    Present the verification checklist to the user and wait for approval. Launch the app in Simulator if not already running.
  </action>
  <verify>
    <automated>cd /Users/kyleharrington/Desktop/AI/Vitamin\ G/VitaminG/VitaminG && xcodebuild build -scheme VitaminG -destination 'platform=iOS Simulator,name=iPhone 16' 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - User types "approved" after verifying all Phase 3 features
    - Three tabs visible: Goals, Stats, Settings
    - Stats tab shows streak cards and heatmap
    - Settings tab shows notification time picker
  </acceptance_criteria>
  <done>User has visually verified all Phase 3 features work correctly.</done>
  <what-built>
    Phase 3 delivers three new capabilities:
    1. Stats tab with global streak, per-tier streaks, completion rates, goal counts, and a 90-day heatmap
    2. Settings tab with DatePicker for notification time selection
    3. TabView navigation with Goals, Stats, and Settings tabs
  </what-built>
  <how-to-verify>
    1. Launch the app in Simulator (iPhone 16)
    2. Verify three tabs appear at the bottom: Goals, Stats, Settings
    3. Tap the Goals tab — confirm existing goal list still works (create a goal, toggle completion, view detail)
    4. Tap the Stats tab — confirm:
       a. Global streak card displays a number (0 if no completions today)
       b. Per-tier streak cards show for each tier with icons and colors
       c. Completion rates and goal counts are shown per tier
       d. Heatmap grid is visible at the bottom with 90-day window
    5. Complete a goal in the Goals tab, then switch to Stats — verify streak updates
    6. Tap the Settings tab — confirm:
       a. "Reminder Time" DatePicker is visible and functional
       b. Notification status shows "Enabled" or "Disabled in System Settings"
    7. Change notification time in Settings — no crash
    8. Type "approved" if everything works, or describe issues
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Verification plan — no new code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| N/A | N/A | N/A | N/A | No new threats — verification only |
</threat_model>

<verification>
1. Full test suite green
2. Human visual verification of Stats, Settings, and TabView navigation
</verification>

<success_criteria>
- All tests pass
- Human approves visual appearance and functionality of Phase 3 features
</success_criteria>

<output>
After completion, create `.planning/phases/03-streaks-stats-notifications/03-04-SUMMARY.md`
</output>
