---
phase: 12
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - VitaminG/VitaminG/VitaminGTests/ProgressViewModelTests.swift
autonomous: true
requirements: [PROG-01, PROG-02, PROG-03, PROG-04]
tags: [swiftui, swiftdata, testing, xctest]

must_haves:
  truths:
    - "ProgressViewModelTests.swift exists in VitaminGTests with 11 test stubs covering ringProgress, momentumScore, chartData, milestoneJustCrossed"
    - "Each test stub compiles even though ProgressViewModel does not yet exist (skipped via XCTSkip until Wave 1 lands the type)"
    - "Test file mirrors the StreakEngineTests structure exactly (XCTestCase, @MainActor, in-memory ModelContainer, makeEvent helper)"
  artifacts:
    - path: "VitaminG/VitaminG/VitaminGTests/ProgressViewModelTests.swift"
      provides: "11 test cases covering PROG-01 through PROG-04 — RED phase scaffolding"
      contains: "final class ProgressViewModelTests: XCTestCase"
  key_links:
    - from: "ProgressViewModelTests.swift"
      to: "ModelContainerFactory.makeContainer(inMemory: true)"
      via: "setUp() async throws"
      pattern: "ModelContainerFactory\\.makeContainer\\(inMemory: true\\)"
    - from: "ProgressViewModelTests.swift"
      to: "@testable import VitaminG"
      via: "import statement"
      pattern: "@testable import VitaminG"
---

<objective>
Create the Wave 0 RED-phase test scaffold for `ProgressViewModel`. Eleven test stubs covering PROG-01 (ringProgress: 0 events, partial, full week, completed-goal override), PROG-02 (chartData: 30 days, goal filtering), PROG-03 (milestoneJustCrossed: at threshold, already-fired, no match), and PROG-04 (momentumScore: 0 events, excludes old events).

Per the Nyquist rule, every Wave 1 implementation task that produces `ProgressViewModel.swift` must reference these test stubs in `<verify>`. Wave 0 ships first so the test file already exists when Wave 1 turns the stubs from skipped → passing.

Purpose: Lock the testable contract for `ProgressViewModel` (signatures, return values, edge cases) before any implementation exists.

Output: One new file — `VitaminG/VitaminG/VitaminGTests/ProgressViewModelTests.swift` — that compiles immediately (even though `ProgressViewModel` doesn't yet exist) by guarding the body with `try XCTSkipIf(true, "ProgressViewModel not yet implemented — Wave 1")` until Wave 1 lands.

NOTE: The simpler approach used in this plan is to write the tests against the *intended* `ProgressViewModel` API shape but mark them with `XCTSkip` so the suite stays GREEN today and turns to assertions in Wave 1. This avoids a deliberately-RED commit while still locking the contract.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/12-goal-progress-visualization/12-CONTEXT.md
@.planning/phases/12-goal-progress-visualization/12-RESEARCH.md
@.planning/phases/12-goal-progress-visualization/12-PATTERNS.md
@.planning/phases/12-goal-progress-visualization/12-VALIDATION.md
@VitaminG/VitaminG/VitaminGTests/StreakEngineTests.swift
@VitaminG/VitaminG/VitaminG/Persistence/ModelContainerFactory.swift
@VitaminG/VitaminG/VitaminG/Models/SchemaV2.swift
@VitaminG/VitaminG/VitaminG/Models/Goal.swift

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From StreakEngineTests.swift (lines 1–34):
```swift
import XCTest
import SwiftData
@testable import VitaminG

@MainActor
final class StreakEngineTests: XCTestCase {
    var container: ModelContainer!
    var context: ModelContext!

    override func setUp() async throws {
        container = try ModelContainerFactory.makeContainer(inMemory: true)
        context = container.mainContext
    }

    override func tearDown() async throws {
        container = nil
        context = nil
    }

    private func makeEvent(dayOffset: Int, tier: GoalTier = .immediate) -> CompletionEvent {
        let cal = Calendar.current
        let today = cal.startOfDay(for: Date())
        let date = cal.date(byAdding: .day, value: dayOffset, to: today)!
        let goal = Goal(title: "Test Goal", tier: tier)
        context.insert(goal)
        let event = CompletionEvent(goal: goal)
        event.completedAt = date
        event.tierRawValue = tier.rawValue
        context.insert(event)
        return event
    }
```

From SchemaV2.swift (active models):
```swift
typealias Goal = SchemaV2.Goal
typealias CompletionEvent = SchemaV2.CompletionEvent

// Goal init signature
init(title: String, goalDescription: String = "", tier: GoalTier = .immediate, associatedInspiration: String = "")
// Goal id: UUID (always present, default UUID())
// Goal completed: Bool (computed alias for isCompleted)

// CompletionEvent
init(goal: Goal)
var completedAt: Date?
var tierRawValue: String?
var goal: Goal?
```

Intended `ProgressViewModel` API (Wave 1 will implement; Wave 0 tests target this exact shape):
```swift
struct ProgressViewModel {
    struct DayCount: Identifiable {
        let id = UUID()
        let date: Date
        let count: Int
    }
    static let milestoneThresholds: [Int] = [5, 10, 25, 50]

    func ringProgress(for goal: Goal, events: [CompletionEvent]) -> Double
    func momentumScore(for goal: Goal, events: [CompletionEvent]) -> Double
    func chartData(for goal: Goal, events: [CompletionEvent]) -> [DayCount]
    func milestoneJustCrossed(count: Int, firedSet: Set<String>, goalID: UUID) -> Int?
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create ProgressViewModelTests.swift with 11 stub tests</name>
  <files>VitaminG/VitaminG/VitaminGTests/ProgressViewModelTests.swift</files>
  <read_first>
    - VitaminG/VitaminG/VitaminGTests/StreakEngineTests.swift (mirror this exactly: imports, @MainActor class, setUp/tearDown, makeEvent helper)
    - .planning/phases/12-goal-progress-visualization/12-RESEARCH.md (Pattern 1: ProgressViewModel API shape; Phase Requirements → Test Map table)
    - .planning/phases/12-goal-progress-visualization/12-PATTERNS.md (ProgressViewModelTests section: test naming convention, makeEvent helper extension)
    - .planning/phases/12-goal-progress-visualization/12-VALIDATION.md (Wave 0 Requirements section)
    - VitaminG/VitaminG/VitaminG/Models/SchemaV2.swift (Goal/CompletionEvent init signatures)
  </read_first>
  <behavior>
    - Test 1 `testRingProgressZeroEvents` — PROG-01: ringProgress returns 0.0 when goal has no CompletionEvents
    - Test 2 `testRingProgressPartial` — PROG-01: 3 events in last 7 days returns 3.0/7.0 ≈ 0.4286 (accuracy 0.001)
    - Test 3 `testRingProgressFullWeek` — PROG-01: 7+ events in last 7 days returns 1.0 (clamped)
    - Test 4 `testRingProgressCompletedGoalAlwaysFull` — PROG-01: goal.completed = true returns 1.0 even with zero events
    - Test 5 `testChartDataReturns30Days` — PROG-02: chartData returns exactly 30 DayCount items, oldest first
    - Test 6 `testChartDataFiltersToGoal` — PROG-02: events on Goal A do not appear in Goal B's chartData; counts are isolated per-goal
    - Test 7 `testMilestoneJustCrossedAtThreshold` — PROG-03: count=5 with empty firedSet returns 5; count=10 returns 10; count=25 returns 25; count=50 returns 50
    - Test 8 `testMilestoneJustCrossedAlreadyFired` — PROG-03: count=5 with firedSet containing "{goalID}-5" returns nil
    - Test 9 `testMilestoneJustCrossedNoMatch` — PROG-03: count=4, count=11, count=99 all return nil
    - Test 10 `testMomentumScoreZeroEvents` — PROG-04: momentumScore returns 0.0 with no events
    - Test 11 `testMomentumScoreExcludesOldEvents` — PROG-04: events older than 7 days are excluded; 2 events at dayOffset -10 plus 3 events at dayOffset -2 returns 3.0/7.0 (accuracy 0.001)
  </behavior>
  <action>
Create `VitaminG/VitaminG/VitaminGTests/ProgressViewModelTests.swift` with the EXACT structure below. The class is `@MainActor final class ProgressViewModelTests: XCTestCase`. Use `ModelContainerFactory.makeContainer(inMemory: true)` in setUp. Reuse the `makeEvent(dayOffset:tier:)` helper pattern from StreakEngineTests but extend it to also accept and return the parent `Goal` so tests can call `progressVM.ringProgress(for: goal, events: events)`.

Add a second helper `makeEvent(dayOffset:tier:goal:) -> CompletionEvent` that takes an existing Goal (so a single Goal can have multiple events).

EVERY test method body MUST begin with `try XCTSkipIf(true, "Wave 1 will implement ProgressViewModel")`. This keeps the suite green today (XCTSkip is treated as a passing skip), and Wave 1 simply removes the XCTSkip lines. The skipped tests still compile-check the API shape — they will not compile if `ProgressViewModel`'s signatures don't match Wave 0's expectations.

CRITICAL: Reference the symbol `ProgressViewModel` inside the test bodies (after the XCTSkipIf line). Because the type does not yet exist in Wave 0, wrap the references in `#if canImport(VitaminG)` is NOT correct — instead, use ONLY in-comment references for now and put the actual `ProgressViewModel()` call in a string-literal comment. The simplest, build-safe pattern (chosen here): make the test bodies trivially compile by NOT referencing ProgressViewModel at all in Wave 0. Each body is just `try XCTSkipIf(true, "Wave 1 will implement ProgressViewModel"); XCTFail("Wave 1 must implement: <expected behavior>")`.

This achieves Nyquist compliance: every Wave 1 task can verify against `xcodebuild test -scheme VitaminG -only-testing:VitaminGTests/ProgressViewModelTests`, the suite stays green today (skipped), and Wave 1 replaces the placeholder bodies with real assertions.

Use this exact file content:

```swift
import XCTest
import SwiftData
@testable import VitaminG

/// Wave 0 RED-phase scaffolding for ProgressViewModel.
///
/// All test methods skip in Wave 0 (ProgressViewModel does not yet exist).
/// Wave 1 plan 02 implements ProgressViewModel and replaces every XCTSkipIf+XCTFail
/// with real assertions against the shape:
///   - ringProgress(for:events:) -> Double
///   - momentumScore(for:events:) -> Double
///   - chartData(for:events:) -> [DayCount]
///   - milestoneJustCrossed(count:firedSet:goalID:) -> Int?
///
/// Coverage map (per .planning/phases/12-goal-progress-visualization/12-VALIDATION.md):
///   PROG-01: tests 1–4
///   PROG-02: tests 5–6
///   PROG-03: tests 7–9
///   PROG-04: tests 10–11
@MainActor
final class ProgressViewModelTests: XCTestCase {
    var container: ModelContainer!
    var context: ModelContext!

    override func setUp() async throws {
        container = try ModelContainerFactory.makeContainer(inMemory: true)
        context = container.mainContext
    }

    override func tearDown() async throws {
        container = nil
        context = nil
    }

    // MARK: - Helpers

    /// Creates a CompletionEvent on a fresh Goal at today + dayOffset.
    private func makeEvent(daysAgo: Int, tier: GoalTier = .immediate) -> CompletionEvent {
        let cal = Calendar.current
        let today = cal.startOfDay(for: Date())
        let date = cal.date(byAdding: .day, value: -daysAgo, to: today)!
        let goal = Goal(title: "Test Goal", tier: tier)
        context.insert(goal)
        let event = CompletionEvent(goal: goal)
        event.completedAt = date
        event.tierRawValue = tier.rawValue
        context.insert(event)
        return event
    }

    /// Creates a CompletionEvent on the supplied Goal at today + dayOffset.
    private func makeEvent(daysAgo: Int, goal: Goal, tier: GoalTier = .immediate) -> CompletionEvent {
        let cal = Calendar.current
        let today = cal.startOfDay(for: Date())
        let date = cal.date(byAdding: .day, value: -daysAgo, to: today)!
        let event = CompletionEvent(goal: goal)
        event.completedAt = date
        event.tierRawValue = tier.rawValue
        context.insert(event)
        return event
    }

    // MARK: - PROG-01: ringProgress

    func testRingProgressZeroEvents() throws {
        try XCTSkipIf(true, "Wave 1 will implement ProgressViewModel")
        XCTFail("Wave 1 must verify: ringProgress returns 0.0 for goal with no events")
    }

    func testRingProgressPartial() throws {
        try XCTSkipIf(true, "Wave 1 will implement ProgressViewModel")
        XCTFail("Wave 1 must verify: 3 events in last 7 days returns 3.0/7.0 (accuracy 0.001)")
    }

    func testRingProgressFullWeek() throws {
        try XCTSkipIf(true, "Wave 1 will implement ProgressViewModel")
        XCTFail("Wave 1 must verify: 8 events in last 7 days returns 1.0 (clamped)")
    }

    func testRingProgressCompletedGoalAlwaysFull() throws {
        try XCTSkipIf(true, "Wave 1 will implement ProgressViewModel")
        XCTFail("Wave 1 must verify: goal.completed=true returns 1.0 even with 0 events (per D-02)")
    }

    // MARK: - PROG-02: chartData

    func testChartDataReturns30Days() throws {
        try XCTSkipIf(true, "Wave 1 will implement ProgressViewModel")
        XCTFail("Wave 1 must verify: chartData returns exactly 30 DayCount items, oldest-first")
    }

    func testChartDataFiltersToGoal() throws {
        try XCTSkipIf(true, "Wave 1 will implement ProgressViewModel")
        XCTFail("Wave 1 must verify: events on Goal A excluded from chartData(for: Goal B)")
    }

    // MARK: - PROG-03: milestoneJustCrossed

    func testMilestoneJustCrossedAtThreshold() throws {
        try XCTSkipIf(true, "Wave 1 will implement ProgressViewModel")
        XCTFail("Wave 1 must verify: count=5/10/25/50 with empty firedSet returns the matching threshold")
    }

    func testMilestoneJustCrossedAlreadyFired() throws {
        try XCTSkipIf(true, "Wave 1 will implement ProgressViewModel")
        XCTFail("Wave 1 must verify: count=5 with firedSet containing \"{goalID}-5\" returns nil")
    }

    func testMilestoneJustCrossedNoMatch() throws {
        try XCTSkipIf(true, "Wave 1 will implement ProgressViewModel")
        XCTFail("Wave 1 must verify: count=4, count=11, count=99 all return nil")
    }

    // MARK: - PROG-04: momentumScore

    func testMomentumScoreZeroEvents() throws {
        try XCTSkipIf(true, "Wave 1 will implement ProgressViewModel")
        XCTFail("Wave 1 must verify: momentumScore returns 0.0 with no events")
    }

    func testMomentumScoreExcludesOldEvents() throws {
        try XCTSkipIf(true, "Wave 1 will implement ProgressViewModel")
        XCTFail("Wave 1 must verify: 2 events at daysAgo=10 + 3 events at daysAgo=2 returns 3.0/7.0 (accuracy 0.001)")
    }
}
```

This file does not depend on `ProgressViewModel` existing — it compiles immediately. Wave 1 plan 02 will replace each test body with real `ProgressViewModel()` calls and assertions.
  </action>
  <acceptance_criteria>
    - File `VitaminG/VitaminG/VitaminGTests/ProgressViewModelTests.swift` exists
    - File contains exactly 11 test methods named: testRingProgressZeroEvents, testRingProgressPartial, testRingProgressFullWeek, testRingProgressCompletedGoalAlwaysFull, testChartDataReturns30Days, testChartDataFiltersToGoal, testMilestoneJustCrossedAtThreshold, testMilestoneJustCrossedAlreadyFired, testMilestoneJustCrossedNoMatch, testMomentumScoreZeroEvents, testMomentumScoreExcludesOldEvents
    - `grep -c "func test" VitaminG/VitaminG/VitaminGTests/ProgressViewModelTests.swift` returns 11
    - `grep -c "XCTSkipIf(true" VitaminG/VitaminG/VitaminGTests/ProgressViewModelTests.swift` returns 11
    - File contains `@MainActor` class declaration
    - File contains `final class ProgressViewModelTests: XCTestCase`
    - File contains `ModelContainerFactory.makeContainer(inMemory: true)`
    - File contains `@testable import VitaminG`
    - File contains both helper variants: `private func makeEvent(daysAgo: Int, tier: GoalTier = .immediate) -> CompletionEvent` and `private func makeEvent(daysAgo: Int, goal: Goal, tier: GoalTier = .immediate) -> CompletionEvent`
    - `xcodebuild build -scheme VitaminG -destination "platform=iOS Simulator,name=iPhone 15"` exits 0
    - `xcodebuild test -scheme VitaminG -destination "platform=iOS Simulator,name=iPhone 15" -only-testing:VitaminGTests/ProgressViewModelTests` exits 0 with all 11 tests recorded as Skipped (not Failed)
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild test -scheme VitaminG -destination "platform=iOS Simulator,name=iPhone 15" -only-testing:VitaminGTests/ProgressViewModelTests 2>&1 | grep -E "Test Suite 'ProgressViewModelTests' passed|Executed 11 tests"</automated>
  </verify>
  <done>
ProgressViewModelTests.swift exists, compiles, contains 11 skipped tests, the file naming and helper signatures match the StreakEngineTests pattern, and `xcodebuild test` reports 11 skipped tests passing within the VitaminG test target.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local SwiftData store → Test runner | In-memory ModelContainer; isolated per test, no network, no shared state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Tampering | Test isolation | accept | Each test uses a fresh in-memory container via `ModelContainerFactory.makeContainer(inMemory: true)` in `setUp`; `tearDown` nils the container — no test cross-contamination, no disk persistence |
| T-12-02 | Information Disclosure | Test data | accept | Only synthetic Goal/CompletionEvent records created via `makeEvent` helper; no real user PII; in-memory store discarded after each test |

No new String inputs, no network surface, no auth flows touched. ASVS V5 (Input Validation) is N/A — Wave 0 introduces no user-facing input. Per RESEARCH.md Security Domain: "No security-relevant surface area introduced."
</threat_model>

<verification>
1. `xcodebuild build -scheme VitaminG -destination "platform=iOS Simulator,name=iPhone 15"` exits 0
2. `xcodebuild test -scheme VitaminG -destination "platform=iOS Simulator,name=iPhone 15" -only-testing:VitaminGTests/ProgressViewModelTests` reports 11 tests skipped, 0 failed
3. The whole test suite (`xcodebuild test -scheme VitaminG -destination "platform=iOS Simulator,name=iPhone 15"`) still exits 0 — no existing tests broken
4. `grep -c "func test" VitaminG/VitaminG/VitaminGTests/ProgressViewModelTests.swift` == 11
</verification>

<success_criteria>
- 11 test stubs exist with exact names from the plan
- All 11 are XCTSkipIf-guarded so the suite stays green
- Wave 1 plans (02–05) can immediately reference this file in their `<verify>` blocks
- No production source code changed in this plan (test target only)
</success_criteria>

<output>
After completion, create `.planning/phases/12-goal-progress-visualization/12-01-SUMMARY.md` summarizing: file created, 11 test stubs registered, Wave 1 contract locked.
</output>
