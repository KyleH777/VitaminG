---
phase: 25-smart-notifications-enhancement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - VitaminG/VitaminG/VitaminG/Services/NotificationPreferences.swift
  - VitaminG/VitaminG/VitaminGTests/NotificationSchedulerPhase25Tests.swift
autonomous: true
requirements:
  - NOTIF-01
  - NOTIF-02
  - NOTIF-03
  - NOTIF-04

must_haves:
  truths:
    - "NotificationPreferences exposes appendCheckInHour(_:), checkInHourHistory(), modalCheckInHour(), nudgeSuggestionDismissed, markNudgeSuggestionDismissed — all reading/writing through the App Group suite group.com.kyleharrington.VitaminG"
    - "NotificationSchedulerPhase25Tests exists as a RED scaffold with failing tests covering tone selection, goal-title formatting, one-shot scheduling, FIFO append, modal computation, and tie-breaking"
    - "Existing NotificationSchedulerTests still compiles; no production signature changes have happened yet — this plan only adds new helpers and a new test file"
  artifacts:
    - path: "VitaminG/VitaminG/VitaminG/Services/NotificationPreferences.swift"
      provides: "checkInHourHistoryKey, nudgeSuggestionDismissedKey, appendCheckInHour, checkInHourHistory, modalCheckInHour, nudgeSuggestionDismissed, markNudgeSuggestionDismissed"
      contains: "checkInHourHistoryKey"
    - path: "VitaminG/VitaminG/VitaminGTests/NotificationSchedulerPhase25Tests.swift"
      provides: "RED test scaffolding for all Phase 25 behaviors"
      contains: "NotificationSchedulerPhase25Tests"
  key_links:
    - from: "NotificationPreferences.appendCheckInHour"
      to: "UserDefaults(suiteName: group.com.kyleharrington.VitaminG)"
      via: "App Group suite write"
      pattern: "UserDefaults\\(suiteName:"
    - from: "NotificationSchedulerPhase25Tests"
      to: "NotificationPreferences static helpers"
      via: "direct static method calls in pure-function tests"
      pattern: "NotificationPreferences\\.(appendCheckInHour|modalCheckInHour|checkInHourHistory)"
---

<objective>
Establish the Phase 25 test scaffolding and the new UserDefaults key surface on `NotificationPreferences` before any production signature changes. The scheduler-side production changes (Plan 02) and the integration wiring (Plan 03) depend on these helpers existing and the RED tests being in place.

Purpose: Wave 0 / TDD scaffolding. By creating the failing tests and the `NotificationPreferences` helpers first, downstream plans implement against a fixed contract instead of inventing it during execution.

Output: Extended `NotificationPreferences.swift` with `checkInHourHistory`, `nudgeSuggestionDismissed`, and modal-hour computation helpers; new `NotificationSchedulerPhase25Tests.swift` file with RED tests for NOTIF-01 through NOTIF-04.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/25-smart-notifications-enhancement/25-CONTEXT.md
@.planning/phases/25-smart-notifications-enhancement/25-RESEARCH.md
@.planning/phases/25-smart-notifications-enhancement/25-PATTERNS.md
@VitaminG/VitaminG/VitaminG/Services/NotificationPreferences.swift
@VitaminG/VitaminG/VitaminGTests/Phase23NotificationTests.swift
@VitaminG/VitaminG/VitaminGTests/NotificationSchedulerPhase14Tests.swift
@VitaminG/VitaminG/VitaminGTests/NotificationSchedulerTests.swift

<interfaces>
<!-- Existing NotificationPreferences surface — extend (do not remove or rename) -->

From VitaminG/VitaminG/VitaminG/Services/NotificationPreferences.swift:
  enum NotificationPreferences
  static let suiteName = "group.com.kyleharrington.VitaminG"
  static let hourKey = "notificationHour"
  static let minuteKey = "notificationMinute"
  static let winHourKey = "winNotificationHour"
  static let winMinuteKey = "winNotificationMinute"
  static let defaultHour: Int
  static let defaultMinute: Int
  static var hour: Int { get }
  static var minute: Int { get }
  static func save(hour: Int, minute: Int)
  static func sharedHour() -> Int
  static func sharedMinute() -> Int
  static func saveWinTime(hour: Int, minute: Int)
  // ...win analogs

<!-- New surface to add (D-08, D-10, D-13) — exact names must match what Plan 02 and Plan 03 reference -->
  static let checkInHourHistoryKey = "checkInHourHistory"
  static let nudgeSuggestionDismissedKey = "nudgeSuggestionDismissed"
  static func appendCheckInHour(_ hour: Int)
  static func checkInHourHistory() -> [Int]
  static func modalCheckInHour() -> Int?
  static var nudgeSuggestionDismissed: Bool { get }
  static func markNudgeSuggestionDismissed()
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend NotificationPreferences with check-in hour history and nudge suggestion dismissed keys</name>
  <!-- EXECUTOR NOTE: Run Task 2 (create test file) before Task 1's verify step. Task 1's verify references test methods created in Task 2. TDD sequence: T2 creates RED tests, T1 turns NOTIF-03 helper tests GREEN. -->
  <files>VitaminG/VitaminG/VitaminG/Services/NotificationPreferences.swift</files>
  <read_first>
    - VitaminG/VitaminG/VitaminG/Services/NotificationPreferences.swift (full file — to mirror existing key/save/read pattern exactly)
    - .planning/phases/25-smart-notifications-enhancement/25-PATTERNS.md (sections: NotificationPreferences.swift — MODIFY, App Group UserDefaults Write Pattern)
    - .planning/phases/25-smart-notifications-enhancement/25-CONTEXT.md (decisions D-08, D-10, D-13)
    - .planning/phases/25-smart-notifications-enhancement/25-RESEARCH.md (Pitfall 4, Pitfall 6, Security Domain: read-time clamp filter { $0 >= 0 && $0 <= 23 })
  </read_first>
  <behavior>
    - appendCheckInHour(15) twice followed by checkInHourHistory() returns [15, 15]
    - appendCheckInHour called 15 times with increasing hour values 0..14 — checkInHourHistory() returns exactly 14 entries (FIFO drop of the oldest), and the first entry is 1 (per D-08 last 14 entries kept)
    - checkInHourHistory() reads from the App Group suite (suiteName: "group.com.kyleharrington.VitaminG") — not UserDefaults.standard
    - modalCheckInHour() on history [8, 8, 9, 8, 9] returns 8 (mode)
    - modalCheckInHour() on history [9, 8, 9, 8] returns 9 (tie broken by earliest first-occurrence index)
    - modalCheckInHour() on history with out-of-range values [25, -1, 8, 8] returns 8 (out-of-range entries filtered before mode)
    - modalCheckInHour() on empty history returns nil
    - nudgeSuggestionDismissed defaults to false when key absent
    - markNudgeSuggestionDismissed() flips nudgeSuggestionDismissed to true via App Group suite write
  </behavior>
  <action>
    Add to `NotificationPreferences` enum (do not remove or rename existing members):

    1. Two string keys per D-08 and D-13:
       - `static let checkInHourHistoryKey = "checkInHourHistory"`
       - `static let nudgeSuggestionDismissedKey = "nudgeSuggestionDismissed"`

    2. `static func appendCheckInHour(_ hour: Int)` — per D-08:
       - Read existing array from `UserDefaults(suiteName: suiteName)?.array(forKey: checkInHourHistoryKey) as? [Int]` (default to `[]`)
       - Append `hour` (do not clamp here; clamp at read in `modalCheckInHour()` per Security Domain mitigation)
       - If resulting `count > 14`, call `removeFirst(count - 14)` to keep the last 14 (FIFO)
       - Write back to the App Group suite only — do NOT mirror to `UserDefaults.standard` (per D-08 and Pitfall 6)

    3. `static func checkInHourHistory() -> [Int]` — App Group suite read:
       - Returns `(UserDefaults(suiteName: suiteName)?.array(forKey: checkInHourHistoryKey) as? [Int]) ?? []`

    4. `static func modalCheckInHour() -> Int?` — per D-10 + Security Domain clamp:
       - Read history via `checkInHourHistory()`
       - Apply `filter { $0 >= 0 && $0 <= 23 }` to drop tampered values (T-25 Tampering mitigation)
       - Return `nil` for empty filtered history
       - Build `counts: [Int: Int]` then find `maxCount = counts.values.max()`
       - Among tied hours, return the hour whose `firstIndex(of:)` in the filtered history is smallest (tie-break by earliest occurrence)

    5. `static var nudgeSuggestionDismissed: Bool` — read-only computed:
       - Returns `UserDefaults(suiteName: suiteName)?.bool(forKey: nudgeSuggestionDismissedKey) ?? false`
       - Use `.bool(forKey:)` so non-bool/absent values safely return false (T-25 Tampering mitigation)

    6. `static func markNudgeSuggestionDismissed()`:
       - `UserDefaults(suiteName: suiteName)?.set(true, forKey: nudgeSuggestionDismissedKey)`

    Access level for all new members: `static` (no `internal`/`public` keyword change; match existing style in this enum). Place new members at the end of the existing `NotificationPreferences` enum.

    Do NOT touch existing `hour`, `minute`, `save`, `sharedHour`, `sharedMinute`, `saveWinTime`, `winHour`, `winMinute` members.
  </action>
  <verify>
    <automated>cd "/Users/kyleharrington/Desktop/AI/Vitamin G/VitaminG/VitaminG" &amp;&amp; xcodebuild -scheme VitaminG -destination "platform=iOS Simulator,name=iPhone 16" -only-testing:VitaminGTests/NotificationSchedulerPhase25Tests/test_appendCheckInHour_fifo14 -only-testing:VitaminGTests/NotificationSchedulerPhase25Tests/test_modalHour_returnsMode -only-testing:VitaminGTests/NotificationSchedulerPhase25Tests/test_modalHour_tieBreakByFirstOccurrence -only-testing:VitaminGTests/NotificationSchedulerPhase25Tests/test_modalHour_filtersOutOfRange -only-testing:VitaminGTests/NotificationSchedulerPhase25Tests/test_nudgeSuggestionDismissed_defaultsFalse -only-testing:VitaminGTests/NotificationSchedulerPhase25Tests/test_markNudgeSuggestionDismissed_setsTrue test 2>&amp;1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - File contains literal string `checkInHourHistoryKey = "checkInHourHistory"`
    - File contains literal string `nudgeSuggestionDismissedKey = "nudgeSuggestionDismissed"`
    - File contains the symbol `appendCheckInHour` (function definition)
    - File contains the symbol `modalCheckInHour` (function definition)
    - File contains the symbol `markNudgeSuggestionDismissed` (function definition)
    - `grep -c "UserDefaults.standard.set.*checkInHourHistory\\|UserDefaults.standard.set.*nudgeSuggestionDismissed" NotificationPreferences.swift` returns 0 (no standard-suite writes for these keys)
    - Existing `hourKey`, `minuteKey`, `winHourKey`, `winMinuteKey`, `save(hour:minute:)`, `saveWinTime` symbols remain in the file (no regression)
    - All 6 listed test names in the verify command PASS
  </acceptance_criteria>
  <done>
    NotificationPreferences exposes the seven new members (`checkInHourHistoryKey`, `nudgeSuggestionDismissedKey`, `appendCheckInHour(_:)`, `checkInHourHistory()`, `modalCheckInHour()`, `nudgeSuggestionDismissed`, `markNudgeSuggestionDismissed()`), all reading and writing through the `group.com.kyleharrington.VitaminG` App Group suite. The 6 NOTIF-03-helper tests in NotificationSchedulerPhase25Tests pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create NotificationSchedulerPhase25Tests RED scaffold for tone, goal titles, one-shot 7 PM, and NOTIF-03 helpers</name>
  <files>VitaminG/VitaminG/VitaminGTests/NotificationSchedulerPhase25Tests.swift</files>
  <read_first>
    - VitaminG/VitaminG/VitaminGTests/Phase23NotificationTests.swift (cap guard + identifier stability test patterns — XCTest setUp/tearDown notification cleanup, testable overload assertions)
    - VitaminG/VitaminG/VitaminGTests/NotificationSchedulerPhase14Tests.swift (UNCalendarNotificationTrigger inspection pattern, XCTSkipIf for permission)
    - VitaminG/VitaminG/VitaminGTests/NotificationSchedulerTests.swift (in-memory ModelContainer pattern, makeContent body inspection, inspirationalMessages reference at line 57/117)
    - .planning/phases/25-smart-notifications-enhancement/25-PATTERNS.md (NotificationSchedulerPhase25Tests.swift — NEW section: imports, class structure, all listed test patterns)
    - .planning/phases/25-smart-notifications-enhancement/25-RESEARCH.md (Phase Requirements → Test Map table)
  </read_first>
  <behavior>
    The new test file must compile and produce a RED test suite (some tests fail because production signatures haven't changed yet; others pass because Task 1 implemented the NotificationPreferences helpers). Specifically:

    - test_makeContent_celebratoryCopy_whenStreakGe7 — FAILS until Plan 02 lands (asserts `makeContent(activeGoals:currentStreak:)` signature exists and body prefix is in `NotificationScheduler.celebratoryCopy`)
    - test_makeContent_neutralBuildingCopy_whenStreak1To6 — FAILS until Plan 02 lands
    - test_makeContent_encouragingCopy_whenStreak0 — FAILS until Plan 02 lands
    - test_makeContent_twoGoalTitles — FAILS until Plan 02 (body contains "\nGoal A\nGoal B")
    - test_makeContent_singleGoal — FAILS until Plan 02 (body contains exactly one "\nGoal A", no second newline-title)
    - test_makeContent_noActiveGoals_bodyIsToneMessageOnly — FAILS until Plan 02
    - test_schedule_oneShotStreakAtRisk_repeats_false — FAILS until Plan 02 (asserts new scheduleOneShotStreakAtRisk method exists, trigger.repeats == false, dateComponents.hour == 19)
    - test_schedule_oneShotSkipped_atCapBoundary — FAILS until Plan 02 (asserts cap-guard returns early when pendingCount >= 60)
    - test_appendCheckInHour_fifo14 — PASSES after Task 1
    - test_modalHour_returnsMode — PASSES after Task 1
    - test_modalHour_tieBreakByFirstOccurrence — PASSES after Task 1
    - test_modalHour_filtersOutOfRange — PASSES after Task 1
    - test_nudgeSuggestionDismissed_defaultsFalse — PASSES after Task 1
    - test_markNudgeSuggestionDismissed_setsTrue — PASSES after Task 1
  </behavior>
  <action>
    Create a new file `NotificationSchedulerPhase25Tests.swift` in `VitaminGTests/`.

    Imports: `XCTest`, `UserNotifications`, `SwiftData`, `@testable import VitaminG`.

    Class declaration:
      - `@MainActor final class NotificationSchedulerPhase25Tests: XCTestCase`
      - `private var container: ModelContainer!`
      - `private let scheduler = NotificationScheduler.shared`

    setUp / tearDown — mirror Phase23NotificationTests:
      - `setUp()`: build in-memory container via `ModelContainerFactory.makeContainer(inMemory: true)` and clear all pending notification requests
      - `tearDown()`: clear all pending notification requests and nil the container
      - Also clear App Group UserDefaults keys `checkInHourHistory` and `nudgeSuggestionDismissed` between tests so tests are isolated

    Tone selection tests (NOTIF-01) — call `scheduler.makeContent(activeGoals:currentStreak:)`:
      - test_makeContent_celebratoryCopy_whenStreakGe7: currentStreak=7, single goal "Goal A", assert body starts with a message from `NotificationScheduler.celebratoryCopy`
      - test_makeContent_neutralBuildingCopy_whenStreak1To6: currentStreak=3, assert body starts with a message from `NotificationScheduler.neutralBuildingCopy`
      - test_makeContent_encouragingCopy_whenStreak0: currentStreak=0, assert body starts with a message from `NotificationScheduler.encouragingCopy`

    Goal-title injection tests (NOTIF-02):
      - test_makeContent_twoGoalTitles: 2 active non-completed goals "Goal A"/"Goal B", currentStreak=3 — assert `content.body.contains("\nGoal A")` AND `content.body.contains("\nGoal B")` AND body has exactly 2 newline-separated title lines after the tone message
      - test_makeContent_singleGoal: 1 active goal "Goal A", currentStreak=3 — assert body contains "\nGoal A" and does NOT contain a third line (count of "\n" in body == 1)
      - test_makeContent_noActiveGoals_bodyIsToneMessageOnly: empty activeGoals array, currentStreak=0 — assert body equals a message from `encouragingCopy` exactly (no trailing newline, no titles)

    One-shot 7 PM tests (NOTIF-04) — call new `scheduler.scheduleOneShotStreakAtRisk(activeGoals:streak:pendingCount:)`:
      - test_schedule_oneShotStreakAtRisk_repeats_false: pendingCount=0, streak=3, after await fetch pending requests, find request with `identifier == NotificationScheduler.globalStreakAtRiskIdentifier`. `XCTSkipIf(request == nil, "permission not granted in test env")`. Cast trigger as `UNCalendarNotificationTrigger`, assert `trigger.repeats == false`, `trigger.dateComponents.hour == 19`, `trigger.dateComponents.minute == 0`.
      - test_schedule_oneShotSkipped_atCapBoundary: pendingCount=60, after await, assert NO request exists with identifier `globalStreakAtRiskIdentifier`

    NOTIF-03 pure helper tests — call `NotificationPreferences` static methods directly (no scheduler):
      - test_appendCheckInHour_fifo14: append 15 hours 0..14 sequentially, assert `checkInHourHistory().count == 14`, assert `checkInHourHistory().first == 1` (oldest entry 0 was dropped)
      - test_modalHour_returnsMode: seed history with `[8, 8, 9, 8, 9]` via 5 appendCheckInHour calls, assert `modalCheckInHour() == 8`
      - test_modalHour_tieBreakByFirstOccurrence: seed `[9, 8, 9, 8]`, assert `modalCheckInHour() == 9`
      - test_modalHour_filtersOutOfRange: seed `[25, -1, 8, 8]`, assert `modalCheckInHour() == 8` (security clamp drops tampered values)
      - test_nudgeSuggestionDismissed_defaultsFalse: ensure App Group key cleared in setUp, assert `NotificationPreferences.nudgeSuggestionDismissed == false`
      - test_markNudgeSuggestionDismissed_setsTrue: call `markNudgeSuggestionDismissed()`, assert `nudgeSuggestionDismissed == true`

    Test isolation helper: add a private `clearAppGroupKeys()` called in setUp that removes both `checkInHourHistoryKey` and `nudgeSuggestionDismissedKey` from `UserDefaults(suiteName: "group.com.kyleharrington.VitaminG")`.

    Do NOT touch `NotificationSchedulerTests.swift` — Plan 02 handles that update as part of the signature migration.
  </action>
  <verify>
    <automated>cd "/Users/kyleharrington/Desktop/AI/Vitamin G/VitaminG/VitaminG" &amp;&amp; xcodebuild -scheme VitaminG -destination "platform=iOS Simulator,name=iPhone 16" -only-testing:VitaminGTests/NotificationSchedulerPhase25Tests build-for-testing 2>&amp;1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - File `VitaminG/VitaminG/VitaminGTests/NotificationSchedulerPhase25Tests.swift` exists
    - File compiles for testing (build-for-testing succeeds — even though some tests will fail at run-time, the test target must build)
    - File contains exactly the 14 test functions named in the behavior block (verified by `grep -c '^    func test_' NotificationSchedulerPhase25Tests.swift` returns 14)
    - File contains `@testable import VitaminG`
    - File contains `@MainActor final class NotificationSchedulerPhase25Tests: XCTestCase`
    - The 6 NOTIF-03 helper tests (test_appendCheckInHour_fifo14, test_modalHour_returnsMode, test_modalHour_tieBreakByFirstOccurrence, test_modalHour_filtersOutOfRange, test_nudgeSuggestionDismissed_defaultsFalse, test_markNudgeSuggestionDismissed_setsTrue) PASS at run-time
    - The 8 makeContent/scheduleOneShotStreakAtRisk tests FAIL at run-time with compiler errors about missing symbols `celebratoryCopy`, `neutralBuildingCopy`, `encouragingCopy`, `makeContent(activeGoals:currentStreak:)`, `scheduleOneShotStreakAtRisk` (RED scaffolding state — Plan 02 turns them GREEN)
  </acceptance_criteria>
  <done>
    NotificationSchedulerPhase25Tests.swift exists, compiles, and produces the expected RED scaffolding pattern: 6 NOTIF-03 helper tests GREEN today, 8 scheduler-side tests waiting for Plan 02 to land before they can compile against the new production symbols.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App Group UserDefaults → app code | Untrusted persisted values that may be corrupted (older app version, manual tampering, simulator state pollution) |
| Test target → @testable production | Tests reach internal static helpers; must not weaken access control on production types |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-01-01 | Tampering | `checkInHourHistory` UserDefaults value contains out-of-range hours (e.g., 25, -1, or a non-Int array) | mitigate | `modalCheckInHour()` applies `filter { $0 >= 0 && $0 <= 23 }` before mode computation; `checkInHourHistory()` uses `as? [Int]` cast which returns nil-or-empty for type-mismatched values, defaulted to `[]` |
| T-25-01-02 | Tampering | `nudgeSuggestionDismissed` UserDefaults key set to non-bool value (e.g., string, number) | mitigate | Read via `UserDefaults.bool(forKey:)` which returns `false` for absent and type-mismatched keys per Apple documentation |
| T-25-01-03 | Information Disclosure | New keys readable by sibling app extensions in the same App Group | accept | App Group is intentionally shared with widgets and a future Watch target; `checkInHourHistory` (hours 0–23) and `nudgeSuggestionDismissed` (Bool) are non-sensitive, behavior-only data |
| T-25-01-04 | Tampering | RED test scaffold accidentally references non-existent production symbols and silently fails to build the whole test target | mitigate | Task 2 verify step is `build-for-testing` which fails fast on compile errors; Plan 02 turns the symbols on before any phase verification gate |
| T-25-01-SC | Tampering | Supply chain — new package installs | accept | No external package dependencies introduced this phase; Phase 25 is 100% first-party Apple frameworks per RESEARCH.md Standard Stack |
</threat_model>

<verification>
- `xcodebuild` build of the test target succeeds (Task 2 RED scaffold compiles)
- All 6 NOTIF-03 helper tests PASS (`NotificationPreferences` extension landed correctly)
- The 8 scheduler-side tests fail to compile against the not-yet-updated NotificationScheduler — this is the expected RED state for Wave 2 to consume
- No production code outside `NotificationPreferences.swift` is modified
- Existing `NotificationSchedulerTests.swift` and `Phase23NotificationTests.swift` files are untouched
</verification>

<success_criteria>
- NotificationPreferences exposes appendCheckInHour, checkInHourHistory, modalCheckInHour, nudgeSuggestionDismissed, markNudgeSuggestionDismissed via the App Group suite
- NotificationSchedulerPhase25Tests.swift exists with 14 test functions and compiles
- 6 NOTIF-03 helper tests pass
- 8 scheduler tests are RED in a known, expected way (waiting for Plan 02)
- Zero modifications to existing test files, NotificationScheduler.swift, GoalViewModel.swift, SettingsView.swift, or VitaminGApp.swift
</success_criteria>

<output>
Create `.planning/phases/25-smart-notifications-enhancement/25-01-SUMMARY.md` when done. Record:
- Exact list of new `NotificationPreferences` members added (names + signatures)
- Exact list of 14 test function names in NotificationSchedulerPhase25Tests.swift
- Confirmation that the 6 helper tests pass and the 8 scheduler tests are RED as expected
- Any deviation from the planned tie-break logic (none expected)
</output>
