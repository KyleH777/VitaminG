---
phase: 24-widget-enhancements
plan: 03
type: execute
wave: 2
depends_on:
  - 24-01
files_modified:
  - VitaminG/VitaminG/VitaminG/Views/StatsView.swift
autonomous: false
requirements:
  - WID-02
tags:
  - widgetkit
  - reload-audit
  - streak-freeze
must_haves:
  truths:
    - "StatsView's Freeze Streak button handler calls WidgetCenter.shared.reloadAllTimelines() immediately after freezeService.freeze() and before viewModel.refresh()."
    - "StatsView imports WidgetKit (added if not already present)."
    - "StreakFreezeService.swift remains pure Foundation — no WidgetKit/SwiftUI imports added to that service."
    - "All v2.0 goal-state mutation sites identified in RESEARCH.md §WID-02 Mutation Site Audit either call WidgetCenter.shared.reloadAllTimelines() directly OR transitively via GoalViewModel.reloadWidgetTimelines() — confirmed by grep evidence in the SUMMARY."
    - "A human verifier performs a real-device-equivalent simulator test: freeze a streak via StatsView and confirm the home-screen widget reflects the change without an app relaunch."
  artifacts:
    - path: "VitaminG/VitaminG/VitaminG/Views/StatsView.swift"
      provides: "Updated freeze handler with widget reload"
      contains: "WidgetCenter.shared.reloadAllTimelines"
  key_links:
    - from: "StatsView Freeze Streak Button"
      to: "WidgetCenter.shared.reloadAllTimelines()"
      via: "imperative call in confirmationDialog action closure"
      pattern: "freezeService\\.freeze\\(\\)\\s*\\n\\s*WidgetCenter\\.shared\\.reloadAllTimelines"
---

<objective>
Close the one confirmed widget-reload gap in `StatsView` (D-08) by adding `WidgetCenter.shared.reloadAllTimelines()` immediately after `freezeService.freeze()`. Audit the remaining v2.0 goal-state mutation sites listed in RESEARCH.md §WID-02 to confirm transitive coverage via `GoalViewModel.reloadWidgetTimelines()`. Capture grep-based evidence in the SUMMARY. Human-verify on simulator.

Purpose: Completes WID-02 — `WidgetCenter.shared.reloadAllTimelines()` is called on all v2.0 goal state changes (check-in, freeze used, goal completed).
Output: One-line addition in `StatsView.swift` (+ `import WidgetKit` if missing); a 14-row evidence table in the SUMMARY; human-verified simulator check.
</objective>

<execution_context>
@/Users/kyleharrington/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kyleharrington/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/kyleharrington/Desktop/AI/Vitamin G/.planning/phases/24-widget-enhancements/24-CONTEXT.md
@/Users/kyleharrington/Desktop/AI/Vitamin G/.planning/phases/24-widget-enhancements/24-RESEARCH.md
@/Users/kyleharrington/Desktop/AI/Vitamin G/.planning/phases/24-widget-enhancements/24-PATTERNS.md
@/Users/kyleharrington/Desktop/AI/Vitamin G/VitaminG/VitaminG/VitaminG/Views/StatsView.swift
@/Users/kyleharrington/Desktop/AI/Vitamin G/VitaminG/VitaminG/VitaminG/ViewModels/GoalViewModel.swift

<interfaces>
<!-- The exact insertion site and the audit checklist the executor needs. -->

StatsView.swift current freeze handler (lines 100-112):
  .confirmationDialog(
      "Use your monthly streak freeze?",
      isPresented: $showFreezeConfirmation,
      titleVisibility: .visible
  ) {
      Button("Freeze Streak") {
          freezeService.freeze()
          // <-- INSERT: WidgetCenter.shared.reloadAllTimelines()
          viewModel.refresh(events: events, goals: goals, frozenDates: freezeService.frozenDates)
      }
      Button("Cancel", role: .cancel) {}
  } message: { ... }

StatsView.swift current imports (lines 1-2):
  import SwiftUI
  import SwiftData
  // <-- ADD `import WidgetKit` if not already present

GoalViewModel.reloadWidgetTimelines() (private, GoalViewModel.swift line ~361):
  private func reloadWidgetTimelines() {
      WidgetCenter.shared.reloadAllTimelines()
  }
  // Called by addGoal, updateGoal, addCheckIn, toggleCompletion, delete, updatePublicStatus.
  // Transitive call sites in views (GoalDetailView, GoalListView, GoalGifterCard, StuckDayGiftsSection)
  // invoke these viewModel methods — coverage is inherited.

Audit table (from RESEARCH.md §WID-02 — all 14 sites; only StatsView freeze is the active gap):
  | Site                                     | Coverage path                           | Gap? |
  | GoalViewModel.addGoal()                  | reloadWidgetTimelines (direct)          | None |
  | GoalViewModel.addGoal(input:)            | reloadWidgetTimelines (direct)          | None |
  | GoalViewModel.addCheckIn()               | reloadWidgetTimelines (direct)          | None |
  | GoalViewModel.toggleCompletion()         | reloadWidgetTimelines (direct)          | None |
  | GoalViewModel.updateGoal()               | reloadWidgetTimelines (direct)          | None |
  | GoalViewModel.updateGoal(input:)         | reloadWidgetTimelines (direct)          | None |
  | GoalViewModel.delete()                   | reloadWidgetTimelines (direct)          | None |
  | GoalViewModel.updateGoalPublicStatus()   | WidgetCenter direct call                | None |
  | StatsView freeze handler                 | MISSING                                 | YES (D-08) — THIS PLAN |
  | GoalGifterCard.addGiftedGoal()           | goalVM.addGoal -> reloadWidgetTimelines | None (transitive) |
  | StuckDayGiftsSection.addStuckDayGift()   | goalVM.addGoal -> reloadWidgetTimelines | None (transitive) |
  | GoalDetailView check-in                  | viewModel.addCheckIn -> reload          | None (transitive) |
  | GoalListView toggle completion           | viewModel.toggleCompletion -> reload    | None (transitive) |
  | GoalDetailView toggle completion         | viewModel.toggleCompletion -> reload    | None (transitive) |
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Insert WidgetCenter.shared.reloadAllTimelines() in StatsView freeze handler + audit grep evidence</name>
  <files>VitaminG/VitaminG/VitaminG/Views/StatsView.swift</files>
  <read_first>
    - VitaminG/VitaminG/VitaminG/Views/StatsView.swift (current freeze handler at line 105-108; current imports at lines 1-2)
    - VitaminG/VitaminG/VitaminG/ViewModels/GoalViewModel.swift (lines 361-363 for reloadWidgetTimelines pattern; lines 125, 152, 215, 294, 314, 340, 346, 354 for direct call sites)
    - VitaminG/VitaminG/VitaminG/Views/Explore/GoalGifterCard.swift (transitive coverage — verify it calls goalVM.addGoal)
    - VitaminG/VitaminG/VitaminG/Views/Explore/StuckDayGiftsSection.swift (transitive coverage — verify it calls goalVM.addGoal)
    - VitaminG/VitaminG/VitaminG/Views/GoalDetailView.swift (transitive coverage — verify check-in and toggleCompletion call viewModel methods)
    - VitaminG/VitaminG/VitaminG/Views/GoalListView.swift (transitive coverage — verify toggleCompletion call)
    - .planning/phases/24-widget-enhancements/24-RESEARCH.md §WID-02 Mutation Site Audit
  </read_first>
  <action>
    Step 1 — Edit `StatsView.swift`: If `import WidgetKit` is not on line 3 (current file has only `import SwiftUI` and `import SwiftData`), add `import WidgetKit` as line 3. In the `Button("Freeze Streak")` action closure inside `confirmationDialog`, insert the single line `WidgetCenter.shared.reloadAllTimelines()` between `freezeService.freeze()` and `viewModel.refresh(events: events, goals: goals, frozenDates: freezeService.frozenDates)`. Do not change `StreakFreezeService.swift` — it must remain pure Foundation per D-08.

    Step 2 — Audit evidence collection. Run these greps from the project root and capture each command's output for the SUMMARY:
    - `grep -n "reloadWidgetTimelines\\|WidgetCenter.shared.reloadAllTimelines" "VitaminG/VitaminG/VitaminG/ViewModels/GoalViewModel.swift"` — expect ≥ 8 matches (one per mutation method + the private helper definition).
    - `grep -n "goalVM.addGoal\\|viewModel.addGoal" "VitaminG/VitaminG/VitaminG/Views/Explore/GoalGifterCard.swift" "VitaminG/VitaminG/VitaminG/Views/Explore/StuckDayGiftsSection.swift"` — expect ≥ 2 matches confirming transitive coverage.
    - `grep -n "viewModel.addCheckIn\\|viewModel.toggleCompletion" "VitaminG/VitaminG/VitaminG/Views/GoalDetailView.swift" "VitaminG/VitaminG/VitaminG/Views/GoalListView.swift"` — expect ≥ 3 matches confirming view-layer transitive coverage.
    - `grep -rn "WidgetCenter.shared.reloadAllTimelines\\|reloadWidgetTimelines" "VitaminG/VitaminG/VitaminG/"` (excluding tests) — full inventory; the SUMMARY must reproduce the 14-row audit table from RESEARCH.md with current grep line numbers filled in.
    - `grep -n "import " "VitaminG/VitaminG/VitaminG/Services/StreakFreezeService.swift"` — expect ONLY `import Foundation` (Pitfall — service stays pure).

    Step 3 — Record findings in the SUMMARY as a 14-row markdown table with columns: Site | File:Line | Mutation | Reload Path | Gap?. Mark the StatsView row as `None (CLOSED)` post-fix. If ANY row reveals a previously-unknown gap not in RESEARCH.md's audit, STOP and flag back to the planner — do not silently add fixes that were not in the locked D-08/D-09 scope.
  </action>
  <verify>
    <automated>cd "/Users/kyleharrington/Desktop/AI/Vitamin G/VitaminG/VitaminG" &amp;&amp; xcodebuild build -scheme VitaminG -destination 'platform=iOS Simulator,name=iPhone 16' 2>&amp;1 | tail -10 &amp;&amp; grep -c "WidgetCenter.shared.reloadAllTimelines()" "VitaminG/VitaminG/VitaminG/Views/StatsView.swift"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "^import WidgetKit" "VitaminG/VitaminG/VitaminG/Views/StatsView.swift"` returns exactly one match.
    - `grep -n "WidgetCenter.shared.reloadAllTimelines" "VitaminG/VitaminG/VitaminG/Views/StatsView.swift"` returns exactly one match.
    - The sequence `freezeService.freeze()` followed by `WidgetCenter.shared.reloadAllTimelines()` followed by `viewModel.refresh(` appears in StatsView.swift in that exact order. Verify by running: `awk '/freezeService.freeze\\(\\)/{found=1} found &amp;&amp; /WidgetCenter.shared.reloadAllTimelines/{print NR; exit}' "VitaminG/VitaminG/VitaminG/Views/StatsView.swift"` — must print a line number greater than the line number returned by `grep -n "freezeService.freeze()" "VitaminG/VitaminG/VitaminG/Views/StatsView.swift"`.
    - `grep -c "import " "VitaminG/VitaminG/VitaminG/Services/StreakFreezeService.swift"` filtered with `| grep -v '^#'` returns exactly 1 (Foundation only — service purity guarded).
    - `grep -n "import " "VitaminG/VitaminG/VitaminG/Services/StreakFreezeService.swift" | grep -v '^#' | grep -v "import Foundation"` returns 0 lines.
    - `xcodebuild build -scheme VitaminG -destination 'platform=iOS Simulator,name=iPhone 16'` exits 0.
    - `xcodebuild test ... -only-testing:VitaminGTests/Phase24WidgetDataProviderTests` still exits 0 (no regression on Plan 24-01).
    - SUMMARY contains a 14-row audit table matching RESEARCH.md §WID-02 with the StatsView row updated to `None (CLOSED — D-08)`.
  </acceptance_criteria>
  <done>StatsView freeze handler calls WidgetCenter.shared.reloadAllTimelines() between freeze() and viewModel.refresh(); WidgetKit imported; StreakFreezeService still pure Foundation; 14-row audit table recorded; project builds.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification — freeze a streak and confirm widget reloads without app relaunch</name>
  <what-built>One-line widget-reload fix in StatsView freeze handler closing the WID-02 gap.</what-built>
  <how-to-verify>
    1. Build and run the app on iPhone 16 simulator (Cmd+R). Wait for launch.
    2. Add the systemMedium "Goals" widget to the simulator home screen (if not already added from Plan 24-02 verification).
    3. In the app, ensure you have at least one Immediate-tier goal with `durationDays = 7`. Check in for at least 1 day so a positive streak (≥ 1) is visible on the Home tab and widget. Background the app and confirm the widget shows the streak count.
    4. Re-foreground the app, navigate to Stats (from the Home tab). Tap the streak freeze affordance (the UI that triggers `showFreezeConfirmation = true`) — confirm the alert "Use your monthly streak freeze?" appears.
    5. Tap "Freeze Streak". The confirmation dialog dismisses.
    6. Without quitting or relaunching the app, background it (swipe up to home screen). Within ~5 seconds, observe the widget on the home screen: the streak count and/or goal title/progress must reflect the post-freeze state (i.e., the widget data has refreshed — at minimum, the timeline has been reloaded; visually, if frozen days extend the visible streak, the count may persist; either way, the widget displays current data with no app relaunch required).
    7. Optional cross-check: open the app again, navigate to Stats; confirm `freezeService.frozenDates` now contains today's date (or the missed date). Background again; confirm widget still displays without staleness.
    8. As an additional smoke test for the audit, perform a goal check-in via Goal Detail (Phase 18 path) and confirm the widget reflects the new completionEvents count (progress bar widens). This validates the transitive coverage from `GoalViewModel.addCheckIn`.
  </how-to-verify>
  <read_first>
    - .planning/phases/24-widget-enhancements/24-VALIDATION.md §Manual-Only Verifications
    - .planning/phases/24-widget-enhancements/24-RESEARCH.md §WID-02 Mutation Site Audit
  </read_first>
  <action>Run the app on iPhone 16 simulator, walk through steps 1-8. Capture a screenshot before the freeze (showing pre-freeze widget state) and a screenshot after the freeze (showing post-freeze widget state) and save both to `.planning/phases/24-widget-enhancements/screenshots/24-03-freeze-{before,after}.png`. If the widget does NOT update without app relaunch, document the failure precisely and block — do not type "approved". A failure here means D-08 did not actually wire the reload (or the WidgetCenter import is missing); re-check Task 1's edits.</action>
  <verify>
    <human-check>All 8 verification steps pass; before/after screenshots saved; verifier types "approved".</human-check>
  </verify>
  <acceptance_criteria>
    - 2 screenshots exist under `.planning/phases/24-widget-enhancements/screenshots/24-03-freeze-before.png` and `.planning/phases/24-widget-enhancements/screenshots/24-03-freeze-after.png`.
    - Human verifier confirms the widget reflected the streak-freeze action without an app relaunch.
    - Human verifier confirms step 8 — a goal check-in also updates the widget (transitive path verified).
  </acceptance_criteria>
  <resume-signal>Type "approved" once steps 1-8 pass and both screenshots are saved, or describe the failed step (step number, expected, actual).</resume-signal>
  <done>Widget reload confirmed visually after streak freeze and after goal check-in on iPhone 16 simulator; both screenshots committed under `.planning/phases/24-widget-enhancements/screenshots/`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App MainActor -> WidgetKit | `WidgetCenter.shared.reloadAllTimelines()` is a system API; signals widget process to invalidate the cached timeline and re-run `getTimeline()`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-08 | Tampering | Adding WidgetKit imports to `StreakFreezeService` would couple a Foundation service to a UI framework, violating the project architecture | mitigate | Acceptance criterion enforces `grep` returns ONLY `import Foundation` in `StreakFreezeService.swift`. The reload call is intentionally placed in the View layer (StatsView) per D-08. |
| T-24-09 | Denial of Service | Excessive `reloadAllTimelines()` calls hit the WidgetKit refresh budget | accept | WidgetKit internally throttles refresh requests; one call per freeze action is well within budget. The push-only architecture (`policy: .never`) means widgets never self-poll, so the budget is reserved for app-driven mutations. |
| T-24-10 | Repudiation | Silent failure to wire reload — user freezes streak but widget never updates, creating a perception bug | mitigate | Human verification (Task 2 step 6) is BLOCKING and explicitly tests the no-app-relaunch path. Audit grep evidence (Task 1) provides programmatic proof that the reload call exists at the correct site. |
| T-24-SC | Tampering | npm/pip/cargo installs | accept | No package installs. iOS-only project; system frameworks only. |
</threat_model>

<verification>
- `StatsView.swift` line containing `freezeService.freeze()` is immediately followed by `WidgetCenter.shared.reloadAllTimelines()`.
- `StatsView.swift` imports WidgetKit.
- `StreakFreezeService.swift` still imports ONLY Foundation.
- All 14 audit sites from RESEARCH.md confirmed: 1 closed gap (StatsView), 13 already covered.
- Human verification: freezing a streak triggers widget refresh without app relaunch.
- Human verification: goal check-in also triggers widget refresh (transitive coverage spot-check).
</verification>

<success_criteria>
- WID-02 complete: every v2.0 goal-state mutation site calls `WidgetCenter.shared.reloadAllTimelines()` directly or transitively via `GoalViewModel.reloadWidgetTimelines()`.
- The previously-missing StatsView freeze site now invokes the reload.
- Service layer (`StreakFreezeService`) remains pure Foundation — no architectural drift.
- Audit evidence captured in the SUMMARY for future verification.
</success_criteria>

<output>
Create `/Users/kyleharrington/Desktop/AI/Vitamin G/.planning/phases/24-widget-enhancements/24-03-SUMMARY.md` when done.
</output>
