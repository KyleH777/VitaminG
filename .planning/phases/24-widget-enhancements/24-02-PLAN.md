---
phase: 24-widget-enhancements
plan: 02
type: execute
wave: 2
depends_on:
  - 24-01
files_modified:
  - VitaminG/VitaminG/VitaminGWidget/GoalSummaryWidget.swift
autonomous: false
requirements:
  - WID-01
tags:
  - widgetkit
  - swiftui
  - widget-view
must_haves:
  truths:
    - "The systemMedium GoalSummaryWidget visually displays a flame icon, streak count, and 'day streak' label in the top row when globalStreak > 0."
    - "The bottom row of GoalSummaryWidget shows the active goal title (1 line, truncated tail) plus a thin 4pt linear progress bar when activeGoalTitle != nil and activeGoalProgress != nil."
    - "When activeGoalTitle != nil but activeGoalProgress == nil (open-ended goal), the title shows without any progress bar rendered."
    - "When activeGoalTitle == nil, the bottom row shows 'Add your first goal' in .caption + .secondary foreground."
    - "When globalStreak == 0 AND activeGoalTitle == nil, the top row shows 'Start your streak' in .caption."
    - "The widget gallery preview renders the placeholder state (streak: 7, goal: 'Meditate for 10 minutes', bar at ~43%)."
    - "The 4-tier row layout (TierRowView) is removed entirely; only the new equal-split layout renders."
    - "The widget displayName remains 'Goals'; the description reads 'Your active goal and current streak at a glance.'"
    - "A human verifier confirms the new GoalSummaryWidget layout on iPhone 16 simulator (widget gallery + home screen states)."
  artifacts:
    - path: "VitaminG/VitaminG/VitaminGWidget/GoalSummaryWidget.swift"
      provides: "Redesigned GoalSummaryWidgetView with equal-split streak + active goal layout; TierRowView removed"
      contains: "activeGoalTitle"
  key_links:
    - from: "GoalSummaryWidgetView.body"
      to: "entry.displayData.activeGoalTitle, entry.displayData.activeGoalProgress, entry.displayData.globalStreak"
      via: "WidgetDisplayData (from Plan 24-01)"
      pattern: "entry\\.displayData\\.activeGoalTitle"
    - from: "Capsule fills"
      to: "VGTheme.accentTerra"
      via: "import VGTheme color token"
      pattern: "VGTheme\\.accentTerra"
---

<objective>
Redesign `GoalSummaryWidgetView` from the legacy 4-tier row layout to the approved equal-split layout (D-01 + 24-UI-SPEC.md): top row = flame + streak count + "day streak"; bottom row = active goal title + optional thin 4pt linear progress bar. Replace inline `UIColor { t in ... }` literal with `VGTheme.accentTerra`. Update widget `.description` copy. Delete the `TierRowView` private struct entirely (Pitfall 4).

Purpose: This is the user-facing visual deliverable for WID-01. Reads the fields populated by Plan 24-01.
Output: Updated `GoalSummaryWidget.swift` with new view body, removed `TierRowView`, new description string; human-verified visual on simulator.
</objective>

<execution_context>
@/Users/kyleharrington/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kyleharrington/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/kyleharrington/Desktop/AI/Vitamin G/.planning/phases/24-widget-enhancements/24-CONTEXT.md
@/Users/kyleharrington/Desktop/AI/Vitamin G/.planning/phases/24-widget-enhancements/24-UI-SPEC.md
@/Users/kyleharrington/Desktop/AI/Vitamin G/.planning/phases/24-widget-enhancements/24-PATTERNS.md
@/Users/kyleharrington/Desktop/AI/Vitamin G/VitaminG/VitaminG/VitaminGWidget/GoalSummaryWidget.swift
@/Users/kyleharrington/Desktop/AI/Vitamin G/VitaminG/VitaminG/VitaminGWidget/StreakWidget.swift

<interfaces>
<!-- Authoritative contracts the executor needs. Do NOT re-derive. -->

WidgetDisplayData (after Plan 24-01):
  let tierRows: [TierRow]                  // still present — DO NOT render in this view
  let globalStreak: Int
  let activeGoalTitle: String?             // NEW from Plan 24-01
  let activeGoalProgress: Double?          // NEW from Plan 24-01 — nil = no progress bar

GoalEntry (from WidgetTimelineEntry.swift):
  let date: Date
  let displayData: WidgetDisplayData

VGTheme.accentTerra (from VGTheme.swift line ~120):
  static let accentTerra: Color   // adaptive: #C4673A light / #FF8A5C dark — use directly; do NOT inline UIColor literal

GoalSummaryProvider (existing — DO NOT modify):
  func placeholder(in:) -> GoalEntry         { GoalEntry(date: .now, displayData: .placeholder) }
  func getSnapshot(in:completion:)            { completion(GoalEntry(date: .now, displayData: .placeholder)) }
  func getTimeline(in:completion:)            // SwiftData fetch — unchanged; uses Timeline(entries:, policy: .never)

GoalSummaryWidget.configuration:
  StaticConfiguration(kind: "GoalSummaryWidget", provider: GoalSummaryProvider()) { GoalSummaryWidgetView(entry:) }
    .configurationDisplayName("Goals")
    .description("...")    // <-- CHANGE THIS STRING only
    .supportedFamilies([.systemMedium])
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Redesign GoalSummaryWidgetView body + remove TierRowView + update description</name>
  <files>VitaminG/VitaminG/VitaminGWidget/GoalSummaryWidget.swift</files>
  <read_first>
    - VitaminG/VitaminG/VitaminGWidget/GoalSummaryWidget.swift (current file — entire body and TierRowView struct will be replaced/removed)
    - VitaminG/VitaminG/VitaminGWidget/StreakWidget.swift (analog for accessibility + .containerBackground + flame.fill pattern)
    - VitaminG/VitaminG/VitaminG/VGTheme.swift (confirm VGTheme.accentTerra exists)
    - .planning/phases/24-widget-enhancements/24-UI-SPEC.md (Layout Contract + States table + Copywriting Contract)
    - .planning/phases/24-widget-enhancements/24-PATTERNS.md (GoalSummaryWidgetView pattern + Pitfall 4/6 callouts)
  </read_first>
  <behavior>
    - State "Nominal" (globalStreak > 0, activeGoalTitle != nil, activeGoalProgress != nil): renders 2-row VStack — flame.fill + streak + "day streak" on top; goal title + 4pt Capsule bar on bottom.
    - State "No duration" (activeGoalProgress == nil): renders title only, NO progress bar (sentinel-driven — Capsule not in view tree).
    - State "No active goals" (activeGoalTitle == nil, streak > 0): bottom row shows "Add your first goal" in .caption + .secondary.
    - State "Empty / new user" (streak == 0 AND activeGoalTitle == nil): top row shows "Start your streak"; bottom shows "Add your first goal".
    - State "Placeholder/snapshot": widget gallery renders with `WidgetDisplayData.placeholder` — streak 7 + goal "Meditate for 10 minutes" + bar at 43%.
    - Removed: `TierRowView` struct does not exist after edit.
    - Removed: inline `Color(uiColor: UIColor { t in ... })` literal replaced by `VGTheme.accentTerra`.
  </behavior>
  <action>Replace the entire body of `GoalSummaryWidgetView` with a `VStack(alignment: .leading, spacing: 8)` containing: (1) a private computed `streakRow` `some View` returning an HStack(spacing: 4) — when globalStreak > 0: `Image(systemName: "flame.fill").foregroundStyle(VGTheme.accentTerra).font(.caption)` + `Text("\(entry.displayData.globalStreak)").font(.title2.bold().monospacedDigit())` + `Text("day streak").font(.caption).fontWeight(.semibold)` + `Spacer()`; when globalStreak == 0: `Text("Start your streak").font(.caption)` + `Spacer()`; entire HStack uses `.accessibilityElement(children: .combine)` + `.accessibilityLabel(streak > 0 ? "\(streak) day streak" : "Start your streak")`. (2) `if let title = entry.displayData.activeGoalTitle`: call a private `activeGoalRow(title:progress:)` returning a `VStack(alignment: .leading, spacing: 4)` with `Text(title).font(.subheadline).fontWeight(.semibold).lineLimit(1).truncationMode(.tail)` and `if let p = progress { GeometryReader { geo in ZStack(alignment: .leading) { Capsule().fill(VGTheme.accentTerra.opacity(0.20)).frame(height: 4); Capsule().fill(VGTheme.accentTerra).frame(width: geo.size.width * p, height: 4) } }.frame(height: 4).accessibilityHidden(true) }` — the `.frame(height: 4)` AFTER `GeometryReader` is mandatory (Pitfall 6). The activeGoalRow uses `.accessibilityElement(children: .combine)` + `.accessibilityLabel(progress != nil ? "\(title), \(Int((progress ?? 0) * 100))% complete" : title)`. (3) `else`: `Text("Add your first goal").font(.caption).foregroundStyle(.secondary).accessibilityLabel("Add your first goal")`. Root view modifiers: `.padding(12)` then `.containerBackground(.fill.tertiary, for: .widget)` (must be last). DELETE the entire `private struct TierRowView` block (lines 97-133 of the current file) — Pitfall 4. In `GoalSummaryWidget.body`, change `.description("See your top goal for each tier at a glance.")` to `.description("Your active goal and current streak at a glance.")`. Leave `GoalSummaryProvider`, `WidgetContainerCache`, `kind`, `configurationDisplayName("Goals")`, and `.supportedFamilies([.systemMedium])` UNCHANGED — Pitfall: do NOT add `.widgetAccentable()` to either Capsule fill (D-06).</action>
  <verify>
    <automated>cd "/Users/kyleharrington/Desktop/AI/Vitamin G/VitaminG/VitaminG" &amp;&amp; xcodebuild build -scheme VitaminG -destination 'platform=iOS Simulator,name=iPhone 16' 2>&amp;1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - File `VitaminG/VitaminG/VitaminGWidget/GoalSummaryWidget.swift` builds successfully (exit 0).
    - File DOES NOT contain `struct TierRowView` (Pitfall 4 — entirely removed).
    - File DOES NOT contain `ForEach(Array(entry.displayData.tierRows` (legacy loop removed).
    - File DOES NOT contain `Color(uiColor: UIColor { t in` (inline color literal removed).
    - File DOES NOT contain `.widgetAccentable()` anywhere inside `GoalSummaryWidgetView` (D-06).
    - File contains `VGTheme.accentTerra` at least twice (flame foreground + Capsule fill).
    - File contains `VStack(alignment: .leading, spacing: 8)` (root container per D-01).
    - File contains `Image(systemName: "flame.fill")`.
    - File contains `Text("day streak")`.
    - File contains `entry.displayData.activeGoalTitle`.
    - File contains `entry.displayData.activeGoalProgress`.
    - File contains `GeometryReader` followed within 30 lines by `.frame(height: 4)` (Pitfall 6 — GeometryReader height fix).
    - File contains `Capsule()` at least twice (track + fill).
    - File contains `Text("Add your first goal")`.
    - File contains `Text("Start your streak")`.
    - File contains `.containerBackground(.fill.tertiary, for: .widget)`.
    - File contains `.description("Your active goal and current streak at a glance.")` exactly.
    - File DOES NOT contain `.description("See your top goal for each tier at a glance.")` (old copy removed).
    - File still contains `GoalSummaryProvider` and `WidgetContainerCache` unchanged (verify by `grep -c "struct GoalSummaryProvider: TimelineProvider"` == 1).
    - File still contains `Timeline(entries: [entry], policy: .never)` (push-only policy unchanged — non-negotiable constraint).
    - `xcodebuild test ... -only-testing:VitaminGTests/WidgetDataProviderTests` and `... -only-testing:VitaminGTests/Phase24WidgetDataProviderTests` both still exit 0 (no data-layer regression).
  </acceptance_criteria>
  <done>GoalSummaryWidgetView renders new equal-split layout reading activeGoalTitle/activeGoalProgress; TierRowView removed; description copy updated; project builds; both widget data-layer test classes still pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human visual verification of GoalSummaryWidget on simulator</name>
  <what-built>The redesigned `GoalSummaryWidget` (systemMedium) with the new equal-split layout — flame + streak count + "day streak" on top, active goal title + optional 4pt progress bar on bottom — driven by the new `activeGoalTitle`/`activeGoalProgress` fields.</what-built>
  <how-to-verify>
    1. Open Xcode, select scheme `VitaminG`, destination `iPhone 16` simulator. Run the app (Cmd+R) — wait for launch.
    2. From the simulator home screen, long-press an empty area; tap "Edit Home Screen"; tap the `+` button; search "Vitamin G"; select the "Goals" widget; confirm the widget gallery preview shows: streak count `7`, "day streak" label, goal title "Meditate for 10 minutes", and a partially filled thin orange/terra progress bar (~43% full). Confirm the new description text reads "Your active goal and current streak at a glance."
    3. Add the systemMedium widget to the home screen. Return to the app; if no goals exist, create at least one Immediate-tier goal with durationDays = 7 and check it in 3 times to produce ~43% progress. Background the app. Confirm the home-screen widget reflects: correct streak count, correct goal title, and a progress bar at ~43% width.
    4. Delete that goal (or mark all goals complete) from the app; background the app; confirm the widget bottom row now shows "Add your first goal" in muted secondary text (no progress bar).
    5. In Settings > Developer > (or by deleting app data), reset to no goals and no streak; confirm the widget shows "Start your streak" on top and "Add your first goal" on bottom.
    6. Toggle the simulator to Dark Mode (Cmd+Shift+A or Features > Toggle Appearance) and confirm the flame icon + progress bar use the brighter `terraGlow #FF8A5C` color while remaining legible.
    7. Confirm the goal title is truncated with `…` (not wrapped) when the title is long (e.g., "Read 12 books this quarter while also walking 10000 steps").
    8. Confirm NO 4-tier row layout appears anywhere — the old TierRowView has been fully removed.
  </how-to-verify>
  <read_first>
    - .planning/phases/24-widget-enhancements/24-UI-SPEC.md §States: GoalSummaryWidgetView (the canonical state table)
    - .planning/phases/24-widget-enhancements/24-CONTEXT.md (D-01 layout decisions)
  </read_first>
  <action>Build and run on iPhone 16 simulator. Walk through the 8 verification steps above. Capture a screenshot of (a) the widget gallery preview, (b) the home-screen widget in the Nominal state, (c) the home-screen widget in the No-active-goals state, (d) the home-screen widget in Dark Mode. Save screenshots to `.planning/phases/24-widget-enhancements/screenshots/24-02-*.png`. If any step fails, document the failure precisely (state, expected, actual) and BLOCK — do not type "approved".</action>
  <verify>
    <human-check>All 8 verification steps pass; 4 screenshots saved; verifier types "approved".</human-check>
  </verify>
  <acceptance_criteria>
    - 4 screenshots exist under `.planning/phases/24-widget-enhancements/screenshots/24-02-*.png` (gallery, nominal, empty, dark mode).
    - Human verifier explicitly confirms each of the 8 steps; either "approved" OR a structured failure report citing the failed step number is recorded.
  </acceptance_criteria>
  <resume-signal>Type "approved" once all 8 steps pass, or describe the failed step (step number, expected behavior, actual behavior).</resume-signal>
  <done>All 8 visual checks pass on iPhone 16 simulator (light + dark); 4 screenshots committed under `.planning/phases/24-widget-enhancements/screenshots/`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WidgetDisplayData -> SwiftUI render | In-process pass of pre-sanitized goal title to widget view layer. No external input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-04 | Information Disclosure | Goal title displayed on home-screen widget visible to anyone holding the device | accept | Per CONTEXT.md scope, widget shows the user's own goal — same exposure as the lock screen `StreakWidget` already in production. Title already sanitized by `InputSanitizer.sanitize()` at insertion time (T-24-01). No PII beyond what the user authored. |
| T-24-05 | Denial of Service | `GeometryReader` without `.frame(height: 4)` consumes infinite vertical widget space (Pitfall 6) | mitigate | Acceptance criterion enforces `GeometryReader` is followed within 30 lines by `.frame(height: 4)`. Human visual check (Task 2) catches any layout overflow. |
| T-24-06 | Tampering | `.widgetAccentable()` neutralizes accentTerra color on lock-screen tinting (visual contract violation) | mitigate | Acceptance criterion explicitly forbids `.widgetAccentable()` inside `GoalSummaryWidgetView`. D-06 documents the deliberate choice. |
| T-24-07 | Tampering | Legacy `TierRowView` left in place double-renders the widget (Pitfall 4) | mitigate | Acceptance criterion `File DOES NOT contain "struct TierRowView"` — automated grep. Human visual check confirms only the new 2-row layout renders. |
| T-24-SC | Tampering | npm/pip/cargo installs | accept | No package installs. iOS-only project; system frameworks and in-project files only. |
</threat_model>

<verification>
- Project builds for `iPhone 16` simulator with no errors.
- Both `WidgetDataProviderTests` and `Phase24WidgetDataProviderTests` still pass (Plan 24-01 contract unbroken).
- `TierRowView` struct removed; legacy ForEach loop removed; inline UIColor literal removed.
- Human visual verification confirms all 5 widget states (Nominal, No duration, No active goals, Empty, Placeholder) and dark mode adaptation.
</verification>

<success_criteria>
- WID-01 visual slice complete: `GoalSummaryWidgetView` renders the equal-split layout per 24-UI-SPEC.md Layout Contract.
- All 5 documented states render correctly on simulator.
- Widget gallery preview is realistic (placeholder reflects sample goal + 43% progress).
- Description copy updated to "Your active goal and current streak at a glance."
- Dark mode rendering uses `terraGlow` (#FF8A5C) without manual color literals.
</success_criteria>

<output>
Create `/Users/kyleharrington/Desktop/AI/Vitamin G/.planning/phases/24-widget-enhancements/24-02-SUMMARY.md` when done.
</output>
