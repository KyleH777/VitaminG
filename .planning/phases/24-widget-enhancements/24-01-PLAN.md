---
phase: 24-widget-enhancements
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - VitaminG/VitaminG/VitaminG/Services/WidgetDataProvider.swift
  - VitaminG/VitaminG/VitaminGTests/Phase24WidgetDataProviderTests.swift
autonomous: true
requirements:
  - WID-01
tags:
  - widgetkit
  - swiftdata
  - widget-data
must_haves:
  truths:
    - "WidgetDataProvider.build() returns a WidgetDisplayData whose activeGoalTitle is the title of the highest-priority non-completed goal (Immediate > ShortTerm > LongTerm > LifeGoal)."
    - "When the selected active goal has a non-nil durationDays > 0, activeGoalProgress equals completionEvents.count / durationDays, clamped to 0.0...1.0."
    - "When the selected active goal has nil durationDays (or 0), activeGoalProgress is nil — the widget will then skip rendering the progress bar."
    - "When all goals are completed or no goals exist, activeGoalTitle and activeGoalProgress are both nil."
    - "WidgetDisplayData.placeholder carries activeGoalTitle: \"Meditate for 10 minutes\" and activeGoalProgress: 0.43 for a realistic widget gallery preview."
    - "WidgetDisplayData.empty carries nil for both new fields."
    - "Phase24WidgetDataProviderTests.swift exists in VitaminGTests with 7 test methods covering the behaviors above and passes locally on iPhone 16 simulator."
  artifacts:
    - path: "VitaminG/VitaminG/VitaminG/Services/WidgetDataProvider.swift"
      provides: "WidgetDisplayData struct extended with activeGoalTitle/activeGoalProgress; WidgetDataProvider.build() computes both"
      contains: "activeGoalTitle"
    - path: "VitaminG/VitaminG/VitaminGTests/Phase24WidgetDataProviderTests.swift"
      provides: "XCTest coverage for active goal selection + progress computation per D-03/D-04/D-05"
      contains: "Phase24WidgetDataProviderTests"
  key_links:
    - from: "VitaminG/VitaminG/VitaminG/Services/WidgetDataProvider.swift"
      to: "Goal.tier, Goal.isCompleted, Goal.creationDate, Goal.durationDays, Goal.completionEvents"
      via: "pure-Foundation read of in-memory arrays"
      pattern: "GoalTier.ordered.compactMap"
    - from: "VitaminG/VitaminG/VitaminGTests/Phase24WidgetDataProviderTests.swift"
      to: "WidgetDataProvider.build"
      via: "@testable import VitaminG"
      pattern: "WidgetDataProvider.build\\(goals:.+events:"
---

<objective>
Extend `WidgetDisplayData` with two optional fields (`activeGoalTitle: String?`, `activeGoalProgress: Double?`) and extend `WidgetDataProvider.build()` to compute them per D-03 (tier-priority + earliest creationDate) and D-04 (clamped progress, nil when no duration). Create `Phase24WidgetDataProviderTests.swift` (Wave 0 test scaffolding) covering all new logic.

Purpose: Establishes the pure-Foundation data contract that the redesigned `GoalSummaryWidgetView` (Plan 02) reads. Tests are co-located here so the data layer ships GREEN.
Output: Updated `WidgetDataProvider.swift` (single struct extension + computed fields in `build()`); new `Phase24WidgetDataProviderTests.swift` with 7 unit tests.
</objective>

<execution_context>
@/Users/kyleharrington/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kyleharrington/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/kyleharrington/Desktop/AI/Vitamin G/.planning/phases/24-widget-enhancements/24-CONTEXT.md
@/Users/kyleharrington/Desktop/AI/Vitamin G/.planning/phases/24-widget-enhancements/24-RESEARCH.md
@/Users/kyleharrington/Desktop/AI/Vitamin G/.planning/phases/24-widget-enhancements/24-PATTERNS.md
@/Users/kyleharrington/Desktop/AI/Vitamin G/.planning/phases/24-widget-enhancements/24-UI-SPEC.md
@/Users/kyleharrington/Desktop/AI/Vitamin G/VitaminG/VitaminG/VitaminG/Services/WidgetDataProvider.swift
@/Users/kyleharrington/Desktop/AI/Vitamin G/VitaminG/VitaminG/VitaminGTests/WidgetDataProviderTests.swift

<interfaces>
<!-- Authoritative contracts for executor. Do NOT re-derive — use as given. -->
<!-- Source: existing codebase (read once, verified before planning). -->

GoalTier.ordered (from Models/Goal.swift):
  static let ordered: [GoalTier] = [.immediate, .shortTerm, .longTerm, .lifeGoal]

Goal (SwiftData @Model — accessible without `import SwiftData` because Goal lives in the VitaminG module):
  var title: String
  var tier: GoalTier
  var isCompleted: Bool                          // direct, non-optional
  var creationDate: Date?                        // optional — use .distantPast fallback to match existing build() sort
  var durationDays: Int?                         // SchemaV10 line 70; nil = open-ended
  @Relationship var completionEvents: [SchemaV2.CompletionEvent]?   // optional relationship

WidgetDisplayData (current — must extend, NOT replace):
  struct TierRow { let tier: GoalTier; let topGoalTitle: String? }
  let tierRows: [TierRow]      // RETAIN — StreakWidgetView still reads this (Pitfall 5)
  let globalStreak: Int
  // ADD: let activeGoalTitle: String?
  // ADD: let activeGoalProgress: Double?
  static let placeholder = WidgetDisplayData(...)   // MUST be updated with new fields
  static let empty       = WidgetDisplayData(...)   // MUST be updated with new fields

WidgetDataProvider.build(goals:events:calendar:) -> WidgetDisplayData
  // Current signature unchanged — only the returned struct gains fields.

ModelContainerFactory.makeContainer(inMemory: true)   // used by existing WidgetDataProviderTests setUp

XCTest naming convention used in VitaminGTests/WidgetDataProviderTests.swift:
  final class WidgetDataProviderTests: XCTestCase
  override func setUpWithError() throws { container = try ModelContainerFactory.makeContainer(inMemory: true); context = ModelContext(container) }
  override func tearDownWithError() throws { container = nil; context = nil }
  func test_*<scenario>*() throws { ... XCTAssertEqual / XCTAssertNil / XCTAssertNotNil ... }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Phase24WidgetDataProviderTests.swift (Wave 0 RED stubs)</name>
  <files>VitaminG/VitaminG/VitaminGTests/Phase24WidgetDataProviderTests.swift</files>
  <read_first>
    - VitaminG/VitaminG/VitaminGTests/WidgetDataProviderTests.swift (mirror class/setUp/tearDown structure exactly)
    - VitaminG/VitaminG/VitaminG/Services/WidgetDataProvider.swift (current struct shape — tests will reference activeGoalTitle/activeGoalProgress that don't exist yet)
    - .planning/phases/24-widget-enhancements/24-VALIDATION.md (test IDs and -only-testing identifiers)
    - .planning/phases/24-widget-enhancements/24-PATTERNS.md (test method body templates)
  </read_first>
  <behavior>
    - Test 1 `test_activeGoal_immediateWinsOverShortTerm`: insert one .immediate goal (creationDate=1000) and one .shortTerm goal (creationDate=500); assert build(goals:events:).activeGoalTitle == "Immediate Goal" — tier priority beats creationDate.
    - Test 2 `test_activeGoal_earliestCreationDate`: insert two .immediate goals (creationDate=1000 and 2000); assert activeGoalTitle == "Older Goal" — within-tier tiebreak.
    - Test 3 `test_activeGoalProgress_clampedToUnit`: insert one .immediate goal with durationDays=2 plus 5 CompletionEvents; fetch via context; assert activeGoalProgress == 1.0 (clamped).
    - Test 4 `test_activeGoalProgress_nilWhenNoDuration`: insert one .immediate goal with durationDays=nil; assert activeGoalProgress == nil (sentinel — NOT 0.0).
    - Test 5 `test_activeGoal_nilWhenAllCompleted`: insert one .immediate goal with isCompleted=true; assert activeGoalTitle == nil AND activeGoalProgress == nil.
    - Test 6 `test_placeholder_hasActiveGoalFields`: read WidgetDisplayData.placeholder; assert activeGoalTitle == "Meditate for 10 minutes" AND activeGoalProgress == 0.43.
    - Test 7 `test_empty_hasNilActiveGoalFields`: read WidgetDisplayData.empty; assert activeGoalTitle == nil AND activeGoalProgress == nil.
  </behavior>
  <action>Create `VitaminGTests/Phase24WidgetDataProviderTests.swift` modeled exactly on `WidgetDataProviderTests.swift`: imports = `import XCTest`, `import SwiftData`, `@testable import VitaminG`. Class name = `Phase24WidgetDataProviderTests: XCTestCase`. setUpWithError/tearDownWithError must use `ModelContainerFactory.makeContainer(inMemory: true)` and `ModelContext(container)` — identical to the analog. Implement all 7 test methods listed in `<behavior>`. Goal construction uses `Goal(title: ..., tier: ..., durationDays: ...)` (SchemaV10 init); `CompletionEvent` constructed via `CompletionEvent(goal: goal)` and inserted via `context.insert(event)` before fetching. Tests will FAIL to compile until Task 2 adds the `activeGoalTitle`/`activeGoalProgress` fields — that is the expected RED state. Do NOT add fixture seed data or shared helpers; each test is self-contained per the analog file's style.</action>
  <verify>
    <automated>cd "/Users/kyleharrington/Desktop/AI/Vitamin G/VitaminG/VitaminG" &amp;&amp; xcodebuild build-for-testing -scheme VitaminG -destination 'platform=iOS Simulator,name=iPhone 16' 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File `VitaminG/VitaminG/VitaminGTests/Phase24WidgetDataProviderTests.swift` exists.
    - `grep -c "func test_" "VitaminG/VitaminG/VitaminGTests/Phase24WidgetDataProviderTests.swift"` returns 7.
    - File contains exact identifiers: `test_activeGoal_immediateWinsOverShortTerm`, `test_activeGoal_earliestCreationDate`, `test_activeGoalProgress_clampedToUnit`, `test_activeGoalProgress_nilWhenNoDuration`, `test_activeGoal_nilWhenAllCompleted`, `test_placeholder_hasActiveGoalFields`, `test_empty_hasNilActiveGoalFields`.
    - File contains `final class Phase24WidgetDataProviderTests: XCTestCase`.
    - File contains `@testable import VitaminG`.
    - Build is expected to FAIL with "value of type 'WidgetDisplayData' has no member 'activeGoalTitle'" — this RED state is the gate that Task 2 must turn GREEN. Capture the error in the SUMMARY for traceability.
  </acceptance_criteria>
  <done>RED test file committed with 7 test methods referencing `activeGoalTitle`/`activeGoalProgress`; build fails ONLY with the expected "no member" error on the new fields (no unrelated breakage).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend WidgetDisplayData + WidgetDataProvider.build() (GREEN)</name>
  <files>VitaminG/VitaminG/VitaminG/Services/WidgetDataProvider.swift</files>
  <read_first>
    - VitaminG/VitaminG/VitaminG/Services/WidgetDataProvider.swift (current file — must extend, not rewrite)
    - VitaminG/VitaminG/VitaminGTests/Phase24WidgetDataProviderTests.swift (from Task 1 — these tests define the contract)
    - VitaminG/VitaminG/VitaminG/Models/Goal.swift (confirm GoalTier.ordered = [.immediate, .shortTerm, .longTerm, .lifeGoal] and Goal property types)
    - .planning/phases/24-widget-enhancements/24-CONTEXT.md (D-03, D-04, D-05)
    - .planning/phases/24-widget-enhancements/24-PATTERNS.md (active goal selection snippet)
  </read_first>
  <action>Add two stored properties to `WidgetDisplayData` immediately after `globalStreak`: `let activeGoalTitle: String?` and `let activeGoalProgress: Double?`. Update both static instances to populate the new fields: `placeholder` adds `activeGoalTitle: "Meditate for 10 minutes", activeGoalProgress: 0.43`; `empty` adds `activeGoalTitle: nil, activeGoalProgress: nil`. RETAIN existing `tierRows` field unchanged (Pitfall 5 — StreakWidgetView depends on it). In `WidgetDataProvider.build()`, after the existing `tierRows` computation and before the `return`, compute `activeGoal` by mapping `GoalTier.ordered` to the earliest-creationDate non-completed goal per tier (compactMap returns the first tier with a hit), then `.first` to pick the highest-priority tier with any goal. Compute `activeGoalTitle = activeGoal?.title`. Compute `activeGoalProgress` as a closure: guard activeGoal + non-nil durationDays + duration > 0 (else return nil), then `min(1.0, Double(goal.completionEvents?.count ?? 0) / Double(duration))`. Update the `return` to pass both new fields. The file MUST retain `import Foundation` as its ONLY import per Pitfall 3 — do not add SwiftData/SwiftUI/WidgetKit. Implements D-03, D-04, D-05.</action>
  <verify>
    <automated>cd "/Users/kyleharrington/Desktop/AI/Vitamin G/VitaminG/VitaminG" &amp;&amp; xcodebuild test -scheme VitaminG -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:VitaminGTests/Phase24WidgetDataProviderTests 2>&amp;1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^import " "VitaminG/VitaminG/VitaminG/Services/WidgetDataProvider.swift"` returns 1 (Foundation only — Pitfall 3 guard).
    - `grep "import " "VitaminG/VitaminG/VitaminG/Services/WidgetDataProvider.swift"` returns exactly `import Foundation`.
    - File contains `let activeGoalTitle: String?`.
    - File contains `let activeGoalProgress: Double?`.
    - File contains `activeGoalTitle: "Meditate for 10 minutes"` (placeholder).
    - File contains `activeGoalProgress: 0.43` (placeholder).
    - File contains `activeGoalTitle: nil` and `activeGoalProgress: nil` (empty).
    - File contains `GoalTier.ordered.compactMap` (active goal selection per D-03).
    - File contains `min(1.0,` (progress clamp per D-04).
    - File contains `goal.completionEvents?.count ?? 0` (relationship-safe count).
    - File still contains `let tierRows: [TierRow]` (Pitfall 5 — not removed).
    - `xcodebuild test ... -only-testing:VitaminGTests/Phase24WidgetDataProviderTests` exits 0 with "Test Suite 'Phase24WidgetDataProviderTests' passed" — all 7 tests GREEN.
    - Existing `WidgetDataProviderTests` still passes when re-run (no regression): `xcodebuild test ... -only-testing:VitaminGTests/WidgetDataProviderTests` exits 0.
  </acceptance_criteria>
  <done>WidgetDisplayData has activeGoalTitle and activeGoalProgress fields; WidgetDataProvider.build() populates both per D-03/D-04; all 7 Phase24 tests pass; existing WidgetDataProviderTests still pass; file remains pure Foundation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SwiftData container -> WidgetDataProvider.build() | In-process read of Goal/CompletionEvent records from local SwiftData (App Group container). Records originate from app-side validated input (InputSanitizer) and are not externally writable. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-01 | Information Disclosure | `activeGoalTitle` surfaced in WidgetDisplayData | accept | Title is already sanitized at goal creation by `GoalViewModel.validate()` -> `InputSanitizer.sanitize()`; widget render path adds no new exposure surface (read-only, on-device). |
| T-24-02 | Tampering | `WidgetDataProvider` import contamination breaks pure-Foundation contract | mitigate | Acceptance criterion enforces `grep -c "^import "` == 1 and `grep "import"` returns exactly `import Foundation` — blocks SwiftData/SwiftUI/WidgetKit creep (Pitfall 3). |
| T-24-03 | Denial of Service | Integer division by zero when `goal.durationDays == 0` | mitigate | `guard duration > 0` in progress closure returns nil before division; covered implicitly by `test_activeGoalProgress_nilWhenNoDuration` (which exercises the nil path) and by acceptance criterion requiring `min(1.0, ...)` only after the guard. |
| T-24-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan. iOS-only project; system frameworks only. |
</threat_model>

<verification>
- Pure-Foundation contract preserved (single import line).
- All 7 Phase24 tests pass on iPhone 16 simulator.
- Existing WidgetDataProviderTests still pass (no regression to Plan 04's lock-screen widget path).
- `tierRows` field preserved in WidgetDisplayData (StreakWidgetView dependency intact).
</verification>

<success_criteria>
- WID-01 data-layer slice complete: WidgetDisplayData carries activeGoalTitle/activeGoalProgress; WidgetDataProvider.build() computes them per D-03 (Immediate > ShortTerm > LongTerm > LifeGoal, earliest creationDate within tier) and D-04 (clamped 0.0-1.0, nil when durationDays nil/0).
- Test stub file `Phase24WidgetDataProviderTests.swift` exists with 7 GREEN tests.
- `tierRows` field NOT removed from WidgetDisplayData (StreakWidgetView contract preserved).
- File `WidgetDataProvider.swift` still has only `import Foundation`.
</success_criteria>

<output>
Create `/Users/kyleharrington/Desktop/AI/Vitamin G/.planning/phases/24-widget-enhancements/24-01-SUMMARY.md` when done.
</output>
