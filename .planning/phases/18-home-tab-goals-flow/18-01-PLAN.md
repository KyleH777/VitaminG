---
phase: 18-home-tab-goals-flow
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - VitaminG/VitaminG/VitaminGTests/Phase18StreakEngineTests.swift
  - VitaminG/VitaminG/VitaminGTests/Phase18GoalViewModelTests.swift
  - VitaminG/VitaminG/VitaminGTests/Phase18GoalDayGridTests.swift
  - VitaminG/VitaminG/VitaminGTests/Phase18PremadeGoalsTests.swift
  - VitaminG/VitaminG/VitaminGTests/Phase18GoalInputTests.swift
  - VitaminG/VitaminG/VitaminGTests/Phase18QuoteBankTests.swift
autonomous: true
requirements: [HOME-01, HOME-02, GOAL2-01, GOAL2-02, GOAL2-04, GOAL2-05]

must_haves:
  truths:
    - "Test files exist that will fail until Wave 1 implementations land"
    - "Each Phase 18 testable requirement has at least one failing test stub asserting future behavior"
    - "Test suite compiles and runs without modification when implementation is added"
  artifacts:
    - path: "VitaminG/VitaminG/VitaminGTests/Phase18StreakEngineTests.swift"
      provides: "Failing tests for HOME-01 (overall app streak uses StreakEngine) and GOAL2-05 (flame threshold at 3 consecutive days)"
    - path: "VitaminG/VitaminG/VitaminGTests/Phase18GoalViewModelTests.swift"
      provides: "Failing tests for GOAL2-04 (addCheckIn creates CompletionEvent without flipping isCompleted; same-day dedup)"
    - path: "VitaminG/VitaminG/VitaminGTests/Phase18GoalDayGridTests.swift"
      provides: "Failing tests for GOAL2-05 (filled/empty cell logic, Monday-first weekday offset)"
    - path: "VitaminG/VitaminG/VitaminGTests/Phase18PremadeGoalsTests.swift"
      provides: "Failing tests for GOAL2-02 (33 goals across 7 categories, no empty titles)"
    - path: "VitaminG/VitaminG/VitaminGTests/Phase18GoalInputTests.swift"
      provides: "Failing tests for GOAL2-01 (durationDays round-trips through GoalInput -> Goal)"
    - path: "VitaminG/VitaminG/VitaminGTests/Phase18QuoteBankTests.swift"
      provides: "Failing tests for HOME-02 (VGQuoteBank.all flattens all categories; daily rotation deterministic by day-of-year)"
  key_links:
    - from: "Phase 18 plans 02-05"
      to: "Phase18*Tests.swift"
      via: "xcodebuild test invocations"
      pattern: "VitaminGTests/Phase18"
---

<objective>
Create Wave 0 failing test scaffolds for every automated requirement in Phase 18. These tests will fail until subsequent plans (02-05) land implementations, providing Nyquist feedback during execution.

Purpose: Without test stubs, the `<verify>` blocks in later plans have nothing to fail against. This plan creates the assertion surface area required by VALIDATION.md Wave 0 Requirements.
Output: 6 XCTest files in `VitaminGTests/` that compile, fail as expected (red), and become green as Plans 02–05 land their changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-home-tab-goals-flow/18-CONTEXT.md
@.planning/phases/18-home-tab-goals-flow/18-RESEARCH.md
@.planning/phases/18-home-tab-goals-flow/18-PATTERNS.md
@.planning/phases/18-home-tab-goals-flow/18-VALIDATION.md
@CLAUDE.md
</context>

<interfaces>
<!-- Existing test patterns to mirror — extracted from the codebase. -->

From `VitaminG/VitaminG/VitaminGTests/StreakEngineTests.swift` (existing test conventions):
- Uses `import XCTest`, `@testable import VitaminG`
- Classes named `final class XxxTests: XCTestCase`
- Test methods prefixed `test...`
- SwiftData in-memory container: `ModelConfiguration(isStoredInMemoryOnly: true)` (see `SchemaV6Tests.swift` for pattern)

From `VitaminG/VitaminG/VitaminG/Services/StreakEngine.swift`:
- `static func currentStreak(from events: [CompletionEvent], tier: GoalTier? = nil, calendar: Calendar = .current) -> Int`
- Returns count of consecutive days ending today (or 0 if today missed)

From `VitaminG/VitaminG/VitaminG/Models/GoalCategory.swift`:
- `var suggestions: [String]` per case
- `static var allCases: [GoalCategory]`

From `VitaminG/VitaminG/VitaminG/Services/VGQuoteBank.swift`:
- Existing per-category static arrays (no flattened `all` yet — Plan 02 adds it)

From `VitaminG/VitaminG/VitaminG/ViewModels/GoalViewModel.swift`:
- `@Observable @MainActor final class GoalViewModel`
- Existing `func toggleCompletion(goal: Goal, context: ModelContext)` — Plan 02 adds sibling `addCheckIn(for:context:)`

From `VitaminG/VitaminG/VitaminG/Models/GoalInput.swift`:
- Struct with title, tier, frequency, etc. — Plan 02 adds `durationDays: Int?`
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Phase18StreakEngineTests + Phase18GoalViewModelTests + Phase18GoalInputTests</name>
  <files>
    VitaminG/VitaminG/VitaminGTests/Phase18StreakEngineTests.swift,
    VitaminG/VitaminG/VitaminGTests/Phase18GoalViewModelTests.swift,
    VitaminG/VitaminG/VitaminGTests/Phase18GoalInputTests.swift
  </files>
  <read_first>
    VitaminG/VitaminG/VitaminGTests/StreakEngineTests.swift,
    VitaminG/VitaminG/VitaminGTests/GoalViewModelTests.swift,
    VitaminG/VitaminG/VitaminGTests/SchemaV6Tests.swift,
    VitaminG/VitaminG/VitaminG/Services/StreakEngine.swift,
    VitaminG/VitaminG/VitaminG/ViewModels/GoalViewModel.swift,
    VitaminG/VitaminG/VitaminG/Models/GoalInput.swift,
    VitaminG/VitaminG/VitaminG/Models/SchemaV6.swift,
    .planning/phases/18-home-tab-goals-flow/18-VALIDATION.md
  </read_first>
  <action>
    Create three XCTest files mirroring the existing `StreakEngineTests.swift` and `GoalViewModelTests.swift` conventions. Per D-11, D-12, D-13, and Pitfall 2/3 in RESEARCH.md.

    File 1 — `Phase18StreakEngineTests.swift` (final class `Phase18StreakEngineTests: XCTestCase`):
    - `test_overallAppStreak_usesStreakEngineNotMaxCount` — given two goals (Goal A with 47 sporadic events spread across non-consecutive days, Goal B with 3 consecutive recent events), assert `StreakEngine.currentStreak(from: allEvents)` returns the consecutive-day count (3 in this fixture), NOT the max event count (47). Tests HOME-01 bug fix.
    - `test_perGoalStreak_returnsZeroForFutureGap` — events from 5 days ago through 2 days ago with no event today returns 0 (streak broken). Tests GOAL2-05 flame display gating.
    - `test_perGoalStreak_returnsAtLeast3_whenThreeConsecutiveDaysEndingToday` — events for today, yesterday, day-before returns 3 or more (matches flame threshold D-11).
    - `test_perGoalStreak_returnsLessThan3_whenOnlyTwoConsecutiveDays` — events for today + yesterday only returns less than 3 (flame should NOT display).

    File 2 — `Phase18GoalViewModelTests.swift` (final class `Phase18GoalViewModelTests: XCTestCase`):
    - Set up in-memory ModelContainer with `Schema(versionedSchema: SchemaV6.self)` and `ModelConfiguration(isStoredInMemoryOnly: true)` matching the pattern in `SchemaV6Tests.swift`.
    - `test_addCheckIn_createsCompletionEventForGoal` — call `viewModel.addCheckIn(for: goal, context: context)`, assert `goal.completionEvents?.count == 1` and the event's `completedAt` is within 1 second of `Date()`.
    - `test_addCheckIn_doesNotSetIsCompletedFlag` — call addCheckIn; assert `goal.isCompleted == false` afterwards. Tests Pitfall 2 (check-in vs complete conflation).
    - `test_addCheckIn_isIdempotentWithinSameCalendarDay` — call addCheckIn twice in succession; assert `goal.completionEvents?.count == 1` (second call no-ops). Tests Pitfall 3 (same-day duplicate prevention).
    - `test_addCheckIn_allowsSecondCheckInOnNextDay` — insert a CompletionEvent dated yesterday directly; call addCheckIn; assert total events count is 2. Confirms guard is calendar-day scoped, not session scoped.

    File 3 — `Phase18GoalInputTests.swift` (final class `Phase18GoalInputTests: XCTestCase`):
    - `test_goalInput_carriesDurationDays` — instantiate `GoalInput` with `durationDays: 30`, assert the property reads back 30. Tests GOAL2-01 field plumbing.
    - `test_goalInput_durationDaysIsOptional_defaultsToNil` — instantiate `GoalInput` without `durationDays`, assert it equals nil. Confirms additive nature.
    - `test_goalInput_durationDays_persistsToGoalModel` — pass GoalInput with `durationDays: 60` to whatever Goal constructor / GoalViewModel.addGoal sink the executor wires up; fetch the resulting Goal; assert `goal.durationDays == 60`.

    All tests MUST currently FAIL or fail to compile — they reference fields and methods Plan 02 adds. Comment at top of each file: `// Phase 18 Wave 0 — RED tests. Will go GREEN as Plan 02 lands implementation.`

    Do NOT yet add `durationDays` field anywhere — those changes happen in Plan 02. The compile errors are intentional Wave 0 RED state. When Plan 02 lands, these tests turn green.

    File naming MUST be `Phase18*Tests.swift` (not generic names) so the Xcode test target can scope to Phase 18 work with `-only-testing:VitaminGTests/Phase18StreakEngineTests` etc.

    Add files to the Xcode project's VitaminGTests target. If editing `project.pbxproj` is required, follow the same membership pattern as `StreakEngineTests.swift` — the same UUID/PBXFileReference structure.
  </action>
  <verify>
    <automated>cd "/Users/kyleharrington/Desktop/AI/Vitamin G/VitaminG/VitaminG" && xcodebuild build -scheme VitaminG -destination 'platform=iOS Simulator,name=iPhone 16' 2>&1 | tail -20 && echo "---RED-STATE-CHECK---" && xcodebuild test -scheme VitaminG -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:VitaminGTests/Phase18StreakEngineTests -only-testing:VitaminGTests/Phase18GoalViewModelTests -only-testing:VitaminGTests/Phase18GoalInputTests 2>&1 | tail -30 ; echo "Exit code: $? (nonzero = RED, expected at Wave 0)"</automated>
  </verify>
  <acceptance_criteria>
    - Files exist at the three paths in `<files>`.
    - Each file contains `final class Phase18*Tests: XCTestCase`.
    - `grep -c "func test_" VitaminG/VitaminG/VitaminGTests/Phase18StreakEngineTests.swift` is >= 4.
    - `grep -c "func test_" VitaminG/VitaminG/VitaminGTests/Phase18GoalViewModelTests.swift` is >= 4.
    - `grep -c "func test_" VitaminG/VitaminG/VitaminGTests/Phase18GoalInputTests.swift` is >= 3.
    - Tests reference `addCheckIn` and `durationDays` (intentionally not yet defined — compile failure or test failure expected; this is RED state).
    - Files are members of the `VitaminGTests` target in `project.pbxproj` (verified by `grep -c "Phase18" VitaminG/VitaminG/VitaminG.xcodeproj/project.pbxproj` >= 3).
    - **RED state confirmation (expected at Wave 0):** the `xcodebuild test` invocation in `<verify>` returns a NON-ZERO exit code OR reports failing test cases for the new Phase18* test classes. A zero exit code at this point means the tests are passing without Plan 02's implementation — investigate (likely the test bodies are not actually exercising the forward-referenced symbols).
  </acceptance_criteria>
  <done>Three test files exist with named test functions. Build either succeeds (tests fail at runtime) or fails ONLY on the documented forward references to addCheckIn / durationDays / draftDurationDays. The `xcodebuild test` invocation in `<verify>` returns a non-zero exit code at this Wave 0 stage — this RED state is REQUIRED and ACCEPTABLE; Plan 02 turns the tests GREEN. Files are added to VitaminGTests target membership.</done>
</task>

<task type="auto">
  <name>Task 2: Phase18GoalDayGridTests + Phase18PremadeGoalsTests + Phase18QuoteBankTests</name>
  <files>
    VitaminG/VitaminG/VitaminGTests/Phase18GoalDayGridTests.swift,
    VitaminG/VitaminG/VitaminGTests/Phase18PremadeGoalsTests.swift,
    VitaminG/VitaminG/VitaminGTests/Phase18QuoteBankTests.swift
  </files>
  <read_first>
    VitaminG/VitaminG/VitaminGTests/StreakEngineTests.swift,
    VitaminG/VitaminG/VitaminGTests/SchemaV6Tests.swift,
    VitaminG/VitaminG/VitaminG/Models/GoalCategory.swift,
    VitaminG/VitaminG/VitaminG/Services/VGQuoteBank.swift,
    .planning/phases/18-home-tab-goals-flow/18-VALIDATION.md,
    .planning/phases/18-home-tab-goals-flow/18-RESEARCH.md
  </read_first>
  <action>
    Create three XCTest files using the same patterns as Task 1. Per D-09, D-10, RESEARCH.md Pitfall 5, and Open Question 4.

    File 1 — `Phase18GoalDayGridTests.swift` (final class `Phase18GoalDayGridTests: XCTestCase`):
    - The test target is the pure-Swift `daysInMonth` calendar logic from `GoalDayGridView`. Plan 05 implements this; for Wave 0 the test file should declare its expected interface as a top-level test helper or expect a small testable component named `GoalDayGridCalendar` that exposes `daysInMonth(for displayedMonth: Date, calendar: Calendar) -> [Date?]` — Plan 05 will land this as either an internal helper exposed in `@testable import VitaminG` or a static nested type on `GoalDayGridView`.
    - `test_daysInMonth_paddsLeadingForMondayStart` — given a month whose 1st falls on a Friday (e.g. construct Date for 2026-05-01), assert the first 4 entries of returned `[Date?]` are nil (Mon, Tue, Wed, Thu pad), entry index 4 is the 1st (Friday). Tests Pitfall 5 (Monday-first weekday offset).
    - `test_daysInMonth_includesAllDaysOfMonth` — for a 31-day month, total non-nil entries equals 31.
    - `test_daysInMonth_doesNotIncludeFollowingMonth` — last non-nil entry's month component equals displayed month component.
    - `test_completedDaysSet_filtersByGoalId` — given an array of CompletionEvents from 2 different goals, the per-goal completedDays Set contains only events whose `event.goal?.id == goal.id`. Uses Set<Date> of `Calendar.startOfDay` per RESEARCH.md Pattern 4. This logic can live as a static helper on the day grid component.
    - `test_canNavigateForward_falseWhenAlreadyAtCurrentMonth` — given displayedMonth equals current month, the forward navigation gate returns false. Tests month bounds in D-10.

    File 2 — `Phase18PremadeGoalsTests.swift` (final class `Phase18PremadeGoalsTests: XCTestCase`):
    - `test_premadeGoals_totalCountIs33OrGreater` — flatten `GoalCategory.allCases.filter { !$0.suggestions.isEmpty }.flatMap { $0.suggestions }`; assert count >= 33 (matches RESEARCH.md GoalCategory.suggestions counts: body 5 + mind 5 + wellness 5 + money 5 + connection 4 + creative 4 + habit 5 = 33).
    - `test_premadeGoals_excludesOtherCategory` — assert no premade goal belongs to `GoalCategory.other`. RESEARCH.md notes .other has 0 suggestions.
    - `test_premadeGoals_noEmptyTitles` — assert no suggestion string is empty when trimmed.
    - `test_premadeGoals_categoriesCoverAllExpected` — assert the distinct set of categories with suggestions includes `.body, .mind, .wellness, .money, .connection, .creative, .habit`.

    File 3 — `Phase18QuoteBankTests.swift` (final class `Phase18QuoteBankTests: XCTestCase`):
    - `test_VGQuoteBank_all_isNonEmpty` — assert `VGQuoteBank.all.count > 0`. (Plan 02 adds the flattened `all` static property — this test is intentionally RED until then.)
    - `test_VGQuoteBank_all_isDeterministic` — call `VGQuoteBank.all` twice; assert both returns produce identical arrays (no shuffling between calls).
    - `test_dailyQuoteSelection_isStableWithinSameDay` — given a fixed Date, compute `Calendar.current.component(.dayOfYear, from: date) % VGQuoteBank.all.count` twice; assert results are identical. Per RESEARCH.md Open Question 4 recommendation.
    - `test_dailyQuoteSelection_differsAcrossDays` — given two dates 7 days apart that yield different `dayOfYear % count` results, assert the selected quote indices differ when count > 7.

    All tests will fail to compile or fail at runtime until Plan 02 / 05 implementations land — this is intentional RED state. Add comment at top: `// Phase 18 Wave 0 — RED tests. Will go GREEN as Plans 02 & 05 land implementations.`

    Add all three files to the Xcode VitaminGTests target membership in `project.pbxproj` (same pattern as Task 1).
  </action>
  <verify>
    <automated>cd "/Users/kyleharrington/Desktop/AI/Vitamin G/VitaminG/VitaminG" && xcodebuild build -scheme VitaminG -destination 'platform=iOS Simulator,name=iPhone 16' 2>&1 | tail -20 && echo "---RED-STATE-CHECK---" && xcodebuild test -scheme VitaminG -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:VitaminGTests/Phase18GoalDayGridTests -only-testing:VitaminGTests/Phase18PremadeGoalsTests -only-testing:VitaminGTests/Phase18QuoteBankTests 2>&1 | tail -30 ; echo "Exit code: $? (nonzero = RED, expected at Wave 0 — Phase18QuoteBankTests references VGQuoteBank.all which Plan 02 adds; Phase18GoalDayGridTests references helpers which Plan 05 adds; Phase18PremadeGoalsTests may pass since GoalCategory.suggestions already exists)"</automated>
  </verify>
  <acceptance_criteria>
    - Files exist at all three paths in `<files>`.
    - Each file contains `final class Phase18*Tests: XCTestCase`.
    - `grep -c "func test_" VitaminG/VitaminG/VitaminGTests/Phase18GoalDayGridTests.swift` is >= 5.
    - `grep -c "func test_" VitaminG/VitaminG/VitaminGTests/Phase18PremadeGoalsTests.swift` is >= 4.
    - `grep -c "func test_" VitaminG/VitaminG/VitaminGTests/Phase18QuoteBankTests.swift` is >= 4.
    - Tests reference `VGQuoteBank.all` (intentionally not yet defined; RED state).
    - Files are members of the VitaminGTests target in `project.pbxproj` (`grep -c "Phase18GoalDayGrid\|Phase18PremadeGoals\|Phase18QuoteBank" VitaminG/VitaminG/VitaminG.xcodeproj/project.pbxproj` >= 3).
    - **RED state confirmation (expected at Wave 0):** the `xcodebuild test` invocation reports failing test cases for at least Phase18QuoteBankTests (VGQuoteBank.all not yet defined) and Phase18GoalDayGridTests (helpers not yet defined). Phase18PremadeGoalsTests may pass at this stage since `GoalCategory.suggestions` already exists in the codebase — that subset going green early is fine and expected.
  </acceptance_criteria>
  <done>Three test files exist with named test functions. Tests are RED for the forward-referenced symbols (intentional). `xcodebuild test` confirms RED state: at least Phase18QuoteBankTests and Phase18GoalDayGridTests fail; Phase18PremadeGoalsTests may be green early because its dependencies exist already. Files are added to VitaminGTests target membership.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test files → app target | Tests live in VitaminGTests target; use `@testable import VitaminG` for internal access. No cross-process untrusted input — XCTest is fully trusted local execution. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01-01 | Tampering | Test fixtures embedding string literals | accept | Test fixtures use hardcoded Swift literals only; no user input deserialized into tests; risk negligible |
| T-18-01-02 | Information Disclosure | Test artifacts in repo history | accept | No PII / secrets in test data; all fixtures synthetic (e.g. `Date()`-relative values, dummy goal titles) |
| T-18-01-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan; pure XCTest using project's existing imports |
</threat_model>

<verification>
- All 6 test files compile (build succeeds) or fail compilation only on references to symbols that Plan 02/05 will introduce (`addCheckIn`, `durationDays`, `draftDurationDays`, `VGQuoteBank.all`, `GoalDayGridCalendar`/`daysInMonth` helper). Compile failures from these intentional forward references are documented at the top of each test file.
- `xcodebuild build -scheme VitaminG -destination 'platform=iOS Simulator,name=iPhone 16'` either succeeds (preferred — guard the tests behind `#if canImport(...)` or stub feature flags) OR fails with only the documented forward references.
- `xcodebuild test ... -only-testing:VitaminGTests/Phase18*Tests` returns NON-ZERO exit at Wave 0 — this is the RED state being confirmed. Subsequent plans (02 and 05) turn the tests green.
- All test files appear as members of the VitaminGTests target in `project.pbxproj`.
</verification>

<success_criteria>
- 6 test files created under `VitaminG/VitaminG/VitaminGTests/`.
- Each test file is named `Phase18*Tests.swift`.
- Each file contains a `final class Phase18*Tests: XCTestCase`.
- Test functions assert future behavior of: addCheckIn, durationDays, draftDurationDays, VGQuoteBank.all, GoalDayGridCalendar daysInMonth, GoalCategory premade suggestions count.
- Files registered in `project.pbxproj` VitaminGTests target.
- `xcodebuild test` at Wave 0 confirms RED state via non-zero exit code; this is REQUIRED, not a failure.
- HOME-06 not exercised here (dropped per D-04) — acknowledged in plan-level scope notes.
</success_criteria>

<output>
Create `.planning/phases/18-home-tab-goals-flow/18-01-SUMMARY.md` when done — note any test files that compile clean vs which ones intentionally have unresolved-symbol RED state, which symbols Plan 02/05 must introduce, and the captured exit code / failing test list from the Wave 0 `xcodebuild test` run.
</output>
