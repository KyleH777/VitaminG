---
phase: 18-home-tab-goals-flow
plan: 05
type: execute
wave: 3
depends_on: [18-02]
files_modified:
  - VitaminG/VitaminG/VitaminG/Views/Components/GoalDayGridView.swift
  - VitaminG/VitaminG/VitaminG/Views/CheckInCelebrationView.swift
  - VitaminG/VitaminG/VitaminG/Views/GoalDetailView.swift
autonomous: true
requirements: [GOAL2-04, GOAL2-05]

must_haves:
  truths:
    - "GoalDayGridView renders a Monday-first 7-column calendar grid of the current month with filled circles for completed days and empty circles for missed/future days"
    - "GoalDayGridView supports chevron navigation to prior months bounded by goal.startDate (or creationDate) and cannot navigate past current month"
    - "GoalDetailView shows a 'Check in for today' button that calls GoalViewModel.addCheckIn(for:context:) and presents CheckInCelebrationView as a fullScreenCover"
    - "CheckInCelebrationView displays the overall app streak from StreakEngine.currentStreak(from: allEvents), auto-dismisses after 2 seconds, and shows a Back to Goals button at all times"
    - "Check-in button shows disabled / already-checked-in state when goal has a CompletionEvent dated today"
    - "Flame icon appears in GoalDetailView header when per-goal consecutive streak is at least 3 days"
  artifacts:
    - path: "VitaminG/VitaminG/VitaminG/Views/Components/GoalDayGridView.swift"
      provides: "Calendar-month day grid component for GoalDetailView"
      min_lines: 120
    - path: "VitaminG/VitaminG/VitaminG/Views/CheckInCelebrationView.swift"
      provides: "Full-screen cover celebration with confetti, app streak, auto-dismiss"
      min_lines: 80
    - path: "VitaminG/VitaminG/VitaminG/Views/GoalDetailView.swift"
      provides: "Day grid + check-in CTA wired through addCheckIn + celebration; flame icon in header"
      contains: "addCheckIn"
  key_links:
    - from: "GoalDetailView Check in for today CTA"
      to: "GoalViewModel.addCheckIn(for:context:)"
      via: "Button action"
      pattern: "addCheckIn\\(for:"
    - from: "GoalDetailView Check in for today CTA"
      to: "CheckInCelebrationView"
      via: ".fullScreenCover(isPresented: $showingCheckInCelebration)"
      pattern: "CheckInCelebrationView"
    - from: "CheckInCelebrationView streakCount"
      to: "StreakEngine.currentStreak(from: allEvents)"
      via: "computed appStreak passed at presentation time"
      pattern: "StreakEngine\\.currentStreak"
    - from: "GoalDayGridView completed cells"
      to: "CompletionEvent.completedAt (filtered by goal id)"
      via: "Set<Date> of startOfDay values"
      pattern: "Calendar.current.startOfDay"
---

<objective>
Add the calendar-month day grid to the Goal Detail page, a Check in for today button that creates a CompletionEvent and shows a full-screen celebration, and a flame icon in the header when the per-goal streak is at least 3 days.

Purpose: Implements GOAL2-04 (check-in celebration with overall app streak, auto-dismiss) and GOAL2-05 (day grid + check-in CTA + flame icon).
Output: 2 new views (GoalDayGridView, CheckInCelebrationView) + GoalDetailView wiring, all consuming Plan 02's addCheckIn.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-home-tab-goals-flow/18-CONTEXT.md
@.planning/phases/18-home-tab-goals-flow/18-RESEARCH.md
@.planning/phases/18-home-tab-goals-flow/18-PATTERNS.md
@.planning/phases/18-home-tab-goals-flow/18-UI-SPEC.md
@.planning/phases/18-home-tab-goals-flow/18-02-SUMMARY.md
@CLAUDE.md
</context>

<interfaces>
From Plan 02:
- GoalViewModel.addCheckIn(for goal: Goal, context: ModelContext) — inserts CompletionEvent, guards same-day duplicate, does NOT flip isCompleted.
- StreakEngine.currentStreak(from:) used directly for overall streak and per-goal streak.

From existing codebase:
- MilestoneCelebrationView (Views/MilestoneCelebrationView.swift) — exact confetti + dark overlay pattern to replicate (lines 1-166 per PATTERNS.md). 60-particle Canvas + TimelineView, golden-angle scatter, hue-varied.
- GoalDetailView (Views/GoalDetailView.swift):
    - @Query private var allEvents: [CompletionEvent] is already present.
    - Existing private var goalEvents: [CompletionEvent] { allEvents.filter { $0.goal?.id == goal.id } } — reuse for day grid.
    - body is a ScrollView VStack with sections: headerSection, publicToggleSection, quoteCardSection, progressSection, notesSection, actionsSection.
- VGTheme tokens: accentSage (completed days, success state), accentTerra (active state, today highlight, chevron color), accentGold (flame icon), separator (cell strokes), surface (card background), textPrimary, textMuted, sand (dark-surface text).

For Phase18GoalDayGridTests to pass, the calendar logic must be testable. Expose a static helper inside GoalDayGridView OR a sibling namespace named GoalDayGridCalendar — pick one:
- Option A (preferred): make the helper a static internal method on GoalDayGridView itself: `static func daysInMonth(for displayedMonth: Date, calendar: Calendar) -> [Date?]` and `static func canNavigateForward(displayedMonth: Date, calendar: Calendar) -> Bool`.
- Option B: declare a nested namespace `enum GoalDayGridCalendar { static func daysInMonth(...) -> [Date?] }` so tests can import @testable.
The test file `Phase18GoalDayGridTests.swift` (Plan 01) references these helpers — match the symbol name used there. If a mismatch exists, prefer renaming the test references over inventing a different name (test was a scaffold; production naming is canonical).

UI-SPEC §GoalDayGridView:
- LazyVGrid with 7 columns, GridItem(.fixed(36), spacing: 8).
- Column headers ["M","T","W","T","F","S","S"] — SF Pro 13pt Semibold uppercase, textMuted.
- Day cells: Circle 32pt; completed → fill accentSage, white checkmark 10pt; today (not completed) → strokeBorder accentTerra 2pt + accentTerra 0.15 fill; future/missed → fill surface + strokeBorder separator 1pt.
- Month header: "May 2026" Cormorant SemiBold 20pt VGTheme.textPrimary, between leading/trailing chevron buttons (accentTerra, 28pt hit target, accessibility labels "Previous month" / "Next month").
- Section wrapper: VGTheme.surface bg, cornerRadius 14, padding 16pt.

UI-SPEC §CheckInCelebrationView:
- Color.black.opacity(0.92) full-screen background.
- 60-particle Canvas confetti (copy from MilestoneCelebrationView).
- checkmark.circle.fill 64pt accentSage icon, badge spring-scales from 0.3 to 1.0.
- "You showed up." heading — SF Pro Rounded 34pt Semibold white.
- "🔥 {N} day streak" — SF Pro Monospaced 20pt Semibold, accentTerra (light) / terraGlow (dark).
- "Keep showing up." sub-label — SF Pro 16pt Regular white opacity 0.7.
- Manual dismiss "Back to Goals" button — full-width, minHeight 44pt, accentTerra background, white text, cornerRadius 12, padding horizontal 24, padding bottom 48.
- Auto-dismiss: DispatchQueue.main.asyncAfter(deadline: .now() + 2.0) { onDismiss() }.
- Accessibility: reduceMotion suppresses confetti and spring animation; UIAccessibility.post(.announcement, "Check-in complete! \(N) day streak").
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create GoalDayGridView component</name>
  <files>
    VitaminG/VitaminG/VitaminG/Views/Components/GoalDayGridView.swift
  </files>
  <read_first>
    VitaminG/VitaminG/VitaminG/Views/GoalDetailView.swift,
    VitaminG/VitaminG/VitaminG/Views/GoalListView.swift,
    VitaminG/VitaminG/VitaminG/Services/StreakEngine.swift,
    VitaminG/VitaminG/VitaminG/VGTheme.swift,
    VitaminG/VitaminG/VitaminGTests/Phase18GoalDayGridTests.swift,
    .planning/phases/18-home-tab-goals-flow/18-PATTERNS.md,
    .planning/phases/18-home-tab-goals-flow/18-UI-SPEC.md
  </read_first>
  <action>
    Per D-09, D-10, D-11, RESEARCH.md Pattern 4 + Pitfall 5, PATTERNS.md GoalDayGridView section, UI-SPEC §GoalDayGridView.

    Create struct `GoalDayGridView: View` in `Views/Components/GoalDayGridView.swift`.

    Properties:
    - let goal: Goal
    - let completionEvents: [CompletionEvent]   // pass allEvents from GoalDetailView; component filters per-goal internally
    - @State private var displayedMonth: Date = Date()
    - private let columns = Array(repeating: GridItem(.fixed(36), spacing: 8), count: 7)
    - private let dayHeaders = ["M","T","W","T","F","S","S"]

    Computed members:
    - private var completedDays: Set<Date> — Set of Calendar.current.startOfDay(for: event.completedAt) for events where event.goal?.id == goal.id (use compactMap + filter, never force-unwrap).
    - private var daysInMonth: [Date?] — calls the static helper static func daysInMonth(for displayedMonth: Date, calendar: Calendar) -> [Date?].
    - private var canNavigateForward: Bool — calls static func canNavigateForward(displayedMonth: Date, calendar: Calendar) -> Bool.
    - private var canNavigateBack: Bool — true if `Calendar.current.date(byAdding: .month, value: -1, to: displayedMonth)` is on or after the month containing `goal.startDate ?? goal.creationDate`.

    Static helpers (exposed for tests):
    - static func daysInMonth(for displayedMonth: Date, calendar: Calendar) -> [Date?]
        var cal = calendar
        cal.firstWeekday = 2   // Monday — Pitfall 5
        guard let range = cal.range(of: .day, in: .month, for: displayedMonth),
              let firstDay = cal.date(from: cal.dateComponents([.year, .month], from: displayedMonth))
        else { return [] }
        let weekdayOffset = (cal.component(.weekday, from: firstDay) - cal.firstWeekday + 7) % 7
        var days: [Date?] = Array(repeating: nil, count: weekdayOffset)
        for day in range {
            if let date = cal.date(byAdding: .day, value: day - 1, to: firstDay) {
                days.append(date)
            }
        }
        return days
    - static func canNavigateForward(displayedMonth: Date, calendar: Calendar) -> Bool
        let thisMonth = calendar.dateComponents([.year, .month], from: Date())
        let shown = calendar.dateComponents([.year, .month], from: displayedMonth)
        guard let ty = thisMonth.year, let tm = thisMonth.month, let sy = shown.year, let sm = shown.month else { return false }
        if sy < ty { return true }
        if sy == ty && sm < tm { return true }
        return false

    Body layout:
    - VStack(alignment: .leading, spacing: 12) wrapping:
        - Month header HStack: leading chevron button (Image "chevron.left", 16pt, VGTheme.accentTerra, disabled when !canNavigateBack, accessibility "Previous month", 28pt minimum hit target via .frame(minWidth:28,minHeight:28)) + Spacer + Text(monthTitle, VGTheme.serif(20, weight: .semibold), VGTheme.textPrimary) + Spacer + trailing chevron button (chevron.right, mirrors leading button, disabled when !canNavigateForward, accessibility "Next month").
        - Column headers row: LazyVGrid(columns: columns, spacing: 8) of Text(header).font(.system(size: 13, weight: .semibold)).textCase(.uppercase).foregroundStyle(VGTheme.textMuted) for each entry in dayHeaders.
        - Day cells: LazyVGrid(columns: columns, spacing: 8) ForEach(daysInMonth.indices, id: \\.self) { i in dayCell(date: daysInMonth[i]) }.
    - Outer modifiers: .padding(16), .frame(maxWidth: .infinity, alignment: .leading), .background(VGTheme.surface), .clipShape(RoundedRectangle(cornerRadius: 14)).

    dayCell(date: Date?) view (per UI-SPEC):
    - If date is nil (leading pad): Circle().fill(.clear).frame(width: 32, height: 32). Wrap in .frame(width: 36, height: 36) so the LazyVGrid cell preserves alignment. accessibilityHidden(true).
    - If date is non-nil:
        - Calculate isCompleted = completedDays.contains(Calendar.current.startOfDay(for: date)).
        - Calculate isToday = Calendar.current.isDateInToday(date).
        - isFuture = date > Date() (start of day) — future dates are NOT marked completed even if a stray event exists.
        - Style:
            - isCompleted: Circle().fill(VGTheme.accentSage) + Image("checkmark") inside (.font(.system(size: 10, weight: .semibold)).foregroundStyle(.white)).
            - isToday and !isCompleted: Circle().fill(VGTheme.accentTerra.opacity(0.15)).strokeBorder(VGTheme.accentTerra, lineWidth: 2). Text showing day number inside, VGTheme.accentTerra.
            - Otherwise (missed or future): Circle().fill(VGTheme.surface).strokeBorder(VGTheme.separator, lineWidth: 1). Text(day number) VGTheme.textMuted.
        - 32pt diameter, wrapped in 36pt frame.
        - .accessibilityLabel("{dayName}, {dateString}, {completed/not completed}").

    Month navigation methods:
    - private func navigateMonth(by months: Int) — uses Calendar.current.date(byAdding: .month, value: months, to: displayedMonth) ?? displayedMonth, with the bounds check (won't go past today or before goal.startDate). Animate the change with withAnimation(.easeInOut).
    - private var monthTitle: String — DateFormatter with "LLLL yyyy" returning e.g. "May 2026".

    Constraints:
    - Only VGTheme tokens, no raw hex.
    - Force-unwraps forbidden on date math — use guards.
    - No third-party deps.
    - Tests (Phase18GoalDayGridTests) reference the static helpers; ensure symbol names match.
    - If Phase18GoalDayGridTests uses a different symbol name (e.g. `GoalDayGridCalendar`), update the test file in this same task to reference `GoalDayGridView.daysInMonth(for:calendar:)` and `GoalDayGridView.canNavigateForward(displayedMonth:calendar:)`. Plan 01's tests were intentional scaffolds — production naming wins.
  </action>
  <verify>
    <automated>cd "/Users/kyleharrington/Desktop/AI/Vitamin G/VitaminG/VitaminG" && xcodebuild build -scheme VitaminG -destination 'platform=iOS Simulator,name=iPhone 16' 2>&1 | tail -15 && xcodebuild test -scheme VitaminG -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:VitaminGTests/Phase18GoalDayGridTests 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - File VitaminG/VitaminG/VitaminG/Views/Components/GoalDayGridView.swift exists.
    - grep -c "struct GoalDayGridView: View" VitaminG/VitaminG/VitaminG/Views/Components/GoalDayGridView.swift == 1.
    - grep -c "calendar.firstWeekday = 2\|cal.firstWeekday = 2" VitaminG/VitaminG/VitaminG/Views/Components/GoalDayGridView.swift >= 1 (Monday-first override).
    - grep -c "static func daysInMonth" VitaminG/VitaminG/VitaminG/Views/Components/GoalDayGridView.swift == 1.
    - grep -c "static func canNavigateForward" VitaminG/VitaminG/VitaminG/Views/Components/GoalDayGridView.swift == 1.
    - grep -c "LazyVGrid" VitaminG/VitaminG/VitaminG/Views/Components/GoalDayGridView.swift >= 2 (header row + day cells).
    - grep -c "accessibilityLabel(\"Previous month\")\|accessibilityLabel(\"Next month\")" VitaminG/VitaminG/VitaminG/Views/Components/GoalDayGridView.swift >= 2.
    - File added to VitaminG app target in project.pbxproj (verify via grep).
    - xcodebuild build succeeds. Phase18GoalDayGridTests is GREEN.
  </acceptance_criteria>
  <done>GoalDayGridView compiles as a reusable component. Calendar logic is correct for Monday-first and respects month bounds. Tests are green.</done>
</task>

<task type="auto">
  <name>Task 2: Create CheckInCelebrationView</name>
  <files>
    VitaminG/VitaminG/VitaminG/Views/CheckInCelebrationView.swift
  </files>
  <read_first>
    VitaminG/VitaminG/VitaminG/Views/MilestoneCelebrationView.swift,
    VitaminG/VitaminG/VitaminG/Services/StreakEngine.swift,
    VitaminG/VitaminG/VitaminG/VGTheme.swift,
    .planning/phases/18-home-tab-goals-flow/18-PATTERNS.md,
    .planning/phases/18-home-tab-goals-flow/18-UI-SPEC.md
  </read_first>
  <action>
    Per D-12, D-13, RESEARCH.md Pattern 6, PATTERNS.md CheckInCelebrationView section, UI-SPEC §CheckInCelebrationView.

    Create struct CheckInCelebrationView: View in Views/CheckInCelebrationView.swift.

    Properties:
    - let streakCount: Int   // overall app streak — caller passes StreakEngine.currentStreak(from: allEvents)
    - let onDismiss: () -> Void
    - @Environment(\.accessibilityReduceMotion) private var reduceMotion
    - @State private var badgeScale: Double = 0.3
    - @State private var badgeOpacity: Double = 0.0

    Body:
    - ZStack {
        Color.black.opacity(0.92).ignoresSafeArea()
        confettiView.ignoresSafeArea().accessibilityHidden(true)
        VStack(spacing: 24) {
            Spacer()
            Image(systemName: "checkmark.circle.fill")
                .font(.system(size: 64))
                .foregroundStyle(VGTheme.accentSage)
                .scaleEffect(badgeScale)
                .opacity(badgeOpacity)
                .accessibilityLabel("Check-in complete!")
            Text("You showed up.")
                .font(.system(size: 34, weight: .semibold, design: .rounded))
                .foregroundStyle(.white)
                .multilineTextAlignment(.center)
                .padding(.horizontal, 32)
            Text("🔥 \(streakCount) day streak")
                .font(.system(size: 20, weight: .semibold, design: .monospaced))
                .foregroundStyle(VGTheme.accentTerra)
            Text("Keep showing up.")
                .font(.system(size: 16))
                .foregroundStyle(.white.opacity(0.7))
            Spacer()
            Button("Back to Goals") { onDismiss() }
                .font(.system(size: 16, weight: .semibold, design: .rounded))
                .frame(maxWidth: .infinity, minHeight: 44)
                .background(VGTheme.accentTerra)
                .foregroundStyle(.white)
                .clipShape(RoundedRectangle(cornerRadius: 12))
                .padding(.horizontal, 24)
                .padding(.bottom, 48)
                .accessibilityLabel("Back to Goals")
        }
      }
      .onAppear {
          // Auto-dismiss after 2 seconds (D-13)
          DispatchQueue.main.asyncAfter(deadline: .now() + 2.0) { onDismiss() }
          UIAccessibility.post(notification: .announcement,
                              argument: "Check-in complete! \(streakCount) day streak")
          if reduceMotion {
              badgeScale = 1.0
              badgeOpacity = 1.0
          } else {
              withAnimation(.spring(response: 0.5, dampingFraction: 0.7)) {
                  badgeScale = 1.0
                  badgeOpacity = 1.0
              }
          }
      }

    Confetti view (copy verbatim from MilestoneCelebrationView.swift lines 123-141 — 60 particles, golden-angle scatter, hue-varied):
    - private var confettiView: some View {
          TimelineView(.animation) { timeline in
              Canvas { context, size in
                  let now = timeline.date.timeIntervalSinceReferenceDate
                  let count = 60
                  for i in 0..<count {
                      let seed = Double(i) * 137.5
                      let x = (sin(seed + now * 0.8 + Double(i)) * 0.5 + 0.5) * size.width
                      let rawY = (now * 80.0 + seed * 3.7).truncatingRemainder(dividingBy: size.height)
                      let y = rawY < 0 ? rawY + size.height : rawY
                      let hue = (seed / 360.0).truncatingRemainder(dividingBy: 1.0)
                      let color = Color(hue: hue, saturation: 0.8, brightness: 0.9)
                      let rect = CGRect(x: x - 4, y: y - 4, width: 8, height: 8)
                      context.fill(Path(rect), with: .color(color))
                  }
              }
          }
      }

    Reduce-motion guard: when reduceMotion is true, suppress confetti by gating the Canvas behind `if !reduceMotion` — render an empty Color.clear instead. Badge skips the spring animation (instant set to 1.0).

    Key difference from MilestoneCelebrationView: NO badge persistence call (no saveBadgeToProfile or similar) — check-in celebration fires per-event, not per-milestone.

    Constraints:
    - File added to VitaminG app target.
    - Only VGTheme tokens (plus the explicit .white and Color.black.opacity used in the overlay/text per UI-SPEC).
    - No third-party deps.
    - The streakCount parameter is the overall app streak per D-12 — caller's responsibility to pass StreakEngine.currentStreak(from: allEvents), not per-goal.
  </action>
  <verify>
    <automated>cd "/Users/kyleharrington/Desktop/AI/Vitamin G/VitaminG/VitaminG" && xcodebuild build -scheme VitaminG -destination 'platform=iOS Simulator,name=iPhone 16' 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - File VitaminG/VitaminG/VitaminG/Views/CheckInCelebrationView.swift exists.
    - grep -c "struct CheckInCelebrationView: View" VitaminG/VitaminG/VitaminG/Views/CheckInCelebrationView.swift == 1.
    - grep -c "DispatchQueue.main.asyncAfter(deadline: .now() + 2.0)" VitaminG/VitaminG/VitaminG/Views/CheckInCelebrationView.swift >= 1.
    - grep -c "You showed up." VitaminG/VitaminG/VitaminG/Views/CheckInCelebrationView.swift >= 1.
    - grep -c "Back to Goals" VitaminG/VitaminG/VitaminG/Views/CheckInCelebrationView.swift >= 1.
    - grep -c "accessibilityReduceMotion" VitaminG/VitaminG/VitaminG/Views/CheckInCelebrationView.swift >= 1.
    - grep -c "TimelineView(.animation)" VitaminG/VitaminG/VitaminG/Views/CheckInCelebrationView.swift >= 1 (confetti).
    - grep -c "saveBadge\|persistMilestone" VitaminG/VitaminG/VitaminG/Views/CheckInCelebrationView.swift == 0 (no per-milestone persistence — per-event celebration).
    - File added to VitaminG app target in project.pbxproj.
    - xcodebuild build succeeds.
  </acceptance_criteria>
  <done>CheckInCelebrationView compiles and is added to the app target. Auto-dismiss, accessibility announcement, and reduce-motion behavior are present.</done>
</task>

<task type="auto">
  <name>Task 3: Wire GoalDetailView — embed day grid, add check-in CTA, flame header icon</name>
  <files>
    VitaminG/VitaminG/VitaminG/Views/GoalDetailView.swift
  </files>
  <read_first>
    VitaminG/VitaminG/VitaminG/Views/GoalDetailView.swift,
    VitaminG/VitaminG/VitaminG/Views/Components/GoalDayGridView.swift,
    VitaminG/VitaminG/VitaminG/Views/CheckInCelebrationView.swift,
    VitaminG/VitaminG/VitaminG/ViewModels/GoalViewModel.swift,
    VitaminG/VitaminG/VitaminG/Services/StreakEngine.swift,
    .planning/phases/18-home-tab-goals-flow/18-PATTERNS.md,
    .planning/phases/18-home-tab-goals-flow/18-UI-SPEC.md
  </read_first>
  <action>
    Per GOAL2-04, GOAL2-05, D-11, D-12, D-13, RESEARCH.md Open Question 3 (Check in for today coexists with Mark as Complete).

    State additions to GoalDetailView (alongside existing @State properties):
    - @State private var showingCheckInCelebration = false

    Computed helpers:
    - private var goalStreak: Int { StreakEngine.currentStreak(from: goalEvents) } — uses existing goalEvents computed property (which already filters allEvents by goal id).
    - private var appStreak: Int { StreakEngine.currentStreak(from: allEvents) } — overall app streak for the celebration screen (per D-12).
    - private var isCheckedInToday: Bool { goalEvents.contains(where: { Calendar.current.isDateInToday($0.completedAt ?? .distantPast) }) }

    Layout changes in body's ScrollView VStack:
    - Insert a NEW section between quoteCardSection and progressSection (or wherever the existing progress chart is — read the file to confirm exact placement):
        VStack(alignment: .leading, spacing: 12) {
            HStack {
                Text("YOUR MONTH")
                    .font(.system(size: 10, weight: .bold))
                    .kerning(1.2)
                    .foregroundStyle(VGTheme.muted)
                Spacer()
            }
            .padding(.horizontal, 16)

            GoalDayGridView(goal: goal, completionEvents: allEvents)
                .padding(.horizontal, 0)

            Button {
                viewModel.addCheckIn(for: goal, context: modelContext)
                showingCheckInCelebration = true
            } label: {
                HStack(spacing: 8) {
                    Image(systemName: isCheckedInToday ? "checkmark.circle.fill" : "checkmark.circle")
                    Text(isCheckedInToday ? "Checked in today" : "Check in for today")
                }
                .font(.system(.body, design: .rounded).weight(.semibold))
                .frame(maxWidth: .infinity)
                .padding(.vertical, 14)
            }
            .buttonStyle(.borderedProminent)
            .tint(VGTheme.accentSage)
            .disabled(isCheckedInToday)
            .padding(.horizontal, 16)
            .accessibilityLabel(isCheckedInToday ? "Already checked in today" : "Check in for today")

            if goalStreak > 0 {
                HStack(spacing: 6) {
                    Text("🔥 \(goalStreak) day streak")
                        .font(.system(size: 14, weight: .semibold, design: .monospaced))
                        .foregroundStyle(VGTheme.accentTerra)
                }
                .padding(.horizontal, 16)
            }
        }

    .fullScreenCover modifier on the ScrollView (or wherever existing covers live):
    - .fullScreenCover(isPresented: $showingCheckInCelebration) {
          CheckInCelebrationView(streakCount: appStreak, onDismiss: { showingCheckInCelebration = false })
      }

    Flame icon in header section (D-11 + GOAL2-05):
    - Locate the existing headerSection. Append a flame icon to the title row when goalStreak >= 3:
        if goalStreak >= 3 {
            HStack(spacing: 4) {
                Image(systemName: "flame.fill").foregroundStyle(VGTheme.accentGold)
                Text("\(goalStreak)").font(.system(size: 14, weight: .semibold, design: .monospaced)).foregroundStyle(VGTheme.accentGold)
            }
            .accessibilityLabel("\(goalStreak) day streak — on fire!")
        }

    Coexistence rule (Open Question 3):
    - DO NOT remove the existing "Mark as Complete" button in actionsSection. It permanently completes the goal (sets isCompleted = true). Check in for today is the daily action and is independent. Both CTAs coexist.

    Disabled-state visual for already-checked-in:
    - When isCheckedInToday is true, the button label changes to "Checked in today", the icon switches to checkmark.circle.fill, .disabled(true) applies, and the button is visually muted (the .buttonStyle(.borderedProminent) with .tint(VGTheme.accentSage) handles this — verify with iOS 17 system rendering).

    Constraints:
    - Force-unwraps forbidden on optional CompletionEvent.completedAt — use ?? .distantPast.
    - No new ViewModels; the existing viewModel: GoalViewModel handles addCheckIn.
    - Only VGTheme tokens.
    - No raw hex.
    - The "Your month" overline (UI-SPEC §Day grid section title) is in caps per the overline convention; the underlying UI-SPEC also calls it "Your month" — use uppercase per the section header convention used elsewhere in HomeView.
  </action>
  <verify>
    <automated>cd "/Users/kyleharrington/Desktop/AI/Vitamin G/VitaminG/VitaminG" && xcodebuild build -scheme VitaminG -destination 'platform=iOS Simulator,name=iPhone 16' 2>&1 | tail -15 && xcodebuild test -scheme VitaminG -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:VitaminGTests/Phase18StreakEngineTests 2>&1 | tail -10 && xcodebuild test -scheme VitaminG -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:VitaminGTests/Phase18GoalViewModelTests 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "GoalDayGridView(goal: goal" VitaminG/VitaminG/VitaminG/Views/GoalDetailView.swift >= 1 (component embedded).
    - grep -c "viewModel.addCheckIn(for: goal" VitaminG/VitaminG/VitaminG/Views/GoalDetailView.swift >= 1 (CTA wired to addCheckIn).
    - grep -c "CheckInCelebrationView" VitaminG/VitaminG/VitaminG/Views/GoalDetailView.swift >= 1.
    - grep -c "showingCheckInCelebration" VitaminG/VitaminG/VitaminG/Views/GoalDetailView.swift >= 3 (state + Button action + fullScreenCover).
    - grep -c "isCheckedInToday" VitaminG/VitaminG/VitaminG/Views/GoalDetailView.swift >= 2 (computed + disabled binding).
    - grep -c "flame.fill" VitaminG/VitaminG/VitaminG/Views/GoalDetailView.swift >= 1.
    - grep -c "accentGold" VitaminG/VitaminG/VitaminG/Views/GoalDetailView.swift >= 1 (flame color).
    - grep -c "Check in for today" VitaminG/VitaminG/VitaminG/Views/GoalDetailView.swift >= 1.
    - Existing "Mark as Complete" button is still present (grep -c "Mark as Complete" >= 1).
    - xcodebuild build succeeds.
    - Phase18StreakEngineTests, Phase18GoalViewModelTests, Phase18GoalDayGridTests are all GREEN.
  </acceptance_criteria>
  <done>GoalDetailView shows the day grid, fires the check-in flow including celebration, displays flame in header when per-goal streak >= 3, and preserves the existing Mark as Complete action.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User taps "Check in for today" | Tap triggers GoalViewModel.addCheckIn which guards same-day dup; no user-supplied data crosses boundary. |
| CompletionEvent.completedAt → calendar comparisons | Optional Date with ?? .distantPast guard in every comparison; safe under nil. |
| streakCount Int → UI Text | Plain Int interpolation; no script injection vector in SwiftUI Text. |
| Confetti Canvas Color(hue:saturation:brightness:) | Pure compute; no external input; safe. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-05-01 | Tampering | Duplicate CompletionEvent insertion | mitigate | Two layers of defense: (1) GoalViewModel.addCheckIn early-returns on isDateInToday match (Plan 02); (2) GoalDetailView button is .disabled(isCheckedInToday) so the user cannot trigger the action twice. |
| T-18-05-02 | Tampering | Future-dated CompletionEvent appearing in grid | mitigate | GoalDayGridView dayCell explicitly checks isFuture before rendering completed style — future dates always render as missed/future, never filled. |
| T-18-05-03 | Information Disclosure | Streak count exposed to user | accept | Self-only display; no cross-account leakage. |
| T-18-05-04 | Denial of Service | Confetti TimelineView CPU on long-lived screen | mitigate | Auto-dismiss after 2 seconds bounds runtime; reduce-motion suppresses Canvas entirely; CheckInCelebrationView never persists. |
| T-18-05-05 | Denial of Service | GoalDayGridView recomputing daysInMonth on every render | accept | n <= 42 cells; cost negligible; pure Foundation Calendar API. |
| T-18-05-06 | Elevation of Privilege | "Mark as Complete" coexisting with daily check-in | mitigate | Two distinct actions with distinct semantics; addCheckIn never flips isCompleted; toggleCompletion is unchanged from v1.0 behavior. RESEARCH.md Pitfall 2 documented. |
| T-18-05-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan; pure SwiftUI/Foundation. |
</threat_model>

<verification>
- xcodebuild build succeeds.
- xcodebuild test -only-testing:VitaminGTests/Phase18StreakEngineTests is GREEN (per-goal streak + flame threshold).
- xcodebuild test -only-testing:VitaminGTests/Phase18GoalViewModelTests is GREEN (addCheckIn + dedup).
- xcodebuild test -only-testing:VitaminGTests/Phase18GoalDayGridTests is GREEN (Monday-first offset + month bounds).
- Manual on simulator: open a goal that has no events; verify "Your month" grid renders with all empty/missed circles for past dates and today highlighted; verify "Check in for today" button is enabled.
- Manual: tap "Check in for today"; celebration appears with current overall app streak; auto-dismisses in ~2 seconds OR user taps "Back to Goals"; goal grid updates today cell to filled accentSage; button becomes "Checked in today" and is disabled.
- Manual: with goal having 3+ consecutive recent check-ins, verify flame icon (accentGold) appears in header.
</verification>

<success_criteria>
- GoalDayGridView component exists and renders Monday-first calendar grid with correct filled/empty/today states.
- CheckInCelebrationView exists, mirrors MilestoneCelebrationView's confetti pattern, auto-dismisses, shows app streak.
- GoalDetailView embeds GoalDayGridView, presents CheckInCelebrationView via fullScreenCover, calls GoalViewModel.addCheckIn on tap, shows flame icon at >= 3 days, and preserves the existing Mark as Complete CTA.
- All Phase18* tests GREEN end-to-end.
</success_criteria>

<output>
Create .planning/phases/18-home-tab-goals-flow/18-05-SUMMARY.md when done. Document the canonical static helper names landed on GoalDayGridView (so future planners know which test references stay vs which were updated), and any visual notes about the celebration screen.
</output>
