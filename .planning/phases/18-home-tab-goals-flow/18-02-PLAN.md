---
phase: 18-home-tab-goals-flow
plan: 02
type: execute
wave: 1
depends_on: [18-01]
files_modified:
  - VitaminG/VitaminG/VitaminG/Models/SchemaV6.swift
  - VitaminG/VitaminG/VitaminG/Models/GoalInput.swift
  - VitaminG/VitaminG/VitaminG/ViewModels/GoalCreationWizardViewModel.swift
  - VitaminG/VitaminG/VitaminG/ViewModels/GoalViewModel.swift
  - VitaminG/VitaminG/VitaminG/Services/VGQuoteBank.swift
autonomous: true
requirements: [HOME-01, HOME-02, GOAL2-01, GOAL2-02, GOAL2-04]

must_haves:
  truths:
    - "Goal model carries an optional durationDays Int? field, persisted to SwiftData"
    - "GoalInput carries durationDays and forwards it into the created Goal"
    - "GoalCreationWizardViewModel exposes draftDurationDays: Int? backing state for the Step 3 duration field"
    - "GoalCreationWizardViewModel exposes configure(fromPremade title:category:) which sets selectedCategory + draftTitle and jumps to currentStep = 2"
    - "GoalViewModel.addCheckIn(for:context:) creates a CompletionEvent without flipping isCompleted and is idempotent within the same calendar day"
    - "VGQuoteBank exposes a static all array flattening every per-category quote source"
    - "Wave 0 tests for HOME-01, HOME-02, GOAL2-01, GOAL2-04 turn GREEN"
  artifacts:
    - path: "VitaminG/VitaminG/VitaminG/Models/SchemaV6.swift"
      provides: "Lightweight additive nil-default durationDays: Int? on Goal"
      contains: "var durationDays: Int?"
    - path: "VitaminG/VitaminG/VitaminG/Models/GoalInput.swift"
      provides: "durationDays plumbed through GoalInput"
      contains: "durationDays"
    - path: "VitaminG/VitaminG/VitaminG/ViewModels/GoalCreationWizardViewModel.swift"
      provides: "draftDurationDays backing state + configure(fromPremade:category:) entry point for the Need-ideas path"
      contains: "draftDurationDays"
    - path: "VitaminG/VitaminG/VitaminG/ViewModels/GoalViewModel.swift"
      provides: "addCheckIn(for:context:) method"
      contains: "func addCheckIn"
    - path: "VitaminG/VitaminG/VitaminG/Services/VGQuoteBank.swift"
      provides: "Flattened static all quote list"
      contains: "static var all"
  key_links:
    - from: "GoalCreationWizardViewModel.configure(fromPremade:)"
      to: "GoalCreationWizardView .onAppear pre-fill (consumed in Plan 03)"
      via: "set selectedCategory + draftTitle + currentStep = 2"
      pattern: "configure\\(fromPremade"
    - from: "GoalCreationWizardViewModel.draftDurationDays"
      to: "Step3DetailsScreen duration picker binding (consumed in Plan 03)"
      via: "Two-way binding; flushed into GoalInput.durationDays on save"
      pattern: "draftDurationDays"
    - from: "GoalViewModel.addCheckIn"
      to: "GoalDetailView 'Check in for today' CTA (consumed in Plan 05)"
      via: "Inserts CompletionEvent without flipping isCompleted"
      pattern: "addCheckIn\\(for:"
    - from: "VGQuoteBank.all"
      to: "HomeView quoteSection (consumed in Plan 04)"
      via: "dayOfYear % all.count daily rotation"
      pattern: "VGQuoteBank\\.all"
---

<objective>
Build the foundation layer that downstream UI plans (03, 04, 05) consume. Add a durationDays field to the Goal model and GoalInput, add a matching draftDurationDays draft state on GoalCreationWizardViewModel, add addCheckIn to GoalViewModel, add a premade-goal pre-fill method to GoalCreationWizardViewModel, and add VGQuoteBank.all for daily quote rotation.

Purpose: All Phase 18 UI plans depend on these symbols. Wave 0 tests reference them as forward declarations — this plan turns those tests green and exposes contracts for Plans 03–05.
Output: 5 modified files providing the schema field, three view-model additions (draftDurationDays, addCheckIn, configure(fromPremade:)), and the flattened quote list, with no UI changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-home-tab-goals-flow/18-CONTEXT.md
@.planning/phases/18-home-tab-goals-flow/18-RESEARCH.md
@.planning/phases/18-home-tab-goals-flow/18-PATTERNS.md
@CLAUDE.md
</context>

<interfaces>
Existing pattern in GoalViewModel.swift (toggleCompletion lines 114-139):
- func toggleCompletion(goal: Goal, context: ModelContext) inserts a CompletionEvent AND flips goal.isCompleted. addCheckIn mirrors the event insertion but skips the isCompleted flip.

Existing pattern in GoalCreationWizardViewModel.swift (configure(from:) lines 64-82):
- func configure(from goal: Goal) sets all draft fields to match an existing Goal for edit mode. configure(fromPremade:category:) is its sibling for the Need-ideas path.

Existing pattern in SchemaV6.swift:
- Goal model uses optional fields with nil defaults. Adding var durationDays: Int? = nil is a SwiftData lightweight migration (per RESEARCH.md Open Question 1 resolution).

Existing pattern in VGQuoteBank.swift:
- Per-category static arrays (e.g. static let body: [VGQuote]). The new static var all: [VGQuote] concatenates them.

Consumer contracts (do not change without updating consumers in Plans 03–05):
- GoalViewModel.addCheckIn(for goal: Goal, context: ModelContext) — Plan 05 GoalDetailView calls this
- GoalCreationWizardViewModel.configure(fromPremade title: String, category: GoalCategory) — Plan 03 GoalCreationWizardView .onAppear calls this when premadeGoal init param is non-nil (Pattern 8)
- GoalCreationWizardViewModel.draftDurationDays: Int? — Plan 03 Step3DetailsScreen binds to this; the wizard save flow writes it into GoalInput.durationDays
- VGQuoteBank.all: [VGQuote] — Plan 04 HomeView quoteSection reads this
- Goal.durationDays: Int? — Plan 03 Step3DetailsScreen writes this transitively (via GoalInput)
- GoalInput.durationDays: Int? — Plan 03 wizard save flow uses this
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add durationDays to Goal (SchemaV6) and GoalInput</name>
  <files>
    VitaminG/VitaminG/VitaminG/Models/SchemaV6.swift,
    VitaminG/VitaminG/VitaminG/Models/GoalInput.swift
  </files>
  <read_first>
    VitaminG/VitaminG/VitaminG/Models/SchemaV6.swift,
    VitaminG/VitaminG/VitaminG/Models/GoalInput.swift,
    VitaminG/VitaminG/VitaminG/Models/VitaminGMigrationPlan.swift,
    VitaminG/VitaminG/VitaminGTests/SchemaV6Tests.swift,
    VitaminG/VitaminG/VitaminGTests/Phase18GoalInputTests.swift
  </read_first>
  <action>
    Add an optional durationDays: Int? field to the Goal model in SchemaV6.swift as a SwiftData additive lightweight migration. Per RESEARCH.md Open Question 1 (RESOLVED): optional with nil default qualifies for lightweight migration — no SchemaV9 bump required.

    Specifics:
    - In SchemaV6.swift, locate the @Model class Goal. Add a new stored property var durationDays: Int? = nil alongside the other optional fields. Maintain the project convention that all SwiftData properties are optional or have defaults (per CLAUDE.md). Do NOT use @Attribute(.unique) (CLAUDE.md prohibits this on CloudKit-synced fields).
    - In Goal's init (the one that takes the full parameter list — match existing initializer signatures), add durationDays: Int? = nil as a parameter and assign it: self.durationDays = durationDays. Verify any other Goal constructors (e.g. a convenience init) still compile with the additive param defaulting to nil.
    - In GoalInput.swift, add var durationDays: Int? (default nil) to the struct. Update the memberwise initializer or any explicit init to accept the new field. Update GoalInput's toGoal / apply(to:) helper (whichever pattern exists — read the file first) so the GoalInput value is propagated into the Goal on save.
    - Do NOT bump the schema version, add a new SchemaV9 file, or modify VitaminGMigrationPlan.swift. Optional-with-default is a SwiftData lightweight migration; no migration code is required.
    - Do NOT modify any UI files in this task — Step3DetailsScreen wiring happens in Plan 03.

    Wave 0 test alignment:
    - Phase18GoalInputTests.test_goalInput_carriesDurationDays passes once the GoalInput field exists.
    - Phase18GoalInputTests.test_goalInput_durationDaysIsOptional_defaultsToNil passes once nil-default is in place.
    - Phase18GoalInputTests.test_goalInput_durationDays_persistsToGoalModel passes when GoalInput round-trips through to Goal.durationDays.
  </action>
  <verify>
    <automated>cd "/Users/kyleharrington/Desktop/AI/Vitamin G/VitaminG/VitaminG" && xcodebuild test -scheme VitaminG -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:VitaminGTests/Phase18GoalInputTests 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "var durationDays: Int?" VitaminG/VitaminG/VitaminG/Models/SchemaV6.swift returns >= 1.
    - grep -c "durationDays" VitaminG/VitaminG/VitaminG/Models/GoalInput.swift returns >= 2 (declaration + assignment in toGoal/apply).
    - xcodebuild build -scheme VitaminG -destination 'platform=iOS Simulator,name=iPhone 16' succeeds with no compile errors related to durationDays.
    - xcodebuild test ... -only-testing:VitaminGTests/Phase18GoalInputTests returns all green.
    - No new Schema file created (! ls VitaminG/VitaminG/VitaminG/Models/SchemaV9.swift is true).
    - No @Attribute(.unique) introduced anywhere in this diff.
  </acceptance_criteria>
  <done>Goal model and GoalInput both carry durationDays as an optional with nil default. Phase18GoalInputTests is green. Existing tests (SchemaV6Tests, GoalViewModelTests) remain green.</done>
</task>

<task type="auto">
  <name>Task 2: Add GoalViewModel.addCheckIn, GoalCreationWizardViewModel.draftDurationDays, and configure(fromPremade:)</name>
  <files>
    VitaminG/VitaminG/VitaminG/ViewModels/GoalViewModel.swift,
    VitaminG/VitaminG/VitaminG/ViewModels/GoalCreationWizardViewModel.swift
  </files>
  <read_first>
    VitaminG/VitaminG/VitaminG/ViewModels/GoalViewModel.swift,
    VitaminG/VitaminG/VitaminG/ViewModels/GoalCreationWizardViewModel.swift,
    VitaminG/VitaminG/VitaminG/Models/SchemaV6.swift,
    VitaminG/VitaminG/VitaminG/Models/GoalInput.swift,
    VitaminG/VitaminG/VitaminGTests/Phase18GoalViewModelTests.swift,
    VitaminG/VitaminG/VitaminG/Models/GoalCategory.swift
  </read_first>
  <action>
    Add one ViewModel method to GoalViewModel and two additions (one stored property + one method) to GoalCreationWizardViewModel. Per RESEARCH.md Pattern 3 (addCheckIn), Pattern 7 (configure fromPremade), Pattern 8 (premadeGoal init param), and CONTEXT.md D-06.

    GoalViewModel.swift — add addCheckIn(for goal: Goal, context: ModelContext):
    - Place the method immediately after the existing toggleCompletion(goal:context:) method.
    - Method signature: func addCheckIn(for goal: Goal, context: ModelContext).
    - Guard against same-day duplicates (Pitfall 3 from RESEARCH.md): early-return when goal.completionEvents?.contains(where: { Calendar.current.isDateInToday($0.completedAt ?? .distantPast) }) == true.
    - Insert a new CompletionEvent, set its completedAt = Date(), tierRawValue = goal.tierRawValue, context.insert(event), then event.goal = goal (matching the order in toggleCompletion).
    - Do NOT set goal.isCompleted (this is the critical difference from toggleCompletion; per Pitfall 2 and D-12).
    - Call rescheduleNotification(context: context) and reloadWidgetTimelines() as toggleCompletion does (notification + widget freshness).
    - Use goal.completionEvents ?? [] style guards on any optional collection access per RESEARCH.md Anti-Patterns.

    GoalCreationWizardViewModel.swift — add draftDurationDays backing state (UNCONDITIONAL — required for Plan 03):
    - Add a stored property: var draftDurationDays: Int? = nil. Place it alongside the other draft fields (e.g. near draftTitle, draftTier). This is the source of truth for the Step 3 "How long?" picker that Plan 03 introduces.
    - In configure(from goal: Goal) (existing edit-mode setup, lines 64-82): add a line that loads the goal's duration into the draft: self.draftDurationDays = goal.durationDays. Place it alongside the other field assignments inside configure(from:).
    - In reset() (if such a method exists; otherwise wherever draft fields are zeroed): set self.draftDurationDays = nil. Match the project's existing reset pattern.
    - Trace from the existing wizard save flow (the buildInput() / commit() / makeGoalInput() helper — read the file to identify) and wire draftDurationDays into the GoalInput initializer so the value reaches Goal.durationDays on save. The Plan 02 Task 1 GoalInput change already exposes durationDays — this task ensures the wizard's save path populates it.
    - This addition is UNCONDITIONAL — do not gate it on "if not already added." Plan 03 expects draftDurationDays to exist when its Step3DetailsScreen task runs.

    GoalCreationWizardViewModel.swift — add configure(fromPremade title: String, category: GoalCategory):
    - Place the method immediately after the existing configure(from goal: Goal) method.
    - Method body: call reset() first to clear any prior draft state, then set selectedCategory = category, set draftTitle = title, set currentStep = 2. Order matters — reset() first to avoid leaking stale state from a prior incomplete creation. (Because reset() sets draftDurationDays = nil, the user picks a duration in Step 3.)
    - The jump to step 2 (Step 3 details screen — zero-indexed) is required by D-06: the premade goal already supplies category (Step 1) and name (Step 2), so the user lands on Step 3 for tier/duration/start date/reminder/privacy.
    - Do NOT add a new published/observable property; the existing currentStep is the source of truth.

    Both classes keep their existing @MainActor @Observable final class declarations.

    Wave 0 test alignment:
    - Phase18GoalViewModelTests.test_addCheckIn_* (4 tests) all turn green.
    - Phase18GoalInputTests.test_goalInput_durationDays_persistsToGoalModel — if this test exercises the full wizard save path, it benefits from draftDurationDays plumbing here; if it tests GoalInput directly (Task 1), no extra change needed.
    - Phase18StreakEngineTests (Plan 01 tests) remain green — those test StreakEngine, not GoalViewModel.
  </action>
  <verify>
    <automated>cd "/Users/kyleharrington/Desktop/AI/Vitamin G/VitaminG/VitaminG" && xcodebuild test -scheme VitaminG -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:VitaminGTests/Phase18GoalViewModelTests 2>&1 | tail -10 && xcodebuild test -scheme VitaminG -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:VitaminGTests/Phase18GoalInputTests 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "func addCheckIn(for goal: Goal, context: ModelContext)" VitaminG/VitaminG/VitaminG/ViewModels/GoalViewModel.swift == 1.
    - grep -c "isDateInToday" VitaminG/VitaminG/VitaminG/ViewModels/GoalViewModel.swift >= 1 (same-day dedup guard).
    - grep -c "var draftDurationDays: Int?" VitaminG/VitaminG/VitaminG/ViewModels/GoalCreationWizardViewModel.swift == 1 (declaration).
    - grep -c "draftDurationDays" VitaminG/VitaminG/VitaminG/ViewModels/GoalCreationWizardViewModel.swift >= 3 (declaration + load in configure(from:) + write into GoalInput on save).
    - grep -c "func configure(fromPremade" VitaminG/VitaminG/VitaminG/ViewModels/GoalCreationWizardViewModel.swift == 1.
    - grep -c "currentStep = 2" VitaminG/VitaminG/VitaminG/ViewModels/GoalCreationWizardViewModel.swift >= 1 inside the new configure(fromPremade:) method.
    - addCheckIn body does NOT contain goal.isCompleted = true (verified by reading the method body — the only isCompleted = true occurrence in this file must remain inside the pre-existing toggleCompletion method).
    - xcodebuild test ... -only-testing:VitaminGTests/Phase18GoalViewModelTests returns all green.
    - xcodebuild test ... -only-testing:VitaminGTests/Phase18GoalInputTests round-trip test stays green (durationDays flows from GoalInput through Goal regardless of whether the wizard save path is exercised in the test).
    - Existing GoalViewModelTests and GoalCreationWizardViewModelTests continue to pass.
  </acceptance_criteria>
  <done>addCheckIn, draftDurationDays, and configure(fromPremade:) all exist. Phase18GoalViewModelTests is green. Existing ViewModel tests remain green. No regression in toggleCompletion behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Add VGQuoteBank.all flattened static quote array</name>
  <files>
    VitaminG/VitaminG/VitaminG/Services/VGQuoteBank.swift
  </files>
  <read_first>
    VitaminG/VitaminG/VitaminG/Services/VGQuoteBank.swift,
    VitaminG/VitaminG/VitaminGTests/Phase18QuoteBankTests.swift
  </read_first>
  <action>
    Per RESEARCH.md Open Question 4 (RESOLVED): add static var all: [VGQuote] to VGQuoteBank that concatenates every per-category static array. This is the foundation for HOME-02 daily rotation in Plan 04.

    Specifics:
    - Locate the VGQuoteBank type in Services/VGQuoteBank.swift.
    - Read every existing per-category static array (e.g. static let body, static let mind, etc.). Use whatever VGQuote type / VGQuoteCategory enum exists in the file.
    - Add static var all: [VGQuote] that returns the concatenation of every category array. Use var (computed) so the array is built at access time, not at type initialization. Order: concatenate in declaration order (deterministic — required by Phase18QuoteBankTests.test_VGQuoteBank_all_isDeterministic).
    - Do NOT shuffle, randomize, or de-duplicate the result. Each call to VGQuoteBank.all MUST return the same array.
    - Do NOT add any new quote text — only flatten existing arrays. Per CLAUDE.md, no third-party deps; pure Swift array concatenation.
    - Document the new property with a doc comment: /// Flattened list of every quote across all categories. Stable order across calls. Used by daily quote rotation in HomeView.
  </action>
  <verify>
    <automated>cd "/Users/kyleharrington/Desktop/AI/Vitamin G/VitaminG/VitaminG" && xcodebuild test -scheme VitaminG -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:VitaminGTests/Phase18QuoteBankTests 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "static var all" VitaminG/VitaminG/VitaminG/Services/VGQuoteBank.swift >= 1.
    - The new all property is computed by concatenating existing per-category statics, not by introducing new quote literals (no new string literals added to VGQuoteBank.swift; the diff is a single computed property addition).
    - Successive calls return identical arrays (covered by test_VGQuoteBank_all_isDeterministic).
    - xcodebuild test ... -only-testing:VitaminGTests/Phase18QuoteBankTests returns all green.
    - No shuffling/randomization APIs (.shuffled(), .randomElement()) appear in the new code.
  </acceptance_criteria>
  <done>VGQuoteBank.all exists as a computed concatenation of all category arrays. Phase18QuoteBankTests is green. No other tests regress.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User goal input → SwiftData | New durationDays field receives Int values from UI in Plan 03; this plan just declares the field. Validation enforced via GoalViewModel.sanitize()/validate() patterns used by other fields. |
| SwiftData → CloudKit private DB | Goal model field syncs via existing private DB. New optional Int field is additive lightweight migration; no schema regression. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-02-01 | Tampering | Goal.durationDays | mitigate | Field is optional Int? with nil default; downstream UI (Plan 03) must constrain to reasonable range (e.g. 1..365). Plan 03 enforces the UI gate; this plan only declares the field. |
| T-18-02-02 | Tampering | Same-day duplicate check-in (addCheckIn) | mitigate | Guard clause uses Calendar.current.isDateInToday on completedAt of existing events; early return prevents inflation per RESEARCH.md Pitfall 3. |
| T-18-02-03 | Information Disclosure | CompletionEvent created in addCheckIn | accept | CompletionEvent contains no PII; tierRawValue + timestamp only; same shape as existing toggleCompletion path. |
| T-18-02-04 | Denial of Service | VGQuoteBank.all computed every read | accept | Per-call concatenation cost is O(n) where n < 100; HomeView reads once per render; negligible. If profiling shows hot path, cache later. |
| T-18-02-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan; pure Swift edits using existing project types. |
</threat_model>

<verification>
- xcodebuild build succeeds.
- xcodebuild test -only-testing:VitaminGTests/Phase18GoalInputTests is GREEN.
- xcodebuild test -only-testing:VitaminGTests/Phase18GoalViewModelTests is GREEN.
- xcodebuild test -only-testing:VitaminGTests/Phase18QuoteBankTests is GREEN.
- Existing tests (StreakEngineTests, SchemaV6Tests, GoalViewModelTests, GoalCreationWizardViewModelTests) remain GREEN.
- No new Schema file added; no migration step.
- No third-party dependency introduced (verified by no changes to Package.swift / xcodeproj package references).
</verification>

<success_criteria>
- Goal.durationDays: Int? exists in SchemaV6.swift with nil default.
- GoalInput.durationDays plumbed through to Goal on save.
- GoalViewModel.addCheckIn(for:context:) exists with same-day dedup guard and does NOT flip goal.isCompleted.
- GoalCreationWizardViewModel.draftDurationDays: Int? exists, is loaded from Goal in configure(from:), is reset by reset(), and is flushed into GoalInput on save.
- GoalCreationWizardViewModel.configure(fromPremade:category:) exists, calls reset(), sets category + title, jumps to currentStep = 2.
- VGQuoteBank.all exists, deterministic, concatenates all per-category arrays.
- All Wave 0 Phase18*Tests that test these symbols are GREEN: Phase18GoalInputTests, Phase18GoalViewModelTests, Phase18QuoteBankTests, plus per-goal streak tests in Phase18StreakEngineTests (those pass naturally once GoalViewModel.addCheckIn is in place; the StreakEngine itself is unchanged).
</success_criteria>

<output>
Create .planning/phases/18-home-tab-goals-flow/18-02-SUMMARY.md when done. Note: any forward-reference symbols still RED (the GoalDayGridCalendar helper from Phase18GoalDayGridTests is intentionally left RED here — Plan 05 lands it).
</output>
