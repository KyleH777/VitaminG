---
phase: 18-home-tab-goals-flow
plan: 03
type: execute
wave: 2
depends_on: [18-02]
files_modified:
  - VitaminG/VitaminG/VitaminG/Views/GoalCreation/GoalEntryChoiceView.swift
  - VitaminG/VitaminG/VitaminG/Views/GoalCreation/PremadeGoalsListView.swift
  - VitaminG/VitaminG/VitaminG/Views/GoalCreation/GoalCreationWizardView.swift
  - VitaminG/VitaminG/VitaminG/Views/GoalCreation/Step3DetailsScreen.swift
  - VitaminG/VitaminG/VitaminG/Views/GoalListView.swift
autonomous: true
requirements: [GOAL2-01, GOAL2-02, GOAL2-03]

must_haves:
  truths:
    - "Tapping +add in GoalListView opens GoalEntryChoiceView as a sheet with 3 visible path cards"
    - "Selecting 'Need ideas' navigates to PremadeGoalsListView (pushed inside the sheet's NavigationStack) showing every GoalCategory's suggestions grouped by category"
    - "Tapping a premade goal emits the (title, category) via onSelect callback up to the parent, which dismisses the choice sheet then presents GoalCreationWizardView with premadeGoal pre-fill"
    - "GoalCreationWizardView accepts an optional premadeGoal:(String, GoalCategory)? init parameter and, in .onAppear, calls wizardVM.configure(fromPremade:category:) on its OWN internal VM when premadeGoal is non-nil"
    - "Selecting 'Already have a goal' dismisses the sheet and presents the wizard at Step 2 (zero-indexed step 1) via startAtStep parameter"
    - "Selecting 'Build my own goal' dismisses the sheet and presents the wizard at Step 1 (zero-indexed step 0)"
    - "Step 3 of the wizard captures duration (days), start date, tier, reminder time, and public/private toggle, all bound to GoalCreationWizardViewModel.draftDurationDays (added in Plan 02)"
    - "Saved goals carry durationDays through to the SwiftData Goal model"
  artifacts:
    - path: "VitaminG/VitaminG/VitaminG/Views/GoalCreation/GoalEntryChoiceView.swift"
      provides: "3-path choice sheet with NavigationStack hosting PremadeGoalsListView push; onSelectWizard + onSelectPremade callbacks (no orphan VM)"
      min_lines: 60
    - path: "VitaminG/VitaminG/VitaminG/Views/GoalCreation/PremadeGoalsListView.swift"
      provides: "Hardcoded pre-made goals list sourced from GoalCategory.suggestions; emits selection via onSelect callback (no orphan VM)"
      min_lines: 60
    - path: "VitaminG/VitaminG/VitaminG/Views/GoalCreation/GoalCreationWizardView.swift"
      provides: "Extended init with startAtStep + premadeGoal parameters; .onAppear applies premadeGoal pre-fill to the wizard's own internal VM"
      contains: "premadeGoal"
    - path: "VitaminG/VitaminG/VitaminG/Views/GoalCreation/Step3DetailsScreen.swift"
      provides: "Duration field + start date + tier + reminder + privacy; binds to draftDurationDays"
      contains: "draftDurationDays"
    - path: "VitaminG/VitaminG/VitaminG/Views/GoalListView.swift"
      provides: "+add button now presents GoalEntryChoiceView, with wizardStartStep + pendingPremadeGoal state for downstream wizard injection"
      contains: "GoalEntryChoiceView"
  key_links:
    - from: "GoalListView '+add' button"
      to: "GoalEntryChoiceView (sheet)"
      via: ".sheet(isPresented: $showingGoalEntryChoice)"
      pattern: "GoalEntryChoiceView"
    - from: "PremadeGoalsListView row tap"
      to: "onSelect(title, category) callback (parent stores in pendingPremadeGoal)"
      via: "callback param; no local ViewModel writes"
      pattern: "onSelect"
    - from: "GoalEntryChoiceView callbacks"
      to: "parent's pendingPremadeGoal / wizardStartStep state"
      via: "onSelectWizard + onSelectPremade closures"
      pattern: "onSelectPremade"
    - from: "GoalCreationWizardView(premadeGoal:) .onAppear"
      to: "wizardVM.configure(fromPremade:category:) on the wizard's OWN VM"
      via: "init param + .onAppear branch"
      pattern: "configure\\(fromPremade"
    - from: "GoalEntryChoiceView wizard paths"
      to: "GoalCreationWizardView(startAtStep:, premadeGoal:)"
      via: "dismiss + present from parent"
      pattern: "startAtStep"
    - from: "Step3DetailsScreen duration field"
      to: "GoalCreationWizardViewModel.draftDurationDays (added in Plan 02)"
      via: "TextField/Picker binding"
      pattern: "draftDurationDays"
---

<objective>
Build the user-facing goal-creation entry flow: a 3-path choice sheet (GoalEntryChoiceView), a pre-made goals list (PremadeGoalsListView), wizard plumbing for startAtStep + premadeGoal, a duration field in Step 3 bound to Plan 02's draftDurationDays, and the GoalListView +add hook. HomeView's +add wiring lives in Plan 04 (same sheet, different parent view).

Purpose: Implements GOAL2-01 (3-step wizard with duration), GOAL2-02 (pre-made goals path), GOAL2-03 (already-have-a-goal path), and the entry-choice routing required by D-05/D-06/D-08.
Output: 2 new view files, 3 modified view files, all wired through the existing GoalCreationWizardViewModel and Plan 02's configure(fromPremade:), draftDurationDays, and Goal.durationDays. Pre-fill flows via Pattern 5 callbacks + Pattern 8 init param — NO orphan ViewModels.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-home-tab-goals-flow/18-CONTEXT.md
@.planning/phases/18-home-tab-goals-flow/18-RESEARCH.md
@.planning/phases/18-home-tab-goals-flow/18-PATTERNS.md
@.planning/phases/18-home-tab-goals-flow/18-UI-SPEC.md
@.planning/phases/18-home-tab-goals-flow/18-02-SUMMARY.md
@CLAUDE.md
</context>

<interfaces>
From Plan 02 (already landed in Wave 1):
- GoalCreationWizardViewModel.configure(fromPremade title: String, category: GoalCategory) — sets category + draftTitle and jumps to currentStep = 2.
- GoalCreationWizardViewModel.draftDurationDays: Int? — backing state for the Step 3 duration picker; flushed into GoalInput on save.
- Goal.durationDays: Int? in SchemaV6.
- GoalInput.durationDays: Int? plumbed through to Goal.

From existing codebase:
- GoalCategory (Models/GoalCategory.swift): var suggestions: [String], var emoji: String, var subtitle: String. allCases includes .body, .mind, .wellness, .money, .connection, .creative, .habit (excluding .other since suggestions are empty).
- GoalCreationWizardView existing init: init(isOnboarding: Bool = false, editingGoal: Goal? = nil, onComplete: (() -> Void)? = nil). This task adds startAtStep: Int = 0 AND premadeGoal: (title: String, category: GoalCategory)? = nil as additive parameters.
- GoalCreationWizardView wraps itself in NavigationStack when !isOnboarding (lines 39-53 of GoalCreationWizardView.swift). RESEARCH.md Pitfall 4 forbids nesting another NavigationStack inside GoalEntryChoiceView that presents this wizard.
- VGTheme tokens: VGTheme.sandLight, VGTheme.surface, VGTheme.accentTerra, VGTheme.separator, VGTheme.textPrimary, VGTheme.textMuted, VGTheme.serif(34), VGTheme.serif(18), VGTheme.serif(20, weight: .semibold).
- UI-SPEC: GoalEntryChoiceView path icons "lightbulb.fill", "pencil", "wand.and.stars"; titles "Need ideas" / "Already have a goal" / "Build my own goal"; sheet title "Start your goal"; card min height 72pt; cornerRadius 16; stroke VGTheme.separator 1pt.
- UI-SPEC: PremadeGoalsListView header text "My goals, your journey starts here…", category section headers, row card with category emoji + title + chevron.

Sheet routing pattern (RESEARCH.md Pattern 5 + Pattern 8 + Pitfall 4 + Pitfall 8):
- Parent view (GoalListView, HomeView) owns:
  @State var showingGoalEntryChoice = false
  @State var showingWizard = false
  @State var wizardStartStep: Int = 0
  @State var pendingPremadeGoal: (title: String, category: GoalCategory)? = nil
- .sheet(isPresented: $showingGoalEntryChoice) presents GoalEntryChoiceView(
    onSelectWizard: { step in ... },
    onSelectPremade: { title, category in ... })
- onSelectWizard(step): parent sets wizardStartStep = step, pendingPremadeGoal = nil, showingGoalEntryChoice = false, then DispatchQueue.main.asyncAfter(0.35) sets showingWizard = true.
- onSelectPremade(title, category): parent sets pendingPremadeGoal = (title, category), wizardStartStep = 2, showingGoalEntryChoice = false, then DispatchQueue.main.asyncAfter(0.35) sets showingWizard = true.
- .sheet(isPresented: $showingWizard) presents GoalCreationWizardView(startAtStep: wizardStartStep, premadeGoal: pendingPremadeGoal).
- The wizard's .onAppear (modified in Task 2) reads premadeGoal and calls wizardVM.configure(fromPremade:category:) on its OWN internal VM, OR uses startAtStep if no premadeGoal.
- PremadeGoalsListView is pushed INSIDE GoalEntryChoiceView's own NavigationStack. PremadeGoalsListView holds NO ViewModel — it takes an onSelect: (String, GoalCategory) -> Void callback and emits the selection. GoalEntryChoiceView's NavigationLink-presented PremadeGoalsListView forwards that selection up via its own onSelectPremade callback to the parent (so the chain is: row tap → PremadeGoalsListView.onSelect → GoalEntryChoiceView.onSelectPremade → parent's pendingPremadeGoal state).

**ANTI-PATTERN BLOCKED:** Do NOT add `@State private var wizardVM = GoalCreationWizardViewModel()` to PremadeGoalsListView or GoalEntryChoiceView. The wizard sheet (when presented later by the parent) creates its OWN VM — any writes to a picker-local VM are silently discarded. See RESEARCH.md Pitfall 8.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create GoalEntryChoiceView + PremadeGoalsListView (callback pattern, no orphan VMs)</name>
  <files>
    VitaminG/VitaminG/VitaminG/Views/GoalCreation/GoalEntryChoiceView.swift,
    VitaminG/VitaminG/VitaminG/Views/GoalCreation/PremadeGoalsListView.swift
  </files>
  <read_first>
    VitaminG/VitaminG/VitaminG/Views/GoalListView.swift,
    VitaminG/VitaminG/VitaminG/Views/HomeView.swift,
    VitaminG/VitaminG/VitaminG/Models/GoalCategory.swift,
    VitaminG/VitaminG/VitaminG/VGTheme.swift,
    VitaminG/VitaminG/VitaminG/ViewModels/GoalCreationWizardViewModel.swift,
    .planning/phases/18-home-tab-goals-flow/18-UI-SPEC.md,
    .planning/phases/18-home-tab-goals-flow/18-PATTERNS.md
  </read_first>
  <action>
    Per D-05, D-06, D-08, UI-SPEC §GoalEntryChoiceView / §PremadeGoalsListView, RESEARCH.md Pattern 5 (callback routing), and RESEARCH.md Pitfall 8 (no orphan VMs).

    File 1 — GoalEntryChoiceView.swift (new): SwiftUI View struct displayed in a .sheet.
    - Public API:
      struct GoalEntryChoiceView: View {
          @Environment(\.dismiss) private var dismiss
          let onSelectWizard: (Int) -> Void           // step: 0 = "Build my own goal", 1 = "Already have a goal"
          let onSelectPremade: (String, GoalCategory) -> Void  // (premade title, category)
          var body: some View { ... }
      }
    - DO NOT add @State for any GoalCreationWizardViewModel. This view never owns a wizard VM. All selection state is emitted upward via the two callbacks.
    - Body wraps content in a NavigationStack. navigationTitle "Start your goal", navigationBarTitleDisplayMode .inline. Toolbar cancellationAction button "Cancel" calls dismiss().
    - Background: VGTheme.sandLight.ignoresSafeArea().
    - VStack(spacing: 12) with 3 path cards. Outer padding .padding(.horizontal, 24).
    - Path cards (per UI-SPEC):
        - Card 1 ("Need ideas", lightbulb.fill icon): wrapped in NavigationLink(destination: PremadeGoalsListView(onSelect: { title, category in
              onSelectPremade(title, category)
              dismiss()
          })). Subtitle: "Browse pre-made goals by category".
        - Card 2 ("Already have a goal", pencil icon): Button { onSelectWizard(1); dismiss() }. Subtitle: "Jump straight in — fill in what you know".
        - Card 3 ("Build my own goal", wand.and.stars icon): Button { onSelectWizard(0); dismiss() }. Subtitle: "Step-by-step: category, name, schedule".
    - Each card uses helper pathCard(icon: String, title: String, subtitle: String) returning the card content. Card visual: HStack with leading 44x44 rounded-rect "icon tile" (VGTheme.accentTerra.opacity(0.15) fill + accentTerra SF Symbol 20pt), VStack with title (VGTheme.serif(18) Cormorant Regular, VGTheme.textPrimary) + subtitle (.system(size: 12), VGTheme.textMuted), and trailing chevron.right (VGTheme.textMuted, 12pt). Card padding 16pt, frame(maxWidth: .infinity, minHeight: 72, alignment: .leading), background VGTheme.surface, clipShape RoundedRectangle(cornerRadius: 16), overlay RoundedRectangle(cornerRadius: 16).strokeBorder(VGTheme.separator, lineWidth: 1).
    - Buttons use buttonStyle(.plain) so the card chrome is preserved.
    - Accessibility: .accessibilityLabel on each card describing the action. NavigationLink card uses accessibilityHint "Browse pre-made goals".

    File 2 — PremadeGoalsListView.swift (new): SwiftUI View struct shown via NavigationLink push from GoalEntryChoiceView.
    - Public API:
      struct PremadeGoalsListView: View {
          let onSelect: (String, GoalCategory) -> Void
          var body: some View { ... }
      }
    - DO NOT add @State private var wizardVM. DO NOT add @State private var showingWizard. DO NOT present any .sheet from this view. DO NOT call configure(fromPremade:) here. This view's only responsibility is to render the list and emit the selection.
    - Data: define a local PremadeGoal struct (Identifiable: let id = UUID(), title: String, category: GoalCategory). Build the grouped data source via:
        let groupedGoals: [(category: GoalCategory, goals: [PremadeGoal])] = GoalCategory.allCases
            .filter { !$0.suggestions.isEmpty }
            .map { cat in
                (category: cat,
                 goals: cat.suggestions.map { suggestion in PremadeGoal(title: suggestion, category: cat) })
            }
    - Layout: ScrollView with VStack(alignment: .leading, spacing: 20). Top heading "My goals, your journey starts here…" rendered as Cormorant Garamond Regular 34pt (VGTheme.serif(34)), VGTheme.clay (or VGTheme.textPrimary if clay is unavailable — read VGTheme.swift first). Below heading, for each group in groupedGoals: sectionHeader(group.category.title.uppercased()) using SF Pro 13pt Semibold uppercase, kerning 1.0, VGTheme.muted; then VStack(spacing: 8) of premadeGoalRow per goal.
    - premadeGoalRow(_ premadeGoal: PremadeGoal): HStack(spacing: 14) with category.emoji (font .title2), VStack title (.system(size: 13.5, weight: .medium), VGTheme.textPrimary, lineLimit 2) + category.subtitle (.system(size: 11), VGTheme.textMuted), Spacer, chevron.right (12pt VGTheme.textMuted). Card wrapper: padding 14, VGTheme.surface bg, cornerRadius 14, strokeBorder VGTheme.separator 1pt. The whole row is a Button { onSelect(premadeGoal.title, premadeGoal.category) }.
    - .navigationTitle("Pre-made goals"), .navigationBarTitleDisplayMode(.inline).
    - Background: VGTheme.sandLight.ignoresSafeArea().
    - Per UI-SPEC accent reservation: chevron is textMuted (not accentTerra). "Add" affordance is the chevron + tap target — no separate "Add" button text needed.

    Both views: do NOT add new NavigationStack inside PremadeGoalsListView — it inherits the GoalEntryChoiceView's NavigationStack.
    Both views: every interactive element MUST have .accessibilityLabel. Decorative elements use .accessibilityHidden(true).
    Both views: ONLY use VGTheme tokens; no raw hex Color literals.
    Both views: NO @State holding a GoalCreationWizardViewModel. NO local sheet presentation of GoalCreationWizardView. Selection state flows up via callbacks (Pattern 5) so the parent can inject pre-fill into the wizard's OWN VM via Pattern 8 (Task 2).

    Wave 0 test alignment:
    - Phase18PremadeGoalsTests asserts on GoalCategory.suggestions (data layer) — those pass without view-layer changes since they test the data the new view reads. Run them to confirm.
  </action>
  <verify>
    <automated>cd "/Users/kyleharrington/Desktop/AI/Vitamin G/VitaminG/VitaminG" && xcodebuild build -scheme VitaminG -destination 'platform=iOS Simulator,name=iPhone 16' 2>&1 | tail -15 && xcodebuild test -scheme VitaminG -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:VitaminGTests/Phase18PremadeGoalsTests 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - File VitaminG/VitaminG/VitaminG/Views/GoalCreation/GoalEntryChoiceView.swift exists.
    - File VitaminG/VitaminG/VitaminG/Views/GoalCreation/PremadeGoalsListView.swift exists.
    - grep -c "struct GoalEntryChoiceView" VitaminG/VitaminG/VitaminG/Views/GoalCreation/GoalEntryChoiceView.swift == 1.
    - grep -c "struct PremadeGoalsListView" VitaminG/VitaminG/VitaminG/Views/GoalCreation/PremadeGoalsListView.swift == 1.
    - grep -c "let onSelectWizard: (Int) -> Void" VitaminG/VitaminG/VitaminG/Views/GoalCreation/GoalEntryChoiceView.swift == 1.
    - grep -c "let onSelectPremade: (String, GoalCategory) -> Void" VitaminG/VitaminG/VitaminG/Views/GoalCreation/GoalEntryChoiceView.swift == 1.
    - grep -c "let onSelect: (String, GoalCategory) -> Void" VitaminG/VitaminG/VitaminG/Views/GoalCreation/PremadeGoalsListView.swift == 1.
    - grep -c "lightbulb.fill\|pencil\|wand.and.stars" VitaminG/VitaminG/VitaminG/Views/GoalCreation/GoalEntryChoiceView.swift >= 3.
    - grep -c "Need ideas\|Already have a goal\|Build my own goal" VitaminG/VitaminG/VitaminG/Views/GoalCreation/GoalEntryChoiceView.swift >= 3.
    - grep -c "GoalCategory.allCases" VitaminG/VitaminG/VitaminG/Views/GoalCreation/PremadeGoalsListView.swift >= 1.
    - **Orphan-VM checks (Pitfall 8 enforcement):**
      - grep -c "@State.*wizardVM\|@State.*GoalCreationWizardViewModel" VitaminG/VitaminG/VitaminG/Views/GoalCreation/PremadeGoalsListView.swift == 0
      - grep -c "@State.*wizardVM\|@State.*GoalCreationWizardViewModel" VitaminG/VitaminG/VitaminG/Views/GoalCreation/GoalEntryChoiceView.swift == 0
      - grep -c "configure(fromPremade" VitaminG/VitaminG/VitaminG/Views/GoalCreation/PremadeGoalsListView.swift == 0  (no direct VM call from picker)
      - grep -c "configure(fromPremade" VitaminG/VitaminG/VitaminG/Views/GoalCreation/GoalEntryChoiceView.swift == 0
      - grep -c "GoalCreationWizardView(" VitaminG/VitaminG/VitaminG/Views/GoalCreation/PremadeGoalsListView.swift == 0 (no wizard presentation from picker)
      - grep -c "GoalCreationWizardView(" VitaminG/VitaminG/VitaminG/Views/GoalCreation/GoalEntryChoiceView.swift == 0 (no wizard presentation from choice sheet)
      - grep -c "\\.sheet(" VitaminG/VitaminG/VitaminG/Views/GoalCreation/PremadeGoalsListView.swift == 0 (parent presents wizard, not this view)
    - No NavigationStack inside PremadeGoalsListView (grep -c "NavigationStack" PremadeGoalsListView.swift == 0).
    - GoalEntryChoiceView contains exactly one NavigationStack.
    - Both files added to the VitaminG app target in project.pbxproj.
    - xcodebuild build succeeds; Phase18PremadeGoalsTests is GREEN.
  </acceptance_criteria>
  <done>Both new view files compile. They are reachable via project target membership. PremadeGoalsListView enumerates GoalCategory suggestions and emits selections via onSelect callback. NEITHER view holds a GoalCreationWizardViewModel @State. Pre-fill plumbing happens in Task 2 via the wizard's own .onAppear.</done>
</task>

<task type="auto">
  <name>Task 2: Extend GoalCreationWizardView with startAtStep + premadeGoal init params; add duration field to Step3DetailsScreen</name>
  <files>
    VitaminG/VitaminG/VitaminG/Views/GoalCreation/GoalCreationWizardView.swift,
    VitaminG/VitaminG/VitaminG/Views/GoalCreation/Step3DetailsScreen.swift
  </files>
  <read_first>
    VitaminG/VitaminG/VitaminG/Views/GoalCreation/GoalCreationWizardView.swift,
    VitaminG/VitaminG/VitaminG/Views/GoalCreation/Step3DetailsScreen.swift,
    VitaminG/VitaminG/VitaminG/ViewModels/GoalCreationWizardViewModel.swift,
    VitaminG/VitaminG/VitaminG/Models/GoalInput.swift,
    VitaminG/VitaminG/VitaminG/Models/SchemaV6.swift,
    VitaminG/VitaminG/VitaminG/Models/GoalCategory.swift,
    .planning/phases/18-home-tab-goals-flow/18-UI-SPEC.md
  </read_first>
  <action>
    Per D-07, GOAL2-01, UI-SPEC §GoalCreationWizardView Step 3, and RESEARCH.md Pattern 8 + Common Pitfalls #7 + Pitfall 8.

    GoalCreationWizardView.swift:
    - Extend init to accept TWO new additive parameters alongside existing isOnboarding, editingGoal, onComplete:
        init(isOnboarding: Bool = false,
             editingGoal: Goal? = nil,
             startAtStep: Int = 0,                                            // NEW
             premadeGoal: (title: String, category: GoalCategory)? = nil,     // NEW
             onComplete: (() -> Void)? = nil)
    - Store both as let startAtStep: Int and let premadeGoal: (title: String, category: GoalCategory)? on the View struct.
    - In .onAppear, apply pre-fill with this priority order (editingGoal > premadeGoal > startAtStep > default Step 1):
        .onAppear {
            if let goal = editingGoal {
                wizardVM.configure(from: goal)
            } else if let pg = premadeGoal {
                wizardVM.configure(fromPremade: pg.title, category: pg.category)
            } else if startAtStep > 0 {
                wizardVM.currentStep = startAtStep
            }
        }
      This is critical: configure(fromPremade:) runs on the wizard's OWN internal wizardVM (the @State the wizard already owns). This is the entire fix for Pitfall 8 — pre-fill writes go to the VM the wizard is bound to, not an orphan VM from the picker.
    - Do NOT modify the NavigationStack guard in wizardContent (lines 39-53). Per RESEARCH.md Pitfall 4 + Anti-Pattern: wizard wraps itself in NavigationStack unless isOnboarding. Plan 03 leaves this exact structure intact.
    - Do NOT modify step routing logic, draft state, or onComplete behavior.

    Step3DetailsScreen.swift — add duration field:
    - Add a "How long?" duration row alongside the existing tier picker, reminder time, and public/private toggle. Place it at the top of the form, immediately after the screen heading and step dots, before the existing tier picker.
    - UI choice: use a segmented Picker with options { "1 week" (7), "1 month" (30), "3 months" (90), "Custom" }. When "Custom" is selected, reveal a TextField (.keyboardType(.numberPad)) bound to a String state whose Int conversion writes to wizardVM.draftDurationDays. When a preset is selected, write the preset day count directly.
    - Add a local @State (e.g. var durationSelection: DurationOption = .month) to track which segment is selected. Bind the picker via Picker(selection: $durationSelection) over a local enum DurationOption { case week, month, threeMonths, custom }. On selection change (.onChange(of: durationSelection)), write the corresponding Int (or nil for unselected/empty custom) into wizardVM.draftDurationDays.
    - **Plan 02 has already added wizardVM.draftDurationDays: Int? = nil unconditionally** (Plan 02 Task 2, acceptance criterion: `grep -c "var draftDurationDays: Int?" GoalCreationWizardViewModel.swift == 1`). Do NOT add it here. If Step3DetailsScreen cannot find draftDurationDays at edit time, this is a Plan 02 regression — investigate before patching here.
    - The wizard's existing save flow (buildInput() / commit() / makeGoalInput()) was wired in Plan 02 Task 2 to pass draftDurationDays into GoalInput.durationDays. Step3DetailsScreen does NOT need to touch the save flow — only the picker → draftDurationDays write path.
    - Edit-mode (configure(from goal:)) was wired in Plan 02 Task 2 to load goal.durationDays into draftDurationDays. Step3DetailsScreen does NOT need to handle initial value loading — bind the local durationSelection from draftDurationDays in .onAppear (e.g. if draftDurationDays == 7 → .week, 30 → .month, 90 → .threeMonths, else if non-nil → .custom, else default → .month).
    - Visual: section label "HOW LONG?" using sectionLabel helper (per PATTERNS.md Step3DetailsScreen section label pattern). Segmented Picker uses .pickerStyle(.segmented). Custom TextField rows reuse the existing white card container (padding 14, RoundedRectangle 12pt, shadow). When Custom is selected, validate input is 1..365 — if out of range, show inline error text in red below the field and disable the save button.
    - Typography constraint per UI-SPEC: do NOT introduce CormorantGaramond-Medium weight. Use SemiBold or Regular only.
    - All new code uses VGTheme tokens — no raw hex.

    Wave 0 test alignment:
    - Phase18GoalInputTests should be GREEN already (Plan 02 round-tripped the field through GoalInput). This task confirms the UI surface populates the field; round-trip test should remain GREEN.
  </action>
  <verify>
    <automated>cd "/Users/kyleharrington/Desktop/AI/Vitamin G/VitaminG/VitaminG" && xcodebuild build -scheme VitaminG -destination 'platform=iOS Simulator,name=iPhone 16' 2>&1 | tail -15 && xcodebuild test -scheme VitaminG -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:VitaminGTests/Phase18GoalInputTests 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "startAtStep" VitaminG/VitaminG/VitaminG/Views/GoalCreation/GoalCreationWizardView.swift >= 3 (init param, stored property, onAppear branch).
    - grep -c "premadeGoal" VitaminG/VitaminG/VitaminG/Views/GoalCreation/GoalCreationWizardView.swift >= 3 (init param, stored property, onAppear branch).
    - grep -c "wizardVM.configure(fromPremade:" VitaminG/VitaminG/VitaminG/Views/GoalCreation/GoalCreationWizardView.swift >= 1 (the wizard's OWN VM gets pre-filled — the entire Pitfall 8 fix).
    - grep -c "startAtStep > 0" VitaminG/VitaminG/VitaminG/Views/GoalCreation/GoalCreationWizardView.swift >= 1.
    - grep -c "draftDurationDays" VitaminG/VitaminG/VitaminG/Views/GoalCreation/Step3DetailsScreen.swift >= 1 (write from picker).
    - grep -c "HOW LONG\?\|How long" VitaminG/VitaminG/VitaminG/Views/GoalCreation/Step3DetailsScreen.swift >= 1 (section label).
    - grep -c "CormorantGaramond-Medium" VitaminG/VitaminG/VitaminG/Views/GoalCreation/Step3DetailsScreen.swift == 0 (typography constraint).
    - The existing NavigationStack guard in GoalCreationWizardView.swift (the if isOnboarding else NavigationStack pattern) is unchanged — visual diff shows only the init params, stored properties, and onAppear branches as additions.
    - xcodebuild build succeeds and Phase18GoalInputTests round-trips durationDays through GoalInput → Goal.
  </acceptance_criteria>
  <done>Wizard accepts startAtStep AND premadeGoal init parameters. .onAppear applies premadeGoal pre-fill to the wizard's OWN internal wizardVM via configure(fromPremade:category:) — fixing Pitfall 8. Step 3 captures duration via the picker; durationDays flows from picker → wizardVM.draftDurationDays (added in Plan 02) → GoalInput → Goal on save. No regression in onboarding-mode behavior (isOnboarding still bypasses NavigationStack).</done>
</task>

<task type="auto">
  <name>Task 3: Wire GoalListView '+add' to GoalEntryChoiceView with pendingPremadeGoal + wizard fallback sheet</name>
  <files>
    VitaminG/VitaminG/VitaminG/Views/GoalListView.swift
  </files>
  <read_first>
    VitaminG/VitaminG/VitaminG/Views/GoalListView.swift,
    VitaminG/VitaminG/VitaminG/Views/GoalCreation/GoalEntryChoiceView.swift,
    VitaminG/VitaminG/VitaminG/Views/GoalCreation/GoalCreationWizardView.swift,
    VitaminG/VitaminG/VitaminG/Models/GoalCategory.swift,
    .planning/phases/18-home-tab-goals-flow/18-PATTERNS.md,
    .planning/phases/18-home-tab-goals-flow/18-UI-SPEC.md
  </read_first>
  <action>
    Per D-05 and RESEARCH.md Pattern 5 (Sheet + Wizard Presentation with callbacks). HomeView's identical wiring is handled in Plan 04 — this task only updates GoalListView.

    Specifics:
    - Add state on GoalListView (alongside existing @State private var showingAddGoal):
        @State private var showingGoalEntryChoice = false
        @State private var showingWizard = false
        @State private var wizardStartStep: Int = 0
        @State private var pendingPremadeGoal: (title: String, category: GoalCategory)? = nil
    - Replace the existing single sheet trigger. Where the code currently does `.sheet(isPresented: $showingAddGoal) { GoalCreationWizardView() }` (around line 49 of GoalListView.swift), change to TWO chained sheets:
        - .sheet(isPresented: $showingGoalEntryChoice) {
              GoalEntryChoiceView(
                  onSelectWizard: { step in
                      wizardStartStep = step
                      pendingPremadeGoal = nil
                      showingGoalEntryChoice = false
                      DispatchQueue.main.asyncAfter(deadline: .now() + 0.35) {
                          showingWizard = true
                      }
                  },
                  onSelectPremade: { title, category in
                      wizardStartStep = 2
                      pendingPremadeGoal = (title, category)
                      showingGoalEntryChoice = false
                      DispatchQueue.main.asyncAfter(deadline: .now() + 0.35) {
                          showingWizard = true
                      }
                  })
          }
        - .sheet(isPresented: $showingWizard) {
              GoalCreationWizardView(startAtStep: wizardStartStep, premadeGoal: pendingPremadeGoal)
          }
    - Change the existing +add Button action from `showingAddGoal = true` to `showingGoalEntryChoice = true`. The old `showingAddGoal` state variable should be removed if it is now unused (verify with a grep over GoalListView.swift; if it has other call sites, keep it but route the +add button to the new state).
    - The +add button visual (capsule terra, "+ New goal", existing accessibility label) is unchanged — only the action changes.
    - The 0.35-second delay is required because SwiftUI cannot present two sheets back-to-back without the first fully dismissing; this matches the timing used in similar sheet transitions in the project.
    - If the EmptyStateView in GoalListView also has a "Create your first goal" button currently triggering showingAddGoal, route it to showingGoalEntryChoice as well.

    Do NOT modify HomeView in this task — Plan 04 handles HomeView's identical sheet wiring.
    Do NOT change any other GoalListView sections (header, list rows, GoalCardView).
  </action>
  <verify>
    <automated>cd "/Users/kyleharrington/Desktop/AI/Vitamin G/VitaminG/VitaminG" && xcodebuild build -scheme VitaminG -destination 'platform=iOS Simulator,name=iPhone 16' 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "showingGoalEntryChoice" VitaminG/VitaminG/VitaminG/Views/GoalListView.swift >= 3 (declaration + button action + sheet presentation).
    - grep -c "pendingPremadeGoal" VitaminG/VitaminG/VitaminG/Views/GoalListView.swift >= 3 (declaration + onSelectPremade write + wizard sheet read).
    - grep -c "GoalEntryChoiceView" VitaminG/VitaminG/VitaminG/Views/GoalListView.swift >= 1.
    - grep -c "onSelectPremade" VitaminG/VitaminG/VitaminG/Views/GoalListView.swift >= 1.
    - grep -c "onSelectWizard" VitaminG/VitaminG/VitaminG/Views/GoalListView.swift >= 1.
    - grep -c "GoalCreationWizardView(startAtStep:" VitaminG/VitaminG/VitaminG/Views/GoalListView.swift >= 1.
    - grep -c "premadeGoal: pendingPremadeGoal" VitaminG/VitaminG/VitaminG/Views/GoalListView.swift >= 1.
    - grep -c "DispatchQueue.main.asyncAfter" VitaminG/VitaminG/VitaminG/Views/GoalListView.swift >= 1 (sheet transition delay).
    - No remaining direct present-wizard-from-tap call (verified by ensuring the only GoalCreationWizardView( occurrences are in the new wizard-fallback .sheet block).
    - xcodebuild build succeeds.
  </acceptance_criteria>
  <done>+add on GoalListView opens GoalEntryChoiceView. Choice paths route to GoalCreationWizardView with the correct startAtStep AND premadeGoal payload. Build is green. Pre-fill flows: PremadeGoalsListView row → onSelect callback → GoalEntryChoiceView.onSelectPremade → parent's pendingPremadeGoal state → wizard init param → wizard's own VM via .onAppear (Task 2).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Pre-made goal title strings → wizardVM.draftTitle (via configure(fromPremade:)) | Pre-made strings are compile-time literals from GoalCategory.suggestions. No user input on this path until Step 3 where the user can edit title. Sanitization happens via existing GoalViewModel.sanitize() before save. |
| User-entered duration days → Goal.durationDays | Number-pad TextField input; UI constrains to 1..365 with inline validation. Out-of-range values disable the save button (defense in depth). |
| GoalEntryChoiceView callbacks → parent sheet state | Callbacks pass either an Int (step 0 or 1) or a (String, GoalCategory) tuple. String origin is a hardcoded GoalCategory.suggestions literal; GoalCategory is a typed enum. No untrusted deserialization. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-03-01 | Tampering | Custom duration TextField | mitigate | Validate Int conversion + range (1..365); disable save button on invalid input; show inline error text per ASVS V5.1. |
| T-18-03-02 | Tampering | Pre-made goal title injection | accept | Source is hardcoded Swift literals in GoalCategory.suggestions; no user input flows into draftTitle on this path. Subsequent sanitize() in GoalViewModel applies on save anyway. |
| T-18-03-03 | Information Disclosure | Goal Title field re-uses existing sanitize() | accept | Existing GoalInput validation chain (sanitize + validate) covers all goal title persistence, including edits to pre-filled titles. |
| T-18-03-04 | Repudiation | startAtStep parameter mistakenly skipping required steps | mitigate | Wizard preserves existing step validation gates (e.g. Step 1 must have a category selected before Save). configure(fromPremade) sets category before currentStep = 2, so Step 3 has all upstream state required. |
| T-18-03-05 | Tampering | Stale orphan VM pre-fill silently discarded | mitigate | Pitfall 8 fix: PremadeGoalsListView holds no VM; selection flows via callback to the parent, which injects it via the wizard's premadeGoal init param. The wizard's .onAppear writes to its OWN VM. Acceptance criteria assert no @State VM in picker views. |
| T-18-03-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan; pure Swift edits using existing SwiftUI/SwiftData types. |
</threat_model>

<verification>
- xcodebuild build -scheme VitaminG succeeds.
- xcodebuild test -only-testing:VitaminGTests/Phase18PremadeGoalsTests is GREEN.
- xcodebuild test -only-testing:VitaminGTests/Phase18GoalInputTests is GREEN (round-trip including UI-side durationDays write).
- Manual on simulator: tap +add in Goals tab → choice sheet appears → tap each of 3 paths → correct destination presented (PremadeGoalsListView pushed inline / wizard sheet at step 0 / wizard sheet at step 1).
- Manual on simulator: pick a category section in PremadeGoalsListView, tap a goal → sheet dismisses → wizard sheet appears at Step 3 with category + title pre-filled (the entire Pitfall 8 fix path). If Step 3 shows empty category/title, the callback chain is broken — investigate.
- Manual: complete a wizard with a non-nil duration; verify the saved Goal in SwiftData has the durationDays value (re-open the goal for editing — duration is preserved in Step 3).
</verification>

<success_criteria>
- GoalEntryChoiceView and PremadeGoalsListView are present, render in light mode, follow VGTheme palette, and meet UI-SPEC visual spec (path icons, sheet title, card stroke).
- Neither GoalEntryChoiceView nor PremadeGoalsListView holds a @State GoalCreationWizardViewModel (Pitfall 8 prevention).
- Selection from PremadeGoalsListView flows upward via callback → parent state → wizard init param → wizard's own VM in .onAppear.
- GoalCreationWizardView accepts both startAtStep AND premadeGoal init params and jumps/pre-fills accordingly on appear.
- Step3DetailsScreen renders a duration input (segmented presets + custom field), which writes to wizardVM.draftDurationDays (added unconditionally by Plan 02) and ends up on Goal.durationDays after save.
- GoalListView's +add button opens GoalEntryChoiceView and the 3 paths route correctly, with the Need-ideas path correctly conveying (title, category) into the wizard's own VM.
- HOME-04 / +add hookup from HomeView is NOT this plan's scope — that lands in Plan 04. This plan exposes the GoalEntryChoiceView API for Plan 04 to consume.
</success_criteria>

<output>
Create .planning/phases/18-home-tab-goals-flow/18-03-SUMMARY.md when done. Include: file paths, key API points (GoalEntryChoiceView.init signature with both callbacks, PremadeGoalsListView.init signature with onSelect, GoalCreationWizardView.init signature with startAtStep + premadeGoal), confirmation that no orphan VMs exist in picker views, and any visual screenshots or notes about manual verification of the pre-fill round-trip.
</output>
