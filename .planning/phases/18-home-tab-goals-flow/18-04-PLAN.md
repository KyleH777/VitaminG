---
phase: 18-home-tab-goals-flow
plan: 04
type: execute
wave: 3
depends_on: [18-02, 18-03]
files_modified:
  - VitaminG/VitaminG/VitaminG/Views/HomeView.swift
autonomous: true
requirements: [HOME-01, HOME-02, HOME-03, HOME-04, HOME-05, HOME-06]

must_haves:
  truths:
    - "HomeView header shows the user's display name and overall app streak count, sourced from StreakEngine.currentStreak(from: completionEvents)"
    - "HomeView shows a Quote of the Day rotating daily, sourced from VGQuoteBank.all using dayOfYear modulo length"
    - "HomeView shows a Community Goal card between the quote section and My Goals section when an active UserChallenge exists; the card hides cleanly when no active challenge"
    - "HomeView has a Stats nav row that pushes to AppRoute.stats"
    - "HomeView has a My Goals section with an inline +add button that presents GoalEntryChoiceView; each goal row shows a flame icon when per-goal consecutive streak is at least 3 days"
    - "HomeView no longer shows the Daily Wins / Gratitude entry (dailyWinsEntry removed per D-04 — HOME-06 is dropped from Phase 18)"
  artifacts:
    - path: "VitaminG/VitaminG/VitaminG/Views/HomeView.swift"
      provides: "Restructured Home dashboard per Phase 18 requirements"
      contains: "StreakEngine.currentStreak"
  key_links:
    - from: "HomeView headerSection streak badge"
      to: "StreakEngine.currentStreak(from: completionEvents)"
      via: "appStreak computed property"
      pattern: "StreakEngine\\.currentStreak"
    - from: "HomeView quoteSection"
      to: "VGQuoteBank.all"
      via: "dayOfYear modulo length"
      pattern: "VGQuoteBank\\.all"
    - from: "HomeView communityGoalSection"
      to: "UserChallenge @Query filtered to active status"
      via: "@Query private var userChallenges; first(where: statusRaw == active)"
      pattern: "UserChallenge"
    - from: "HomeView quickStatsRow"
      to: "AppRoute.stats"
      via: "NavigationLink(value: AppRoute.stats)"
      pattern: "AppRoute\\.stats"
    - from: "HomeView secondaryGoalsSection +add"
      to: "GoalEntryChoiceView (sheet)"
      via: ".sheet(isPresented: $showingGoalEntryChoice)"
      pattern: "GoalEntryChoiceView"
    - from: "HomeView goalRow flame icon"
      to: "per-goal StreakEngine.currentStreak"
      via: "goalStreak helper, threshold >= 3"
      pattern: "flame.fill"
---

<objective>
Rebuild the Home tab to match Phase 18 requirements: fix the buggy streak source, replace the hardcoded quote bank with VGQuoteBank.all, add a Community Goal card between quote and My Goals, promote Stats to a single tappable nav card, add inline +add in My Goals wired to GoalEntryChoiceView, add flame icons on 3+ streak goals, and remove the Daily Wins entry section.

Purpose: Implements HOME-01, HOME-02, HOME-03, HOME-04, HOME-05 in a single file edit. HOME-06 is explicitly DROPPED per D-04 — its requirement is acknowledged here (in the requirements field) but the dailyWinsEntry section is removed, not implemented.
Output: HomeView.swift restructured around the new section order with VGTheme tokens, accessibility labels, and the consumer-side wiring for Plan 02's symbols and Plan 03's GoalEntryChoiceView.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-home-tab-goals-flow/18-CONTEXT.md
@.planning/phases/18-home-tab-goals-flow/18-RESEARCH.md
@.planning/phases/18-home-tab-goals-flow/18-PATTERNS.md
@.planning/phases/18-home-tab-goals-flow/18-UI-SPEC.md
@.planning/phases/18-home-tab-goals-flow/18-02-SUMMARY.md
@.planning/phases/18-home-tab-goals-flow/18-03-SUMMARY.md
@CLAUDE.md
</context>

<interfaces>
From Plan 02:
- VGQuoteBank.all: [VGQuote] — flattened deterministic quote array.
- StreakEngine.currentStreak(from: [CompletionEvent], tier: GoalTier? = nil) -> Int — existing API, unchanged.

From Plan 03:
- struct GoalEntryChoiceView: View with init(onSelectWizard: (Int) -> Void).
- GoalCreationWizardView(startAtStep:) — wizard sheet API.

From existing HomeView.swift (current bug to fix):
- Lines 134-136: `private var currentStreak: Int { goals.compactMap { $0.completionEvents?.count }.max() ?? 0 }` — REMOVE.
- Lines 298-318: dailyWinsEntry section — REMOVE (per D-04).
- Lines 175-227: primaryGoalCard / checkInCTA — REPURPOSE as community goal card.
- Lines 266-294: quickStatsRow with NavigationLink(value: AppRoute.stats) — KEEP the NavigationLink, RESHAPE the card per D-02.
- @Query private var goals: [Goal], @Query private var completionEvents: [CompletionEvent], @Query private var userChallenges: [UserChallenge] — already present.

From existing AppRoute (Navigation/AppRoute.swift):
- AppRoute.stats — destination case for stats view; HomeView's navigationDestination already routes this case to StatsView.

From existing UserChallenge model:
- statusRaw: String? where "active" indicates an active challenge.
- template: ChallengeTemplate? with durationDays: Int?.
- totalCheckIns: Int? for community progress numerator.

UI-SPEC layout order (top to bottom):
1. headerSection (greeting + display name + streak badge + bell)
2. quoteSection (today's quote card with terra left border)
3. communityGoalSection (NEW — between quote and My Goals; hidden when no active challenge)
4. quickStatsRow (RESHAPED — single tappable Stats nav card per D-02)
5. secondaryGoalsSection (My Goals — with inline +add and flame icons)
6. stayCloseSection (kept as-is per RESEARCH.md A3)
7. (REMOVED) dailyWinsEntry — per D-04
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Fix streak source, swap quote source, reshape Stats row, remove dailyWinsEntry</name>
  <files>
    VitaminG/VitaminG/VitaminG/Views/HomeView.swift
  </files>
  <read_first>
    VitaminG/VitaminG/VitaminG/Views/HomeView.swift,
    VitaminG/VitaminG/VitaminG/Services/StreakEngine.swift,
    VitaminG/VitaminG/VitaminG/Services/VGQuoteBank.swift,
    VitaminG/VitaminG/VitaminG/Navigation/AppRoute.swift,
    VitaminG/VitaminG/VitaminG/VGTheme.swift,
    .planning/phases/18-home-tab-goals-flow/18-PATTERNS.md,
    .planning/phases/18-home-tab-goals-flow/18-UI-SPEC.md
  </read_first>
  <action>
    Per HOME-01 (Pitfall 1), HOME-02 (Open Question 4), HOME-05 (D-02), D-04 drop.

    Streak fix (HOME-01):
    - Remove the existing `private var currentStreak: Int { goals.compactMap { $0.completionEvents?.count }.max() ?? 0 }` (around lines 134-136).
    - Add a replacement: `private var appStreak: Int { StreakEngine.currentStreak(from: completionEvents) }`. This uses the existing @Query private var completionEvents: [CompletionEvent] already on the view.
    - Update every consumer of the old `currentStreak` symbol (header streakBadge, any accessibility labels, any other reference) to read `appStreak` instead. Search the file for `currentStreak` occurrences and replace.
    - The streak badge visual (capsule, accentTerra, "{N}" rendered in .monospaced) is unchanged — only the source changes.

    Quote source swap (HOME-02):
    - Replace the existing hardcoded 4-quote array in quoteSection (around lines 139-171) with VGQuoteBank.all rotation.
    - Add a computed property: `private var todaysQuote: String { let all = VGQuoteBank.all; guard !all.isEmpty else { return "" }; let dayOfYear = Calendar.current.ordinality(of: .day, in: .year, for: Date()) ?? 1; let index = (dayOfYear - 1) % all.count; return all[index].text }` — adapt `.text` to the actual property name on VGQuote (read VGQuoteBank.swift to confirm).
    - Wire the quote Text view to render todaysQuote. Preserve all existing visual treatment (VGTheme.serifItalic(16), VGTheme.textSecondary, lineSpacing 4, the "TODAY'S DOSE" overline, the terra left-border accent, the surface card background).
    - Do NOT introduce randomness; the same dayOfYear must produce the same quote within a calendar day (covered by Phase18QuoteBankTests).

    Stats row reshape (HOME-05 / D-02):
    - Locate quickStatsRow (around lines 266-294). KEEP the outer NavigationLink(value: AppRoute.stats) wrapping.
    - REPLACE the inner HStack of statCell()s with a single tappable card:
        HStack(spacing: 14) {
            ZStack {
                RoundedRectangle(cornerRadius: 12)
                    .fill(VGTheme.accentTerra.opacity(0.15))
                    .frame(width: 40, height: 40)
                Image(systemName: "chart.bar.fill")
                    .font(.system(size: 18))
                    .foregroundStyle(VGTheme.accentTerra)
            }
            VStack(alignment: .leading, spacing: 2) {
                Text("Your Stats")
                    .font(.system(size: 16, weight: .regular))
                    .foregroundStyle(VGTheme.textPrimary)
                Text("Active goals, streaks, badges")
                    .font(.system(size: 12))
                    .foregroundStyle(VGTheme.textMuted)
            }
            Spacer()
            Image(systemName: "chevron.right")
                .font(.system(size: 12, weight: .semibold))
                .foregroundStyle(VGTheme.textMuted)
        }
        .padding(.horizontal, 18)
        .padding(.vertical, 14)
        .background(VGTheme.surface)
        .clipShape(RoundedRectangle(cornerRadius: 14))
        .overlay(RoundedRectangle(cornerRadius: 14).strokeBorder(VGTheme.separator, lineWidth: 1))
    - Wrap in .buttonStyle(.plain) modifier on the outer NavigationLink to preserve the card chrome.
    - Outer .padding(.horizontal, 24).padding(.top, 16) kept from existing.
    - Update accessibility: .accessibilityLabel("View your statistics") .accessibilityHint("Active goals, streaks, badges").

    Remove dailyWinsEntry (D-04, HOME-06 dropped):
    - Delete the dailyWinsEntry section (lines 298-318 of current HomeView.swift) including any private helper function definition.
    - Remove the call site from the body's VStack ordering.
    - Do NOT delete DailyWinsView.swift itself — only remove the Home entry point. The view remains reachable from other navigation paths (and may be referenced by other phases).
    - HOME-06 is acknowledged-but-dropped: this requirement appears in the plan's requirements frontmatter (for traceability) and is satisfied by REMOVAL per D-04.

    Do NOT modify in this task: headerSection, primaryGoalCard (yet — Task 2 repurposes), secondaryGoalsSection (Task 2 enhances), stayCloseSection.
  </action>
  <verify>
    <automated>cd "/Users/kyleharrington/Desktop/AI/Vitamin G/VitaminG/VitaminG" && xcodebuild build -scheme VitaminG -destination 'platform=iOS Simulator,name=iPhone 16' 2>&1 | tail -15 && xcodebuild test -scheme VitaminG -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:VitaminGTests/Phase18QuoteBankTests 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "StreakEngine.currentStreak" VitaminG/VitaminG/VitaminG/Views/HomeView.swift >= 1.
    - grep -c "goals.compactMap.*completionEvents..count.*max" VitaminG/VitaminG/VitaminG/Views/HomeView.swift == 0 (old buggy expression removed).
    - grep -c "VGQuoteBank.all" VitaminG/VitaminG/VitaminG/Views/HomeView.swift >= 1.
    - grep -c "dailyWinsEntry" VitaminG/VitaminG/VitaminG/Views/HomeView.swift == 0 (section removed).
    - grep -c "Your Stats" VitaminG/VitaminG/VitaminG/Views/HomeView.swift >= 1 (reshaped Stats nav row copy).
    - grep -c "AppRoute.stats" VitaminG/VitaminG/VitaminG/Views/HomeView.swift >= 1 (NavigationLink destination preserved).
    - grep -c "chart.bar.fill" VitaminG/VitaminG/VitaminG/Views/HomeView.swift >= 1 (new Stats icon).
    - xcodebuild build succeeds.
    - Phase18QuoteBankTests is GREEN.
  </acceptance_criteria>
  <done>Header reads from StreakEngine; quote source is VGQuoteBank.all rotated by day-of-year; Stats row is a single tappable card; dailyWinsEntry is gone. Build green.</done>
</task>

<task type="auto">
  <name>Task 2: Add community goal card section + enhance My Goals (+add hookup + flame icons)</name>
  <files>
    VitaminG/VitaminG/VitaminG/Views/HomeView.swift
  </files>
  <read_first>
    VitaminG/VitaminG/VitaminG/Views/HomeView.swift,
    VitaminG/VitaminG/VitaminG/Views/CommunityGoalsLandingView.swift,
    VitaminG/VitaminG/VitaminG/Services/StreakEngine.swift,
    VitaminG/VitaminG/VitaminG/Views/GoalCreation/GoalEntryChoiceView.swift,
    VitaminG/VitaminG/VitaminG/Views/GoalCreation/GoalCreationWizardView.swift,
    .planning/phases/18-home-tab-goals-flow/18-PATTERNS.md,
    .planning/phases/18-home-tab-goals-flow/18-UI-SPEC.md
  </read_first>
  <action>
    Per HOME-03 (D-01), HOME-04 (D-11), and RESEARCH.md Pattern 9.

    Community Goal section (HOME-03 / D-01):
    - Add a new private view section `communityGoalSection` (or `communityGoalCard`) placed in the body VStack ordering BETWEEN quoteSection and the My Goals section.
    - Data source: @Query private var userChallenges: [UserChallenge] is already on the view. Add a computed `private var primaryChallenge: UserChallenge? { userChallenges.first(where: { ($0.statusRaw ?? "") == "active" }) }`.
    - Hide-when-empty (Pitfall 6): the section is wrapped in `if let challenge = primaryChallenge { communityGoalCard(challenge) }` so when no active challenge exists, neither the section nor the overline appears.
    - Card layout per UI-SPEC §HomeView Community Goal Card:
        - Overline: "COMMUNITY GOAL" — SF Pro 13pt Semibold uppercase, kerning 1.2, VGTheme.muted.
        - Title: challenge.template?.title ?? "Community Challenge" — VGTheme.serif(20, weight: .semibold), VGTheme.sand, lineLimit 2.
        - Stat line: "{N} people participating" using whatever participant count is available on UserChallenge (use challenge.template?.activeParticipants ?? 0 or a reasonable existing property; if no participant field exists, omit the line rather than fabricate).
        - Progress bar: full-width Capsule, height 6pt, background VGTheme.surface2 (fallback to Color.white.opacity(0.15) if surface2 doesn't exist on VGTheme — read VGTheme.swift to confirm), overlay accentTerra fill at width proportional to communityProgress.
        - communityProgress: Double computed as `min(1.0, Double(challenge.totalCheckIns ?? 0) / Double(max(1, challenge.template?.durationDays ?? 90)))` per RESEARCH.md Pattern 9.
        - Completion label trailing: "{percent}% complete" — SF Pro 13pt Semibold, accentTerra.
    - Card wrapper: VStack(spacing: 12), padding 22pt internal (preserved legacy value per UI-SPEC Note 7), background Color.white.opacity(0.07), cornerRadius 20, strokeBorder Color.white.opacity(0.1) lineWidth 1. Outer .padding(.horizontal, 24).padding(.top, 20).
    - Tap action: NavigationLink(value: AppRoute.community) if such a route exists; otherwise wrap in a tap gesture pushing to AppRoute.communityGoalLanding or the closest existing route. If no obvious destination route exists, leave the card non-interactive in Phase 18 (Phase 21 wires the full community goal detail flow per RESEARCH.md A5). Document which route was chosen in the task SUMMARY.
    - Accessibility: .accessibilityElement(children: .combine) and .accessibilityLabel describing the goal title + percent.

    My Goals section enhancement (HOME-04 / D-11):
    - Locate secondaryGoalsSection. Confirm there is a section header HStack with the "MY GOALS" overline label (existing) — add an inline trailing "+ Add" button:
        HStack {
            Text("MY GOALS").font(.system(size: 10, weight: .bold)).kerning(1.2).foregroundStyle(VGTheme.muted)
            Spacer()
            Button { showingGoalEntryChoice = true } label: {
                Text("+ Add").font(.system(size: 13, weight: .semibold)).foregroundStyle(VGTheme.accentTerra)
            }
            .frame(minHeight: 44)
            .accessibilityLabel("Add a new goal")
        }
        .padding(.horizontal, 24)
    - Add state to HomeView (at the top of struct alongside existing @State):
        @State private var showingGoalEntryChoice = false
        @State private var showingWizard = false
        @State private var wizardStartStep: Int = 0
    - Add two chained .sheet modifiers on the outer ScrollView (or wherever existing sheets are attached) — same pattern as Plan 03 used in GoalListView:
        .sheet(isPresented: $showingGoalEntryChoice) {
            GoalEntryChoiceView(onSelectWizard: { step in
                wizardStartStep = step
                showingGoalEntryChoice = false
                DispatchQueue.main.asyncAfter(deadline: .now() + 0.35) { showingWizard = true }
            })
        }
        .sheet(isPresented: $showingWizard) {
            GoalCreationWizardView(startAtStep: wizardStartStep)
        }

    Flame icon on goal rows (D-11 / GOAL2-05):
    - Add a helper `private func goalStreak(_ goal: Goal) -> Int { StreakEngine.currentStreak(from: completionEvents.filter { $0.goal?.id == goal.id }) }`.
    - Inside the existing goalRow(_:) helper (around lines 437-471), insert before the trailing chevron:
        if goalStreak(goal) >= 3 {
            Image(systemName: "flame.fill")
                .font(.system(size: 14))
                .foregroundStyle(VGTheme.accentGold)
                .accessibilityLabel("\(goalStreak(goal)) day streak — on fire!")
        }
    - Per UI-SPEC accent reservation: flame uses accentGold (NOT accentTerra). accentTerra is reserved for primary interactive accent.

    Empty state copy (UI-SPEC):
    - If goals is empty AND no community challenge exists, the secondaryGoalsSection shows an empty state:
        heading "Ready to start?" + body "Tap + Add to set your first goal — small steps, taken daily." + a primary terra "Add a goal" button that sets showingGoalEntryChoice = true.

    Do NOT modify other sections (headerSection, quoteSection, stayCloseSection, the new communityGoalSection in this same task — only build it once).
  </action>
  <verify>
    <automated>cd "/Users/kyleharrington/Desktop/AI/Vitamin G/VitaminG/VitaminG" && xcodebuild build -scheme VitaminG -destination 'platform=iOS Simulator,name=iPhone 16' 2>&1 | tail -15 && xcodebuild test -scheme VitaminG -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:VitaminGTests/Phase18StreakEngineTests 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "COMMUNITY GOAL" VitaminG/VitaminG/VitaminG/Views/HomeView.swift >= 1.
    - grep -c "primaryChallenge\|userChallenges.first" VitaminG/VitaminG/VitaminG/Views/HomeView.swift >= 1.
    - grep -c "communityProgress" VitaminG/VitaminG/VitaminG/Views/HomeView.swift >= 1.
    - grep -c "if let challenge = primaryChallenge\|if let primaryChallenge" VitaminG/VitaminG/VitaminG/Views/HomeView.swift >= 1 (hide-when-empty guard).
    - grep -c "showingGoalEntryChoice" VitaminG/VitaminG/VitaminG/Views/HomeView.swift >= 3 (state + button action + sheet).
    - grep -c "GoalEntryChoiceView" VitaminG/VitaminG/VitaminG/Views/HomeView.swift >= 1.
    - grep -c "GoalCreationWizardView(startAtStep:" VitaminG/VitaminG/VitaminG/Views/HomeView.swift >= 1.
    - grep -c "flame.fill" VitaminG/VitaminG/VitaminG/Views/HomeView.swift >= 1.
    - grep -c "accentGold" VitaminG/VitaminG/VitaminG/Views/HomeView.swift >= 1 (flame color).
    - grep -c "goalStreak" VitaminG/VitaminG/VitaminG/Views/HomeView.swift >= 2 (helper definition + at least one call site).
    - xcodebuild build succeeds.
    - Phase18StreakEngineTests is GREEN (confirms StreakEngine usage is the source of truth).
  </acceptance_criteria>
  <done>HomeView shows Community Goal card when active challenge exists, hides it when none. +Add opens GoalEntryChoiceView with correct sheet routing. Goal rows render flame icons when per-goal streak >= 3 days. Build green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CloudKit → UserChallenge → HomeView render | UserChallenge fields are optional; force-unwraps avoided per CLAUDE.md. Display defaults are safe ("Community Challenge" fallback title, 0/90 progress fallback) so nil fields don't crash. |
| @Query CompletionEvent → StreakEngine.currentStreak | StreakEngine already handles nil completedAt via compactMap; safe. |
| User taps +add → sheet presentation | No untrusted input crossing here; pure UI state transition. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-04-01 | Tampering | Community goal title from CloudKit | mitigate | UserChallenge / ChallengeTemplate persists via project's existing CloudKit sync; titles already pass through ProfanityFilter in v1.0 Phase 14. No new validation surface added here. |
| T-18-04-02 | Denial of Service | VGQuoteBank.all O(n) flatten per render | accept | n < 100; SwiftUI re-renders are cheap relative to text composition; acceptable. |
| T-18-04-03 | Information Disclosure | Streak count exposed in UI | accept | User's own streak shown to self only; no cross-account leakage. |
| T-18-04-04 | Tampering | flame.fill threshold gate (3) | mitigate | Threshold defined in single helper goalStreak(_:) using StreakEngine.currentStreak; centralized so changes don't drift across the file. |
| T-18-04-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan; only modifies one Swift file. |
</threat_model>

<verification>
- xcodebuild build -scheme VitaminG -destination 'platform=iOS Simulator,name=iPhone 16' succeeds.
- xcodebuild test -only-testing:VitaminGTests/Phase18StreakEngineTests is GREEN (HOME-01 fix + GOAL2-05 flame threshold logic).
- xcodebuild test -only-testing:VitaminGTests/Phase18QuoteBankTests is GREEN (HOME-02 daily rotation).
- Manual on simulator: open app → Home tab → verify header shows current streak (not max completion count); verify quote rendered; verify community goal card visible only when active UserChallenge exists; verify Stats row tap pushes to StatsView; verify +Add opens choice sheet; verify flame icon on any goal with 3+ consecutive day check-ins.
- Manual: confirm Daily Wins entry section no longer appears on Home.
</verification>

<success_criteria>
- HomeView reads appStreak from StreakEngine.currentStreak(from: completionEvents).
- HomeView quote section reads VGQuoteBank.all with dayOfYear rotation.
- HomeView communityGoalSection is conditional on active UserChallenge; renders with progress bar when present, vanishes when absent.
- HomeView quickStatsRow is a single tappable card pushing AppRoute.stats.
- HomeView secondaryGoalsSection has inline +Add presenting GoalEntryChoiceView and 3+ streak goals show flame.fill in accentGold.
- HomeView dailyWinsEntry removed entirely (HOME-06 acknowledged + dropped).
- All Phase18*Tests stay GREEN.
</success_criteria>

<output>
Create .planning/phases/18-home-tab-goals-flow/18-04-SUMMARY.md when done. Document the chosen route for community goal card tap (if any), any visual screenshots of the Home dashboard restructure, and confirmation that HOME-06 is dropped (not implemented).
</output>
