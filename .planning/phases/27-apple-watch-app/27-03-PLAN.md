---
phase: 27-apple-watch-app
plan: 03
type: execute
wave: 2
depends_on: [27-02]
files_modified:
  - VitaminG/VitaminGWatch/Shared/WatchReceiver.swift
  - VitaminG/VitaminGWatch/VGWatchApp.swift
  - VitaminG/VitaminGWatch/Screens/TodayGlanceView.swift
  - VitaminG/VitaminG/VitaminGTests/WatchReceiverTests.swift
autonomous: true
requirements: [WATCH-02]

must_haves:
  truths:
    - "Watch app activates WCSession at VGWatchApp.init() so the session is ready before any view renders"
    - "When iPhone pushes an applicationContext containing a 'snapshot' Data entry, the Watch decodes WatchSnapshot and writes 5 keys to UserDefaults(suiteName: 'group.com.kyleharrington.VitaminGWatch')"
    - "WatchReceiver calls WidgetCenter.shared.reloadAllTimelines() immediately after writing UserDefaults so the complication refreshes"
    - "TodayGlanceView reads live snapshot fields from Watch-scoped UserDefaults instead of the hardcoded 'Day 65' / progress 0.72 / 72%"
    - "TodayGlanceView header reads 'Day {globalStreak}' (D-02) and renders the active goal title below the ring"
  artifacts:
    - path: "VitaminG/VitaminGWatch/Shared/WatchReceiver.swift"
      provides: "watchOS-side WCSession singleton + applicationContext decoder + UserDefaults writer"
      contains: "final class WatchReceiver: NSObject, WCSessionDelegate"
    - path: "VitaminG/VitaminGWatch/VGWatchApp.swift"
      provides: "Watch app entry point with WatchReceiver activation in init()"
      contains: "WatchReceiver.shared.activate()"
    - path: "VitaminG/VitaminGWatch/Screens/TodayGlanceView.swift"
      provides: "Live snapshot-driven TodayGlanceView (Day X + ring + active goal title)"
  key_links:
    - from: "VGWatchApp.init()"
      to: "WatchReceiver.shared.activate()"
      via: "explicit call inside init body"
      pattern: "WatchReceiver\\.shared\\.activate"
    - from: "WatchReceiver.session(_:didReceiveApplicationContext:)"
      to: "UserDefaults(suiteName: 'group.com.kyleharrington.VitaminGWatch')"
      via: "decode WatchSnapshot from Data + .set per field with watchSnapshot_ prefix"
      pattern: "watchSnapshot_(activeGoalTitle|activeGoalProgress|globalStreak|hasCheckedInToday|activeGoalId)"
    - from: "WatchReceiver"
      to: "WidgetCenter.shared.reloadAllTimelines"
      via: "called from didReceiveApplicationContext after UserDefaults write"
      pattern: "WidgetCenter\\.shared\\.reloadAllTimelines"
    - from: "TodayGlanceView"
      to: "UserDefaults(suiteName: 'group.com.kyleharrington.VitaminGWatch')"
      via: "@AppStorage backed by Watch App Group suite"
      pattern: "watchSnapshot_"
---

<objective>
Wire the watchOS side of the WATCH-02 data path: a Watch-target WCSession delegate that activates at app launch, decodes incoming snapshots into Watch App Group UserDefaults, reloads the complication timeline, and a `TodayGlanceView` that reads its display state from those UserDefaults instead of hardcoded values.

Purpose: The iOS side of WATCH-02 was built in Wave 1 (Plan 02). This wave closes the loop on the Watch side so that an iPhone push surfaces real data in the Watch app's first screen. Wave 3 plugs the same UserDefaults into the new Widget Extension complication.

Output: `WatchReceiver.swift` shipped on the Watch target; `VGWatchApp.swift` activates it; `TodayGlanceView.swift` displays live snapshot data with the "Day {globalStreak}" header and the active goal title below the ring; `WatchReceiverTests.swift` flipped from RED to GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/27-apple-watch-app/27-CONTEXT.md
@.planning/phases/27-apple-watch-app/27-RESEARCH.md
@.planning/phases/27-apple-watch-app/27-PATTERNS.md
@.planning/phases/27-apple-watch-app/27-VALIDATION.md
@VitaminG/VitaminGWatch/VGWatchApp.swift
@VitaminG/VitaminGWatch/VGWatchContentView.swift
@VitaminG/VitaminGWatch/Screens/TodayGlanceView.swift
@VitaminG/VitaminGWatch/Shared/VGRingView.swift
@VitaminG/VitaminGWatch/Shared/VGWatchTheme.swift

<interfaces>
Watch App Group suite identifier (locked in Plan 01):
- `group.com.kyleharrington.VitaminGWatch` — used by both Watch app and Watch Widget Extension
- iOS app group `group.com.kyleharrington.VitaminG` is NOT usable from watchOS targets (different filesystem container)

WatchSnapshot type (from Plan 02 — must be added to Watch target membership in this wave):
- `struct WatchSnapshot: Codable, Equatable`
- Fields: `activeGoalTitle: String?`, `activeGoalProgress: Double`, `globalStreak: Int`, `hasCheckedInToday: Bool`, `activeGoalId: String?`
- Decoding: `try JSONDecoder().decode(WatchSnapshot.self, from: data)`

UserDefaults key prefix (CONTEXT.md Claude's Discretion — already locked):
- `watchSnapshot_activeGoalTitle` (String?)
- `watchSnapshot_activeGoalProgress` (Double)
- `watchSnapshot_globalStreak` (Int)
- `watchSnapshot_hasCheckedInToday` (Bool)
- `watchSnapshot_activeGoalId` (String?)

WCSessionDelegate on watchOS (CRITICAL — Pitfall 6 in RESEARCH.md):
- The Watch-side delegate MUST NOT include `sessionDidBecomeInactive(_:)` or `sessionDidDeactivate(_:)` — these methods DO NOT EXIST in the watchOS version of the protocol. Including them causes a compile error.
- Required watchOS delegate method: `session(_:activationDidCompleteWith:error:)` only.

Existing hardcoded values to replace in TodayGlanceView.swift (lines 22, 33, 36):
- `Text("65")` (header streak number) → `Text("\(globalStreak)")`
- `VGRingView(progress: 0.72, ...)` → `VGRingView(progress: activeGoalProgress, ...)`
- `Text("72")` (center ring percent) → `Text("\(Int(activeGoalProgress * 100))")`
- New element below ring: `Text(activeGoalTitle ?? "")` with small white opacity 0.65 styling (CONTEXT.md D-02)

VGWatchApp.swift current state (10 lines, no init body):
- `@main struct VGWatchApp: App` with `var body: some Scene { WindowGroup { VGWatchContentView() } }`
- This wave adds an `init() { WatchReceiver.shared.activate() }` block
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement WatchReceiver on Watch target + add WatchSnapshot to Watch target membership</name>
  <files>VitaminG/VitaminGWatch/Shared/WatchReceiver.swift</files>
  <read_first>
    - VitaminG/VitaminGWatch/Shared/VGWatchTheme.swift (existing Watch-target Shared file — analog for file placement)
    - VitaminG/VitaminG/VitaminG/Models/WatchSnapshot.swift (created in Plan 02 — must be added to VitaminGWatch target membership for this file to compile)
    - .planning/phases/27-apple-watch-app/27-RESEARCH.md (Pattern 3 — WatchReceiver structure; Pitfall 6 — no sessionDidBecomeInactive on watchOS)
    - .planning/phases/27-apple-watch-app/27-PATTERNS.md (Pattern Assignment for WatchReceiver.swift — singleton, no UIKit imports)
    - VitaminG/VitaminG/VitaminGTests/WatchReceiverTests.swift (the RED stubs from Plan 01 — make them GREEN in Task 3)
  </read_first>
  <behavior>
    - WatchReceiver is a `final class` inheriting from `NSObject`, conforming to `WCSessionDelegate`.
    - Exposes `static let shared` and `private override init() { super.init() }` (mirrors NotificationScheduler pattern).
    - `private let suiteName = "group.com.kyleharrington.VitaminGWatch"` (exact identifier — must match entitlements from Plan 01).
    - `func activate()` guards on `WCSession.isSupported()`, assigns `WCSession.default.delegate = self`, calls `WCSession.default.activate()`. Idempotent.
    - `func session(_:didReceiveApplicationContext:)` extracts `applicationContext["snapshot"] as? Data`, decodes WatchSnapshot via JSONDecoder, writes 5 keys to UserDefaults with the `watchSnapshot_` prefix, then calls `WidgetCenter.shared.reloadAllTimelines()`.
    - `func session(_:activationDidCompleteWith:error:)` body: optional `#if DEBUG print(...)`.
    - DOES NOT include `sessionDidBecomeInactive(_:)` or `sessionDidDeactivate(_:)` — those methods don't exist on watchOS (Pitfall 6).
    - DOES NOT implement `didReceiveUserInfo` — Watch never receives userInfo; only iPhone does (Wave 4 handles iPhone-side receive). Omit it entirely.
  </behavior>
  <action>
    Create `VitaminG/VitaminGWatch/Shared/WatchReceiver.swift` as a member of the VitaminGWatch target ONLY (not the iOS app target, not the VitaminGTests target). Imports: `Foundation`, `WatchConnectivity`, `WidgetKit`.

    The Watch target must also gain `WatchSnapshot.swift` in its Target Membership (in Xcode: select VitaminG/VitaminG/VitaminG/Models/WatchSnapshot.swift → File Inspector → Target Membership → tick VitaminGWatch). The file lives in the iOS app's directory structure but compiles into both targets. If the file uses any iOS-only imports (e.g., `import SwiftData`), wrap the `build(goals:events:)` static function in `#if os(iOS)` so the Watch target compiles only the Codable struct + placeholder constant. The Watch needs the type for decoding; it does NOT need `build()` because the Watch only decodes, never builds.

    `writeToUserDefaults(snapshot:)` (private method called from didReceiveApplicationContext):
    - `let defaults = UserDefaults(suiteName: suiteName)`
    - `defaults?.set(snapshot.activeGoalTitle, forKey: "watchSnapshot_activeGoalTitle")`
    - `defaults?.set(snapshot.activeGoalProgress, forKey: "watchSnapshot_activeGoalProgress")`
    - `defaults?.set(snapshot.globalStreak, forKey: "watchSnapshot_globalStreak")`
    - `defaults?.set(snapshot.hasCheckedInToday, forKey: "watchSnapshot_hasCheckedInToday")`
    - `defaults?.set(snapshot.activeGoalId, forKey: "watchSnapshot_activeGoalId")`

    Wrap decode/write errors in `#if DEBUG print("[WatchReceiver] decode error: \\(error)") #endif` if decode throws.

    Do not import SwiftData in this file.
  </action>
  <verify>
    <automated>test -f "VitaminG/VitaminGWatch/Shared/WatchReceiver.swift" && cd "VitaminG/VitaminG" && xcodebuild build -scheme VitaminG -destination 'platform=iOS Simulator,name=iPhone 16' 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - File `VitaminG/VitaminGWatch/Shared/WatchReceiver.swift` exists
    - `grep -c "final class WatchReceiver: NSObject, WCSessionDelegate" VitaminG/VitaminGWatch/Shared/WatchReceiver.swift` outputs `1`
    - `grep -c "static let shared = WatchReceiver()" VitaminG/VitaminGWatch/Shared/WatchReceiver.swift` outputs `1`
    - `grep -c 'group\.com\.kyleharrington\.VitaminGWatch' VitaminG/VitaminGWatch/Shared/WatchReceiver.swift` outputs at least `1`
    - `grep -c "watchSnapshot_" VitaminG/VitaminGWatch/Shared/WatchReceiver.swift` outputs at least `5` (one set call per field)
    - `grep -c "WidgetCenter.shared.reloadAllTimelines" VitaminG/VitaminGWatch/Shared/WatchReceiver.swift` outputs at least `1`
    - `grep -c "sessionDidBecomeInactive\|sessionDidDeactivate" VitaminG/VitaminGWatch/Shared/WatchReceiver.swift` outputs `0` (must NOT include these — Pitfall 6)
    - `xcodebuild build -scheme VitaminG` exits 0 (both iOS and Watch targets compile)
  </acceptance_criteria>
  <done>
    WatchReceiver.swift compiles in VitaminGWatch target. WatchSnapshot.swift is shared between iOS and Watch via Target Membership.
  </done>
</task>

<task type="auto">
  <name>Task 2: Activate WatchReceiver in VGWatchApp.init() and wire TodayGlanceView to live data</name>
  <files>VitaminG/VitaminGWatch/VGWatchApp.swift, VitaminG/VitaminGWatch/Screens/TodayGlanceView.swift</files>
  <read_first>
    - VitaminG/VitaminGWatch/VGWatchApp.swift (current — 10 lines, no init body, only the SwiftUI App scaffold)
    - VitaminG/VitaminGWatch/Screens/TodayGlanceView.swift (current — hardcoded "Day 65", progress 0.72, "72")
    - VitaminG/VitaminGWatch/Shared/VGWatchTheme.swift (VGWatch.terraSoft / serif font usage already in TodayGlanceView)
    - .planning/phases/27-apple-watch-app/27-CONTEXT.md (D-01 — ring driven by activeGoalProgress; D-02 — "Day {globalStreak}" header + active goal title below ring)
    - .planning/phases/27-apple-watch-app/27-PATTERNS.md (TodayGlanceView modify section — exact replacements per line)
  </read_first>
  <action>
    **Step A — VGWatchApp.swift:** Add an explicit `init()` body that calls `WatchReceiver.shared.activate()`. Activation must happen here (not in `.task`) so the WCSession is ready before the first view renders — see RESEARCH.md Pitfall 1 (cold launch from complication tap). Keep the existing `body` scene unchanged. Final shape:
    - `import SwiftUI`
    - `import WatchConnectivity` (required if any direct WCSession reference is used; otherwise just call the singleton)
    - `@main struct VGWatchApp: App { init() { WatchReceiver.shared.activate() } var body: some Scene { WindowGroup { VGWatchContentView() } } }`

    **Step B — TodayGlanceView.swift:** Replace the three hardcoded values with `@AppStorage` properties backed by the Watch App Group UserDefaults, and add the active goal title below the ring. Use `@AppStorage` declarations with a custom store, one per field:
    - `@AppStorage("watchSnapshot_globalStreak", store: UserDefaults(suiteName: "group.com.kyleharrington.VitaminGWatch"))` (Int, default 0)
    - `@AppStorage("watchSnapshot_activeGoalProgress", store: UserDefaults(suiteName: "group.com.kyleharrington.VitaminGWatch"))` (Double, default 0.0)
    - `@AppStorage("watchSnapshot_activeGoalTitle", store: UserDefaults(suiteName: "group.com.kyleharrington.VitaminGWatch"))` (String, default "")
    - `@AppStorage("watchSnapshot_hasCheckedInToday", store: UserDefaults(suiteName: "group.com.kyleharrington.VitaminGWatch"))` (Bool, default false)

    Substitutions (referencing current TodayGlanceView.swift lines):
    - Line 22: `Text("65")` → `Text("\\(globalStreak)")` (D-02)
    - Line 33: `VGRingView(progress: 0.72, ...)` → `VGRingView(progress: activeGoalProgress, ...)` (D-01)
    - Line 36: `Text("72")` → `Text("\\(Int(activeGoalProgress * 100))")` (D-01)

    NEW: Below the ZStack ring (after line 48), add the active goal title:
    - `Text(activeGoalTitle.isEmpty ? "" : activeGoalTitle)` with `.font(.system(size: 10, weight: .semibold))`, `.foregroundColor(.white.opacity(0.65))`, `.lineLimit(1)`, `.padding(.top, 4)`, `.padding(.horizontal, 14)`
    - When activeGoalTitle is empty render nothing (zero-height) — accept SwiftUI's natural collapse of empty Text.

    DO NOT touch the Check In button behavior in this wave — Wave 4 (Plan 05) rewires that. Leave the `Button { checkedIn = true } label: ...` block unchanged for now. The `checkedIn` @State stays.

    Add `import WatchConnectivity` only if you reference WCSession directly. Keep `import SwiftUI`.
  </action>
  <verify>
    <automated>cd "VitaminG/VitaminG" && xcodebuild build -scheme VitaminG -destination 'platform=iOS Simulator,name=iPhone 16' 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "WatchReceiver.shared.activate()" VitaminG/VitaminGWatch/VGWatchApp.swift` outputs `1`
    - `grep -c "init()" VitaminG/VitaminGWatch/VGWatchApp.swift` outputs at least `1`
    - `grep -c '@AppStorage.*watchSnapshot_' VitaminG/VitaminGWatch/Screens/TodayGlanceView.swift` outputs at least `4` (one per @AppStorage declaration)
    - `grep -c 'Text("65")' VitaminG/VitaminGWatch/Screens/TodayGlanceView.swift` outputs `0` (hardcoded value removed)
    - `grep -c 'progress: 0.72' VitaminG/VitaminGWatch/Screens/TodayGlanceView.swift` outputs `0` (hardcoded value removed)
    - `grep -c 'Text("72")' VitaminG/VitaminGWatch/Screens/TodayGlanceView.swift` outputs `0` (hardcoded value removed)
    - `grep -c "globalStreak\|activeGoalProgress\|activeGoalTitle" VitaminG/VitaminGWatch/Screens/TodayGlanceView.swift` outputs at least `3`
    - `xcodebuild build -scheme VitaminG` exits 0 (Watch target compiles with live data wiring)
  </acceptance_criteria>
  <done>
    VGWatchApp activates WatchReceiver at init. TodayGlanceView renders live snapshot data via @AppStorage and shows the active goal title below the ring. Check In button behavior unchanged for this wave.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Make WatchReceiverTests GREEN by exercising didReceiveApplicationContext</name>
  <files>VitaminG/VitaminG/VitaminGTests/WatchReceiverTests.swift</files>
  <read_first>
    - VitaminG/VitaminG/VitaminGTests/WatchReceiverTests.swift (the RED stubs from Plan 01)
    - VitaminG/VitaminGWatch/Shared/WatchReceiver.swift (created in Task 1)
    - VitaminG/VitaminG/VitaminG/Models/WatchSnapshot.swift (created in Plan 02)
    - VitaminG/VitaminG/VitaminGTests/Phase24WidgetDataProviderTests.swift (test fixture pattern)
  </read_first>
  <behavior>
    Tests must NOT instantiate WCSession (forbidden in unit test environment). Instead, tests construct a WatchSnapshot, encode it as Data, build an `applicationContext: [String: Any] = ["snapshot": data]` dictionary, and call a testable wrapper method that performs the decode + UserDefaults write. The wrapper takes the dictionary and a UserDefaults instance (injectable), bypassing WCSession entirely.

    Each test creates a fresh in-memory UserDefaults via `UserDefaults(suiteName: "test-watch-snapshot-\\(UUID().uuidString)")`, calls the wrapper, then reads the keys back to assert.

    Test method behaviors:
    - `test_didReceiveApplicationContext_writesActiveGoalTitleToUserDefaults`: snapshot has activeGoalTitle = "Read every day"; after call, defaults.string(forKey: "watchSnapshot_activeGoalTitle") == "Read every day".
    - `test_didReceiveApplicationContext_writesActiveGoalProgressToUserDefaults`: progress = 0.42; defaults.double(forKey:) ≈ 0.42 (within 1e-6).
    - `test_didReceiveApplicationContext_writesGlobalStreakToUserDefaults`: globalStreak = 12; defaults.integer(forKey:) == 12.
    - `test_didReceiveApplicationContext_writesHasCheckedInTodayToUserDefaults`: hasCheckedInToday = true; defaults.bool(forKey:) == true.
    - `test_didReceiveApplicationContext_writesActiveGoalIdToUserDefaults`: activeGoalId = UUID().uuidString; defaults.string(forKey:) equals that string.
  </behavior>
  <action>
    Modify WatchReceiver.swift to add a testable wrapper method that performs the decode + write WITHOUT requiring WCSession:
    - `func processApplicationContext(_ applicationContext: [String: Any], into defaults: UserDefaults?)` — extracts "snapshot" Data, decodes WatchSnapshot, writes five keys via the provided defaults.
    - Refactor `session(_:didReceiveApplicationContext:)` to call `processApplicationContext(applicationContext, into: UserDefaults(suiteName: suiteName))` then `WidgetCenter.shared.reloadAllTimelines()` only when defaults is non-nil.

    The wrapper must compile in both VitaminGWatch and VitaminGTests targets. Add WatchReceiver.swift to VitaminGTests target membership in Xcode (or wrap the testable wrapper in `#if !os(watchOS)`-compatible code — prefer Target Membership since the type is needed by tests). UserDefaults APIs and JSONDecoder work on all platforms.

    Then, in `WatchReceiverTests.swift`, replace every XCTFail body with the real assertion described in <behavior>. Create a per-test ephemeral suiteName using `UUID().uuidString` so test isolation is guaranteed. Call `processApplicationContext` directly. Tear down by removing the suite via `defaults.removePersistentDomain(forName:)`.

    Do NOT add `WidgetCenter` import to the test file — tests do not call reload. Tests only verify the UserDefaults write path.
  </action>
  <verify>
    <automated>cd "VitaminG/VitaminG" && xcodebuild test -scheme VitaminG -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:VitaminGTests/WatchReceiverTests 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "XCTFail" VitaminG/VitaminG/VitaminGTests/WatchReceiverTests.swift` outputs `0` (no XCTFail stubs remain)
    - `grep -c "processApplicationContext" VitaminG/VitaminG/VitaminGTests/WatchReceiverTests.swift` outputs at least `5` (one per test)
    - `grep -c "processApplicationContext" VitaminG/VitaminGWatch/Shared/WatchReceiver.swift` outputs at least `2` (declaration + call from delegate method)
    - `xcodebuild test ... -only-testing:VitaminGTests/WatchReceiverTests` exits 0 (all 5 tests GREEN)
  </acceptance_criteria>
  <done>
    WatchReceiver exposes a testable wrapper. WatchReceiverTests.swift contains real assertions against UserDefaults state. All 5 tests pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| iPhone → Watch WCSession applicationContext | Trusted channel between paired devices |
| Watch App Group UserDefaults | Local-to-watch storage shared between Watch app and Watch Widget Extension; not accessible from iOS |
| TodayGlanceView @AppStorage binding | Reads from Watch App Group suite; reactive on UserDefaults change |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-03-01 | Tampering | WatchSnapshot decode failure | mitigate | Decode wrapped in try?; on failure, UserDefaults retains previous values (no crash, no partial write) |
| T-27-03-02 | Information Disclosure | UserDefaults suiteName misconfiguration | mitigate | Plan 01 + Plan 03 both reference the exact same `group.com.kyleharrington.VitaminGWatch` identifier; acceptance criteria grep-check the literal string |
| T-27-03-03 | Denial of Service | Session not activated before first push | mitigate | activate() called in VGWatchApp.init() per RESEARCH.md Pitfall 1; idempotent |
| T-27-03-04 | Spoofing | Foreign app writing to Watch App Group | accept | App Group access is gated by entitlement signing — only signed VitaminGWatch processes can write |
</threat_model>

<verification>
- `xcodebuild build -scheme VitaminG -destination 'platform=iOS Simulator,name=iPhone 16'` exits 0
- `xcodebuild test -scheme VitaminG -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:VitaminGTests/WatchReceiverTests` exits 0
- TodayGlanceView preview renders with zero progress / empty title in Simulator (live data path active but no snapshot yet)
- WatchSessionManagerTests remain RED (Wave 4 makes them GREEN — expected)
</verification>

<success_criteria>
1. `WatchReceiver.swift` exists on the Watch target with no `sessionDidBecomeInactive`/`sessionDidDeactivate` references.
2. `VGWatchApp.init()` calls `WatchReceiver.shared.activate()` before any view renders.
3. `TodayGlanceView` displays live `Day {globalStreak}` header, `activeGoalProgress` ring, `Int(activeGoalProgress * 100)` percent text, and `activeGoalTitle` below the ring (or empty when no goal).
4. WatchReceiver decodes incoming applicationContext into 5 UserDefaults keys with `watchSnapshot_` prefix and reloads all widget timelines.
5. WatchReceiverTests GREEN.
</success_criteria>

<output>
Create `.planning/phases/27-apple-watch-app/27-03-SUMMARY.md` when done.
</output>
