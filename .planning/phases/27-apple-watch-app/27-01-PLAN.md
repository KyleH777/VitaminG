---
phase: 27-apple-watch-app
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - VitaminG/VitaminG/VitaminG.xcodeproj/project.pbxproj
  - VitaminG/VitaminGWatch/VitaminGWatch.entitlements
  - VitaminG/VitaminGWatchWidget/VitaminGWatchWidget.entitlements
  - VitaminG/VitaminG/VitaminGTests/WatchSnapshotTests.swift
  - VitaminG/VitaminG/VitaminGTests/WatchSessionManagerTests.swift
  - VitaminG/VitaminG/VitaminGTests/WatchReceiverTests.swift
autonomous: false
requirements: [WATCH-02, WATCH-03]
user_setup:
  - service: apple-developer-portal
    why: "Watch-scoped App Group identifier must be registered before UserDefaults(suiteName:) works at runtime"
    dashboard_config:
      - task: "Register group.com.kyleharrington.VitaminGWatch under Identifiers > App Groups"
        location: "Apple Developer Portal > Certificates, Identifiers & Profiles > Identifiers > App Groups > +"

must_haves:
  truths:
    - "VitaminGWatch target deployment is watchOS 10.0 (raised from 7.0)"
    - "group.com.kyleharrington.VitaminGWatch App Group is registered in Apple Developer Portal and listed in both Watch target entitlements"
    - "VitaminGTests contains three RED test stub files for WatchSnapshot, WatchSessionManager, WatchReceiver"
  artifacts:
    - path: "VitaminG/VitaminGWatch/VitaminGWatch.entitlements"
      provides: "Watch app entitlement granting access to Watch-scoped App Group"
      contains: "group.com.kyleharrington.VitaminGWatch"
    - path: "VitaminG/VitaminGWatchWidget/VitaminGWatchWidget.entitlements"
      provides: "Watch Widget Extension entitlement granting access to Watch-scoped App Group"
      contains: "group.com.kyleharrington.VitaminGWatch"
    - path: "VitaminG/VitaminG/VitaminGTests/WatchSnapshotTests.swift"
      provides: "RED unit-test stubs for WatchSnapshot.build()"
    - path: "VitaminG/VitaminG/VitaminGTests/WatchSessionManagerTests.swift"
      provides: "RED unit-test stubs for WatchSessionManager.handleCheckIn()"
    - path: "VitaminG/VitaminG/VitaminGTests/WatchReceiverTests.swift"
      provides: "RED unit-test stubs for WatchReceiver UserDefaults write"
  key_links:
    - from: "project.pbxproj"
      to: "VitaminGWatch target build settings"
      via: "WATCHOS_DEPLOYMENT_TARGET = 10.0 in both Debug and Release"
      pattern: "WATCHOS_DEPLOYMENT_TARGET = 10.0"
    - from: "VitaminGWatch.entitlements"
      to: "Apple Developer Portal App Group registration"
      via: "com.apple.security.application-groups array entry"
      pattern: "group\\.com\\.kyleharrington\\.VitaminGWatch"
---

<objective>
Establish the foundation Phase 27 cannot proceed without: raise the watchOS deployment target from 7.0 to 10.0 (required for WidgetKit complications and STATE.md locked decision), register the Watch-scoped App Group, create entitlements files for both the Watch app and the new Watch Widget Extension target, and create the three RED XCTest stub files referenced by 27-VALIDATION.md so all subsequent waves have a target test file to make GREEN.

Purpose: Without watchOS 10.0 minimum, the WidgetKit complication APIs do not compile. Without the Watch-scoped App Group, UserDefaults(suiteName:) returns nil at runtime and the Watch app and Watch Widget Extension cannot share snapshot data. Without RED tests, downstream waves have no Nyquist-compliant <automated> verify targets.

Output: Deployment target raised, two entitlements files created, three test stubs committed, and developer-portal App Group registered (human checkpoint).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-apple-watch-app/27-CONTEXT.md
@.planning/phases/27-apple-watch-app/27-RESEARCH.md
@.planning/phases/27-apple-watch-app/27-PATTERNS.md
@.planning/phases/27-apple-watch-app/27-VALIDATION.md
@VitaminG/VitaminG/VitaminGWidget/VitaminGWidget.entitlements
@VitaminG/VitaminG/VitaminGTests/Phase24WidgetDataProviderTests.swift

<interfaces>
Pre-existing entitlements file shape (VitaminG/VitaminG/VitaminGWidget/VitaminGWidget.entitlements):
- plist root, single key `com.apple.security.application-groups`, value is an `<array>` of `<string>` entries.
- Existing iOS group identifier: `group.com.kyleharrington.VitaminG` — DO NOT reuse for Watch targets.

Pre-existing pbxproj watchOS deployment target lines (VitaminG/VitaminG/VitaminG.xcodeproj/project.pbxproj):
- Line 553: `WATCHOS_DEPLOYMENT_TARGET = 7.0;` (Debug configuration of VitaminGWatch target)
- Line 978: `WATCHOS_DEPLOYMENT_TARGET = 7.0;` (Release configuration of VitaminGWatch target)

Test file template (from VitaminGTests/Phase24WidgetDataProviderTests.swift):
- `import XCTest`, `import SwiftData`, `@testable import VitaminG`
- `final class <Name>: XCTestCase`
- `setUpWithError` uses `ModelContainerFactory.makeContainer(inMemory: true)`
- Test method names follow `test_<feature>_<scenario>()`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Raise VitaminGWatch deployment target from watchOS 7.0 to 10.0</name>
  <files>VitaminG/VitaminG/VitaminG.xcodeproj/project.pbxproj</files>
  <read_first>
    - VitaminG/VitaminG/VitaminG.xcodeproj/project.pbxproj (current state — lines 553 and 978 contain WATCHOS_DEPLOYMENT_TARGET = 7.0)
    - .planning/STATE.md (locked decision: "watchOS 10.0 minimum for Watch target")
    - .planning/phases/27-apple-watch-app/27-RESEARCH.md (section "Watchos Deployment Target: Immediate Action Required")
  </read_first>
  <action>
    Edit project.pbxproj and change every occurrence of `WATCHOS_DEPLOYMENT_TARGET = 7.0;` to `WATCHOS_DEPLOYMENT_TARGET = 10.0;`. Only the VitaminGWatch target build configurations contain this key — confirm via grep that exactly two occurrences exist on lines 553 and 978 before editing, and that both belong to the VitaminGWatch target (not the iOS app target, which uses IPHONEOS_DEPLOYMENT_TARGET). Do NOT modify IPHONEOS_DEPLOYMENT_TARGET. Do NOT add any other entries. Use the Edit tool — never `sed`.
  </action>
  <verify>
    <automated>cd "VitaminG/VitaminG" && grep -c "WATCHOS_DEPLOYMENT_TARGET = 10.0" VitaminG.xcodeproj/project.pbxproj | grep -q "^2$" && ! grep -q "WATCHOS_DEPLOYMENT_TARGET = 7.0" VitaminG.xcodeproj/project.pbxproj</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "WATCHOS_DEPLOYMENT_TARGET = 10.0" VitaminG/VitaminG/VitaminG.xcodeproj/project.pbxproj` outputs `2`
    - `grep -c "WATCHOS_DEPLOYMENT_TARGET = 7.0" VitaminG/VitaminG/VitaminG.xcodeproj/project.pbxproj` outputs `0`
    - `IPHONEOS_DEPLOYMENT_TARGET` lines in the same file are unchanged from baseline
  </acceptance_criteria>
  <done>
    project.pbxproj contains exactly two `WATCHOS_DEPLOYMENT_TARGET = 10.0;` entries (Debug + Release for VitaminGWatch target) and zero `WATCHOS_DEPLOYMENT_TARGET = 7.0;` entries.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create VitaminGWatch and VitaminGWatchWidget entitlements files</name>
  <files>VitaminG/VitaminGWatch/VitaminGWatch.entitlements, VitaminG/VitaminGWatchWidget/VitaminGWatchWidget.entitlements</files>
  <read_first>
    - VitaminG/VitaminG/VitaminGWidget/VitaminGWidget.entitlements (analog — entitlements plist structure)
    - .planning/phases/27-apple-watch-app/27-RESEARCH.md (section "App Group Configuration — Critical Details" — confirms `group.com.kyleharrington.VitaminGWatch` identifier, NOT the iOS group)
    - .planning/phases/27-apple-watch-app/27-PATTERNS.md (sections for VitaminGWatch.entitlements and VitaminGWatchWidget.entitlements)
  </read_first>
  <action>
    Create the VitaminGWatchWidget directory if it does not already exist (it will be a new sibling of VitaminGWatch under VitaminG/). Then create two plist XML entitlements files, each containing a single `com.apple.security.application-groups` key whose value is an array with the single string `group.com.kyleharrington.VitaminGWatch`. Use the exact same XML declaration, DOCTYPE, and plist version="1.0" structure as VitaminGWidget.entitlements. The Watch app entitlements file path is `VitaminG/VitaminGWatch/VitaminGWatch.entitlements`. The Widget Extension entitlements file path is `VitaminG/VitaminGWatchWidget/VitaminGWatchWidget.entitlements`. Both files use the Watch-scoped App Group identifier `group.com.kyleharrington.VitaminGWatch` — NEVER the iOS group `group.com.kyleharrington.VitaminG` (Pitfall: iOS and watchOS App Group containers are different physical filesystem locations per RESEARCH.md confirmed by DTS engineer).
  </action>
  <verify>
    <automated>test -f "VitaminG/VitaminGWatch/VitaminGWatch.entitlements" && test -f "VitaminG/VitaminGWatchWidget/VitaminGWatchWidget.entitlements" && grep -l "group.com.kyleharrington.VitaminGWatch" "VitaminG/VitaminGWatch/VitaminGWatch.entitlements" "VitaminG/VitaminGWatchWidget/VitaminGWatchWidget.entitlements"</automated>
  </verify>
  <acceptance_criteria>
    - Both files exist at the exact paths in <files>
    - Each file contains the literal string `group.com.kyleharrington.VitaminGWatch` inside a `<string>` element
    - Neither file contains the iOS group identifier `group.com.kyleharrington.VitaminG` (without `Watch` suffix) as a standalone entry — `grep '<string>group\.com\.kyleharrington\.VitaminG</string>' <file>` returns no matches
    - XML structure matches VitaminGWidget.entitlements: `<?xml version="1.0" encoding="UTF-8"?>` declaration, `<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">`, `<plist version="1.0">`, single `<dict>` with `com.apple.security.application-groups` key
  </acceptance_criteria>
  <done>
    Both entitlements files exist with correct content and structure. Both reference only the Watch-scoped App Group identifier.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create RED XCTest stub files for WatchSnapshot, WatchSessionManager, WatchReceiver</name>
  <files>VitaminG/VitaminG/VitaminGTests/WatchSnapshotTests.swift, VitaminG/VitaminG/VitaminGTests/WatchSessionManagerTests.swift, VitaminG/VitaminG/VitaminGTests/WatchReceiverTests.swift</files>
  <read_first>
    - VitaminG/VitaminG/VitaminGTests/Phase24WidgetDataProviderTests.swift (XCTestCase setup pattern, ModelContainerFactory usage, test naming convention)
    - .planning/phases/27-apple-watch-app/27-VALIDATION.md (section "Wave 0 Requirements" and "Per-Task Verification Map" — defines required test file names and target requirements)
    - .planning/phases/27-apple-watch-app/27-RESEARCH.md (Pattern 2 for WatchSnapshot fields; Pattern 3 for WatchReceiver UserDefaults keys; Pattern 4 for WatchSessionManager handleCheckIn)
  </read_first>
  <action>
    Create three RED XCTest files. Each must follow the existing pattern from Phase24WidgetDataProviderTests.swift (imports XCTest, SwiftData, @testable import VitaminG; final class XCTestCase; setUpWithError with `ModelContainerFactory.makeContainer(inMemory: true)`). Mark every test body with `XCTFail("RED stub — WatchSnapshot/WatchSessionManager/WatchReceiver not yet implemented in Plan 02/03/04")` so the suite fails until the target type exists. Tests reference types that do not yet exist (`WatchSnapshot`, `WatchSessionManager`, `WatchReceiver`) only inside `// TODO:` comments — never in compile-time code in this wave. The test bodies must compile today (no references to symbols that don't exist).

    Required test methods (placeholder bodies all containing XCTFail):

    WatchSnapshotTests.swift (WATCH-02 — Wave 1 makes these GREEN):
    - `test_build_returnsActiveGoalTitle_fromHighestPriorityTier()`
    - `test_build_returnsActiveGoalProgress_zeroToOne()`
    - `test_build_returnsGlobalStreak_fromStreakEngine()`
    - `test_build_returnsHasCheckedInToday_trueWhenTodayEventExists()`
    - `test_build_returnsActiveGoalId_uuidString()`
    - `test_codable_roundTripsThroughJSONEncoderDecoder()`

    WatchSessionManagerTests.swift (WATCH-03 — Wave 4 makes these GREEN):
    - `test_handleCheckIn_invokesAddCheckInForCorrectGoal()`
    - `test_handleCheckIn_callsCancelGlobalStreakAtRiskNudge()`
    - `test_handleCheckIn_callsWidgetCenterReloadAllTimelines()`
    - `test_handleCheckIn_ignoresPayloadWithoutActionCheckIn()`
    - `test_handleCheckIn_ignoresPayloadWithInvalidGoalId()`

    WatchReceiverTests.swift (WATCH-02 — Wave 2 makes these GREEN):
    - `test_didReceiveApplicationContext_writesActiveGoalTitleToUserDefaults()`
    - `test_didReceiveApplicationContext_writesActiveGoalProgressToUserDefaults()`
    - `test_didReceiveApplicationContext_writesGlobalStreakToUserDefaults()`
    - `test_didReceiveApplicationContext_writesHasCheckedInTodayToUserDefaults()`
    - `test_didReceiveApplicationContext_writesActiveGoalIdToUserDefaults()`

    All UserDefaults keys referenced in TODO comments use the prefix `watchSnapshot_` per CONTEXT.md decision (Claude's Discretion section). All test files belong to the VitaminGTests target; do NOT add to the VitaminGWatch target.
  </action>
  <verify>
    <automated>test -f "VitaminG/VitaminG/VitaminGTests/WatchSnapshotTests.swift" && test -f "VitaminG/VitaminG/VitaminGTests/WatchSessionManagerTests.swift" && test -f "VitaminG/VitaminG/VitaminGTests/WatchReceiverTests.swift" && cd "VitaminG/VitaminG" && xcodebuild -scheme VitaminG -destination 'platform=iOS Simulator,name=iPhone 16' build-for-testing 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - All three files exist at the exact paths in <files>
    - `xcodebuild ... build-for-testing` exits 0 (test files compile)
    - `grep -c "XCTFail" VitaminG/VitaminG/VitaminGTests/WatchSnapshotTests.swift` outputs at least `6` (one per declared test)
    - `grep -c "XCTFail" VitaminG/VitaminG/VitaminGTests/WatchSessionManagerTests.swift` outputs at least `5`
    - `grep -c "XCTFail" VitaminG/VitaminG/VitaminGTests/WatchReceiverTests.swift` outputs at least `5`
    - Each file begins with `import XCTest` and `@testable import VitaminG`
    - No file imports `WatchConnectivity` or `WidgetKit` (those are Wave 1+ implementation concerns)
    - `grep -c "watchSnapshot_" VitaminG/VitaminG/VitaminGTests/WatchReceiverTests.swift` outputs at least `5` (one per UserDefaults key in TODO comments)
  </acceptance_criteria>
  <done>
    All three RED test files exist, compile under VitaminGTests target, and contain XCTFail placeholders for every declared test method. The test bundle builds cleanly.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <what-built>
    Deployment target raised to watchOS 10.0; two Watch entitlements files created referencing `group.com.kyleharrington.VitaminGWatch`; three RED XCTest stub files committed.
  </what-built>
  <how-to-verify>
    The next two steps require manual action in Xcode and the Apple Developer Portal — Claude cannot perform these via CLI/API:

    1. **Register Watch App Group in Apple Developer Portal:**
       - Visit https://developer.apple.com/account/resources/identifiers/list/applicationGroup
       - Click `+` to register a new App Group
       - Identifier: `group.com.kyleharrington.VitaminGWatch`
       - Description: `Vitamin G Watch Group`
       - Confirm the group appears in the list after creation

    2. **Wire entitlements + new VitaminGWatchWidget target in Xcode:**
       - Open `VitaminG/VitaminG/VitaminG.xcodeproj` in Xcode
       - Select VitaminGWatch target → Signing & Capabilities → + Capability → App Groups → tick `group.com.kyleharrington.VitaminGWatch`
       - Xcode will link to the entitlements file Claude created in Task 2
       - File > New > Target > watchOS > Widget Extension; product name `VitaminGWatchWidget`; embedded in `VitaminGWatch`; uncheck "Include Configuration Intent"
       - VitaminGWatchWidget target → Signing & Capabilities → + Capability → App Groups → tick `group.com.kyleharrington.VitaminGWatch`
       - Replace Xcode's auto-generated VitaminGWatchWidget.entitlements with the one Claude created in Task 2 (or confirm content matches)
       - VitaminGWatchWidget target Build Settings: confirm `WATCHOS_DEPLOYMENT_TARGET = 10.0`
       - Select `VGRingView.swift` and `VGWatchTheme.swift` in the file inspector → Target Membership → tick `VitaminGWatchWidget` (Pitfall 7 — required for the widget to use VGWatch tokens and the ring component)
       - Delete the boilerplate Swift file Xcode generated inside `VitaminGWatchWidget/` (Plan 04 will create `VitaminGWatchWidget.swift` from scratch)

    3. **Confirm the project builds:**
       - In Xcode, select the VitaminG scheme → iPhone 16 simulator → Cmd+B
       - Build must succeed with zero errors and zero warnings related to entitlements or App Groups
       - Run `xcodebuild test -scheme VitaminG -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:VitaminGTests/WatchSnapshotTests` — expect RED (test failures with `XCTFail` messages); a clean compile but failing tests is the desired Wave 0 state
  </how-to-verify>
  <resume-signal>
    Type `approved` once: (a) the App Group is registered in the developer portal, (b) both Watch targets have the App Group entitlement applied in Xcode, (c) VitaminGWatchWidget target exists with VGRingView/VGWatchTheme in its Target Membership, (d) the project builds cleanly. Type `blocked: <reason>` if any step fails (e.g., portal permissions issue, build error).
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Apple Developer Portal | External service holding the App Group identifier registration; runtime UserDefaults(suiteName:) depends on this being correct |
| Xcode entitlements wiring | Local-to-developer; mistakes here silently break runtime sharing without compile errors |
| project.pbxproj edit | Hand-editing the project file risks corruption; mitigated by single-key, single-value edits and grep verification |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-01-01 | Tampering | project.pbxproj edit | mitigate | Use Edit tool with exact-match string; verify with grep count of 2 occurrences of new value, 0 of old |
| T-27-01-02 | Information Disclosure | App Group identifier reuse | mitigate | Force Watch group identifier (`...VitaminGWatch`) to be distinct from iOS group; pattern-match in acceptance criteria forbids reuse |
| T-27-01-03 | Denial of Service | Wrong App Group at runtime | mitigate | Human checkpoint requires portal registration BEFORE any runtime UserDefaults read in Wave 2+ |
| T-27-01-04 | Tampering | Test stub file paths | mitigate | Stub files live in VitaminGTests/ target only — cannot affect production binary; isolated to test bundle |
</threat_model>

<verification>
After human checkpoint approval:
- `grep -c "WATCHOS_DEPLOYMENT_TARGET = 10.0" VitaminG/VitaminG/VitaminG.xcodeproj/project.pbxproj` returns 2
- `test -f VitaminG/VitaminGWatch/VitaminGWatch.entitlements && test -f VitaminG/VitaminGWatchWidget/VitaminGWatchWidget.entitlements` exits 0
- `xcodebuild -scheme VitaminG -destination 'platform=iOS Simulator,name=iPhone 16' build-for-testing` exits 0
- App Group `group.com.kyleharrington.VitaminGWatch` exists in Apple Developer Portal Identifiers list
</verification>

<success_criteria>
1. watchOS deployment target raised to 10.0 in both Debug and Release configurations of VitaminGWatch target.
2. `VitaminGWatch.entitlements` and `VitaminGWatchWidget.entitlements` exist with the Watch-scoped App Group identifier and are wired in Xcode.
3. `WatchSnapshotTests.swift`, `WatchSessionManagerTests.swift`, `WatchReceiverTests.swift` compile in VitaminGTests target and fail with XCTFail messages (RED state).
4. VitaminGWatchWidget target exists in Xcode, has `VGRingView.swift` and `VGWatchTheme.swift` in its Target Membership, and shares the Watch App Group.
5. Apple Developer Portal lists `group.com.kyleharrington.VitaminGWatch` under App Groups.
</success_criteria>

<output>
Create `.planning/phases/27-apple-watch-app/27-01-SUMMARY.md` when done.
</output>
