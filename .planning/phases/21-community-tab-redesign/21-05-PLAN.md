---
phase: 21-community-tab-redesign
plan: 05
type: execute
wave: 3
depends_on:
  - 21-03
  - 21-04
files_modified:
  - VitaminG/VitaminG/VitaminG/Views/Community/GlimpsesCarouselSection.swift
  - VitaminG/VitaminG/VitaminG/Views/Community/ActiveTodaySection.swift
  - VitaminG/VitaminG/VitaminG/Views/Community/GlowingSpotlightSection.swift
  - VitaminG/VitaminG/VitaminG/Views/Community/GlobalFeedSection.swift
  - VitaminG/VitaminG/VitaminG/ViewModels/CommunityHubViewModel.swift
autonomous: true
requirements:
  - COMM-02
  - COMM-03
  - COMM-04
  - COMM-05
  - COMM-06
  - COMM-07
  - SOC-01
  - SOC-02
  - SOC-03

must_haves:
  truths:
    - "GlimpsesCarouselSection auto-advances every 5 seconds using Timer.publish().autoconnect(), pauses on manual DragGesture"
    - "Tapping a GlimpseCard triggers router.pendingPublicProfileRecordID = glimpse.username (per D-02)"
    - "Tapping applause on a GlimpseCard calls viewModel.writeApplauseForUser (per D-03)"
    - "ActiveTodaySection shows horizontal scrolling avatar strip; tapping any avatar sets router.pendingPublicProfileRecordID"
    - "GlowingSpotlightSection uses selectGlowingUser from CommunityHubViewModel; has gold ring border; shows ApplauseButtonView per D-03"
    - "GlobalFeedSection uses LazyVStack of CommunityPostCard with 🔥 reaction (fire already in ReactionType); reply button opens CommunityReplySheetView (not CommentSheetView)"
    - "GlobalFeedSection compose CTA opens PostComposeSheet with category nil for global posts"
    - "All empty states match exact copywriting from UI-SPEC §Copywriting"
  artifacts:
    - path: "VitaminG/VitaminG/VitaminG/Views/Community/GlimpsesCarouselSection.swift"
      provides: "TabView(.page) carousel with 5s auto-advance + DragGesture pause + applause + nav tap"
    - path: "VitaminG/VitaminG/VitaminG/Views/Community/ActiveTodaySection.swift"
      provides: "Horizontal ScrollView of UserPresenceItem avatar cards with profile nav tap"
    - path: "VitaminG/VitaminG/VitaminG/Views/Community/GlowingSpotlightSection.swift"
      provides: "Hero card with gold ring, username (20pt serif), goal title, ApplauseButtonView"
    - path: "VitaminG/VitaminG/VitaminG/Views/Community/GlobalFeedSection.swift"
      provides: "LazyVStack CommunityPostCard feed with optimistic reaction, reply sheet, compose CTA"
  key_links:
    - from: "GlimpsesCarouselSection"
      to: "AppRouter.pendingPublicProfileRecordID"
      via: "router.pendingPublicProfileRecordID = glimpse.username on card tap"
      pattern: "pendingPublicProfileRecordID"
    - from: "GlimpsesCarouselSection"
      to: "ApplauseButtonView"
      via: "ApplauseButtonView embedded in GlimpseCard with viewModel.writeApplauseForUser closure"
      pattern: "ApplauseButtonView"
    - from: "GlowingSpotlightSection"
      to: "CommunityHubViewModel.glowingUser"
      via: "reads viewModel.glowingUser directly; nil → empty state copy"
      pattern: "glowingUser"
    - from: "GlobalFeedSection"
      to: "CommunityReplySheetView"
      via: ".sheet(isPresented: $showReplySheet) { CommunityReplySheetView(parentPostID: activeReplyPostID, ...) }"
      pattern: "CommunityReplySheetView"
---

<objective>
Build the four Community tab section sub-views: GlimpsesCarouselSection, ActiveTodaySection, GlowingSpotlightSection, and GlobalFeedSection. These are the visual building blocks that CommunityTabView (Plan 06) will assemble into the final hub layout.

Purpose: Keeping each section in its own file (per RESEARCH.md §Recommended Project Structure) makes each independently testable and avoids the anti-pattern of a 600-line CommunityTabView. Plan 06 is a thin assembly file once these components exist.

Output: Four section component Swift files, each reading from CommunityHubViewModel bindings.
</objective>

<execution_context>
@/Users/kyleharrington/Desktop/AI/Vitamin G/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kyleharrington/Desktop/AI/Vitamin G/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/kyleharrington/Desktop/AI/Vitamin G/.planning/ROADMAP.md
@/Users/kyleharrington/Desktop/AI/Vitamin G/.planning/phases/21-community-tab-redesign/21-CONTEXT.md
@/Users/kyleharrington/Desktop/AI/Vitamin G/.planning/phases/21-community-tab-redesign/21-PATTERNS.md
@/Users/kyleharrington/Desktop/AI/Vitamin G/.planning/phases/21-community-tab-redesign/21-UI-SPEC.md

<interfaces>
<!-- CommunityHubViewModel (Plan 03 output) -->
// @MainActor @Observable final class CommunityHubViewModel
// var glimpses: [GoalGlimpseItem] = []
// var activeUsers: [UserPresenceItem] = []
// var glowingUser: GoalGlimpseItem? = nil
// var feedPosts: [CKRecord] = []
// func writeApplauseForUser(recipientUsername: String, giverUsername: String) async
// func canApplaud(recipientUsername: String) -> Bool

<!-- ApplauseButtonView (Plan 03 output) -->
// struct ApplauseButtonView: View
// init(recipientUsername: String, giverUsername: String, canApplaud: Bool, onApplaud: () -> Void)

<!-- CommunityReplySheetView (Plan 04 output) -->
// struct CommunityReplySheetView: View
// init(parentPostID: String, authorDisplayName: String?, authorColorHex: String?, onWriteReply: (String) async -> Bool)

<!-- PostComposeSheet (Plan 04 extended) -->
// existing .sheet presentation: .sheet(isPresented: $showCompose) { PostComposeSheet(viewModel:category:authorDisplayName:authorColorHex:) }

<!-- CommunityPostCard existing interface (from PATTERNS.md lines 478–516) -->
// struct CommunityPostCard: View
// init(post: CKRecord, currentUserReaction: ReactionType?, accentColor: Color,
//      onReact: (ReactionType) -> Void, onReport: () -> Void, onComment: (() -> Void)? = nil)
// Note: .fire reaction already added to ReactionType in Plan 02; CommunityPostCard updated in Plan 06

<!-- AvatarView interface -->
// struct AvatarView: View
// init(displayName: String, avatarColorHex: String?, photoData: Data?, size: CGFloat)

<!-- AppRouter interface -->
// @Observable final class AppRouter { var pendingPublicProfileRecordID: String? = nil }

<!-- VGTheme tokens -->
// VGTheme.background, VGTheme.surface, VGTheme.accentTerra, VGTheme.accentGold
// VGTheme.textPrimary, VGTheme.textMuted, VGTheme.separator
// VGTheme.serif(28), VGTheme.serif(20)

<!-- UI-SPEC section layout specs -->
// GlimpseCard: height 180pt, AvatarView(size:40), username 14pt semibold, goalTitle 14pt regular, progress% 14pt textMuted
// ActiveTodaySection: horizontal scroll, AvatarView(size:44), username 14pt textMuted, spacing 12pt
// GlowingSpotlightSection: RoundedRectangle(cornerRadius:20), VGTheme.surface bg, gold ring 2pt, 24pt internal padding
//   - "Glowing This Week" label: 14pt textMuted allcaps tracking 0.5
//   - username: 20pt serif (VGTheme.serif(20))
//   - goal title: 17pt body
// GlobalFeedSection: LazyVStack spacing 12pt

<!-- Carousel timer pattern (PATTERNS.md) -->
// let timer = Timer.publish(every: 5, on: .main, in: .common).autoconnect()
// .onReceive(timer) { _ in guard !isDragging, !glimpses.isEmpty else { return }; withAnimation(.easeInOut(duration: 0.4)) { currentIndex = (currentIndex + 1) % glimpses.count } }
// .simultaneousGesture(DragGesture().onChanged { _ in isDragging = true }.onEnded { _ in isDragging = false })

<!-- CommunityFeedView optimistic reaction pattern (PATTERNS.md lines 303–325) -->
// @State private var localReactionByPostID: [CKRecord.ID: ReactionType] = [:]
// private func handleReact(post: CKRecord, type: ReactionType) { ... optimistic toggle ... }

<!-- GlobalFeedSection empty state copy (UI-SPEC) -->
// "Community is quiet right now" / "Be the first to share today."
<!-- ActiveTodaySection empty state copy -->
// "Nobody active in the last 2 hours. Be the first — open the app!"
<!-- GlimpsesCarouselSection empty state copy -->
// "No glimpses yet today" / "Check in on a goal to appear here."
<!-- GlowingSpotlightSection empty state copy -->
// "Check back later — someone will be glowing this week."
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build GlimpsesCarouselSection + ActiveTodaySection</name>
  <files>
    VitaminG/VitaminG/VitaminG/Views/Community/GlimpsesCarouselSection.swift
    VitaminG/VitaminG/VitaminG/Views/Community/ActiveTodaySection.swift
  </files>
  <read_first>
    - VitaminG/VitaminG/VitaminG/Views/Explore/TrendingNowSection.swift (section sub-view pattern — receives ViewModel array, no @Query)
    - VitaminG/VitaminG/VitaminG/Views/Explore/StuckDayGiftsSection.swift (horizontal scroll list pattern)
    - VitaminG/VitaminG/VitaminG/Views/AvatarView.swift (AvatarView interface)
    - VitaminG/VitaminG/VitaminG/Navigation/AppRouter.swift (pendingPublicProfileRecordID)
    - VitaminG/VitaminG/VitaminG/.planning/phases/21-community-tab-redesign/21-UI-SPEC.md (§Component Inventory — GlimpsesCarouselSection, GlimpseCard, ActiveTodaySection)
  </read_first>
  <action>
    Create GlimpsesCarouselSection.swift:

    `struct GlimpsesCarouselSection: View`. Properties: `let glimpses: [GoalGlimpseItem]`, `let giverUsername: String`, `let canApplaud: (String) -> Bool`, `let onApplaud: (String) -> Void`, `let onTapCard: (GoalGlimpseItem) -> Void`. State: `@State private var currentIndex = 0`, `@State private var isDragging = false`. Private timer: `let timer = Timer.publish(every: 5, on: .main, in: .common).autoconnect()`.

    Body: if glimpses.isEmpty — empty state VStack: Text("No glimpses yet today") (17pt body VGTheme.textPrimary) + Text("Check in on a goal to appear here.") (14pt VGTheme.textMuted). Else: TabView(selection: $currentIndex) { ForEach(Array(glimpses.enumerated()), id: \.offset) { index, glimpse in GlimpseCard(glimpse:giverUsername:canApplaud:onApplaud:onTapCard:).tag(index) } }.tabViewStyle(.page(indexDisplayMode: .automatic)).frame(height: 180).onReceive(timer) { _ in guard !isDragging, !glimpses.isEmpty else { return }; withAnimation(.easeInOut(duration: 0.4)) { currentIndex = (currentIndex + 1) % glimpses.count } }.simultaneousGesture(DragGesture().onChanged { _ in isDragging = true }.onEnded { _ in isDragging = false }). Accessibility: `.accessibilityLabel("Today's Glimpses. \(glimpses.count) cards.")` on the TabView.

    Private struct `GlimpseCard: View`. Shows: AvatarView(displayName: glimpse.username, avatarColorHex: glimpse.authorColorHex, photoData: nil, size: 40). Username Text (14pt semibold, VGTheme.textPrimary). Goal title Text (14pt regular, VGTheme.textSecondary). Progress Text ("\(glimpse.progressPercent)%", 14pt VGTheme.textMuted). If glimpse.photoFileURL != nil: AsyncImage(url: glimpse.photoFileURL) .frame(width: 44, height: 44).clipShape(RoundedRectangle(cornerRadius: 8)). ApplauseButtonView(recipientUsername: glimpse.username, giverUsername: giverUsername, canApplaud: canApplaud(glimpse.username), onApplaud: { onApplaud(glimpse.username) }). Card background: VGTheme.surface, padding 12pt vertical / 16pt horizontal, clipShape RoundedRectangle(cornerRadius:16). Touch target ≥44pt on the whole card Button. Accessibility: `.accessibilityLabel("\(glimpse.username)'s goal: \(glimpse.goalTitle), \(glimpse.progressPercent) percent complete")`. onTapGesture (not conflicting with applause button): `onTapCard(glimpse)`.

    Create ActiveTodaySection.swift:

    `struct ActiveTodaySection: View`. Properties: `let activeUsers: [UserPresenceItem]`, `let onTapUser: (UserPresenceItem) -> Void`. Body: if activeUsers.isEmpty — Text("Nobody active in the last 2 hours. Be the first — open the app!") (14pt VGTheme.textMuted, padding horizontal 16). Else: ScrollView(.horizontal, showsIndicators: false) { HStack(spacing: 12) { ForEach(activeUsers) { user in Button { onTapUser(user) } label: { VStack(spacing: 6) { AvatarView(displayName: user.username, avatarColorHex: user.authorColorHex, photoData: nil, size: 44); Text(user.username).font(.system(size: 14)).fontDesign(.rounded).foregroundStyle(VGTheme.textMuted).lineLimit(1) } }.buttonStyle(.plain).frame(minWidth: 44, minHeight: 44).accessibilityLabel("\(user.username), active today. Tap to view profile.") } }.padding(.horizontal, 16) }.
  </action>
  <verify>
    <automated>xcodebuild build -scheme VitaminG -destination 'platform=iOS Simulator,name=iPhone 16' 2>&1 | grep -E "error:" | head -20</automated>
  </verify>
  <done>Build succeeds. grep "Timer.publish(every: 5" GlimpsesCarouselSection.swift returns ≥ 1 match. grep "isDragging" GlimpsesCarouselSection.swift returns ≥ 2 matches. grep "accessibilityLabel.*active today" ActiveTodaySection.swift returns ≥ 1 match. grep "showsIndicators: false" ActiveTodaySection.swift returns ≥ 1 match.</done>
</task>

<task type="auto">
  <name>Task 2: Build GlowingSpotlightSection + GlobalFeedSection</name>
  <files>
    VitaminG/VitaminG/VitaminG/Views/Community/GlowingSpotlightSection.swift
    VitaminG/VitaminG/VitaminG/Views/Community/GlobalFeedSection.swift
    VitaminG/VitaminG/VitaminG/ViewModels/CommunityHubViewModel.swift
  </files>
  <read_first>
    - VitaminG/VitaminG/VitaminG/Views/CommunityFeedView.swift (full file — feed list pattern, optimistic reaction handleReact, compose CTA pattern, error state pattern)
    - VitaminG/VitaminG/VitaminG/Views/CommunityGoalsLandingView.swift (hero card surface pattern — elevated card with internal padding)
    - VitaminG/VitaminG/VitaminG/Views/Components/CommunityPostCard.swift (onComment closure pattern, CKRecord reaction fields)
    - VitaminG/VitaminG/VitaminG/.planning/phases/21-community-tab-redesign/21-UI-SPEC.md (§Component Inventory GlowingSpotlightSection, GlobalFeedSection, §Copywriting)
    - VitaminG/VitaminG/VitaminG/VGTheme.swift (VGTheme.accentGold, VGTheme.heroBackground or VGTheme.surface, serif typography)
  </read_first>
  <action>
    Create GlowingSpotlightSection.swift:

    `struct GlowingSpotlightSection: View`. Properties: `let glowingUser: GoalGlimpseItem?`, `let giverUsername: String`, `let canApplaud: (String) -> Bool`, `let onApplaud: (String) -> Void`, `let onTapUser: (GoalGlimpseItem) -> Void`. Body: if glowingUser == nil — Text("Check back later — someone will be glowing this week.") (14pt VGTheme.textMuted, padding horizontal 16). Else — a hero card VStack(spacing: 12) inside a ZStack to allow the gold ring border. Card contents: Text("GLOWING THIS WEEK") (14pt VGTheme.textMuted .uppercased(), kerning 0.5). AvatarView(displayName: user.username, avatarColorHex: user.authorColorHex, photoData: nil, size: 64). Text(user.username) using VGTheme.serif(20). Text(user.goalTitle) (17pt body, VGTheme.textSecondary). ApplauseButtonView(recipientUsername: user.username, giverUsername: giverUsername, canApplaud: canApplaud(user.username), onApplaud: { onApplaud(user.username) }). Card styling: VStack background VGTheme.surface, padding 24pt all sides, clipShape RoundedRectangle(cornerRadius:20), overlay(RoundedRectangle(cornerRadius:20).stroke(VGTheme.accentGold, lineWidth: 2)), padding .horizontal(16). Whole card: Button { onTapUser(user) } to navigate to profile; use .buttonStyle(.plain) so embedded ApplauseButtonView retains its own button styling.

    Create GlobalFeedSection.swift:

    `struct GlobalFeedSection: View`. Properties: `let feedPosts: [CKRecord]`, `let isLoading: Bool`, `let loadError: String?`, `@Bindable var viewModel: CommunityHubViewModel`, `let authorDisplayName: String?`, `let authorColorHex: String?`. State: `@State private var localReactionByPostID: [CKRecord.ID: ReactionType] = [:]`, `@State private var showReplySheet: Bool = false`, `@State private var activeReplyPostID: String = ""`, `@State private var showCompose = false`.

    Body: VStack(alignment: .leading, spacing: 12) { if isLoading && feedPosts.isEmpty — ProgressView().frame(maxWidth:.infinity).padding(.top, 32). Else if let errorMsg = loadError, feedPosts.isEmpty — Text(errorMsg) (14pt VGTheme.textMuted, textAlignment .center, padding). Else if feedPosts.isEmpty — VStack { Text("Community is quiet right now") (17pt body semibold VGTheme.textPrimary); Text("Be the first to share today.") (14pt VGTheme.textMuted) }. Else — LazyVStack(spacing: 12) { ForEach(feedPosts, id: \.recordID) { post in CommunityPostCard(post: post, currentUserReaction: localReactionByPostID[post.recordID], accentColor: VGTheme.accentTerra, onReact: { type in handleReact(post: post, type: type) }, onReport: { Task { await handleReport(post: post) } }, onComment: { activeReplyPostID = post.recordID.recordName; showReplySheet = true }) } }.padding(.horizontal, 16). Compose CTA button (per PATTERNS.md GlobalFeedSection pattern): `Button { showCompose = true } label: { Label("Share a moment", systemImage: "square.and.pencil")... }` styled per CommunityFeedView compose button pattern.

    Private func `handleReact(post:type:)`: copy the optimistic mutual-exclusion reaction toggle from CommunityFeedView (PATTERNS.md lines 303–325). Calls `viewModel.toggleReaction(recordID:reactionType:add:)` via Task — add `func toggleReaction(recordID:reactionType:add:) async` to CommunityHubViewModel (delegates to CommunityService.toggleReaction).

    Private func `handleReport(post:) async`: calls `viewModel.reportPost(recordID:reporterID:)` — add to CommunityHubViewModel (delegates to CommunityService.reportPost).

    `.sheet(isPresented: $showReplySheet)` — when showReplySheet is true, present `CommunityReplySheetView(parentPostID: activeReplyPostID, authorDisplayName: authorDisplayName, authorColorHex: authorColorHex, onWriteReply: { text in await viewModel.writeReply(parentPostID: activeReplyPostID, text: text, authorDisplayName: authorDisplayName, authorColorHex: authorColorHex) })`. Set `activeReplyPostID = post.recordID.recordName` before setting `showReplySheet = true` in the onComment closure. Add `func writeReply(parentPostID:text:authorDisplayName:authorColorHex:) async -> Bool` to CommunityHubViewModel (delegates to CommunityService.writeReply). `.sheet(isPresented: $showCompose)` presents PostComposeSheet wired to the global feed (category = "community" or nil global key consistent with fetchGlobalPosts predicate).
  </action>
  <verify>
    <automated>xcodebuild test -scheme VitaminG -destination 'platform=iOS Simulator,name=iPhone 16' -testPlan VitaminGTests 2>&1 | tail -5</automated>
  </verify>
  <done>Build succeeds and full test suite passes. grep "GLOWING THIS WEEK" GlowingSpotlightSection.swift returns ≥ 1 match. grep "accentGold" GlowingSpotlightSection.swift ≥ 1 match (gold ring). grep "LazyVStack" GlobalFeedSection.swift ≥ 1 match. grep "CommunityReplySheetView" GlobalFeedSection.swift ≥ 1 match. grep "showReplySheet" GlobalFeedSection.swift ≥ 2 matches. grep "Community is quiet right now" GlobalFeedSection.swift ≥ 1 match (exact empty state copy). All Phase21* test classes GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CloudKit public DB records → UIKit rendering | CKRecord field strings from GoalGlimpse, UserPresence displayed as Text() in SwiftUI |
| User profile photo URL (Application Support) → AsyncImage | photoFileURL from GoalGlimpseItem is a local file URL, not a remote URL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-05-01 | Tampering | GoalGlimpseItem.goalTitle in GlimpseCard Text | accept | Text sanitized at write time via InputSanitizer.sanitizeForPublic() in CommunityService.writeGlimpse (Plan 02). SwiftUI Text() has no code execution surface. |
| T-21-05-02 | Information Disclosure | GlowingSpotlightSection shows username + goal publicly | accept | These are public CloudKit records by design — user has already published their goal publicly |
| T-21-05-03 | Spoofing | Carousel timer fires while user is interacting | mitigate | isDragging guard in onReceive(timer) prevents auto-advance during manual swipe — carousel respects user intent |
| T-21-05-04 | Tampering | photoFileURL from Application Support | accept | Files written by app only (CommunityService.fetchGlimpses copies from CKAsset to Application Support) — no user-controlled path traversal |
| T-21-05-SC | Tampering | npm/pip/cargo installs | accept | No package manager installs — all Apple-native |
</threat_model>

<verification>
- Build clean with no new errors
- Full test suite: xcodebuild test passes with all Phase21* test classes GREEN
- grep "Timer.publish(every: 5" GlimpsesCarouselSection.swift ≥ 1 match
- grep "isDragging" GlimpsesCarouselSection.swift ≥ 2 matches (onChanged + onEnded)
- grep "GLOWING THIS WEEK" GlowingSpotlightSection.swift ≥ 1 match
- grep "accentGold" GlowingSpotlightSection.swift ≥ 1 match
- grep "CommunityReplySheetView" GlobalFeedSection.swift ≥ 1 match
- grep "LazyVStack" GlobalFeedSection.swift ≥ 1 match
- grep "showReplySheet" GlobalFeedSection.swift ≥ 2 matches (sheet binding + toggle)
</verification>

<success_criteria>
All four section components build cleanly. Full test suite green. Carousel timer and drag-pause pattern correct. Glowing hero card has gold ring and correct allcaps label. GlobalFeedSection wires CommunityReplySheetView via isPresented+activeReplyPostID pattern (String conformance safe). All empty states use exact UI-SPEC copy.
</success_criteria>

<output>
Create `.planning/phases/21-community-tab-redesign/21-05-SUMMARY.md` when done.
</output>
