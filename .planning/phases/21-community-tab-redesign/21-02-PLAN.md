---
phase: 21-community-tab-redesign
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - VitaminG/VitaminG/VitaminG/Services/CommunityService.swift
  - VitaminG/VitaminG/VitaminG/ViewModels/GoalViewModel.swift
autonomous: true
requirements:
  - COMM-01
  - COMM-02
  - COMM-04
  - COMM-05
  - COMM-06
  - COMM-07
  - SOC-01
  - SOC-02
  - SOC-03

must_haves:
  truths:
    - "CommunityService exposes all 9 new static async methods for GoalGlimpse, UserPresence, Applause, CommunityReply, and global feed"
    - "GoalGlimpse upsert strategy is enforced: fetch-or-create keyed on username + dayKey"
    - "GoalViewModel.addCheckIn fires a fire-and-forget GoalGlimpse upsert at check-in time"
    - "All user-generated string fields pass through InputSanitizer.sanitizeForPublic() before any CKRecord write"
    - "ReactionType enum has .fire case with fieldKey == 'fireCount'"
    - "CKAsset photoAsset reads copy fileURL to Application Support immediately"
  artifacts:
    - path: "VitaminG/VitaminG/VitaminG/Services/CommunityService.swift"
      provides: "writeGlimpse, fetchGlimpses, writeUserPresence, fetchActiveUsers, writeApplause, fetchReceivedApplause, fetchGlowingUser, fetchGlobalPosts, writeReply, fetchReplies"
      exports: ["writeGlimpse", "fetchGlimpses", "writeUserPresence", "fetchActiveUsers", "writeApplause", "fetchReceivedApplause", "fetchGlowingUser", "fetchGlobalPosts", "writeReply", "fetchReplies"]
    - path: "VitaminG/VitaminG/VitaminG/ViewModels/GoalViewModel.swift"
      provides: "addCheckIn extended signature with username and colorHex parameters; GoalGlimpse write injected"
      contains: "CommunityService.writeGlimpse"
  key_links:
    - from: "GoalViewModel.addCheckIn"
      to: "CommunityService.writeGlimpse"
      via: "fire-and-forget Task { await ... } at end of addCheckIn"
      pattern: "CommunityService.writeGlimpse"
    - from: "CommunityService.writeGlimpse"
      to: "CloudKit public DB"
      via: "CKContainer.default().publicCloudDatabase.save(record)"
      pattern: "publicCloudDatabase.save"
    - from: "CommunityService.fetchGlimpses"
      to: "CKAsset.fileURL"
      via: "FileManager.default.copyItem(at: src, to: dest) before use"
      pattern: "applicationSupportDirectory"
---

<objective>
Extend CommunityService with all 9 new CloudKit methods needed by Phase 21, add the ReactionType.fire case, and inject the GoalGlimpse write into GoalViewModel.addCheckIn.

Purpose: These service methods are the data-access layer that CommunityHubViewModel (Plan 03) will call. Having them written first with clear signatures allows Plan 03 to wire the ViewModel without touching service internals. GoalViewModel injection ensures Today's Glimpses populate automatically from check-ins without any separate user action.

Output: CommunityService.swift with 9 new static funcs + extended ReactionType enum; GoalViewModel.swift with addCheckIn extended signature.
</objective>

<execution_context>
@/Users/kyleharrington/Desktop/AI/Vitamin G/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kyleharrington/Desktop/AI/Vitamin G/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/kyleharrington/Desktop/AI/Vitamin G/.planning/ROADMAP.md
@/Users/kyleharrington/Desktop/AI/Vitamin G/.planning/phases/21-community-tab-redesign/21-CONTEXT.md
@/Users/kyleharrington/Desktop/AI/Vitamin G/.planning/phases/21-community-tab-redesign/21-RESEARCH.md
@/Users/kyleharrington/Desktop/AI/Vitamin G/.planning/phases/21-community-tab-redesign/21-PATTERNS.md

<interfaces>
<!-- From CommunityService.swift — existing patterns to replicate -->
// static let postRecordType = "CommunityPost"
// static func createPost(text:imageData:category:authorDisplayName:authorColorHex:) async throws -> CKRecord
// static func fetchPosts(category:limit:) async throws -> [CKRecord]
// static func toggleReaction(recordID:reactionType:add:) async throws -> CKRecord
// static func reportPost(recordID:reporterID:) async throws -> Int
// static func compressToJPEG(_:maxBytes:) -> Data?
// enum ReactionType: String { case thumbsUp; case heart }
// CloudKit write: let db = CKContainer.default().publicCloudDatabase; return try await db.save(record)
// Conflict retry: } catch let error as CKError where error.code == .serverRecordChanged { ... }
// Fetch pattern: let query = CKQuery(recordType:predicate:); query.sortDescriptors = [...]; let (results,_) = try await db.records(matching:resultsLimit:)

<!-- GoalViewModel.addCheckIn current signature -->
// func addCheckIn(for goal: Goal, context: ModelContext)
// Callers: GoalDetailView, CheckInCelebrationView — both have @Query var profiles: [UserProfile]

<!-- CommunityHubModels value types (from Plan 01) -->
// struct GoalGlimpseItem: Identifiable, Sendable { id, username, goalTitle, progressPercent, authorColorHex, photoFileURL, dayKey }
// struct UserPresenceItem: Identifiable, Sendable { id, username, authorColorHex, lastActiveDate }
// struct ApplauseItem: Identifiable, Sendable { id, giverUsername, recipientUsername, creationDate }
// struct CommunityReplyItem: Identifiable, Sendable { id, parentPostID, text, authorDisplayName, authorColorHex, creationDate }

<!-- CKRecord field schemas from RESEARCH.md -->
// GoalGlimpse: username(String), goalTitle(String), progressPercent(Int64), authorColorHex(String), photoAsset(CKAsset?), dayKey(String)
// UserPresence: username(String), authorColorHex(String), lastActiveDate(DateTime)
// Applause: giverUsername(String), recipientUsername(String)
// CommunityReply: parentPostID(String), text(String), authorDisplayName(String), authorColorHex(String)

<!-- CKAsset copy pattern (STATE.md constraint) -->
// let dest = FileManager.default.urls(for: .applicationSupportDirectory, in: .userDomainMask)[0]
//     .appendingPathComponent(UUID().uuidString + ".jpg")
// try? FileManager.default.copyItem(at: asset.fileURL!, to: dest)
// Use dest URL, not asset.fileURL
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add ReactionType.fire + 9 new CommunityService methods</name>
  <files>VitaminG/VitaminG/VitaminG/Services/CommunityService.swift</files>
  <read_first>
    - VitaminG/VitaminG/VitaminG/Services/CommunityService.swift (full file — existing patterns, postRecordType constant, ReactionType enum, createPost, fetchPosts, toggleReaction, compressToJPEG, conflict retry pattern)
    - VitaminG/VitaminG/VitaminG/Services/InputSanitizer.swift (sanitizeForPublic signature)
  </read_first>
  <action>
    In CommunityService.swift:

    1. Add `case fire` to the existing `ReactionType` enum. Add `case .fire: return "fireCount"` to the fieldKey switch. No other enum changes.

    2. Add new record type constants: `static let glimpseRecordType = "GoalGlimpse"`, `static let presenceRecordType = "UserPresence"`, `static let applauseRecordType = "Applause"`, `static let replyRecordType = "CommunityReply"`.

    3. Add `static func writeGlimpse(username: String, goalTitle: String, progressPercent: Int, authorColorHex: String, photoData: Data? = nil) async`. Upsert logic: compute todayKey = ISO8601DateFormatter with format "yyyy-MM-dd" from Date(). Fetch existing with NSPredicate `"username == %@ AND dayKey == %@", username, todayKey` resultsLimit:1. If found, reuse that CKRecord (fetch record by ID from result). If not found, create new CKRecord(recordType: glimpseRecordType). Set fields: username, goalTitle via InputSanitizer.sanitizeForPublic(), progressPercent as Int64, authorColorHex, dayKey. For photoData: use compressToJPEG(_:maxBytes:500_000), write to tmp URL, set as CKAsset, defer removal. Save. Catch CKError.serverRecordChanged and retry once with fresh record fetch. Catch all other errors silently (fire-and-forget).

    4. Add `static func fetchGlimpses(dayKey: String) async throws -> [GoalGlimpseItem]`. Predicate: `"dayKey == %@", dayKey`. Sort by creationDate descending. Map CKRecord → GoalGlimpseItem. For photoAsset: if record has a CKAsset, copy fileURL to Application Support using `FileManager.default.copyItem(at:to:)` and set photoFileURL to the copied dest URL. If copy fails, set photoFileURL = nil.

    5. Add `static func writeUserPresence(username: String, authorColorHex: String) async`. Upsert keyed on username (fetch NSPredicate `"username == %@"`). Create or update lastActiveDate = Date(). Sanitize username. Save. Catch all errors silently.

    6. Add `static func fetchActiveUsers() async throws -> [UserPresenceItem]`. Predicate: `"lastActiveDate > %@", Date().addingTimeInterval(-7200)`. Sort by lastActiveDate descending. Map CKRecord → UserPresenceItem. resultsLimit: 20.

    7. Add `static func writeApplause(giverUsername: String, recipientUsername: String) async throws`. Create new CKRecord(recordType: applauseRecordType). Set giverUsername and recipientUsername via InputSanitizer.sanitizeForPublic(). Save. Catch CKError.serverRecordChanged and retry once.

    8. Add `static func fetchReceivedApplause(recipientUsername: String) async throws -> [ApplauseItem]`. Predicate: `"recipientUsername == %@", recipientUsername`. Sort by creationDate descending. resultsLimit: 20. Map to ApplauseItem.

    9. Add `static func fetchGlowingUser(dayKey: String) async throws -> [GoalGlimpseItem]`. Predicate: `"dayKey >= %@", sevenDaysAgoKey` where sevenDaysAgoKey is a "yyyy-MM-dd" string 7 days ago. Sort by creationDate descending. resultsLimit: 50. Return GoalGlimpseItems (same mapping as fetchGlimpses).

    10. Add `static func fetchGlobalPosts(limit: Int = 50) async throws -> [CKRecord]`. Predicate: `"reportCount < 3"`. Sort by creationDate descending. Map using `results.compactMap { try? $0.1.get() }`. This is the nil-category global feed.

    11. Add `static func writeReply(parentPostID: String, text: String, authorDisplayName: String?, authorColorHex: String?) async throws -> Bool`. Profanity check first: if ProfanityFilter.containsProfanity(text) { return false }. Create CKRecord(recordType: replyRecordType). Set parentPostID, text via InputSanitizer.sanitizeForPublic(), authorDisplayName via sanitize with fallback "Anonymous", authorColorHex with fallback "". Save. Catch CKError.serverRecordChanged retry once. Return true on success, false on final failure.

    12. Add `static func fetchReplies(parentPostID: String) async throws -> [CommunityReplyItem]`. Predicate: `"parentPostID == %@", parentPostID`. Sort by creationDate ascending. resultsLimit: 50. Map to CommunityReplyItem.

    Every string field set on any CKRecord must pass through InputSanitizer.sanitizeForPublic() before assignment. Every CKRecord field read uses optional binding or nil coalescing — no force casts.
  </action>
  <verify>
    <automated>xcodebuild build -scheme VitaminG -destination 'platform=iOS Simulator,name=iPhone 16' 2>&1 | grep -E "error:" | head -20</automated>
  </verify>
  <done>Build succeeds. grep "case fire" confirms ReactionType.fire added. grep "writeGlimpse\|fetchGlimpses\|writeUserPresence\|fetchActiveUsers\|writeApplause\|fetchReceivedApplause\|fetchGlowingUser\|fetchGlobalPosts\|writeReply\|fetchReplies" in CommunityService.swift returns 10 matches.</done>
</task>

<task type="auto">
  <name>Task 2: Inject GoalGlimpse write into GoalViewModel.addCheckIn (per D-01)</name>
  <files>VitaminG/VitaminG/VitaminG/ViewModels/GoalViewModel.swift</files>
  <read_first>
    - VitaminG/VitaminG/VitaminG/ViewModels/GoalViewModel.swift (full file — addCheckIn signature, caller pattern, existing imports)
    - VitaminG/VitaminG/VitaminG/Views/GoalDetailView.swift (to see how addCheckIn is called and what profile data is available)
    - VitaminG/VitaminG/VitaminG/Views/CheckInCelebrationView.swift (second call site for addCheckIn)
  </read_first>
  <action>
    In GoalViewModel.swift, extend the addCheckIn function signature to: `func addCheckIn(for goal: Goal, context: ModelContext, username: String = "", colorHex: String = "")`. The default empty strings ensure all existing call sites continue to compile without modification (backward-compatible additive extension). At the very end of addCheckIn, after all existing logic (after WidgetCenter reload, after streak recalculation), add a fire-and-forget Task block: `Task { await CommunityService.writeGlimpse(username: username, goalTitle: goal.title ?? "", progressPercent: computedProgressInt, authorColorHex: colorHex) }` where computedProgressInt is derived from the goal's completionEvents count vs durationDays (same calculation used in the existing goal card). If durationDays is zero or nil, default progressPercent to 0. Do not block the main actor — the Task runs detached from the UI. Add `import CloudKit` only if not already present. Do not move or modify any existing addCheckIn logic above the injection point.

    Update callers: in GoalDetailView.swift and CheckInCelebrationView.swift, update the addCheckIn call to pass `username: profiles.first?.displayName ?? ""` and `colorHex: profiles.first?.avatarColorHex ?? ""` where profiles is the existing `@Query private var profiles: [UserProfile]`. If profiles is not already @Query'd in those views, add it.
  </action>
  <verify>
    <automated>xcodebuild build -scheme VitaminG -destination 'platform=iOS Simulator,name=iPhone 16' 2>&1 | grep -E "error:" | head -20</automated>
  </verify>
  <done>Build succeeds. grep "CommunityService.writeGlimpse" in GoalViewModel.swift returns at least 1 match. Existing GoalViewModelTests still pass (call sites compile with new default parameters).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SwiftUI View → CommunityService | User-typed text (reply text, post text, username) crosses into CloudKit public DB write |
| GoalViewModel → CommunityService | goal.title (SwiftData user-controlled field) crosses into CloudKit public DB |
| CloudKit public DB → iOS app | GoalGlimpse photoAsset fileURL crosses temp filesystem boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-02-01 | Tampering | CommunityService string writes | mitigate | InputSanitizer.sanitizeForPublic() called on every String field before CKRecordValue assignment — enforced in every new method |
| T-21-02-02 | Information Disclosure | CommunityReply write | mitigate | ProfanityFilter.containsProfanity() gating before any CloudKit write; writeReply returns false without writing if flagged |
| T-21-02-03 | Elevation of Privilege | Applause farming (multiple per day) | accept | UserDefaults gate enforced in CommunityHubViewModel (Plan 03); CloudKit-level rate limiting not available — acceptable for v2.0 trust model |
| T-21-02-04 | Tampering | CKAsset fileURL path traversal | accept | compressToJPEG processes only Data + UIImage — no user-controlled file paths; CKAsset copy uses UUID-named destination under Application Support |
| T-21-02-05 | Spoofing | giverUsername / recipientUsername in Applause records | accept | CloudKit public DB has no server-side auth on record creation; username sourced from UserProfile (SwiftData, device-local) — consistent with all other Phase 21 username sourcing |
| T-21-02-SC | Tampering | npm/pip/cargo installs | accept | No package manager installs — Apple-native CloudKit only |
</threat_model>

<verification>
- xcodebuild build succeeds with zero new errors
- grep -c "func writeGlimpse\|func fetchGlimpses\|func writeUserPresence\|func fetchActiveUsers\|func writeApplause\|func fetchReceivedApplause\|func fetchGlowingUser\|func fetchGlobalPosts\|func writeReply\|func fetchReplies" CommunityService.swift returns 10
- grep "case fire" CommunityService.swift confirms ReactionType.fire added
- grep "CommunityService.writeGlimpse" GoalViewModel.swift returns ≥ 1 match
- grep "applicationSupportDirectory" CommunityService.swift confirms CKAsset copy pattern present
- grep "sanitizeForPublic" CommunityService.swift returns ≥ 9 matches (one per text field per new method)
</verification>

<success_criteria>
Build clean. All 9 new service methods present with correct signatures. ReactionType.fire.fieldKey == "fireCount". GoalViewModel.addCheckIn fires GoalGlimpse write. All string writes sanitized. CKAsset reads copy to Application Support.
</success_criteria>

<output>
Create `.planning/phases/21-community-tab-redesign/21-02-SUMMARY.md` when done.
</output>
