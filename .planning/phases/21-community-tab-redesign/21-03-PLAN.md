---
phase: 21-community-tab-redesign
plan: 03
type: execute
wave: 2
depends_on:
  - 21-01
  - 21-02
files_modified:
  - VitaminG/VitaminG/VitaminG/ViewModels/CommunityHubViewModel.swift
  - VitaminG/VitaminG/VitaminG/Views/Community/ApplauseButtonView.swift
  - VitaminG/VitaminG/VitaminG/Views/Community/ApplauseStreamOverlay.swift
  - VitaminG/VitaminG/VitaminGTests/Phase21CommunityHubViewModelTests.swift
  - VitaminG/VitaminG/VitaminGTests/Phase21ApplauseDailyGateTests.swift
  - VitaminG/VitaminG/VitaminGTests/Phase21GlowingSelectionTests.swift
autonomous: true
requirements:
  - COMM-02
  - COMM-04
  - COMM-05
  - SOC-01
  - SOC-02
  - SOC-03

must_haves:
  truths:
    - "CommunityHubViewModel orchestrates all 5 CloudKit section fetches via async let in loadAll()"
    - "Each CloudKit call has a test override closure (fetchGlimpsesOverride etc.) following the CommunityFeedViewModel pattern"
    - "Applause daily gate: canApplaud returns false after markApplauseGiven for same recipient same day; true for different recipient (per D-04)"
    - "selectGlowingUser uses Calendar(identifier: .iso8601).component(.weekOfYear) — NOT Calendar.current"
    - "ApplauseButtonView shows floating 👏 + giverUsername animating easeOut(duration:1.0) offset -80pt; suppressed under accessibilityReduceMotion"
    - "Phase21CommunityHubViewModelTests, Phase21ApplauseDailyGateTests, Phase21GlowingSelectionTests all GREEN"
  artifacts:
    - path: "VitaminG/VitaminG/VitaminG/ViewModels/CommunityHubViewModel.swift"
      provides: "CommunityHubViewModel @MainActor @Observable with loadAll(), applause gate, selectGlowingUser"
      exports: ["CommunityHubViewModel"]
      contains: "async let"
    - path: "VitaminG/VitaminG/VitaminG/Views/Community/ApplauseButtonView.swift"
      provides: "ApplauseButtonView reusable component with daily gate and float animation"
      exports: ["ApplauseButtonView"]
    - path: "VitaminG/VitaminG/VitaminG/Views/Community/ApplauseStreamOverlay.swift"
      provides: "ApplauseStreamOverlay GeometryReader overlay for ProfileView SOC-02"
      exports: ["ApplauseStreamOverlay"]
  key_links:
    - from: "CommunityHubViewModel.loadAll"
      to: "CommunityService.fetchGlimpses"
      via: "async let g = fetchGlimpsesOverride ?? CommunityService.fetchGlimpses"
      pattern: "fetchGlimpsesOverride"
    - from: "ApplauseButtonView"
      to: "CommunityHubViewModel.markApplauseGiven"
      via: "onApplaud closure"
      pattern: "onApplaud"
    - from: "CommunityHubViewModel.canApplaud"
      to: "UserDefaults vg_community_applauseGiven"
      via: "JSONDecoder [String:Date] dict lookup"
      pattern: "vg_community_applauseGiven"
---

<objective>
Build CommunityHubViewModel (the central coordinator for all 5 Community tab sections) and the two applause components (ApplauseButtonView, ApplauseStreamOverlay), then advance the Wave 0 RED tests to GREEN.

Purpose: CommunityHubViewModel is the single @Observable data source for the rebuilt CommunityTabView. Without it, the hub views in Plans 05 and 06 have nothing to bind to. ApplauseButtonView must exist as a standalone reusable component so both GlowingSpotlightSection and GlimpsesCarouselSection can embed it without duplication.

Output: CommunityHubViewModel.swift + ApplauseButtonView.swift + ApplauseStreamOverlay.swift; three test files turned GREEN.
</objective>

<execution_context>
@/Users/kyleharrington/Desktop/AI/Vitamin G/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kyleharrington/Desktop/AI/Vitamin G/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/kyleharrington/Desktop/AI/Vitamin G/.planning/ROADMAP.md
@/Users/kyleharrington/Desktop/AI/Vitamin G/.planning/phases/21-community-tab-redesign/21-CONTEXT.md
@/Users/kyleharrington/Desktop/AI/Vitamin G/.planning/phases/21-community-tab-redesign/21-RESEARCH.md
@/Users/kyleharrington/Desktop/AI/Vitamin G/.planning/phases/21-community-tab-redesign/21-PATTERNS.md
@/Users/kyleharrington/Desktop/AI/Vitamin G/.planning/phases/21-community-tab-redesign/21-UI-SPEC.md

<interfaces>
<!-- CommunityFeedViewModel pattern to replicate exactly -->
<!-- From ViewModels/CommunityFeedViewModel.swift -->
@MainActor
@Observable
final class CommunityFeedViewModel {
    var posts: [CKRecord] = []
    var isLoading: Bool = false
    var submitError: String? = nil
    var fetchOverride: ((String, Int) async throws -> [CKRecord])? = nil
    var createOverride: ((String, Data?, String, String?, String?) async throws -> CKRecord)? = nil

    func loadPosts(category: String, limit: Int = 50) async {
        isLoading = true
        defer { isLoading = false }
        do {
            posts = try await (fetchOverride?(category, limit) ?? CommunityService.fetchPosts(category:limit:))
        } catch {
            if posts.isEmpty { posts = [] }
        }
    }
}

<!-- ExploreViewModel UserDefaults gate pattern (for applause gate D-04) -->
<!-- From ViewModels/ExploreViewModel.swift -->
private let applauseKey = "vg_community_applauseGiven"  // new key; matches RESEARCH.md Pattern 3

// Check gate ([String:Date] dictionary):
func canApplaud(recipientUsername: String) -> Bool {
    guard let data = UserDefaults.standard.data(forKey: applauseKey),
          let dict = try? JSONDecoder().decode([String: Date].self, from: data),
          let last = dict[recipientUsername]
    else { return true }
    return !Calendar.current.isDateInToday(last)
}

// Write gate:
func markApplauseGiven(to recipientUsername: String) {
    var dict: [String: Date] = [:]
    if let data = UserDefaults.standard.data(forKey: applauseKey),
       let existing = try? JSONDecoder().decode([String: Date].self, from: data) {
        dict = existing
    }
    dict[recipientUsername] = Date()
    if let encoded = try? JSONEncoder().encode(dict) {
        UserDefaults.standard.set(encoded, forKey: applauseKey)
    }
}

<!-- CheckInCelebrationView animation pattern -->
<!-- @Environment(\.accessibilityReduceMotion) private var reduceMotion -->
<!-- withAnimation(.easeOut(duration: 1.0)) { floatOffset = -80; floatOpacity = 0 } -->
<!-- DispatchQueue.main.asyncAfter(deadline: .now() + 1.1) { showFloat = false } -->

<!-- CommunityHubModels (Plan 01 output) -->
// struct GoalGlimpseItem: Identifiable, Sendable
// struct UserPresenceItem: Identifiable, Sendable
// struct ApplauseItem: Identifiable, Sendable
// struct CommunityReplyItem: Identifiable, Sendable

<!-- CommunityService new methods (Plan 02 output) -->
// static func fetchGlimpses(dayKey:) async throws -> [GoalGlimpseItem]
// static func fetchActiveUsers() async throws -> [UserPresenceItem]
// static func fetchGlowingUser(dayKey:) async throws -> [GoalGlimpseItem]
// static func fetchGlobalPosts(limit:) async throws -> [CKRecord]
// static func writeApplause(giverUsername:recipientUsername:) async throws
// static func fetchReceivedApplause(recipientUsername:) async throws -> [ApplauseItem]

<!-- AppRouter pattern for public profile navigation -->
<!-- From Navigation/AppRouter.swift -->
// @Observable final class AppRouter { var pendingPublicProfileRecordID: String? = nil }
// ContentView handles .sheet(item:) binding on this property

<!-- VGTheme tokens referenced in UI-SPEC -->
// VGTheme.accentGold — applause float color
// VGTheme.textMuted — secondary labels
// VGTheme.surface — card backgrounds
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build CommunityHubViewModel — 5-section coordinator</name>
  <files>VitaminG/VitaminG/VitaminG/ViewModels/CommunityHubViewModel.swift</files>
  <read_first>
    - VitaminG/VitaminG/VitaminG/ViewModels/CommunityFeedViewModel.swift (full file — @Observable pattern, test overrides, error handling)
    - VitaminG/VitaminG/VitaminG/ViewModels/ExploreViewModel.swift (UserDefaults gate pattern for applause)
    - VitaminG/VitaminG/VitaminGTests/Phase21CommunityHubViewModelTests.swift (RED stubs to satisfy)
    - VitaminG/VitaminG/VitaminGTests/Phase21ApplauseDailyGateTests.swift (RED stubs to satisfy)
    - VitaminG/VitaminG/VitaminGTests/Phase21GlowingSelectionTests.swift (RED stubs to satisfy)
  </read_first>
  <behavior>
    - loadAll() calls all 5 fetch overrides (or real service calls) via async let — when all 5 override flags are injected, all 5 are called
    - canApplaud("alice") returns true when no prior entry exists in UserDefaults
    - canApplaud("alice") returns false after markApplauseGiven(to: "alice") same day
    - canApplaud("bob") returns true after markApplauseGiven(to: "alice") — per-recipient gate
    - selectGlowingUser([item1, item2, item3]) returns item at index Calendar(identifier: .iso8601).component(.weekOfYear, from: Date()) % 3
    - selectGlowingUser([]) returns nil
    - selectGlowingUser called twice with same input returns identical result (deterministic)
    - glowingUser is set to selectGlowingUser result after fetchGlowingUser completes
    - writeApplauseForUser(recipientUsername:giverUsername:) calls CommunityService.writeApplause then markApplauseGiven — only when canApplaud returns true
  </behavior>
  <action>
    Create VitaminG/VitaminG/VitaminG/ViewModels/CommunityHubViewModel.swift.

    Declaration: `@MainActor @Observable final class CommunityHubViewModel`. Imports: Foundation, Observation, CloudKit.

    Published state: `var glimpses: [GoalGlimpseItem] = []`, `var activeUsers: [UserPresenceItem] = []`, `var glowingUser: GoalGlimpseItem? = nil`, `var feedPosts: [CKRecord] = []`, `var receivedApplause: [ApplauseItem] = []`, `var isLoading = false`, `var loadError: String? = nil`.

    Test overrides (one per CloudKit call, nil in production):
    `var fetchGlimpsesOverride: ((String) async throws -> [GoalGlimpseItem])? = nil`,
    `var fetchActiveUsersOverride: (() async throws -> [UserPresenceItem])? = nil`,
    `var fetchGlowingUserOverride: ((String) async throws -> [GoalGlimpseItem])? = nil`,
    `var fetchFeedOverride: ((Int) async throws -> [CKRecord])? = nil`,
    `var fetchApplauseOverride: ((String) async throws -> [ApplauseItem])? = nil`.

    `func loadAll(username: String) async`: set isLoading = true, defer reset. Compute todayKey using ISO8601DateFormatter "yyyy-MM-dd". Use async let for all 5 fetches in parallel. On each catch, preserve existing state (empty array if first load, otherwise keep prior).

    `func selectGlowingUser(from eligible: [GoalGlimpseItem]) -> GoalGlimpseItem?`: guard !eligible.isEmpty. Use `Calendar(identifier: .iso8601).component(.weekOfYear, from: Date())`. Return `eligible[week % eligible.count]`.

    Applause gate: private constant `let applauseKey = "vg_community_applauseGiven"`. Implement `func canApplaud(recipientUsername: String) -> Bool` and `func markApplauseGiven(to: String)` using JSONEncoder/Decoder [String:Date] dict as shown in RESEARCH.md Pattern 3. Accept a userDefaults parameter (default: UserDefaults.standard) for test isolation.

    `func writeApplauseForUser(recipientUsername: String, giverUsername: String) async`: guard canApplaud(recipientUsername), then try await CommunityService.writeApplause(giverUsername:recipientUsername:), then markApplauseGiven(to: recipientUsername). Catch errors silently — applause failure should not surface an error to the user.

    Error constants as `static let` strings (never expose raw localizedDescription to UI).

    Then update Phase21CommunityHubViewModelTests.swift, Phase21ApplauseDailyGateTests.swift, Phase21GlowingSelectionTests.swift to replace XCTFail stubs with real assertions against the implemented methods. Use UserDefaults(suiteName: "test.\(UUID().uuidString)") in test setUp to isolate applause gate state.
  </action>
  <verify>
    <automated>xcodebuild test -scheme VitaminG -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:VitaminGTests/Phase21CommunityHubViewModelTests -only-testing:VitaminGTests/Phase21ApplauseDailyGateTests -only-testing:VitaminGTests/Phase21GlowingSelectionTests 2>&1 | tail -20</automated>
  </verify>
  <done>All three test files show ** TEST SUCCEEDED **. grep "async let" CommunityHubViewModel.swift confirms parallel fetch. grep "iso8601" CommunityHubViewModel.swift confirms calendar usage. grep "vg_community_applauseGiven" CommunityHubViewModel.swift returns ≥ 1 match.</done>
</task>

<task type="auto">
  <name>Task 2: Build ApplauseButtonView + ApplauseStreamOverlay</name>
  <files>
    VitaminG/VitaminG/VitaminG/Views/Community/ApplauseButtonView.swift
    VitaminG/VitaminG/VitaminG/Views/Community/ApplauseStreamOverlay.swift
  </files>
  <read_first>
    - VitaminG/VitaminG/VitaminG/Views/CheckInCelebrationView.swift (withAnimation + State animation pattern, reduceMotion usage)
    - VitaminG/VitaminG/VitaminG/Views/Explore/ExploreConfettiOverlay.swift (GeometryReader overlay pattern, staggered animation)
    - VitaminG/VitaminG/VitaminG/Views/Components/ReactionPill.swift (accessibilityLabel pattern)
    - VitaminG/VitaminG/VitaminG/VGTheme.swift (VGTheme.accentGold, VGTheme.textMuted tokens)
    - VitaminG/VitaminG/VitaminG/.planning/phases/21-community-tab-redesign/21-UI-SPEC.md (§Interaction States — Applause Button, §Accessibility, §Animation Contracts)
  </read_first>
  <action>
    Create VitaminG/VitaminG/VitaminG/Views/Community/ApplauseButtonView.swift.

    `struct ApplauseButtonView: View`. Properties: `let recipientUsername: String`, `let giverUsername: String`, `let canApplaud: Bool`, `let onApplaud: () -> Void`. State: `@State private var showFloat = false`, `@State private var floatOffset: CGFloat = 0`, `@State private var floatOpacity: Double = 0`. Environment: `@Environment(\.accessibilityReduceMotion) private var reduceMotion`.

    Body: ZStack(alignment: .bottom). Button shows "👏" at opacity 1.0 when canApplaud, 0.4 when not. `.disabled(!canApplaud)`. `.accessibilityLabel(canApplaud ? "Applaud \(recipientUsername)" : "Already applauded today")`. `.accessibilityAddTraits(.isButton)`. Below button when canApplaud is false: Text("Come back tomorrow to applaud again.") in 14pt VGTheme.textMuted (per UI-SPEC copywriting). Float overlay: when `showFloat`, VStack(spacing: 2) with Text("👏") + Text(giverUsername).font(.system(size: 14)).fontDesign(.rounded), colored VGTheme.accentGold, offset by floatOffset, opacity floatOpacity. Touch target minimum 44pt: `.frame(minWidth: 44, minHeight: 44)` on the button.

    Private func `triggerApplause()`: guard canApplaud. Call onApplaud(). If !reduceMotion: set showFloat=true, floatOffset=0, floatOpacity=1.0, withAnimation(.easeOut(duration: 1.0)) { floatOffset = -80; floatOpacity = 0 }. DispatchQueue.main.asyncAfter(deadline: .now() + 1.1) { showFloat = false }. Else (reduceMotion): just call onApplaud() with no animation.

    Create VitaminG/VitaminG/VitaminG/Views/Community/ApplauseStreamOverlay.swift.

    `struct ApplauseStreamOverlay: View`. Properties: `let receivedApplause: [ApplauseItem]`. Environment: `@Environment(\.accessibilityReduceMotion) private var reduceMotion`.

    Body: GeometryReader { geo in ZStack { ForEach(Array(receivedApplause.prefix(10).enumerated()), id: \.offset) { i, item in if !reduceMotion { FloatingApplauseItemView(item: item, delay: Double(i) * 0.3, containerSize: geo.size) } } } }.allowsHitTesting(false). Private `struct FloatingApplauseItemView: View` with `@State private var offset: CGFloat = 0` and `@State private var opacity: Double = 0`. Uses `.task` to trigger animation after delay: `try? await Task.sleep(nanoseconds: UInt64(delay * 1_000_000_000))` then withAnimation(.easeOut(duration: 1.5)) { offset = -200; opacity = 0 } and initial opacity = 1.0. Random horizontal start position: `CGFloat.random(in: 20...(containerSize.width - 20))`.
  </action>
  <verify>
    <automated>xcodebuild build -scheme VitaminG -destination 'platform=iOS Simulator,name=iPhone 16' 2>&1 | grep -E "error:" | head -20</automated>
  </verify>
  <done>Build succeeds. Both files exist. grep "accessibilityReduceMotion" in ApplauseButtonView.swift confirms reduce motion guard. grep "easeOut(duration: 1.0)" in ApplauseButtonView.swift confirms animation spec. grep "allowsHitTesting(false)" in ApplauseStreamOverlay.swift confirms overlay safety.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User tap → applause write | User taps applause button; giverUsername from local UserProfile crosses into CloudKit public DB |
| CloudKit → ProfileView overlay | ApplauseItem records fetched from public DB are rendered as floating animation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-03-01 | Elevation of Privilege | Applause daily gate bypass | accept | UserDefaults can be cleared by the OS or user; no cryptographic enforcement. Acceptable for v2.0 social trust model — consistent with gifter gate in Phase 20 |
| T-21-03-02 | Spoofing | giverUsername injected into Applause record | accept | Username sourced from UserProfile SwiftData (device-local); same trust model as all other username sourcing in v2.0 |
| T-21-03-03 | Information Disclosure | ApplauseStreamOverlay rendering all received applause | accept | Only username strings displayed; no PII beyond self-disclosed public display name |
| T-21-03-04 | Denial of Service | loadAll() parallel CloudKit fetch timeout | mitigate | async let parallelism means slowest call bounds total time; each section independently falls back to empty array on error — UI is never blocked |
| T-21-03-SC | Tampering | npm/pip/cargo installs | accept | No package manager installs — all Apple-native |
</threat_model>

<verification>
- Three test files (Phase21CommunityHubViewModelTests, Phase21ApplauseDailyGateTests, Phase21GlowingSelectionTests) all pass: xcodebuild test returns TEST SUCCEEDED
- grep "async let" CommunityHubViewModel.swift confirms parallel fetch in loadAll()
- grep "iso8601" CommunityHubViewModel.swift confirms ISO8601 calendar usage for Glowing selection
- grep "vg_community_applauseGiven" CommunityHubViewModel.swift ≥ 1 match
- grep "accessibilityReduceMotion" ApplauseButtonView.swift ≥ 1 match
- grep "allowsHitTesting(false)" ApplauseStreamOverlay.swift ≥ 1 match
- Full suite: xcodebuild test full suite returns TEST SUCCEEDED with no regressions
</verification>

<success_criteria>
All 3 test files GREEN. CommunityHubViewModel exists with 5 async let parallel fetches, applause gate, and deterministic Glowing selection. ApplauseButtonView and ApplauseStreamOverlay compile with animation and accessibility patterns correct.
</success_criteria>

<output>
Create `.planning/phases/21-community-tab-redesign/21-03-SUMMARY.md` when done.
</output>
