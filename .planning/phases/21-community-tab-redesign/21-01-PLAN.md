---
phase: 21-community-tab-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - VitaminG/VitaminG/VitaminG/Models/CommunityHubModels.swift
  - VitaminG/VitaminG/VitaminGTests/Phase21CommunityHubViewModelTests.swift
  - VitaminG/VitaminG/VitaminGTests/Phase21ApplauseDailyGateTests.swift
  - VitaminG/VitaminG/VitaminGTests/Phase21GlowingSelectionTests.swift
  - VitaminG/VitaminG/VitaminGTests/Phase21ReplyTests.swift
autonomous: true
requirements:
  - COMM-02
  - COMM-04
  - COMM-05
  - COMM-06
  - SOC-01

must_haves:
  truths:
    - "GoalGlimpseItem, UserPresenceItem, ApplauseItem value types exist and are importable by all Phase 21 files"
    - "Four XCTest files exist with failing RED stubs covering COMM-02 carousel timer, COMM-04 active-today filter, COMM-05 deterministic selection, COMM-06 fire reaction + loadAll, COMM-06 reply profanity gate, SOC-01 daily gate"
    - "Test stubs compile and fail (RED state confirmed)"
  artifacts:
    - path: "VitaminG/VitaminG/VitaminG/Models/CommunityHubModels.swift"
      provides: "GoalGlimpseItem, UserPresenceItem, ApplauseItem, CommunityReplyItem value types"
      exports: ["GoalGlimpseItem", "UserPresenceItem", "ApplauseItem", "CommunityReplyItem"]
    - path: "VitaminG/VitaminG/VitaminGTests/Phase21CommunityHubViewModelTests.swift"
      provides: "RED stubs for COMM-02 timer, COMM-04 filter, COMM-06 loadAll + fire reaction"
      contains: "class Phase21CommunityHubViewModelTests"
    - path: "VitaminG/VitaminG/VitaminGTests/Phase21ApplauseDailyGateTests.swift"
      provides: "RED stubs for SOC-01 daily gate (same-day block, per-recipient)"
      contains: "class Phase21ApplauseDailyGateTests"
    - path: "VitaminG/VitaminG/VitaminGTests/Phase21GlowingSelectionTests.swift"
      provides: "RED stub for COMM-05 deterministic weekOfYear selection"
      contains: "class Phase21GlowingSelectionTests"
    - path: "VitaminG/VitaminG/VitaminGTests/Phase21ReplyTests.swift"
      provides: "RED stubs for COMM-06 reply write + profanity gate"
      contains: "class Phase21ReplyTests"
  key_links:
    - from: "Phase21CommunityHubViewModelTests"
      to: "CommunityHubModels.swift"
      via: "GoalGlimpseItem + UserPresenceItem used in test fixtures"
      pattern: "GoalGlimpseItem"
    - from: "Phase21ApplauseDailyGateTests"
      to: "ApplauseItem"
      via: "canApplaud / markApplauseGiven logic under test"
      pattern: "ApplauseItem"
---

<objective>
Wave 0 scaffolding: create the shared value-type model file and four RED XCTest stub files that establish the Nyquist test contract before any production code is written.

Purpose: Every substantive logic item (carousel timer behaviour, active-today filter, Glowing This Week deterministic selection, fire reaction field key, loadAll parallel fetch, reply profanity gate, applause daily gate) has a failing test before the implementation plan runs. This ensures executors receive immediate feedback when logic diverges from spec.

Output: 1 model file (CommunityHubModels.swift) + 4 test stub files.
</objective>

<execution_context>
@/Users/kyleharrington/Desktop/AI/Vitamin G/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kyleharrington/Desktop/AI/Vitamin G/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/kyleharrington/Desktop/AI/Vitamin G/.planning/ROADMAP.md
@/Users/kyleharrington/Desktop/AI/Vitamin G/.planning/phases/21-community-tab-redesign/21-CONTEXT.md
@/Users/kyleharrington/Desktop/AI/Vitamin G/.planning/phases/21-community-tab-redesign/21-VALIDATION.md

<interfaces>
<!-- Existing test file structure to replicate -->
<!-- From VitaminGTests/ExploreViewModelTests.swift — test class structure pattern -->
import XCTest
@testable import VitaminG

final class ExploreViewModelTests: XCTestCase {
    func test_someFeature() async throws { ... }
}

<!-- From VitaminGTests/CommunityFeedViewModelTests.swift — override pattern -->
final class CommunityFeedViewModelTests: XCTestCase {
    var viewModel: CommunityFeedViewModel!
    override func setUp() async throws {
        viewModel = await CommunityFeedViewModel()
    }
}

<!-- Applause gate key from RESEARCH.md Pattern 3 -->
// UserDefaults key: "vg_community_applauseGiven"
// Value: Data (JSONEncoder([String: Date]))

<!-- GoalGlimpse CKRecord fields from RESEARCH.md -->
// "username" String, "goalTitle" String, "progressPercent" Int64, "authorColorHex" String,
// "photoAsset" CKAsset?, "dayKey" String

<!-- UserPresence fields -->
// "username" String, "authorColorHex" String, "lastActiveDate" DateTime (Date)

<!-- Applause fields -->
// "giverUsername" String, "recipientUsername" String

<!-- CommunityReply fields -->
// "parentPostID" String, "text" String, "authorDisplayName" String, "authorColorHex" String
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create CommunityHubModels.swift — shared value types</name>
  <files>VitaminG/VitaminG/VitaminG/Models/CommunityHubModels.swift</files>
  <read_first>
    - VitaminG/VitaminG/VitaminG/Models/ExploreModels.swift (value type pattern — StuckDayGift, TrendingGoal)
    - VitaminG/VitaminG/VitaminG/Services/CommunityService.swift (CKRecord field names to match)
  </read_first>
  <behavior>
    - GoalGlimpseItem: struct with id (String = UUID string), username, goalTitle, progressPercent (Int), authorColorHex (String), photoFileURL (URL?), dayKey (String)
    - UserPresenceItem: struct with id (String), username, authorColorHex (String), lastActiveDate (Date)
    - ApplauseItem: struct with id (String), giverUsername (String), recipientUsername (String), creationDate (Date)
    - CommunityReplyItem: struct with id (String), parentPostID (String), text (String), authorDisplayName (String), authorColorHex (String), creationDate (Date)
    - All structs conform to Identifiable, Sendable
    - No CloudKit imports in this file — pure value types; CommunityService maps CKRecord → these types
  </behavior>
  <action>
    Create VitaminG/VitaminG/VitaminG/Models/CommunityHubModels.swift with four structs: GoalGlimpseItem, UserPresenceItem, ApplauseItem, CommunityReplyItem. Each struct is public (internal) and conforms to Identifiable (id: String) and Sendable. Import Foundation only. Field names must exactly match the CKRecord field keys documented in RESEARCH.md so CommunityService (Plan 02) can map them without ambiguity. No @Model attributes — these are CloudKit value types, not SwiftData models.
  </action>
  <verify>
    <automated>xcodebuild build -scheme VitaminG -destination 'platform=iOS Simulator,name=iPhone 16' 2>&1 | grep -E "(error:|BUILD FAILED)" | head -20</automated>
  </verify>
  <done>File compiles cleanly; four exported structs are visible to VitaminGTests target; no SwiftData imports.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create four RED test stub files for Nyquist coverage</name>
  <files>
    VitaminG/VitaminG/VitaminGTests/Phase21CommunityHubViewModelTests.swift
    VitaminG/VitaminG/VitaminGTests/Phase21ApplauseDailyGateTests.swift
    VitaminG/VitaminG/VitaminGTests/Phase21GlowingSelectionTests.swift
    VitaminG/VitaminG/VitaminGTests/Phase21ReplyTests.swift
  </files>
  <read_first>
    - VitaminG/VitaminG/VitaminGTests/CommunityFeedViewModelTests.swift (test class structure, setUp pattern, override pattern)
    - VitaminG/VitaminG/VitaminGTests/ExploreViewModelTests.swift (async test pattern, @testable import)
    - VitaminG/VitaminG/VitaminG/.planning/phases/21-community-tab-redesign/21-VALIDATION.md (exact behaviors each test must cover)
  </read_first>
  <behavior>
    Phase21CommunityHubViewModelTests:
    - test_loadAll_callsAllFiveFetchOverrides: creates CommunityHubViewModel with all overrides injected, calls loadAll(), asserts all 5 override call flags are true — FAILS (CommunityHubViewModel does not exist yet)
    - test_activeToday_excludesUsersOlderThan2Hours: injects fetchActiveUsersOverride that returns a mix of fresh and stale UserPresenceItems, verifies stale ones are filtered — FAILS
    - test_fireReactionFieldKey_isFireCount: asserts ReactionType.fire.fieldKey == "fireCount" — FAILS (fire case not yet added)

    Phase21ApplauseDailyGateTests:
    - test_canApplaud_returnsTrueWhenNoPriorApplause: fresh UserDefaults, canApplaud(recipientUsername: "alice") == true — FAILS (function does not exist)
    - test_canApplaud_returnsFalseAfterApplaudingSameRecipientToday: markApplauseGiven(to: "alice"), then canApplaud("alice") == false — FAILS
    - test_canApplaud_perRecipient_trueForDifferentRecipient: markApplauseGiven(to: "alice"), canApplaud("bob") == true — FAILS

    Phase21GlowingSelectionTests:
    - test_selectGlowingUser_deterministic_sameWeekSameResult: call selectGlowingUser twice with same eligible array, assert results are identical — FAILS (function does not exist)
    - test_selectGlowingUser_returnsNilForEmptyArray: assert selectGlowingUser([]) == nil — FAILS
    - test_selectGlowingUser_usesISO8601Calendar: verify result index == Calendar(identifier: .iso8601).component(.weekOfYear, from: Date()) % eligible.count — FAILS

    Phase21ReplyTests:
    - test_writeReply_rejectsProfaneText: writeReply with profane text returns false (profanity gate) — FAILS (function does not exist)
    - test_writeReply_sanitizesTextBeforeWrite: capture sanitized text via override, assert it equals InputSanitizer.sanitizeForPublic(original) — FAILS
  </behavior>
  <action>
    Create the four test files using XCTest. Each imports XCTest and @testable import VitaminG. Test methods call functions that do not yet exist (CommunityHubViewModel, ApplauseService/gate functions, selectGlowingUser, CommunityService.writeReply) — these calls must cause compile-time or runtime failures (RED). Use XCTFail("Not yet implemented") as the body for any test whose target function signature is not yet defined, paired with a comment showing the expected assertion so Plan 03 implementors know exactly what to make pass. Do NOT use @available guards — all targets are iOS 17+. Key: "vg_community_applauseGiven" for applause gate UserDefaults. Isolate UserDefaults in setUp by using a test-specific suite: UserDefaults(suiteName: "test.applause.\(UUID().uuidString)").
  </action>
  <verify>
    <automated>xcodebuild build -scheme VitaminG -destination 'platform=iOS Simulator,name=iPhone 16' 2>&1 | grep -c "error:" || true</automated>
  </verify>
  <done>All four test files exist and compile (even if tests fail at runtime due to missing production types); test names match exactly the behaviors in 21-VALIDATION.md; no force-unwraps in test setup.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test code → production models | Test files import @testable VitaminG; no user data crosses this boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-01-01 | Tampering | CommunityHubModels value types | accept | Pure value types with no mutable global state; Sendable conformance prevents data races |
| T-21-01-SC | Tampering | npm/pip/cargo installs | accept | No package manager installs in this plan — Apple-native only |
</threat_model>

<verification>
Build succeeds (zero Swift errors) and all four test files exist with correct class names:
- grep -r "class Phase21CommunityHubViewModelTests" confirms file exists
- grep -r "class Phase21ApplauseDailyGateTests" confirms file exists
- grep -r "class Phase21GlowingSelectionTests" confirms file exists
- grep -r "class Phase21ReplyTests" confirms file exists
- grep -r "struct GoalGlimpseItem" VitaminG/VitaminG/VitaminG/Models/CommunityHubModels.swift confirms model exists
</verification>

<success_criteria>
CommunityHubModels.swift compiles with 4 exported structs; 4 RED test files exist in VitaminGTests; full build passes (no new errors introduced).
</success_criteria>

<output>
Create `.planning/phases/21-community-tab-redesign/21-01-SUMMARY.md` when done.
</output>
