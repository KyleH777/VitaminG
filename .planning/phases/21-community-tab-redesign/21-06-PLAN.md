---
phase: 21-community-tab-redesign
plan: 06
type: execute
wave: 3
depends_on:
  - 21-03
  - 21-04
  - 21-05
files_modified:
  - VitaminG/VitaminG/VitaminG/Views/CommunityTabView.swift
  - VitaminG/VitaminG/VitaminG/Views/ContentView.swift
  - VitaminG/VitaminG/VitaminG/Views/Components/CommunityPostCard.swift
  - VitaminG/VitaminG/VitaminG/Views/Explore/ExploreView.swift
  - VitaminG/VitaminG/VitaminG/Views/ProfileView.swift
autonomous: false
requirements:
  - COMM-01
  - COMM-02
  - COMM-03
  - COMM-04
  - COMM-05
  - COMM-06
  - COMM-07
  - SOC-01
  - SOC-02
  - SOC-03

user_setup:
  - service: CloudKit Console
    why: "New record types (GoalGlimpse, UserPresence, Applause, CommunityReply) and updated CommunityPost (fireCount field) must be deployed to Production before real-device testing"
    dashboard_config:
      - task: "Create GoalGlimpse record type with Queryable index on 'username' and 'dayKey' fields"
        location: "CloudKit Console → iCloud.com.kyleharrington.VitaminG → Schema → Record Types"
      - task: "Create UserPresence record type with Queryable index on 'username' and 'lastActiveDate' fields"
        location: "CloudKit Console → iCloud.com.kyleharrington.VitaminG → Schema → Record Types"
      - task: "Create Applause record type with Queryable index on 'giverUsername' and 'recipientUsername' fields"
        location: "CloudKit Console → iCloud.com.kyleharrington.VitaminG → Schema → Record Types"
      - task: "Create CommunityReply record type with Queryable index on 'parentPostID' field"
        location: "CloudKit Console → iCloud.com.kyleharrington.VitaminG → Schema → Record Types"
      - task: "Add 'fireCount' Int64 field to CommunityPost record type (existing); deploy"
        location: "CloudKit Console → CommunityPost → Add Field"

must_haves:
  truths:
    - "CommunityTabView is rebuilt as a 5-section ScrollView hub (CommunitySegment enum and Feed/Ideas picker removed per D-06)"
    - "Community tab slot in ContentView replaced: CommunityPlaceholderView → CommunityTabView"
    - "CommunityPostCard has 🔥 ReactionPill (fireCount accessor defaulting to 0 for old records)"
    - "ChallengeDiscoveryView and IdeaBoardView appear in ExploreView (per D-06); no longer in CommunityTabView"
    - "ProfileView shows ApplauseStreamOverlay on appear for SOC-02 (ambient applause for self-view)"
    - "UserPresence write fires at app open (at CommunityTabView onAppear)"
    - "Section order in CommunityTabView matches UI-SPEC: COMM-01 card → Glimpses → Active Today → Glowing Spotlight → Feed"
    - "COMM-01 card: taps to CommunityGoalsLandingView; if no UserChallenge shows 'Join a community goal' button to ExploreView"
  artifacts:
    - path: "VitaminG/VitaminG/VitaminG/Views/CommunityTabView.swift"
      provides: "Rebuilt 5-section hub with CommunityHubViewModel, no CommunitySegment enum"
      contains: "CommunityHubViewModel"
    - path: "VitaminG/VitaminG/VitaminG/Views/ContentView.swift"
      provides: "CommunityPlaceholderView replaced with CommunityTabView"
      contains: "CommunityTabView"
    - path: "VitaminG/VitaminG/VitaminG/Views/Components/CommunityPostCard.swift"
      provides: "fireCount accessor + 🔥 ReactionPill"
      contains: "fireCount"
    - path: "VitaminG/VitaminG/VitaminG/Views/Explore/ExploreView.swift"
      provides: "ChallengeDiscoveryView section added (per D-06 migration)"
      contains: "ChallengeDiscoveryView"
    - path: "VitaminG/VitaminG/VitaminG/Views/ProfileView.swift"
      provides: "ApplauseStreamOverlay embedded for SOC-02"
      contains: "ApplauseStreamOverlay"
  key_links:
    - from: "ContentView"
      to: "CommunityTabView"
      via: "NavigationStack { CommunityTabView(selectedTab:$selectedTab) }.tag(AppTab.community)"
      pattern: "CommunityTabView"
    - from: "CommunityTabView"
      to: "CommunityGoalsLandingView"
      via: "NavigationLink or .sheet for COMM-01 card tap"
      pattern: "CommunityGoalsLandingView"
    - from: "CommunityTabView"
      to: "CommunityHubViewModel.loadAll"
      via: ".task { await viewModel.loadAll(username:) }"
      pattern: "loadAll"
    - from: "ExploreView"
      to: "ChallengeDiscoveryView"
      via: "Section added below existing explore sections"
      pattern: "ChallengeDiscoveryView"
    - from: "ProfileView"
      to: "ApplauseStreamOverlay"
      via: ".overlay { ApplauseStreamOverlay(receivedApplause: receivedApplause) } with .task to load"
      pattern: "ApplauseStreamOverlay"
---

<objective>
Assemble the rebuilt CommunityTabView using the components from Plans 03–05, swap ContentView's community slot from CommunityPlaceholderView, extend CommunityPostCard with 🔥 reaction, migrate ChallengeDiscoveryView to ExploreView, and wire the SOC-02 ambient applause stream to ProfileView.

Purpose: This is the integration plan — connecting all components into the live tab experience. Plan 06 contains no new logic; it is purely assembly and wiring. The final human-verify checkpoint confirms the tab looks correct in the running Simulator.

Output: Working Community tab in the app. Every COMM and SOC requirement visible.
</objective>

<execution_context>
@/Users/kyleharrington/Desktop/AI/Vitamin G/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kyleharrington/Desktop/AI/Vitamin G/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/kyleharrington/Desktop/AI/Vitamin G/.planning/ROADMAP.md
@/Users/kyleharrington/Desktop/AI/Vitamin G/.planning/phases/21-community-tab-redesign/21-CONTEXT.md
@/Users/kyleharrington/Desktop/AI/Vitamin G/.planning/phases/21-community-tab-redesign/21-RESEARCH.md
@/Users/kyleharrington/Desktop/AI/Vitamin G/.planning/phases/21-community-tab-redesign/21-PATTERNS.md
@/Users/kyleharrington/Desktop/AI/Vitamin G/.planning/phases/21-community-tab-redesign/21-UI-SPEC.md

<interfaces>
<!-- ContentView.swift community tab block (PATTERNS.md lines 644–654) -->
// BEFORE:
// NavigationStack { CommunityPlaceholderView() }.tag(AppTab.community)
// AFTER:
// NavigationStack { CommunityTabView(selectedTab: $selectedTab) }.tag(AppTab.community)

<!-- CommunityTabView current file (to be fully replaced) -->
// Current: struct CommunityTabView with CommunitySegment enum (Feed/Ideas picker)
// Phase 21: fully replace — remove CommunitySegment enum, remove Feed/Ideas Picker, remove ChallengeDiscoveryView usage from this file

<!-- Section order from UI-SPEC §Section Layout Order -->
// 1. Tab heading "Community" — VGTheme.serif(28), 20pt top, 24pt horizontal
// 2. CommunityGoalCard (COMM-01)
// 3. Section label "Today's Glimpses" + GlimpsesCarouselSection
// 4. Section label "Active Today" + ActiveTodaySection
// 5. GlowingSpotlightSection (no section label — self-labeled in card)
// 6. Section label "Community Feed" + compose button + GlobalFeedSection

<!-- CommunityGoalsLandingView interface (Phase 15 output) -->
// struct CommunityGoalsLandingView: View — expects a UserChallenge
// COMM-01 card: use first active @Query UserChallenge; if nil, show fallback "Join a community goal"

<!-- ExploreView.swift (Phase 20 output) -->
// Existing sections: GoalGifterCard, MoodPromptCard, VitaminShelfSection, TrendingNowSection, StuckDayGiftsSection
// D-06: add ChallengeDiscoveryView as a new section at the BOTTOM of ExploreView scroll

<!-- CommunityPostCard.swift current fire count pattern (PATTERNS.md lines 487–489) -->
// Add: private var fireCount: Int { (post["fireCount"] as? Int) ?? 0 }
// Add: ReactionPill(emoji:"🔥", count:fireCount, isActive:currentUserReaction == .fire, accentColor:accentColor, action:{ onReact(.fire) })

<!-- ProfileView.swift — SOC-02 overlay injection point -->
// ProfileView is the self-view (shows logged-in user's own profile)
// Add @State private var receivedApplause: [ApplauseItem] = []
// Add .overlay(alignment: .bottom) { ApplauseStreamOverlay(receivedApplause: receivedApplause) }
// Add .task { receivedApplause = (try? await CommunityService.fetchReceivedApplause(recipientUsername: currentUsername)) ?? [] }

<!-- AppRouter interface -->
// @Observable final class AppRouter { var pendingPublicProfileRecordID: String? = nil }
// ContentView handles .sheet(item: $router.pendingPublicProfileRecordID) { ... PublicProfileView ... }

<!-- VGTheme section label pattern (ExploreView) -->
// private func sectionLabel(_ text: String) -> some View {
//     Text(text.uppercased()).font(.system(size: 13, weight: .semibold)).kerning(0.4)
//         .foregroundStyle(VGTheme.textMuted).padding(.horizontal, 16)
// }
// UI-SPEC says 20pt semibold for section headings — use 20pt here, not 13pt sectionLabel

<!-- CommunityHubViewModel.writeUserPresence in loadAll -->
// writeUserPresence should also be called at tab appear: CommunityService.writeUserPresence(username:colorHex:)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend CommunityPostCard (🔥 reaction) + Rebuild CommunityTabView + Swap ContentView</name>
  <files>
    VitaminG/VitaminG/VitaminG/Views/Components/CommunityPostCard.swift
    VitaminG/VitaminG/VitaminG/Views/CommunityTabView.swift
    VitaminG/VitaminG/VitaminG/Views/ContentView.swift
  </files>
  <read_first>
    - VitaminG/VitaminG/VitaminG/Views/Components/CommunityPostCard.swift (full file — current field accessors at lines 14–23, reaction pills at lines 81–94, onComment scaffold at lines 95–103)
    - VitaminG/VitaminG/VitaminG/Views/CommunityTabView.swift (full file — to understand current CommunitySegment enum + structure to completely replace)
    - VitaminG/VitaminG/VitaminG/Views/ContentView.swift (lines 29–40 — community tab slot)
    - VitaminG/VitaminG/VitaminG/Views/CommunityGoalsLandingView.swift (UserChallenge parameter interface)
    - VitaminG/VitaminG/VitaminG/Views/Explore/ExploreView.swift (sectionLabel pattern for typography reference)
    - VitaminG/VitaminG/VitaminG/.planning/phases/21-community-tab-redesign/21-UI-SPEC.md (§Section Layout Order, §Typography, §Copywriting)
  </read_first>
  <action>
    In CommunityPostCard.swift: add `private var fireCount: Int { (post["fireCount"] as? Int) ?? 0 }` to the existing field accessors block. In the reaction pills HStack, add a third `ReactionPill(emoji: "🔥", count: fireCount, isActive: currentUserReaction == .fire, accentColor: accentColor, action: { onReact(.fire) })` after the existing heart and thumbsUp pills. No other changes to CommunityPostCard.

    Rebuild CommunityTabView.swift from scratch (full replacement — do not preserve CommunitySegment enum or Feed/Ideas Picker):
    - Struct: `struct CommunityTabView: View`. Properties: `@Binding var selectedTab: AppTab`, `@State private var viewModel = CommunityHubViewModel()`. Environment: `@Environment(\.modelContext) private var modelContext`, `@Environment(AppRouter.self) private var router`. Query: `@Query(filter: #Predicate { $0.isActive == true }) private var activeChallenges: [UserChallenge]`. Profile query: `@Query private var profiles: [UserProfile]`.
    - Computed: `private var currentUsername: String { profiles.first?.displayName ?? "" }`, `private var currentColorHex: String { profiles.first?.avatarColorHex ?? "" }`.
    - Body: ScrollView { VStack(alignment: .leading, spacing: 0) { communityHeading; goalCard; glimpsesSection; activeTodaySection; glowingSection; feedSection }.padding(.bottom, 24) }.background(VGTheme.background.ignoresSafeArea()).navigationTitle("").navigationBarHidden(true).task { await viewModel.loadAll(username: currentUsername) }.onAppear { Task { await CommunityService.writeUserPresence(username: currentUsername, authorColorHex: currentColorHex) } }.refreshable { await viewModel.loadAll(username: currentUsername) }.

    communityHeading: Text("Community").font(VGTheme.serif(28)).foregroundStyle(VGTheme.textPrimary).padding(.top, 20).padding(.horizontal, 24).padding(.bottom, 12).

    goalCard: COMM-01 card. If activeChallenges.first is non-nil — show a tappable card with: ProgressRingView showing collectiveProgress (if available from UserChallenge), participant count, days remaining text. Tap navigates to CommunityGoalsLandingView via NavigationLink(destination: CommunityGoalsLandingView(challenge: challenge)). If activeChallenges.isEmpty — show a fallback card with Text("Join a community goal") (17pt semibold VGTheme.textPrimary) + Button("Explore Goals") { selectedTab = .explore } styled as a secondary CTA. Card background VGTheme.surface, cornerRadius 12, padding 16 all, horizontal margin 16.

    glimpsesSection: HStack section label "TODAY'S GLIMPSES" (20pt semibold rounded, VGTheme.textPrimary, padding horizontal 16, top 24, bottom 8) + GlimpsesCarouselSection(glimpses: viewModel.glimpses, giverUsername: currentUsername, canApplaud: { viewModel.canApplaud(recipientUsername: $0) }, onApplaud: { recipient in Task { await viewModel.writeApplauseForUser(recipientUsername: recipient, giverUsername: currentUsername) } }, onTapCard: { glimpse in router.pendingPublicProfileRecordID = glimpse.username }).

    activeTodaySection: section label "ACTIVE TODAY" + ActiveTodaySection(activeUsers: viewModel.activeUsers, onTapUser: { user in router.pendingPublicProfileRecordID = user.username }).

    glowingSection: GlowingSpotlightSection(glowingUser: viewModel.glowingUser, giverUsername: currentUsername, canApplaud: { viewModel.canApplaud(recipientUsername: $0) }, onApplaud: { recipient in Task { await viewModel.writeApplauseForUser(recipientUsername: recipient, giverUsername: currentUsername) } }, onTapUser: { glimpse in router.pendingPublicProfileRecordID = glimpse.username }).

    feedSection: section label "COMMUNITY FEED" + GlobalFeedSection(feedPosts: viewModel.feedPosts, isLoading: viewModel.isLoading, loadError: viewModel.loadError, viewModel: viewModel, authorDisplayName: currentUsername, authorColorHex: currentColorHex).

    In ContentView.swift: find the block `NavigationStack { CommunityPlaceholderView() }.tag(AppTab.community)` and replace `CommunityPlaceholderView()` with `CommunityTabView(selectedTab: $selectedTab)`. No other ContentView changes.
  </action>
  <verify>
    <automated>xcodebuild build -scheme VitaminG -destination 'platform=iOS Simulator,name=iPhone 16' 2>&1 | grep -E "error:" | head -20</automated>
  </verify>
  <done>Build succeeds. grep "CommunitySegment" CommunityTabView.swift returns 0 matches (old enum removed). grep "CommunityHubViewModel" CommunityTabView.swift returns ≥ 1 match. grep "CommunityTabView" ContentView.swift returns ≥ 1 match (not CommunityPlaceholderView). grep "fireCount" CommunityPostCard.swift returns ≥ 1 match.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate ChallengeDiscoveryView to ExploreView + Wire SOC-02 to ProfileView</name>
  <files>
    VitaminG/VitaminG/VitaminG/Views/Explore/ExploreView.swift
    VitaminG/VitaminG/VitaminG/Views/ProfileView.swift
  </files>
  <read_first>
    - VitaminG/VitaminG/VitaminG/Views/Explore/ExploreView.swift (full file — current section layout, sectionLabel helper, ViewModel wiring)
    - VitaminG/VitaminG/VitaminG/Views/ChallengeDiscoveryView.swift (struct name and required init parameters — to wire correctly into ExploreView)
    - VitaminG/VitaminG/VitaminG/Views/Community/IdeaBoardView.swift (struct name and init parameters)
    - VitaminG/VitaminG/VitaminG/Views/ProfileView.swift (full file — to find correct overlay injection point, existing profile load logic, @Query patterns)
    - VitaminG/VitaminG/VitaminG/Models/CommunityHubModels.swift (ApplauseItem for receivedApplause array type)
  </read_first>
  <action>
    In ExploreView.swift: after the last existing section (StuckDayGiftsSection or whichever is final), add a new separator + section label "DISCOVER CHALLENGES" (same 20pt semibold rounded pattern as other section labels, 24pt top padding). Then add `ChallengeDiscoveryView()` (or with whatever init params it requires — read the file). If ChallengeDiscoveryView requires a ViewModel, instantiate it as @State private var challengeVM = ChallengeViewModel() in ExploreView. Do NOT delete ChallengeDiscoveryView or IdeaBoardView from their current Swift files — only relocate their usage. CommunityTabView.swift no longer references them (the full rebuild in Task 1 already excludes them).

    In ProfileView.swift: read the full file to understand current structure. Add `@State private var receivedApplause: [ApplauseItem] = []` state property. Find the outermost ScrollView or ZStack in the body and add `.overlay(alignment: .bottom) { ApplauseStreamOverlay(receivedApplause: receivedApplause) }` — place the overlay OUTSIDE the scrollable content so it renders above the profile content (use `.ignoresSafeArea()` on the overlay if needed so it covers the full height). Add `.task { if let username = profiles.first?.displayName { receivedApplause = (try? await CommunityService.fetchReceivedApplause(recipientUsername: username)) ?? [] } }` so applause loads on view appear. The overlay is self-fading and non-interactive (Plan 03 set `.allowsHitTesting(false)`) so it will not block profile interaction.
  </action>
  <verify>
    <automated>xcodebuild build -scheme VitaminG -destination 'platform=iOS Simulator,name=iPhone 16' 2>&1 | grep -E "error:" | head -20</automated>
  </verify>
  <done>Build succeeds. grep "ChallengeDiscoveryView" ExploreView.swift ≥ 1 match (wired in Explore). grep "ApplauseStreamOverlay" ProfileView.swift ≥ 1 match. grep "fetchReceivedApplause" ProfileView.swift ≥ 1 match. grep "ChallengeDiscoveryView" CommunityTabView.swift = 0 matches (removed from Community tab).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Full Community tab is now assembled. CommunityTabView has 5 sections: COMM-01 goal card, Today's Glimpses carousel, Active Today, Glowing Spotlight, and Community Feed. ContentView routes to the new CommunityTabView. CommunityPostCard has 🔥 reaction. ChallengeDiscoveryView is in Explore tab. ProfileView has ambient applause stream overlay. CloudKit record types must be deployed to Production (or Development schema) in CloudKit Console before any sections will populate — see user_setup in plan frontmatter.
  </what-built>
  <how-to-verify>
    1. Build and launch in iOS Simulator (iPhone 16, iOS 17+)
    2. Tap the Community tab. Verify: "Community" heading visible at top (Cormorant Garamond serif); COMM-01 goal card present (if enrolled) or "Join a community goal" fallback; section labels "TODAY'S GLIMPSES", "ACTIVE TODAY", "COMMUNITY FEED" visible
    3. Tap Explore tab. Verify: "DISCOVER CHALLENGES" section appears at bottom. Tap Community tab again — verify no Feed/Ideas segment picker is present.
    4. Tap the 👏 button on any Glimpse card or the Glowing Spotlight card (if populated). Verify: emoji floats upward with username label, fades out over ~1 second. Button becomes 40% opacity after first tap.
    5. Tap "Share a moment" compose CTA in Community Feed. Verify: PostComposeSheet appears. Tap camera icon — verify confirmationDialog shows "Photo Library" / "Camera" / "Cancel".
    6. Open own Profile tab (not another user's profile). Verify: no crash; if applause records exist for this user, floating 👏 appear.
    7. Run full test suite: xcodebuild test -scheme VitaminG -destination 'platform=iOS Simulator,name=iPhone 16' -testPlan VitaminGTests 2>&1 | tail -5
    8. All Phase21* test classes show passed. No regressions in other test files.
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues found</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CommunityTabView → router.pendingPublicProfileRecordID | Username string from CloudKit public DB record crosses into AppRouter — triggers PublicProfileView presentation |
| ExploreView → ChallengeDiscoveryView | Challenge data from existing SwiftData @Query crosses into the explore section (same boundary as before migration) |
| ProfileView → ApplauseStreamOverlay | ApplauseItem array from CloudKit public DB crosses into the self-view profile page |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-06-01 | Spoofing | router.pendingPublicProfileRecordID set from CloudKit username | accept | Username from GoalGlimpseItem/UserPresenceItem is display-only; PublicProfileView only shows public data — no privileged action taken from username string |
| T-21-06-02 | Information Disclosure | ApplauseStreamOverlay shows received applause on ProfileView | accept | ProfileView is the self-view — user sees their own received applause only; no third-party applause exposed |
| T-21-06-03 | Elevation of Privilege | ChallengeDiscoveryView in ExploreView uses same ViewModel | accept | ChallengeDiscoveryView is read-only discovery UI; no new write surfaces introduced by relocation |
| T-21-06-04 | Tampering | COMM-01 goal card taps to CommunityGoalsLandingView | accept | CommunityGoalsLandingView is an existing Phase 15 view; no new data write occurs on tap |
| T-21-06-05 | Elevation of Privilege | Community tab triggers writeUserPresence on appear | accept | writeUserPresence writes username + timestamp to CloudKit public DB; consistent with STATE.md Active Today decision (single write at app open); sanitized via InputSanitizer in Plan 02 |
| T-21-06-SC | Tampering | npm/pip/cargo installs | accept | No package manager installs — all Apple-native |
</threat_model>

<verification>
- Build clean with zero new errors
- grep "CommunitySegment" CommunityTabView.swift = 0 matches (old enum fully removed)
- grep "CommunityHubViewModel" CommunityTabView.swift ≥ 1 match
- grep "CommunityTabView" ContentView.swift ≥ 1 match; grep "CommunityPlaceholderView" ContentView.swift = 0 matches
- grep "fireCount" CommunityPostCard.swift ≥ 1 match
- grep "ChallengeDiscoveryView" ExploreView.swift ≥ 1 match
- grep "ApplauseStreamOverlay" ProfileView.swift ≥ 1 match
- Full suite xcodebuild test: TEST SUCCEEDED, no regressions
- Human checkpoint: Community tab renders in Simulator, applause animation plays, compose confirmationDialog shows, Explore tab has Challenge Discovery section
</verification>

<success_criteria>
Community tab fully assembled and running in Simulator. CommunityPlaceholderView gone from ContentView. All 5 sections visible. Applause animation working. ChallengeDiscoveryView in Explore. ProfileView ambient applause overlay present. All Phase21 tests GREEN. Full suite clean.
</success_criteria>

<output>
Create `.planning/phases/21-community-tab-redesign/21-06-SUMMARY.md` when done.
</output>
