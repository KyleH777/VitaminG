---
phase: 15
plan: "15-02"
type: execute
wave: 1
depends_on: []
files_modified:
  - VitaminG/VitaminG/VitaminG/Views/Support/AboutUsView.swift
  - VitaminG/VitaminG/VitaminG/Views/Support/FAQView.swift
autonomous: true
requirements:
  - UIADD-01

must_haves:
  truths:
    - "AboutUsView exists in Views/Support/ with a terra gradient header, serif title, and founder paragraph content"
    - "FAQView exists in Views/Support/ with a DisclosureGroup accordion containing 8-10 Q&As"
    - "Both views use VGTheme colors, .fontDesign(.rounded) for body text, and VGTheme.serif() for headings"
  artifacts:
    - path: "VitaminG/VitaminG/VitaminG/Views/Support/AboutUsView.swift"
      provides: "About Us landing page view"
      exports: ["AboutUsView"]
    - path: "VitaminG/VitaminG/VitaminG/Views/Support/FAQView.swift"
      provides: "FAQ accordion view"
      exports: ["FAQView"]
  key_links:
    - from: "HomeView Stay Close section"
      to: "AboutUsView"
      via: "NavigationLink(destination: AboutUsView())"
      pattern: "AboutUsView"
    - from: "HomeView Stay Close section"
      to: "FAQView"
      via: "NavigationLink(destination: FAQView())"
      pattern: "FAQView"
---

<objective>
Create the two static support views that the Stay Close section in HomeView will link to: AboutUsView (brand story, terra gradient header) and FAQView (DisclosureGroup accordion).

Purpose: These new files are a dependency for Plan 03 (HomeView additions) but have no dependencies themselves — they are pure new files and can be written in Wave 1 in parallel with Plan 01.
Output: Views/Support/AboutUsView.swift, Views/Support/FAQView.swift.
</objective>

<execution_context>
@/Users/kyleharrington/Desktop/AI/Vitamin G/.planning/phases/15-ui-additions-fixes/15-CONTEXT.md
</execution_context>

<context>
@/Users/kyleharrington/Desktop/AI/Vitamin G/.planning/ROADMAP.md

<interfaces>
<!-- VGTheme tokens used in this plan -->
From VGTheme.swift:
```swift
static let terra: Color           // primary accent — use for gradient
static let terraSoft: Color       // lighter terra
static let sand: Color            // warm off-white text on dark backgrounds
static let muted: Color           // subdued text
static let background: Color      // adaptive page background
static let surface: Color         // card backgrounds
static let textPrimary: Color     // primary body text
static let textMuted: Color       // secondary text
static func serif(_ size: CGFloat, weight: Font.Weight = .regular) -> Font
static func serifItalic(_ size: CGFloat) -> Font
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create AboutUsView</name>
  <files>VitaminG/VitaminG/VitaminG/Views/Support/AboutUsView.swift</files>
  <read_first>
    - VitaminG/VitaminG/VitaminG/VGTheme.swift — read to confirm terra gradient colors and serif font helper signature before using them
    - VitaminG/VitaminG/VitaminG/Views/ProfileView.swift — read the heroBanner LinearGradient pattern (lines 75-79) as a reference for terra gradient header style
  </read_first>
  <action>Create the file at the path `VitaminG/VitaminG/VitaminG/Views/Support/AboutUsView.swift`. The directory `Views/Support/` must be created if it does not exist. The view is a NavigationStack-compatible ScrollView. Structure: (1) A terra gradient header block: `LinearGradient(colors: [VGTheme.terra, VGTheme.terraSoft], startPoint: .topLeading, endPoint: .bottomTrailing)` with `.frame(height: 200)`, overlaid with a centered `VStack` containing a VGCapsule icon (size 32, color1: VGTheme.sand, color2: VGTheme.terraSoft) above `Text("About Vitamin G")` in `VGTheme.serif(28, weight: .semibold)` in `VGTheme.sand` color. (2) Below the header, a white/surface content area with `.padding(24)` containing: section label `Text("OUR STORY")` in 10pt semibold kerning 1.2 `VGTheme.muted`; founder paragraph body text (3-4 sentences): "Vitamin G was created for anyone who's ever had a great goal but lost sight of it. We're a small team building tools that make your goals feel less lonely. Every morning, this app puts your goals in front of you — because what you see shapes what you do." Then a second section label `Text("THE TEAM")` with a paragraph: "Built by a solo founder who got tired of forgetting what actually matters. If you have feedback, we'd love to hear it at hello@vitamingapp.com." (3) Apply `.navigationTitle("About Us")` and `.navigationBarTitleDisplayMode(.inline)`. Body text uses `.font(.system(size: 15)).fontDesign(.rounded).foregroundStyle(VGTheme.textPrimary).lineSpacing(5)`. Use `VGTheme.background` as the background. Do not import VGCapsule separately — it is already in the module.</action>
  <verify>
    <automated>test -f "/Users/kyleharrington/Desktop/AI/Vitamin G/VitaminG/VitaminG/VitaminG/Views/Support/AboutUsView.swift" && echo "exists"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `VitaminG/VitaminG/VitaminG/Views/Support/AboutUsView.swift`
    - File declares `struct AboutUsView: View`
    - File contains `LinearGradient` with `VGTheme.terra` and `VGTheme.terraSoft`
    - File contains `Text("About Vitamin G")` using `VGTheme.serif(28`
    - File contains `.navigationTitle("About Us")`
    - File contains `hello@vitamingapp.com`
    - No third-party imports; only `import SwiftUI`
  </acceptance_criteria>
  <done>AboutUsView.swift exists with terra gradient header, serif title, two story paragraphs, and navigation title.</done>
</task>

<task type="auto">
  <name>Task 2: Create FAQView</name>
  <files>VitaminG/VitaminG/VitaminG/Views/Support/FAQView.swift</files>
  <read_first>
    - VitaminG/VitaminG/VitaminG/VGTheme.swift — confirm textPrimary, textMuted, surface, and terra token names before using them
  </read_first>
  <action>Create the file at `VitaminG/VitaminG/VitaminG/Views/Support/FAQView.swift`. Structure: a `List` of `DisclosureGroup` rows. Each row has a bold question label as the disclosure label, and body text as the expanded content. Use exactly these 9 Q&A pairs: (1) "What is Vitamin G?" / "Vitamin G is your daily dose of intentionality. It helps you set goals across four tiers — from quick wins to life goals — and keeps them in front of you every morning." (2) "Is Vitamin G free?" / "Yes, Vitamin G is free to download and use. We may introduce optional premium features in the future." (3) "How do I add my first goal?" / "Tap the Goals tab and hit the + button. You can set a title, tier, and optional inspiration quote." (4) "What are the four tiers?" / "Immediate Wins are today's tasks. Short-Term are this week or month. Long-Term are 3–12 months. Life Goals are your north star." (5) "Does my data sync across devices?" / "Yes — your goals sync automatically via iCloud when you're signed in on multiple devices." (6) "How do notifications work?" / "Vitamin G sends you a daily morning notification with your active goal titles. You can change the time in Settings." (7) "How do challenges work?" / "Browse challenges in the Explore tab, join one, and check in daily. Your streak and progress are tracked automatically." (8) "Is my data private?" / "Your personal goals are stored privately in iCloud. Community posts are public within a challenge category." (9) "How do I contact support?" / "Email us at hello@vitamingapp.com — we read every message and usually reply within 24 hours." Each `DisclosureGroup` question label: `.font(.system(size: 15, weight: .semibold)).fontDesign(.rounded).foregroundStyle(VGTheme.textPrimary)`. Answer text: `.font(.system(size: 14)).fontDesign(.rounded).foregroundStyle(VGTheme.textMuted).lineSpacing(4).padding(.top, 4)`. Apply `@State private var expandedItems: Set<Int> = []` to track expansion state independently per item using the item index. Apply `.navigationTitle("FAQ")` and `.navigationBarTitleDisplayMode(.inline)`. Use `.listStyle(.insetGrouped)` on the List. Use `VGTheme.accentTerra` as the tint for expanded disclosure chevrons via `.tint(VGTheme.accentTerra)`.</action>
  <verify>
    <automated>test -f "/Users/kyleharrington/Desktop/AI/Vitamin G/VitaminG/VitaminG/VitaminG/Views/Support/FAQView.swift" && echo "exists"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `VitaminG/VitaminG/VitaminG/Views/Support/FAQView.swift`
    - File declares `struct FAQView: View`
    - File contains `DisclosureGroup`
    - File contains at least 8 distinct question strings (grep count ≥ 8 for `"?"` within DisclosureGroup labels)
    - File contains `.navigationTitle("FAQ")`
    - File contains `.listStyle(.insetGrouped)`
    - No third-party imports; only `import SwiftUI`
  </acceptance_criteria>
  <done>FAQView.swift exists with a DisclosureGroup List containing 9 Q&As, navigation title, and inset grouped list style.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | Both views are fully static — no user input, no external data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-02-A | Information Disclosure | mailto:hello@vitamingapp.com in AboutUsView | accept | Email is intentionally public support contact; no private data exposed |
</threat_model>

<verification>
- Both files exist under `Views/Support/`
- `grep -c "struct AboutUsView" AboutUsView.swift` → 1
- `grep -c "struct FAQView" FAQView.swift` → 1
- `grep -c "DisclosureGroup" FAQView.swift` → at least 9
</verification>

<success_criteria>
AboutUsView and FAQView exist in Views/Support/ and are importable by HomeView's Stay Close NavigationLinks in Plan 03.
</success_criteria>

<output>
Create `/Users/kyleharrington/Desktop/AI/Vitamin G/.planning/phases/15-ui-additions-fixes/15-02-SUMMARY.md` when done.
</output>
