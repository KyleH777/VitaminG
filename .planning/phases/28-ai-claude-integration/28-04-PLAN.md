---
phase: 28-ai-claude-integration
plan: 04
type: execute
wave: 3
depends_on:
  - 28-03
files_modified:
  - VitaminG/VitaminG/VitaminG/Views/Explore/GoalSuggestionsCard.swift
  - VitaminG/VitaminG/VitaminG/Views/Explore/ExploreView.swift
  - VitaminG/VitaminG.xcodeproj/project.pbxproj
autonomous: false
requirements:
  - AI-01

must_haves:
  truths:
    - "Explore tab shows a 'GOALS FOR YOU' section between Today's Gift and Daily Mood (second position)"
    - "The card shows 3 Claude-generated suggestion rows when online; 3 static suggestions when offline"
    - "Tapping Add Goal on a suggestion adds it as GoalTier.immediate (QuickWin) via GoalViewModel.addGoal"
    - "Tapped suggestion shows checkmark.circle.fill (sage) and dims text to textMuted; card stays visible with all 3 rows"
    - "Card never disappears, never shows an error; fallback is indistinguishable from Claude content"
    - "Suggestions are fetched at most once per calendar day via UserDefaults cache key"
    - "Card does NOT appear in the search branch of ExploreView"
  artifacts:
    - path: "VitaminG/VitaminG/VitaminG/Views/Explore/GoalSuggestionsCard.swift"
      provides: "SwiftUI card with header, 3 suggestion rows, Add Goal button → checkmark transition"
      contains: "struct GoalSuggestionsCard"
    - path: "VitaminG/VitaminG/VitaminG/Views/Explore/ExploreView.swift"
      provides: "Modified ExploreView with GoalSuggestionsCard inserted in second position"
      contains: "GOALS FOR YOU"
  key_links:
    - from: "VitaminG/VitaminG/VitaminG/Views/Explore/GoalSuggestionsCard.swift"
      to: "GoalViewModel.addGoal(input:context:)"
      via: "addSuggestion tap handler"
      pattern: "goalVM\\.addGoal\\(input:"
    - from: "VitaminG/VitaminG/VitaminG/Views/Explore/ExploreView.swift"
      to: "AIViewModel.refreshSuggestionsIfNeeded"
      via: ".task modifier on existingScrollContent"
      pattern: "refreshSuggestionsIfNeeded"
---

<objective>
Wire AI-01 into the Explore tab: create `GoalSuggestionsCard.swift` that renders 3 suggestion rows with "Add Goal" buttons that transition to checkmark on tap, then modify `ExploreView.swift` to (a) hold `@State private var aiViewModel = AIViewModel()`, (b) insert `sectionLabel("GOALS FOR YOU")` and `GoalSuggestionsCard(aiViewModel: aiViewModel)` in `existingScrollContent` between `GoalGifterCard` (Section 1) and `MoodPromptCard` (Section 2), (c) add a `.task` modifier that fetches suggestions via `aiViewModel.refreshSuggestionsIfNeeded(goals: allGoals, events: completionEvents)`, and (d) ensure the card does NOT render in the search branch.

Purpose: Satisfy ROADMAP Phase 28 success criterion 2 and AI-01. Add goal on tap follows the GoalGifterCard → GoalViewModel.addGoal(input:context:) pattern exactly (D-05); QuickWin tier is `GoalTier.immediate` (the existing case, not a new enum value).
Output: Explore tab displays the AI suggestions card in the correct position; tapping a suggestion inserts a QuickWin goal and visually marks the row as added.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-ai-claude-integration/28-CONTEXT.md
@.planning/phases/28-ai-claude-integration/28-RESEARCH.md
@.planning/phases/28-ai-claude-integration/28-PATTERNS.md
@.planning/phases/28-ai-claude-integration/28-UI-SPEC.md
@.planning/phases/28-ai-claude-integration/28-02-SUMMARY.md

<interfaces>
<!-- Types referenced in this plan (from Plan 02) -->

AIViewModel — @MainActor @Observable
  var suggestions: [String]
  var isLoadingSuggestions: Bool
  var addedSuggestionIndices: Set<Int>
  func refreshSuggestionsIfNeeded(goals: [Goal], events: [CompletionEvent]) async

<!-- GoalGifterCard analog (from VitaminG/VitaminG/VitaminG/Views/Explore/GoalGifterCard.swift) — exact pattern to mirror -->

struct GoalGifterCard: View {
    @Bindable var viewModel: ExploreViewModel
    @Environment(\.modelContext) private var modelContext
    @State private var goalVM = GoalViewModel()
    // addGiftedGoal builds GoalInput(tier: .immediate, ...) and calls
    //   try? goalVM.addGoal(input: input, context: modelContext)
}

<!-- Goal model types (from VitaminG/VitaminG/VitaminG/Models/Goal.swift and GoalInput.swift) -->

struct GoalInput {
    init(title: String, tier: GoalTier, category: GoalCategory, frequency: GoalFrequency,
         reminderTime: Date?, isPrivate: Bool, startDate: Date)
}
enum GoalTier: String { case immediate = "Immediate"; /* and others */ }   // .immediate IS QuickWin
enum GoalCategory: String { case other = "Other"; /* and others */ }
enum GoalFrequency: String { case daily = "Daily"; /* and others */ }

func GoalViewModel.addGoal(input: GoalInput, context: ModelContext) throws -> Goal

<!-- ExploreView structure (from VitaminG/VitaminG/VitaminG/Views/Explore/ExploreView.swift) -->

ExploreView — struct: View
  @Query private var allGoals: [Goal]              // (or similar name — read file)
  @Query private var completionEvents: [CompletionEvent]   // (verify in file)
  @State private var viewModel = ExploreViewModel()
  @State private var isSearching: Bool
  @State private var searchText: String
  body: { ScrollView { existingScrollContent } or search branch when isSearching }
  private var existingScrollContent: some View {
      VStack {
          sectionLabel("Today's Gift")
          GoalGifterCard(viewModel: viewModel)
          // INSERT HERE — Plan 28
          sectionLabel("Daily Mood")
          MoodPromptCard(viewModel: viewModel)
          ...
      }
  }
  private func sectionLabel(_ text: String) -> some View // calls text.uppercased()

<!-- VGTheme tokens (from UI-SPEC) -->
VGTheme.accentTerra, VGTheme.accentSage, VGTheme.textPrimary, VGTheme.textMuted,
VGTheme.separator, VGTheme.serif(_:), VGTheme.serifItalic(_:)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create GoalSuggestionsCard.swift with 3-row layout, Add Goal button, checkmark transition</name>
  <files>VitaminG/VitaminG/VitaminG/Views/Explore/GoalSuggestionsCard.swift, VitaminG/VitaminG.xcodeproj/project.pbxproj</files>
  <read_first>
    VitaminG/VitaminG/VitaminG/Views/Explore/GoalGifterCard.swift (full file — exact template for card shell, GoalInput construction, GoalViewModel injection)
    VitaminG/VitaminG/VitaminG/Models/Goal.swift (confirm GoalTier.immediate rawValue "Immediate" — this IS QuickWin per D-05)
    VitaminG/VitaminG/VitaminG/Models/GoalInput.swift (init signature)
    VitaminG/VitaminG/VitaminG/ViewModels/GoalViewModel.swift (confirm addGoal(input:context:) throws -> Goal signature)
    VitaminG/VitaminG/VitaminG/ViewModels/AIViewModel.swift (Plan 02 — addedSuggestionIndices Set<Int>)
    VitaminG/VitaminG/VitaminG/VGTheme.swift (confirm accentTerra, accentSage, textPrimary, textMuted, separator tokens; serif(20) and serifItalic(_:) helpers)
    .planning/phases/28-ai-claude-integration/28-UI-SPEC.md (Component Spec 2: Goal Suggestions Card — exact spacing, typography, colors; Add Goal label revised from "Add")
    .planning/phases/28-ai-claude-integration/28-PATTERNS.md (GoalSuggestionsCard section lines 311-417 — exact addSuggestion code)
    .planning/phases/28-ai-claude-integration/28-RESEARCH.md (Pattern 5 lines 618-664; Anti-patterns 1, 6 — no new GoalTier case)
  </read_first>
  <behavior>
    Behavior 1: Card initial render shows header row ("Goals suggested for you" title + "Based on your goals and interests" subtitle + sparkles icon) and 3 rows for `aiViewModel.suggestions` (which defaults to staticSuggestions until refresh fires).
    Behavior 2: Each row shows the suggestion text on the leading edge and a capsule "Add Goal" button on the trailing edge, with a Divider between rows (except after the last row).
    Behavior 3: Tapping "Add Goal" on row index N: (a) constructs GoalInput(title: suggestions[N], tier: .immediate, category: .other, frequency: .daily, reminderTime: nil, isPrivate: true, startDate: Date()); (b) calls try? goalVM.addGoal(input:context:); (c) on success: triggers UIImpactFeedbackGenerator(style: .medium).impactOccurred(); (d) inserts N into aiViewModel.addedSuggestionIndices with .easeInOut(duration: 0.2) animation; (e) the row's button replaces itself with checkmark.circle.fill in VGTheme.accentSage, and the suggestion text foreground shifts from textPrimary to textMuted.
    Behavior 4: Already-added rows (N ∈ addedSuggestionIndices) are NOT tappable — no further state transitions.
    Behavior 5: While `aiViewModel.isLoadingSuggestions == true`, the 3 rows render in `.redacted(reason: .placeholder)` state but the header row renders normally (per UI-SPEC §Loading skeleton).
  </behavior>
  <action>Create `VitaminG/VitaminG/VitaminG/Views/Explore/GoalSuggestionsCard.swift`. Top: `import SwiftUI` and `import SwiftData`. Struct: `struct GoalSuggestionsCard: View { ... }`.

Properties:
- `@Bindable var aiViewModel: AIViewModel`
- `@Environment(\.modelContext) private var modelContext`
- `@State private var goalVM = GoalViewModel()`

Body structure (mirror UI-SPEC Component Spec 2):

`VStack(alignment: .leading, spacing: 16)` containing:

(1) Header row: `HStack(alignment: .center, spacing: 12)` with:
- `Image(systemName: "sparkles").font(.system(size: 22)).foregroundStyle(VGTheme.accentTerra).frame(width: 48, height: 48)`
- `VStack(alignment: .leading, spacing: 4)` containing `Text("Goals suggested for you").font(VGTheme.serif(20)).foregroundStyle(VGTheme.textPrimary)` and `Text("Based on your goals and interests").font(.system(size: 14)).foregroundStyle(VGTheme.textMuted)`.

(2) Suggestion rows: `VStack(spacing: 0)` with `ForEach(Array(aiViewModel.suggestions.enumerated()), id: \.offset)` producing per-row content:
- Compute `let isAdded = aiViewModel.addedSuggestionIndices.contains(index)`.
- `HStack(alignment: .center, spacing: 8)` with:
  - `Text(suggestion).font(.system(size: 16)).foregroundStyle(isAdded ? VGTheme.textMuted : VGTheme.textPrimary).lineSpacing(4).fixedSize(horizontal: false, vertical: true).frame(maxWidth: .infinity, alignment: .leading)`.
  - Trailing button: if `isAdded`, render `Image(systemName: "checkmark.circle.fill").font(.system(size: 24)).foregroundStyle(VGTheme.accentSage).frame(width: 60, height: 32).accessibilityLabel("Added")`; otherwise render `Button { addSuggestion(title: suggestion, at: index) } label: { Text("Add Goal").font(.system(size: 14, weight: .semibold)).foregroundStyle(.white).padding(.horizontal, 16).padding(.vertical, 8).background(VGTheme.accentTerra).clipShape(Capsule()) }.buttonStyle(.plain).accessibilityLabel("Add goal: \(suggestion)")`.
- Row modifiers: `.padding(.vertical, 16)` (44pt+ touch target), `.contentShape(Rectangle())`.
- After every row except the last, render `Divider().foregroundStyle(VGTheme.separator)`.
- When `aiViewModel.isLoadingSuggestions == true`, wrap the row's text in `.redacted(reason: .placeholder)` and disable the button (UI-SPEC §Loading skeleton).

Outer card modifiers (mirror GoalGifterCard, with corrected padding per UI-SPEC):
- `.padding(16)` (NOT 18 — UI-SPEC §Spacing for grid compliance)
- `.background(LinearGradient(colors: [VGTheme.accentTerra.opacity(0.12), VGTheme.accentTerra.opacity(0.04)], startPoint: .top, endPoint: .bottom))`
- `.clipShape(RoundedRectangle(cornerRadius: 20))`
- `.overlay(RoundedRectangle(cornerRadius: 20).strokeBorder(VGTheme.accentTerra.opacity(0.2), lineWidth: 1))`
- `.padding(.horizontal, 16)`
- `.accessibilityElement(children: .contain)`
- `.accessibilityLabel("Goals suggested for you, \(aiViewModel.suggestions.count) suggestions")`

`private func addSuggestion(title: String, at index: Int)` — EXACT GoalGifterCard pattern adapted:
```text
let input = GoalInput(
    title: title,
    tier: .immediate,          // QuickWin = .immediate (D-05; Anti-pattern 6: do NOT create a new case)
    category: .other,          // Suggestions have no explicit category (Assumption A2)
    frequency: .daily,
    reminderTime: nil,
    isPrivate: true,
    startDate: Date()
)
if (try? goalVM.addGoal(input: input, context: modelContext)) != nil {
    UIImpactFeedbackGenerator(style: .medium).impactOccurred()
    withAnimation(.easeInOut(duration: 0.2)) {
        aiViewModel.addedSuggestionIndices.insert(index)
    }
}
```

Edit `VitaminG/VitaminG.xcodeproj/project.pbxproj` to register `GoalSuggestionsCard.swift` under the VitaminG target's `PBXSourcesBuildPhase` (mirror GoalGifterCard.swift entry).

Constraints:
- Do NOT set `associatedInspiration` on the inserted goal (that field is specific to the gifter card).
- Do NOT remove the row from `aiViewModel.suggestions` on tap — only insert into `addedSuggestionIndices` (D-05).
- Do NOT use a new GoalTier case — `.immediate` IS QuickWin (Anti-pattern 6).
- Do NOT show a sheet or confirmation — single-tap add per D-05.
- Button visible label MUST be exactly `"Add Goal"` (UI-SPEC §Copywriting Contract — revised from "Add").
- Card MUST always show 3 rows (no empty state, no error state — D-07).</action>
  <verify>
    <automated>cd "VitaminG/VitaminG" &amp;&amp; xcodebuild build -scheme VitaminG -destination 'platform=iOS Simulator,name=iPhone 16' 2>&amp;1 | tail -5 &amp;&amp; grep -q '"Add Goal"' VitaminG/Views/Explore/GoalSuggestionsCard.swift &amp;&amp; grep -q 'tier: .immediate' VitaminG/Views/Explore/GoalSuggestionsCard.swift</automated>
  </verify>
  <acceptance_criteria>
    - `VitaminG/VitaminG/VitaminG/Views/Explore/GoalSuggestionsCard.swift` exists with `struct GoalSuggestionsCard: View`
    - File contains `@Bindable var aiViewModel: AIViewModel`
    - File contains `@Environment(\.modelContext) private var modelContext`
    - File contains `@State private var goalVM = GoalViewModel()`
    - File contains literal `Text("Add Goal")` (button label per UI-SPEC §Copywriting)
    - File does NOT contain `Text("Add")` as a button label (assertion: grep `>("Add")` returns 0 — case-sensitive exact match of the old shorter label)
    - File contains `Image(systemName: "sparkles")` (header icon)
    - File contains `Image(systemName: "checkmark.circle.fill")` (added state icon)
    - File contains `tier: .immediate` (source assertion: grep)
    - File contains `category: .other` (source assertion: grep)
    - File contains `try? goalVM.addGoal(input: input, context: modelContext)`
    - File contains `aiViewModel.addedSuggestionIndices.insert(index)`
    - File contains `UIImpactFeedbackGenerator(style: .medium).impactOccurred()`
    - File contains `withAnimation(.easeInOut(duration: 0.2))`
    - File contains `.padding(16)` and does NOT contain `.padding(18)` (UI-SPEC §Spacing)
    - File contains `cornerRadius: 20` (matches GoalGifterCard outer shell)
    - File contains `.accessibilityLabel("Add goal:` and `.accessibilityLabel("Added"`
    - File contains `.redacted(reason: .placeholder)` (loading state)
    - File does NOT create a new GoalTier case (Anti-pattern 6)
    - File does NOT remove suggestions from `aiViewModel.suggestions` (D-05)
    - `VitaminG/VitaminG.xcodeproj/project.pbxproj` registers `GoalSuggestionsCard.swift` under VitaminG target
    - `xcodebuild build -scheme VitaminG -destination 'platform=iOS Simulator,name=iPhone 16'` exits 0
  </acceptance_criteria>
  <done>GoalSuggestionsCard.swift compiles, registers in Xcode target, and meets all UI-SPEC and D-05 requirements. ExploreView wiring follows in Task 2.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Insert GOALS FOR YOU section and GoalSuggestionsCard into ExploreView.existingScrollContent + add .task fetch</name>
  <files>VitaminG/VitaminG/VitaminG/Views/Explore/ExploreView.swift</files>
  <read_first>
    VitaminG/VitaminG/VitaminG/Views/Explore/ExploreView.swift (full file — locate existingScrollContent, search branch gating, @Query/@State properties)
    VitaminG/VitaminG/VitaminG/Views/Explore/GoalGifterCard.swift (Section 1 — insertion goes immediately after this card's call site)
    VitaminG/VitaminG/VitaminG/Views/Explore/MoodPromptCard.swift (Section 2 — insertion goes immediately before this card's call site)
    VitaminG/VitaminG/VitaminG/Views/Explore/GoalSuggestionsCard.swift (Task 1 output)
    VitaminG/VitaminG/VitaminG/ViewModels/AIViewModel.swift (Plan 02 — refreshSuggestionsIfNeeded signature)
    .planning/phases/28-ai-claude-integration/28-PATTERNS.md (ExploreView modification lines 449-500 — exact insertion location and .task placement)
    .planning/phases/28-ai-claude-integration/28-UI-SPEC.md (Component Spec 2 §Search branch — card must NOT appear when isSearching)
  </read_first>
  <behavior>
    Behavior 1: On Explore tab open with no UserDefaults cache for today, `aiViewModel.refreshSuggestionsIfNeeded` fires; while waiting, the card shows redacted rows; on success the card shows Claude suggestions; on offline the card shows the 3 static fallback suggestions.
    Behavior 2: The new section appears between "Today's Gift" (GoalGifterCard) and "Daily Mood" (MoodPromptCard) — second position in the scroll.
    Behavior 3: Section label reads "GOALS FOR YOU" (uppercased via existing sectionLabel helper).
    Behavior 4: When `isSearching == true`, the card is NOT rendered (lives only inside `existingScrollContent`).
    Behavior 5: Tapping Add Goal on a row inserts a QuickWin goal into the SwiftData modelContext.
  </behavior>
  <action>Modify `VitaminG/VitaminG/VitaminG/Views/Explore/ExploreView.swift`:

(i) Add `@State private var aiViewModel = AIViewModel()` to the view's property list, alongside the existing `@State private var viewModel = ExploreViewModel()`.

(ii) Locate `existingScrollContent` computed var. Find the lines:
```
sectionLabel("Today's Gift")
GoalGifterCard(viewModel: viewModel)
```
immediately followed by:
```
sectionLabel("Daily Mood")
MoodPromptCard(viewModel: viewModel)
```

Insert BETWEEN these two pairs, exactly two new lines:
```
sectionLabel("GOALS FOR YOU")
GoalSuggestionsCard(aiViewModel: aiViewModel)
```

(iii) On the same container that owns `existingScrollContent` (the ScrollView or its child VStack inside the non-search branch), add a `.task` modifier:
```
.task {
    await aiViewModel.refreshSuggestionsIfNeeded(goals: allGoals, events: completionEvents)
}
```
Use the exact property names already in ExploreView for goals and completion events (read the file to confirm — likely `allGoals` and `completionEvents`; if completion events are not currently queried in ExploreView, add `@Query private var completionEvents: [CompletionEvent]` adjacent to the existing `@Query` properties).

(iv) Verify the search branch (lines roughly 25-45, where `isSearching` is true) does NOT include `GoalSuggestionsCard`. The card lives only inside `existingScrollContent` — confirm by reading the search branch carefully and asserting no insertion occurred there.

(v) Confirm `sectionLabel("GOALS FOR YOU")` rendering: ExploreView's `sectionLabel(_:)` helper calls `text.uppercased()`; passing already-uppercase "GOALS FOR YOU" is idempotent (UI-SPEC verified).

Constraints:
- Do NOT modify ExploreViewModel.swift (this plan's logic lives in AIViewModel).
- Do NOT change the order of any other sections — only insert between Section 1 (Today's Gift) and Section 2 (Daily Mood).
- Do NOT remove or rename `sectionLabel("Today's Gift")` or `sectionLabel("Daily Mood")`.
- Do NOT render GoalSuggestionsCard inside the `isSearching == true` branch (UI-SPEC §Search branch).
- Do NOT trigger `aiViewModel.refreshSuggestionsIfNeeded` from `VitaminGApp.init` (Pitfall 6).
- Do NOT pass empty arrays — payload must use real `allGoals` and `completionEvents` so Claude prompt is personalized per AI-01.</action>
  <verify>
    <automated>cd "VitaminG/VitaminG" &amp;&amp; xcodebuild test -scheme VitaminG -destination 'platform=iOS Simulator,name=iPhone 16' 2>&amp;1 | tail -10 &amp;&amp; grep -q 'sectionLabel("GOALS FOR YOU")' VitaminG/Views/Explore/ExploreView.swift &amp;&amp; grep -q 'GoalSuggestionsCard(aiViewModel:' VitaminG/Views/Explore/ExploreView.swift &amp;&amp; grep -q 'refreshSuggestionsIfNeeded' VitaminG/Views/Explore/ExploreView.swift</automated>
  </verify>
  <acceptance_criteria>
    - `VitaminG/VitaminG/VitaminG/Views/Explore/ExploreView.swift` contains `@State private var aiViewModel = AIViewModel()`
    - File contains the literal `sectionLabel("GOALS FOR YOU")` (source assertion)
    - File contains `GoalSuggestionsCard(aiViewModel: aiViewModel)` (source assertion)
    - The `sectionLabel("GOALS FOR YOU")` line precedes `GoalSuggestionsCard(aiViewModel: aiViewModel)` and BOTH appear AFTER `GoalGifterCard(viewModel: viewModel)` and BEFORE `MoodPromptCard(viewModel: viewModel)` in source order (assertion: a 4-line `awk`/`grep -n` check confirms line numbers in this order)
    - File contains `.task` block with `aiViewModel.refreshSuggestionsIfNeeded(goals:` (source assertion)
    - File does NOT have `GoalSuggestionsCard` inside any branch gated by `isSearching == true` (assertion: locate the `if isSearching` block and confirm `GoalSuggestionsCard` does not appear within it)
    - Full project compiles: `xcodebuild build -scheme VitaminG -destination 'platform=iOS Simulator,name=iPhone 16'` exits 0
    - Full test suite GREEN: `xcodebuild test -scheme VitaminG -destination 'platform=iOS Simulator,name=iPhone 16'` exits 0
  </acceptance_criteria>
  <done>ExploreView.swift inserts the new section in the correct position (between Today's Gift and Daily Mood), triggers the AI fetch via .task, and excludes the card from the search branch. Project compiles and full test suite is GREEN.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: HUMAN CHECKPOINT — Verify Explore tab GOALS FOR YOU card behavior end-to-end</name>
  <what-built>GoalSuggestionsCard renders in the second position on the Explore tab (between Today's Gift and Daily Mood) with 3 suggestion rows and Add Goal buttons. Suggestions fetch on view appear from the deployed Worker.</what-built>
  <how-to-verify>
    Run the app on the iPhone 16 simulator. Reset the simulator between major checks (delete app or reset UserDefaults) so cache state is fresh.

    Check A — Position and label (visual):
    1. Fresh install, complete onboarding, navigate to Explore tab.
    2. Scroll from top: the order MUST be:
       (i) "Today's Gift" label + GoalGifterCard
       (ii) "GOALS FOR YOU" label + GoalSuggestionsCard
       (iii) "Daily Mood" label + MoodPromptCard
       (iv) ... rest of Explore content
    3. The "GOALS FOR YOU" label MUST be all-caps and use the same font treatment as other Explore section labels.

    Check B — Claude path (online, fresh day):
    1. With internet on and fresh date-key cache, open Explore tab.
    2. Card MUST populate within ~3 seconds with 3 Claude-generated suggestions (typically more specific than the static fallbacks — e.g., "Walk 5,000 steps daily" instead of "Drink 8 glasses of water" if the user already has hydration goals).
    3. Re-enter Explore tab — same 3 suggestions render immediately (cache hit, no network call).

    Check C — Fallback path (offline):
    1. Fresh install, disable internet.
    2. Open Explore tab.
    3. Card MUST show the 3 static fallback suggestions: "Read for 15 minutes daily", "Drink 8 glasses of water", "Meditate for 5 minutes".
    4. NO error message, NO empty card, NO infinite spinner.

    Check D — Add Goal interaction:
    1. Tap "Add Goal" on the first suggestion.
    2. Within ~200ms: button transitions to a green checkmark (checkmark.circle.fill in sage). Suggestion text dims to muted color. Card stays visible with all 3 rows.
    3. Haptic feedback fires (medium impact — confirm on device; simulator may not vibrate).
    4. Navigate to Goals tab — the newly-added goal appears under the QuickWin / Immediate tier. Title matches the suggestion text exactly.
    5. Tap the same row again — NO state change (row is now non-interactive).
    6. Tap "Add Goal" on the second suggestion — same successful add behavior; row 1 remains in checkmark state.

    Check E — Search branch gating:
    1. Open Explore tab. Tap the search field, type any keyword.
    2. While searching, the "GOALS FOR YOU" card MUST NOT appear in the results view.
    3. Clear search — the card reappears.

    Check F — Accessibility:
    1. Enable VoiceOver. Swipe through the card.
    2. VoiceOver MUST announce "Goals suggested for you, 3 suggestions" on the card.
    3. Each Add Goal button MUST announce "Add goal: <suggestion text>".
    4. After tapping, an added row MUST announce "Added".

    If any check fails, describe the specific failure (wrong position, button label wrong, card appears in search, goal not inserted, tier wrong, etc.) and the executor will revise.
  </how-to-verify>
  <resume-signal>Type "approved" if all six checks (A, B, C, D, E, F) pass on the simulator. Otherwise describe the specific failure.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Claude-generated suggestion text → SwiftData Goal.title | Text generated by Claude flows into a persisted SwiftData property; treated as untrusted input per CLAUDE.md ("local SwiftData storage must be treated as untrusted input boundary") |
| User tap → modelContext insert | Single-tap insert path (no confirmation dialog) — same trust as GoalGifterCard add path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-14 | Tampering | Claude suggestion title persisted as Goal.title | mitigate | `GoalViewModel.addGoal(input:context:)` is the existing validated CRUD path; it applies the project's standard input sanitization (per CLAUDE.md "All String inputs must have strict character limits and validation"). This plan does NOT bypass it. |
| T-28-15 | Denial of Service | Card blocked on slow Worker response | mitigate | `URLRequest.timeoutInterval = 10` (Plan 02) bounds wait. While loading, `.redacted(reason: .placeholder)` shows. On timeout, AIProxyService returns staticSuggestions silently — card never blocks. |
| T-28-16 | Information Disclosure | Goal titles in fetch payload | accept | Same data already CloudKit-synced. Documented acceptance in RESEARCH.md Security Domain. |
| T-28-17 | Elevation of Privilege | Stored XSS via Claude text in SwiftUI Text | mitigate | SwiftUI `Text()` renders plain text only — no HTML/JS execution surface. Even malicious Claude output displays as literal characters. |
| T-28-18 | Tampering | Duplicate-tap goal insertion | mitigate | `aiViewModel.addedSuggestionIndices.contains(index)` gate hides the Add Goal button after first successful insert; subsequent taps go to the checkmark image, which has no tap handler. |
</threat_model>

<verification>
After all three tasks complete:

1. Full `xcodebuild build -scheme VitaminG -destination 'platform=iOS Simulator,name=iPhone 16'` exits 0.
2. Full `xcodebuild test -scheme VitaminG -destination 'platform=iOS Simulator,name=iPhone 16'` exits 0 (no regressions in ExploreView/Explore-related tests).
3. Human verification (Task 3 Checks A through F) all pass on the iPhone 16 simulator.
4. Source assertions: GoalSuggestionsCard.swift exists with correct shell, Add Goal label, tier .immediate, addSuggestion handler; ExploreView.swift contains the inserted section in correct order and the .task trigger; search branch excludes the card.
</verification>

<success_criteria>
- GoalSuggestionsCard.swift exists with 3-row layout, Add Goal button, checkmark transition, redacted loading state
- ExploreView.swift inserts "GOALS FOR YOU" section between Today's Gift and Daily Mood (second position per D-04)
- Suggestion tap inserts a Goal with tier `.immediate` (QuickWin per D-05) via GoalViewModel.addGoal
- Tapped suggestion shows checkmark; card stays visible with all 3 rows (D-05)
- Fallback: 3 static suggestions when Worker unreachable (D-07)
- Card does NOT appear in the search branch (UI-SPEC)
- Full project compiles and full test suite GREEN
- Human verification confirms position, Claude path, fallback path, add interaction, search gating, accessibility
- AI-01 satisfied: Explore tab shows personalized suggestions with single-tap add, cached once per day
</success_criteria>

<output>
Create `.planning/phases/28-ai-claude-integration/28-04-SUMMARY.md` when done. Include:
- Confirmation that GoalSuggestionsCard exists in the correct position
- Confirmation that the six human verification checks (A–F) all passed
- Confirmation that added goals appear under the QuickWin/Immediate tier on the Goals tab
- Note that AI-01 view-layer integration is complete and Phase 28 is ready for end-to-end UAT
</output>
