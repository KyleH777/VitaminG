---
phase: 17-onboarding-overhaul
plan: "02"
type: execute
wave: 1
depends_on: [17-01]
files_modified:
  - VitaminG/VitaminG/VitaminG/Views/Onboarding/TermsAndConditionsScreen.swift
  - VitaminG/VitaminG/VitaminG/Views/Onboarding/OnboardingView.swift
autonomous: false
requirements: [AUTH-02]

must_haves:
  truths:
    - "T&C screen is reachable in the onboarding flow after Apple Sign-In for new users"
    - "Tapping 'Read Terms' opens the PDF via QuickLook in a .large sheet"
    - "Tapping 'I Agree — Continue' sets @AppStorage('vg_hasAgreedToTerms') = true and advances to .name"
    - "PDF opens without crash — Bundle.main.url(forResource:withExtension:) returns non-nil"
    - "PDFPreviewView wraps QLPreviewController in UIViewControllerRepresentable"
  artifacts:
    - path: "VitaminG/VitaminG/VitaminG/Views/Onboarding/TermsAndConditionsScreen.swift"
      provides: "T&C step with QuickLook PDF sheet and I Agree button"
      contains: "PDFPreviewView"
    - path: "VitaminG/VitaminG/VitaminG/Resources/Vitamin_G_Terms_and_Conditions.pdf"
      provides: "Bundled T&C PDF"
  key_links:
    - from: "TermsAndConditionsScreen 'I Agree — Continue' button"
      to: "path.append(.name)"
      via: "agree() private func"
      pattern: "path.append(.name)"
    - from: "PDFPreviewView"
      to: "QLPreviewController"
      via: "UIViewControllerRepresentable"
      pattern: "QLPreviewController"

user_setup:
  - service: xcode-bundle-resource
    why: "T&C PDF must be added to the Xcode project's Copy Bundle Resources phase. The file Vitamin_G_Terms_and_Conditions.pdf exists at the project root. It must be copied to VitaminG/VitaminG/VitaminG/Resources/ and added to the VitaminG app target in Xcode. This cannot be done via code alone — Xcode project.pbxproj must reference it."
    env_vars: []
    dashboard_config:
      - task: "Add PDF to Xcode target"
        location: "Xcode → Project Navigator → drag Vitamin_G_Terms_and_Conditions.pdf into VitaminG/Resources/ group → check 'Copy items if needed' → check VitaminG app target → verify in Build Phases → Copy Bundle Resources"
---

<objective>
Create the TermsAndConditionsScreen (new file) with PDFPreviewView embedded, wire it into OnboardingView's .termsAndConditions destination, and bundle the PDF asset.

Purpose: AUTH-02 requires users to read and acknowledge T&C before any identity setup. The screen gates the name entry step.
Output: TermsAndConditionsScreen.swift (with PDFPreviewView), updated OnboardingView.swift destination.
</objective>

<execution_context>
@/Users/kyleharrington/Desktop/AI/Vitamin G/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kyleharrington/Desktop/AI/Vitamin G/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/kyleharrington/Desktop/AI/Vitamin G/.planning/ROADMAP.md
@/Users/kyleharrington/Desktop/AI/Vitamin G/.planning/phases/17-onboarding-overhaul/17-CONTEXT.md
@/Users/kyleharrington/Desktop/AI/Vitamin G/.planning/phases/17-onboarding-overhaul/17-PATTERNS.md
@/Users/kyleharrington/Desktop/AI/Vitamin G/.planning/phases/17-onboarding-overhaul/17-UI-SPEC.md

<interfaces>
<!-- Key patterns extracted from codebase read. -->

NameScreen.swift (closest structural analog — copy its layout pattern):
- Background: VGTheme.sandLight.ignoresSafeArea()
- ZStack(alignment: .bottom) with VStack on top and bottom CTA pinned to bottom
- Back arrow nav row (top-left): HStack { Button { path.removeLast() } label: { Image(systemName: "chevron.left").font(.system(size: 18, weight: .medium)).foregroundStyle(VGTheme.clay) }; Spacer() }.padding(.bottom, 20)
- StepBarView(current: X, total: 7): struct defined in NameScreen.swift lines 121-134 using Capsule(), VGTheme.terra fill for filled, VGTheme.sandMid for unfilled, height 3, spacing 4
- Primary CTA: terra fill, 17pt .semibold, maxWidth .infinity, vertical padding 18, cornerRadius 14, warmWhite text
- .padding(.horizontal, 28) on outer VStack; .padding(.bottom, 36) on CTA container
- .navigationBarHidden(true) on the ZStack

NameScreen.swift @AppStorage pattern (copy for hasAgreedToTerms):
  @AppStorage("vg_hasAgreedToTerms") private var hasAgreedToTerms: Bool = false

UI-SPEC §2. T&CScreen:
- StepBarView(current: 0, total: 6) — T&C is step 0 (NOTE: PATTERNS.md says total: 7; follow PATTERNS.md — 7-step flow is the established count per StepBarView §Shared Patterns)
- Headline: "Before we begin" (42pt CormorantGaramond-Regular, VGTheme.clay)
- Subtitle: "Please read and agree to our Terms & Conditions to continue using Vitamin G." (14pt SF Pro .light, VGTheme.muted, lineSpacing 4)
- "Read Terms" button: outlined capsule (strokeBorder VGTheme.sandMid, VGTheme.clay text, 13pt .semibold)
- Primary CTA copy: "I Agree — Continue"

RESEARCH.md §Area 6 — PDFPreviewView pattern (no codebase analog — use exactly):
- struct PDFPreviewView: UIViewControllerRepresentable
- makeUIViewController: QLPreviewController(), set dataSource = context.coordinator
- Coordinator: NSObject, QLPreviewControllerDataSource
- numberOfPreviewItems returns 1; previewController returns url as NSURL
- Sheet presentation: .sheet(isPresented: $showTerms) { PDFPreviewView(url: url).ignoresSafeArea() }.presentationDetents([.large])

Copywriting (from 17-UI-SPEC.md §Copywriting Contract):
- T&C screen headline: "Before we begin"
- T&C screen body: "Please read and agree to our Terms & Conditions to continue using Vitamin G."
- T&C open PDF button: "Read Terms"
- Primary CTA: "I Agree — Continue"
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <what-built>The T&C PDF must be in Xcode's bundle before the code that reads it can be tested. This is a one-time Xcode GUI operation that cannot be done via code or shell commands.</what-built>
  <how-to-verify>
    1. Open Vitamin G in Xcode
    2. In Project Navigator, right-click the VitaminG/VitaminG/VitaminG/Resources/ group (or create it if it doesn't exist)
    3. Choose "Add Files to VitaminG..."
    4. Navigate to the project root and select Vitamin_G_Terms_and_Conditions.pdf
    5. Check "Copy items if needed"
    6. Check the VitaminG app target checkbox (NOT the widget extension)
    7. Click Add
    8. Go to Project → VitaminG target → Build Phases → Copy Bundle Resources
    9. Verify "Vitamin_G_Terms_and_Conditions.pdf" appears in the list
    10. Run: ls "VitaminG/VitaminG/VitaminG/Resources/Vitamin_G_Terms_and_Conditions.pdf" — should exist
  </how-to-verify>
  <resume-signal>Type "pdf-added" when the PDF appears in Build Phases → Copy Bundle Resources</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Create TermsAndConditionsScreen.swift with PDFPreviewView</name>
  <files>VitaminG/VitaminG/VitaminG/Views/Onboarding/TermsAndConditionsScreen.swift</files>
  <read_first>
    - VitaminG/VitaminG/VitaminG/Views/Onboarding/NameScreen.swift (read completely — copy the ZStack/VStack/CTA pattern, StepBarView declaration, back-arrow nav row, and @AppStorage usage)
    - .planning/phases/17-onboarding-overhaul/17-PATTERNS.md §TermsAndConditionsScreen and §Shared Patterns (provides the exact file structure, PDFPreviewView code, and StepBarView usage)
    - .planning/phases/17-onboarding-overhaul/17-UI-SPEC.md §2. T&CScreen (visual contract for headline, subtitle, Read Terms button, and CTA)
    - .planning/phases/17-onboarding-overhaul/17-UI-SPEC.md §Copywriting Contract (exact copy strings)
  </read_first>
  <action>
    Create a new file at VitaminG/VitaminG/VitaminG/Views/Onboarding/TermsAndConditionsScreen.swift.

    The file contains TWO structs: TermsAndConditionsScreen and PDFPreviewView.

    TermsAndConditionsScreen layout (mirror NameScreen pattern):
    - Imports: SwiftUI, QuickLook
    - @Binding var path: [OnboardingStep]; let onSkip: () -> Void
    - @AppStorage("vg_hasAgreedToTerms") private var hasAgreedToTerms: Bool = false
    - @State private var showTerms = false
    - ZStack(alignment: .bottom) with VGTheme.sandLight.ignoresSafeArea() background
    - Top content VStack (alignment .leading, spacing 0, horizontal padding 28pt, top padding 16pt):
      - Back arrow row: HStack { Button { path.removeLast() } label: { Image(systemName: "chevron.left").font(.system(size: 18, weight: .medium)).foregroundStyle(VGTheme.clay) }; Spacer() }.padding(.bottom, 20)
      - StepBarView(current: 0, total: 7) — T&C is step 0 in the 7-step flow.padding(.bottom, 28)
      - Headline: Text("Before we begin").font(Font.custom("Georgia", size: 42)).foregroundStyle(VGTheme.clay).padding(.bottom, 12)
      - Subtitle: Text("Please read and agree to our Terms & Conditions to continue using Vitamin G.").font(.system(size: 14, weight: .light)).foregroundStyle(VGTheme.muted).lineSpacing(4).fixedSize(horizontal: false, vertical: true).padding(.bottom, 24)
      - "Read Terms" outlined button: capsule-style with strokeBorder VGTheme.sandMid lineWidth 1, Text("Read Terms").font(.system(size: 13, weight: .semibold)).foregroundStyle(VGTheme.clay), padding .vertical 12, .horizontal 20, clipShape Capsule. Tapping sets showTerms = true.
      - Spacer()
    - Bottom CTA VStack (pinned to bottom, padding horizontal 28, bottom 36):
      - Button("I Agree — Continue") action: agree(). Style: 17pt .semibold, maxWidth .infinity, vertical padding 18, background VGTheme.terra, foreground VGTheme.warmWhite, clipShape RoundedRectangle(cornerRadius: 14).
    - .navigationBarHidden(true)
    - .sheet(isPresented: $showTerms):
      Use an if-let guard (NOT force-unwrap) to present PDFPreviewView safely:
        if let url = termsURL { PDFPreviewView(url: url).ignoresSafeArea().presentationDetents([.large]) }
      Do NOT write PDFPreviewView(url: termsURL!) — termsURL! is forbidden outside a DEBUG assert
      (per T-17-02-02: guard with if let to prevent crash when PDF is absent from bundle).

    Private computed vars:
    - termsURL: URL? = Bundle.main.url(forResource: "Vitamin_G_Terms_and_Conditions", withExtension: "pdf")
    - agree() func: sets hasAgreedToTerms = true, then path.append(.name)

    Add #if DEBUG assert: assert(termsURL != nil, "T&C PDF not found in bundle — check Copy Bundle Resources") inside onAppear.
    This fires in debug builds only; the if-let guard in the sheet handles the production case.

    PDFPreviewView struct (in the same file, below TermsAndConditionsScreen):
    - Imports needed: QuickLook already imported at file top
    - struct PDFPreviewView: UIViewControllerRepresentable
    - let url: URL
    - makeUIViewController: creates QLPreviewController(), sets controller.dataSource = context.coordinator, returns controller
    - updateUIViewController: empty
    - makeCoordinator: returns Coordinator(url: url)
    - class Coordinator: NSObject, QLPreviewControllerDataSource — holds let url: URL — numberOfPreviewItems returns 1 — previewController returns url as NSURL

    The StepBarView struct definition lives in NameScreen.swift — do NOT redefine it here. It's already available in the module because it's a struct at file scope (not private). Reference it directly.
  </action>
  <verify>
    <automated>xcodebuild build -scheme VitaminG -destination 'platform=iOS Simulator,name=iPhone 16' 2>&1 | grep -E "(error:|BUILD SUCCEEDED|BUILD FAILED)" | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - TermsAndConditionsScreen.swift exists at VitaminG/VitaminG/VitaminG/Views/Onboarding/TermsAndConditionsScreen.swift
    - File contains "struct TermsAndConditionsScreen: View"
    - File contains "struct PDFPreviewView: UIViewControllerRepresentable"
    - File contains "QLPreviewController()"
    - File contains "vg_hasAgreedToTerms"
    - File contains "I Agree — Continue"
    - File contains "Before we begin"
    - File contains "path.append(.name)" inside the agree() function
    - File contains "presentationDetents([.large])"
    - File does NOT contain "termsURL!" (force-unwrap) outside of a "#if DEBUG" assert block — the sheet must use "if let url = termsURL" guard pattern
    - Build succeeds with zero errors
  </acceptance_criteria>
  <done>TermsAndConditionsScreen.swift created with T&C screen layout and PDFPreviewView; PDF sheet opened via if-let guard (no force-unwrap crash path); file compiles clean.</done>
</task>

<task type="auto">
  <name>Task 3: Wire .termsAndConditions destination in OnboardingView</name>
  <files>VitaminG/VitaminG/VitaminG/Views/Onboarding/OnboardingView.swift</files>
  <read_first>
    - VitaminG/VitaminG/VitaminG/Views/Onboarding/OnboardingView.swift (read current state — find the case .termsAndConditions placeholder from Plan 1 and confirm it's there)
  </read_first>
  <action>
    Replace the placeholder destination for .termsAndConditions:

    BEFORE (placeholder from Plan 1):
      case .termsAndConditions: Text("T&C — Plan 2")

    AFTER (real screen):
      case .termsAndConditions: TermsAndConditionsScreen(path: $path, onSkip: finish)

    No other changes to OnboardingView.swift in this task.
  </action>
  <verify>
    <automated>xcodebuild build -scheme VitaminG -destination 'platform=iOS Simulator,name=iPhone 16' 2>&1 | grep -E "(error:|BUILD SUCCEEDED|BUILD FAILED)" | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - OnboardingView.swift contains "TermsAndConditionsScreen(path: $path, onSkip: finish)" in the navigationDestination switch
    - OnboardingView.swift does NOT contain "Text(\"T&C — Plan 2\")"
    - Build succeeds with zero errors
  </acceptance_criteria>
  <done>.termsAndConditions destination routes to the real TermsAndConditionsScreen; build passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Bundle.main PDF URL → QLPreviewController | PDF sourced from app bundle — read-only Apple-controlled sandbox path; no user-writable surface |
| User taps "I Agree" → @AppStorage write | Simple Bool flag written to UserDefaults — no PII, no network call |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-02-01 | Tampering | vg_hasAgreedToTerms AppStorage write | accept | Bool flag in UserDefaults; restartOnboarding() clears it on "This isn't me"; no server-side enforcement needed for v2.0 |
| T-17-02-02 | Denial of Service | Bundle.main.url returns nil (PDF missing from bundle) | mitigate | assert(termsURL != nil) in DEBUG builds (via onAppear); sheet uses "if let url = termsURL" guard — PDFPreviewView is never constructed with a nil URL; no force-unwrap in production paths |
| T-17-02-03 | Information Disclosure | PDF shown to unauthenticated users | accept | T&C PDF is a public legal document; showing it before identity confirmation is correct and intentional |
| T-17-02-SC | Tampering | npm/pip/cargo installs | accept | No package installs — QuickLook is first-party Apple framework |
</threat_model>

<verification>
After all tasks:
1. xcodebuild build passes with zero errors
2. TermsAndConditionsScreen.swift exists and contains PDFPreviewView
3. OnboardingView.swift routes .termsAndConditions to TermsAndConditionsScreen
4. Vitamin_G_Terms_and_Conditions.pdf appears in Build Phases → Copy Bundle Resources
5. Manual: launch app in simulator, go through Sign in with Apple (new user path), T&C screen appears with "Before we begin" headline, "Read Terms" button, "I Agree — Continue" CTA
</verification>

<success_criteria>
- AUTH-02: T&C PDF is bundled; tapping "Read Terms" opens it in a QuickLook sheet (via if-let guard, no crash risk); "I Agree — Continue" advances to NameScreen and persists vg_hasAgreedToTerms = true
</success_criteria>

<output>
Create `.planning/phases/17-onboarding-overhaul/17-02-SUMMARY.md` when done.
</output>
