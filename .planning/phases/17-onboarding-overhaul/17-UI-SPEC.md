---
phase: 17
slug: onboarding-overhaul
status: draft
shadcn_initialized: false
preset: none
created: 2026-05-16
revised: 2026-05-16
revision_reason: Typography (Issue 1 & 2) and Spacing blocking issues fixed by gsd-ui-researcher per checker report
---

# Phase 17 — UI Design Contract

> Visual and interaction contract for Phase 17: Onboarding Overhaul.
> Generated by gsd-ui-researcher. Verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none — native SwiftUI / VGTheme |
| Preset | not applicable |
| Component library | SwiftUI system components + VGCapsule, VGTheme, StepBarView |
| Icon library | SF Symbols (system) |
| Font | CormorantGaramond-Regular / CormorantGaramond-SemiBold / CormorantGaramond-Italic (serif); SF Pro (system sans-serif) |

Source: `VGTheme.swift` (codebase), CONTEXT.md D-02

---

## Spacing Scale

Vitamin G uses 4-pt multiples with 28pt horizontal page margins (established throughout codebase).

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4pt | Icon gaps, inline spacing |
| sm | 8pt | Label-to-field gaps, tight element pairs |
| md | 16pt | Default vertical element spacing |
| lg | 24pt | Section breaks, padding after headlines |
| xl | 28pt | Horizontal page margins (all screens) |
| 2xl | 40pt | Spacer before CTAs |
| 3xl | 48pt | Large section separator |

Exceptions:
- Bottom CTA safe area inset: 36pt padding (NameScreen pattern), 44pt on LoginScreen / dark-bg screens
- StepBarView bottom margin: 28pt (from NameScreen pattern)
- Profile card padding: 16pt all sides (LoginScreen)
- Notification mock card horizontal padding: 16pt; bottom offset: 32pt (from NotificationOnboardingScreen)
- Touch targets: minimum 44pt height for all interactive elements

Source: direct codebase read (NameScreen.swift, LoginScreen.swift, NotificationOnboardingScreen.swift)

---

## Typography

All font calls must use `VGTheme.serif()`, `VGTheme.serifItalic()`, or `Font.custom()` directly — never `.largeTitle` / `.title` SwiftUI dynamic type for brand-critical text.

### Type Scale (4 sizes, 2 weights)

| Role | Size | Weight / Font | Line Height |
|------|------|---------------|-------------|
| Display Heading | 42pt | CormorantGaramond-Regular | 1.2 (lineSpacing: 4) |
| CTA Button | 17pt | SF Pro .semibold | n/a |
| Body / Subtitle / Secondary Links | 14pt | SF Pro .light | 1.5 (lineSpacing: 4) |
| Label / Caption / State Indicators | 13pt | SF Pro .semibold (ALL CAPS labels) or .light (inline state text) | n/a |

### Typography Rules

- Display headings: CormorantGaramond-Regular at 42pt; italic emphasis within heading uses CormorantGaramond-Italic at same size in VGTheme.terraSoft
- Field labels: 13pt SF Pro .semibold, ALL CAPS, kerning 1.5, VGTheme.muted
- Body copy: 14pt SF Pro .light, lineSpacing 4, VGTheme.muted on light bg; VGTheme.sand.opacity(0.7) on dark bg
- CTA buttons: 17pt SF Pro .semibold
- Secondary links: 14pt SF Pro .light; ghost/muted links at 13pt SF Pro .light
- Large field input (UsernameScreen, NameScreen): 34pt CormorantGaramond-Regular — this is a component-specific detail for text input fields only, not a scale entry. It does not add a fifth scale size; it uses the CormorantGaramond font (not SF Pro) and applies exclusively inside the input field component.
- Username inline state indicators: 13pt SF Pro .light (Checking..., Available, Taken, Invalid)
- Outlined picker buttons (ProfilePictureScreen, T&CScreen): 13pt SF Pro .semibold

Note: `.regular` is not used. All SF Pro usage is either `.semibold` or `.light`. `.medium` is not used.

Source: direct codebase read (NameScreen.swift, NotificationOnboardingScreen.swift, WelcomeScreen.swift, LoginScreen.swift)

---

## Color

### Palette Reference (from VGTheme.swift)

| Name | Light Hex | Dark Hex | Role |
|------|-----------|----------|------|
| sand | #F2E8D9 | #F2E8D9 | Primary text on dark bg; secondary surface |
| sandLight | #FAF5EE | — | Light screen background |
| sandMid | #E8D9C4 | — | Disabled state fill |
| sandDeep | #D4C4A8 | — | Disabled text |
| clay | #3D2F1E | — | Dark screen background; primary text on light bg |
| clayMid | #5A4232 | — | Dark bg secondary (radial gradient) |
| terra | #C4673A | #FF8A5C (glow) | Primary CTA fill; accent underlines; tab indicators |
| terraSoft | #E8956D | — | Gradient partner; italic heading emphasis |
| sage | #7A9E7E | #95D69C (glow) | Success / available confirmation |
| muted | #9A8A78 | sand@0.42 | Body copy, caption, secondary text |
| warmWhite | #FDFAF6 | — | Card backgrounds on dark surfaces |

### 60 / 30 / 10 Contract

| Role | Token | Light Value | Dark Value | Usage |
|------|-------|-------------|------------|-------|
| Dominant (60%) | VGTheme.background / sandLight | #FAF5EE | #16110C | Full-screen backgrounds (NameScreen, LoginScreen, T&CScreen, UsernameScreen, ProfilePictureScreen) |
| Secondary (30%) | VGTheme.clay / heroBackground | #3D2F1E | #16110C | Dark full-screen bg (WelcomeScreen, NotificationOnboardingScreen, CameraPermissionScreen); cards on light bg use VGTheme.warmWhite |
| Accent (10%) | VGTheme.accentTerra | #C4673A | #FF8A5C | Reserved elements listed below |
| Destructive | VGTheme.terra (role: .destructive) | #C4673A | #FF8A5C | Block and Report actions only |

Accent (terra) reserved for:
1. Primary CTA button fill (all screens)
2. Text field underline (active state)
3. StepBarView filled segment
4. Blinking cursor capsule in name fields
5. Username availability feedback — taken state (VGTheme.terra, paired with VGTheme.sage for available state)
6. "This isn't me" and secondary destructive labels (accent-level emphasis only, not fill)

Source: direct codebase read (VGTheme.swift, NameScreen.swift, NotificationOnboardingScreen.swift, LoginScreen.swift)

---

## Screen-by-Screen Visual Contract

### 1. WelcomeScreen (modified)

**Background:** VGTheme.clay full-screen with RadialGradient (clayMid center, clay edges)
**Animation:** Raining tablets (VGCapsule, terra/sage/gold/purple palette) — keep as-is; `.accessibilityReduceMotion` gate maintained
**Layout change (D-02):** Move "GOALS. GROWTH. COMMUNITY." tagline **above** the app icon block (above the RoundedRectangle "G" icon). Keep font: 13pt SF Pro .light, VGTheme.muted, kerning 2.
**Button area (D-01):** Single Sign in with Apple button only. Remove "Create account" button, remove Google stub button, remove "I'll set this up later" ghost button.
- Sign in with Apple button: `.signInWithAppleButtonStyle(.black)`, maxWidth .infinity, minHeight 54pt, cornerRadius 14pt
- Horizontal padding: 28pt; bottom padding: 44pt

### 2. T&CScreen (new)

**Background:** VGTheme.sandLight (light surface — matches NameScreen pattern)
**Layout:**
- Back arrow (top-left, chevron.left, 18pt .semibold, VGTheme.clay)
- StepBarView(current: 0, total: 6) — step 1 of 6, terra fill
- Headline (42pt CormorantGaramond, VGTheme.clay): "Before we begin"
- Subtitle (14pt SF Pro .light, VGTheme.muted, lineSpacing 4): "Please read and agree to our Terms & Conditions to continue using Vitamin G."
- "Read Terms" button: outlined capsule style (strokeBorder VGTheme.sandMid, VGTheme.clay text, 13pt .semibold) — opens QuickLook sheet modal
- PDF sheet: `.sheet(isPresented:) { PDFPreviewView(url:).ignoresSafeArea() }` with `.presentationDetents([.large])`
- Primary CTA: "I Agree — Continue" (terra fill, warmWhite text, 17pt .semibold, cornerRadius 14pt, full-width, 16pt vertical padding)
- Bottom padding: 36pt

### 3. NameScreen (modified)

**Background:** VGTheme.sandLight (unchanged)
**Pre-fill change (D-04):** Field pre-populated from `appleFullName` via OnboardingViewModel if non-nil. User can edit.
**StepBarView:** Update to StepBarView(current: 1, total: 6)
**All other visual elements:** Unchanged (headline, field style, terra underline, terra CTA)

### 4. UsernameScreen (new)

**Background:** VGTheme.sandLight
**Layout:**
- Back arrow + StepBarView(current: 2, total: 6)
- Headline (42pt CormorantGaramond, VGTheme.clay): "Claim your\nusername"
- Subtitle (14pt SF Pro .light, VGTheme.muted): "Choose a unique handle. This is how others find you."
- Field label: "USERNAME" (13pt SF Pro .semibold, ALL CAPS, kerning 1.5, VGTheme.muted)
- Text field: 34pt CormorantGaramond-Regular, VGTheme.clay, terra underline (2pt height Rectangle) — see Typography note on 34pt component detail
- Inline state indicator (beneath field, 8pt top margin):
  - `.idle` — no indicator shown
  - `.checking` — 13pt SF Pro .light, VGTheme.muted: "Checking..." with ProgressView inline
  - `.available` — 13pt SF Pro .light, VGTheme.accentSage: "✓ Available"
  - `.taken` — 13pt SF Pro .light, VGTheme.accentTerra: "✗ Already taken"
  - `.invalid` — 13pt SF Pro .light, VGTheme.accentTerra: "Only letters, numbers, and underscores"
- Continue button: terra fill when `.available`; VGTheme.sandMid fill + VGTheme.sandDeep text when not `.available` (disabled)
- Bottom padding: 36pt

Source: CONTEXT.md D-07, Specifics section (sage for available, terra for taken)

### 5. ProfilePictureScreen (new)

**Background:** VGTheme.sandLight
**Layout:**
- Back arrow + StepBarView(current: 3, total: 6)
- Headline (42pt CormorantGaramond, VGTheme.clay): "Add a photo"
- Subtitle (14pt SF Pro .light, VGTheme.muted): "Help your community recognize you. You can always change this later."
- Avatar preview: 96pt Circle; placeholder fills with VGTheme.sand + "G" initial (VGTheme.serifItalic(40), VGTheme.clay); if photo selected, shows compressed UIImage
- Two picker buttons (side by side, 8pt gap):
  - "Choose from Library" — outlined button (sandMid border, clay text, 13pt .semibold, cornerRadius 12pt, 16pt vertical padding)
  - "Take Photo" — outlined button (same style)
- "Skip for now" link (below buttons, 16pt top margin): 14pt SF Pro .light, VGTheme.muted, centered
- Continue button (enabled whether photo chosen or skipped): terra fill, 17pt .semibold, cornerRadius 14pt, full-width
- Bottom padding: 36pt

### 6. NotificationOnboardingScreen (existing — unchanged)

**All visual elements preserved exactly.** StepBarView is not shown on this screen (dark bg pattern has no step bar). No changes to layout.

Source: direct codebase read (NotificationOnboardingScreen.swift)

### 7. CameraPermissionScreen (new)

**Background:** VGTheme.clay (dark, full-screen) — mirrors NotificationOnboardingScreen exactly
**Layout (mirror of NotificationOnboardingScreen):**
- RadialGradient (clayMid center, clay edges)
- Mock preview card (same glassmorphism treatment: .ultraThinMaterial.opacity(0.3), Color.white.opacity(0.11), strokeBorder Color.white.opacity(0.14)):
  - App icon: terra-fill RoundedRectangle(cornerRadius: 9), 34x34pt, "G" Georgia-Italic 16pt warmWhite
  - Simulated camera viewfinder: show a cropped landscape photo placeholder card or the mock notification card with camera icon overlay
  - Horizontal padding: 28pt
- Headline (42pt Georgia/CormorantGaramond, VGTheme.sand, lineSpacing 4):
  "Share your\n[italic terra: journey.]"
- Subtitle (14pt SF Pro .light, VGTheme.muted): "Your profile picture and goal photos help your community connect with your progress."
- Primary CTA: "Allow Camera" (VGTheme.sand fill, VGTheme.clay text, 17pt .semibold, cornerRadius 14pt, 16pt vertical padding, full-width)
- Secondary link: "Skip for now" (14pt SF Pro .light, VGTheme.sand.opacity(0.55), full-width, 8pt vertical padding)
- Bottom padding: 12pt with VGTheme.clay background

Source: CONTEXT.md Specifics, AUTH-06, RESEARCH.md Area 4

### 8. CommunityGoalOnboardingScreen (existing — unchanged)

No visual changes. StepBarView total count update is handled internally by that screen if it uses one.

### 9. LoginScreen — Welcome Back (modified)

**Background:** VGTheme.sandLight (unchanged)
**Existing elements:** All current elements preserved ("Good to see you again.", profile card, "Having trouble?", "This isn't me")
**Profile card padding:** 16pt all sides
**New element (D-11):** Sign in with Apple button added above the "Having trouble?" link
- Style: `.signInWithAppleButtonStyle(.black)`, frame maxWidth .infinity, minHeight 54pt, cornerRadius 14pt
- Vertical position: between the profile card and "Having trouble?" link
- Vertical spacing: 24pt above and below the Apple button

### 10. ProfileView — Report / Block (PROF-05)

**Context menu targets (D-13):**
- Avatar `Circle` — `.contextMenu` modifier
- Display name `Text` — `.contextMenu` modifier
- Explicit "Report or Block" `Button` below profile card area

**Context menu items:**
- "Report User" — `Label("Report User", systemImage: "flag")`, no destructive role (opens email, not destructive on device)
- "Block User" — `Label("Block User", systemImage: "slash.circle")`, role: `.destructive`

**Block confirmation alert (D-15):**
- Title: "Block [username]?"
- Message: "They won't appear in your community feed."
- "Block" button: role `.destructive`
- "Cancel" button: role `.cancel`

**Report button styling:**
- Explicit button: 14pt SF Pro .light, VGTheme.terra color (not fill), left-aligned or centered below profile card, padding 16pt top

**Visibility guard:** Context menu and explicit button only appear when viewing another user's profile. If ProfileView is self-only, the executor must locate the public profile view target (see RESEARCH.md Area 9 open question).

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Primary CTA — T&C screen | "I Agree — Continue" |
| Primary CTA — Name screen | "Continue" |
| Primary CTA — Username screen (available) | "Continue" |
| Primary CTA — Profile picture screen | "Continue" |
| Primary CTA — Camera permission screen | "Allow Camera" |
| Primary CTA — Notification screen | "Allow Notifications" |
| Secondary link — Camera screen | "Skip for now" |
| Secondary link — Notification screen | "Maybe later" |
| Secondary link — Profile picture screen | "Skip for now" |
| Welcome Back headline | "Good to see you again." |
| Welcome Back subtext | "Your goals and streaks are right where you left them." |
| Welcome Back profile card | "Continue as [name]" / "Tap to jump back in" |
| T&C screen headline | "Before we begin" |
| T&C screen body | "Please read and agree to our Terms & Conditions to continue using Vitamin G." |
| T&C open PDF button | "Read Terms" |
| Name screen headline | "What should we call you?" |
| Name screen body | "Your name shows on your profile and community posts." |
| Name field label | "DISPLAY NAME" |
| Username screen headline | "Claim your username" |
| Username screen body | "Choose a unique handle. This is how others find you." |
| Username field label | "USERNAME" |
| Username available indicator | "✓ Available" |
| Username taken indicator | "✗ Already taken" |
| Username checking indicator | "Checking..." |
| Username invalid indicator | "Only letters, numbers, and underscores" |
| Profile picture screen headline | "Add a photo" |
| Profile picture screen body | "Help your community recognize you. You can always change this later." |
| Camera permission headline | "Share your journey." (italic "journey." in VGTheme.terraSoft) |
| Camera permission body | "Your profile picture and goal photos help your community connect with your progress." |
| Notification permission headline | "Stay on track, every day." (italic "every day." in VGTheme.terraSoft) |
| Notification permission body | "One daily nudge at the time you choose. No noise — just your reminder to keep going." |
| Block confirmation title | "Block [username]?" |
| Block confirmation message | "They won't appear in your community feed." |
| Block button (destructive) | "Block" |
| Report context menu item | "Report User" |
| Block context menu item | "Block User" |
| Username displacement alert title | "Username taken" |
| Username displacement alert body | "That username was just claimed. Please choose another." |
| Username displacement alert CTA | "Try another" |
| Apple re-auth button (LoginScreen) | SignInWithAppleButton system label (.signIn style) |

Source: REQUIREMENTS.md AUTH-05, AUTH-06; CONTEXT.md Specifics; direct codebase read (NotificationOnboardingScreen.swift)

---

## Interaction Contract

### Username Field Debounce
- Fires check 500ms after user stops typing
- Continue button: disabled until state is `.available`
- Disabled button visual: VGTheme.sandMid fill + VGTheme.sandDeep text
- Enabled button visual: VGTheme.terra fill + VGTheme.warmWhite text

### Profile Picture Step
- "Skip for now" link and Continue button both advance to next step
- Selected photo shown immediately in avatar preview circle before Continue
- No confirmation required for photo selection

### T&C PDF Sheet
- Opens as `.presentationDetents([.large])` full sheet
- User dismisses sheet to return to T&C step (QuickLook native dismiss)
- "I Agree — Continue" button is always enabled (no gate on whether PDF was opened)

Source: CONTEXT.md D-08, D-09 — no PDF-read gate; AUTH-02 requires only acknowledgment

### Apple Sign-In on LoginScreen
- Shown as explicit CTA button (not conditional on credential state)
- On success: calls `onSkip()` which sets `hasCompletedOnboarding = true`
- On failure: inline error text below button (13pt SF Pro .light, VGTheme.terra)

### Block List
- Writes to `UserDefaults.standard` key `vg_blockedUserIDs` (JSON-encoded [String])
- Confirmation alert shown before write
- No undo in Phase 17 UI (deferred to Phase 19 Settings)

### Report Action
- Checks `MFMailComposeViewController.canSendMail()` first
- If true: presents `MFMailComposeViewController` as sheet
- If false: opens `mailto:` URL with pre-encoded subject and body
- Subject: `[Vitamin G] Report User: @{username}`
- Recipients: `support@vitamingapp.com` (planner to confirm actual address)

---

## Component Inventory

| Component | Source | Phase 17 Usage |
|-----------|--------|----------------|
| VGTheme | VGTheme.swift | All screens — color and font tokens |
| VGCapsule | existing | WelcomeScreen tablets (unchanged) |
| StepBarView | NameScreen.swift | All new light-bg onboarding screens |
| SignInWithAppleButton | AuthenticationServices | WelcomeScreen, LoginScreen |
| PDFPreviewView | new (QLPreviewController wrap) | T&CScreen |
| CameraPickerView | new (UIImagePickerController wrap) | ProfilePictureScreen |
| MailComposeView | new (MFMailCompose wrap) | ProfileView Report action |
| PhotosPicker (.photosPicker modifier) | PhotosUI | ProfilePictureScreen library picker |
| BlockListService | new | ProfileView Block action |
| UsernameLookupService | new | UsernameScreen debounce check |

---

## Registry Safety

Not applicable. This is a native Swift/SwiftUI iOS project with no web package registry. All components are either built-in Apple frameworks or new custom Swift files.

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| Apple SDK (AuthenticationServices, PhotosUI, QuickLook, MessageUI) | SignInWithAppleButton, PHPickerViewController, QLPreviewController, MFMailComposeViewController | not required — first-party Apple frameworks |
| Custom VGTheme | VGTheme.swift | not required — in-repo |

---

## Accessibility Contract

- All interactive elements: minimum 44pt touch target height (enforced via `.padding(.vertical, 16)` on full-width buttons, or 54pt minHeight for Apple Sign-In)
- Raining tablets animation: gated on `.accessibilityReduceMotion` (existing — preserve)
- "Skip for now" links: must be reachable by VoiceOver as separate interactive elements (not hidden)
- Context menu items: VoiceOver reads `Label("Report User", systemImage: "flag")` — no additional accessibility label needed
- Username state indicators (✓/✗): use both color AND text symbol to convey state (not color alone — passes WCAG 1.4.1)
- Block confirmation alert: system `Alert` is fully accessible by default

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
