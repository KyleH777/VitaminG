---
phase: 26-analytics-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - VitaminG/VitaminG/VitaminGTests/Phase26AnalyticsViewModelTests.swift
  - VitaminG/VitaminG/VitaminGTests/Phase26CSVExportServiceTests.swift
  - VitaminG/VitaminG/VitaminG/Services/CSVExportService.swift
  - VitaminG/VitaminG/VitaminG/Navigation/AppRoute.swift
autonomous: true
requirements:
  - ANLT-04
  - ANLT-02
  - ANLT-03

must_haves:
  truths:
    - "Phase26CSVExportServiceTests.swift compiles and all 4 test methods pass (GREEN)"
    - "Phase26AnalyticsViewModelTests.swift compiles with 5 stub methods that fail with XCTFail (RED)"
    - "CSVExportService.buildGlobalCSV returns a string whose first line is 'goal_name,date,tier,is_frozen'"
    - "CSVExportService.buildGlobalCSV RFC 4180 escapes goal names containing commas or double-quotes"
    - "CSVExportService.buildGoalCSV exists and compiles"
    - "AppRoute.analytics case exists and compiles"
  artifacts:
    - path: "VitaminG/VitaminG/VitaminGTests/Phase26CSVExportServiceTests.swift"
      provides: "GREEN tests for ANLT-04 CSV service"
      exports: ["Phase26CSVExportServiceTests"]
    - path: "VitaminG/VitaminG/VitaminGTests/Phase26AnalyticsViewModelTests.swift"
      provides: "RED stub tests for ANLT-02 and ANLT-03"
      exports: ["Phase26AnalyticsViewModelTests"]
    - path: "VitaminG/VitaminG/VitaminG/Services/CSVExportService.swift"
      provides: "Pure CSV string builder — no SwiftUI or SwiftData dependency"
      contains: "struct CSVExportService"
    - path: "VitaminG/VitaminG/VitaminG/Navigation/AppRoute.swift"
      provides: "analytics route case for Phase 26 navigation"
      contains: "case analytics"
  key_links:
    - from: "Phase26CSVExportServiceTests.swift"
      to: "CSVExportService.swift"
      via: "@testable import VitaminG"
      pattern: "CSVExportService\\.build"
    - from: "Phase26AnalyticsViewModelTests.swift"
      to: "AnalyticsViewModel.swift (Plan 02)"
      via: "XCTFail stubs — RED until Plan 02 implements AnalyticsViewModel"
      pattern: "XCTFail\\("
---

<objective>
Wave 0 test scaffolding + CSVExportService + AppRoute.analytics.

Purpose: Establish the test contract before any view or ViewModel code. CSVExportService is a pure function struct with zero SwiftUI/SwiftData dependency — its tests can be GREEN immediately. AnalyticsViewModel tests are RED stubs that turn GREEN in Plan 02. AppRoute.analytics is needed by Plans 02 and 03.

Output: Phase26CSVExportServiceTests.swift (GREEN, 4 tests), Phase26AnalyticsViewModelTests.swift (RED stubs, 5 tests), CSVExportService.swift (pure struct), AppRoute.swift (analytics case added).
</objective>

<execution_context>
@/Users/kyleharrington/Desktop/AI/Vitamin G/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kyleharrington/Desktop/AI/Vitamin G/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/kyleharrington/Desktop/AI/Vitamin G/.planning/ROADMAP.md
@/Users/kyleharrington/Desktop/AI/Vitamin G/.planning/phases/26-analytics-dashboard/26-CONTEXT.md
@/Users/kyleharrington/Desktop/AI/Vitamin G/.planning/phases/26-analytics-dashboard/26-RESEARCH.md
@/Users/kyleharrington/Desktop/AI/Vitamin G/.planning/phases/26-analytics-dashboard/26-PATTERNS.md
@/Users/kyleharrington/Desktop/AI/Vitamin G/.planning/phases/26-analytics-dashboard/26-VALIDATION.md

<interfaces>
From VitaminG/VitaminG/VitaminG/Navigation/AppRoute.swift (current last case):
  case communityGoals(UserChallenge)     // Phase 15 — UIADD-04, C4
  // Add below: case analytics   // Phase 26 — ANLT-02/03/04

From VitaminG/VitaminG/VitaminG/Services/ConsistencyEngine.swift (pure struct pattern to replicate):
  import Foundation
  struct ConsistencyEngine {
      static func score(events: [CompletionEvent], asOf: Date = .now) -> Int { ... }
  }

From VitaminG/VitaminG/VitaminGTests/NotificationSchedulerPhase25Tests.swift (test scaffold pattern):
  @MainActor
  final class NotificationSchedulerPhase25Tests: XCTestCase {
      private var container: ModelContainer!
      override func setUp() async throws {
          container = try ModelContainerFactory.makeContainer(inMemory: true)
      }
      override func tearDown() async throws {
          container = nil
      }
  }

From VitaminG/VitaminG/VitaminG/Models/SchemaV10.swift (verified field names):
  Goal.creationDate: Date?        // NOT createdAt
  CompletionEvent.completedAt: Date?
  CompletionEvent.tierRawValue: String?
  CompletionEvent.goal: SchemaV10.Goal?

GoalTier raw values (from Goal.swift):
  "Immediate", "Short-Term", "Long-Term", "Life Goal"
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CSVExportService — pure struct with buildGlobalCSV and buildGoalCSV (GREEN)</name>
  <files>VitaminG/VitaminG/VitaminG/Services/CSVExportService.swift, VitaminG/VitaminG/VitaminGTests/Phase26CSVExportServiceTests.swift</files>
  <read_first>
    - VitaminG/VitaminG/VitaminG/Services/ConsistencyEngine.swift — pure struct pattern to replicate exactly (import Foundation only, static methods, no SwiftUI/SwiftData)
    - VitaminG/VitaminG/VitaminGTests/NotificationSchedulerPhase25Tests.swift — test class scaffold with ModelContainerFactory.makeContainer(inMemory: true), setUp/tearDown pattern
    - VitaminG/VitaminG/VitaminG/Models/SchemaV10.swift — verify CompletionEvent and Goal field names
  </read_first>
  <behavior>
    - testCSVHeader: CSVExportService.buildGlobalCSV(events: [], goals: [], frozenDates: []) → result.hasPrefix("goal_name,date,tier,is_frozen") is true
    - testCSVEscaping: goal titled "My, Goal" in event → output row contains "\"My, Goal\"" (RFC 4180 quoted)
    - testIsFrozenColumn: event.completedAt = frozenDate (same calendar day as a date in frozenDates) → row ends with ",true"; event on non-frozen day → row ends with ",false"
    - testSortOrder: two events with completedAt dates day+2 and day+0 → the day+0 row appears before day+2 row in output (date ascending)
  </behavior>
  <action>
    Write Phase26CSVExportServiceTests.swift first with 4 failing tests (testCSVHeader, testCSVEscaping, testIsFrozenColumn, testSortOrder), then implement CSVExportService.swift to make all 4 pass.

    CSVExportService.swift: import Foundation only (no SwiftUI, no SwiftData). Pure struct with all static methods. Docblock: "Pure struct service — no SwiftUI or SwiftData dependency. Builds an in-memory CSV string for export via ShareLink. Columns: goal_name, date, tier, is_frozen. Date format: ISO 8601 YYYY-MM-DD (D-11)."

    Implement two static methods:
    - buildGlobalCSV(events: [CompletionEvent], goals: [Goal], frozenDates: [Date]) -> String
    - buildGoalCSV(goal: Goal, events: [CompletionEvent], frozenDates: [Date]) -> String

    Both methods: header row "goal_name,date,tier,is_frozen" is first line. Use ISO8601DateFormatter with formatOptions = [.withFullDate] for YYYY-MM-DD dates. Sort events by completedAt ascending (distantPast for nil). Skip events where completedAt is nil. Build frozenDaySet as Set of Calendar.current.startOfDay(for:) for each date in frozenDates. Mark is_frozen "true" if event's startOfDay(completedAt) is in frozenDaySet, else "false". buildGoalCSV filters events to only those where event.goal?.persistentModelID == goal.persistentModelID before building rows.

    RFC 4180 csvEscaped extension: private extension String { var csvEscaped: String { let escaped = replacingOccurrences(of: "\"", with: "\"\""); return "\"\(escaped)\"" } }. Apply csvEscaped to goal_name field (event.goal?.title ?? "Unknown") and tier field (event.tierRawValue ?? "unknown"). Join rows with "\n".

    Phase26CSVExportServiceTests.swift: import XCTest, SwiftData, @testable import VitaminG. @MainActor final class Phase26CSVExportServiceTests: XCTestCase. private var container: ModelContainer!. setUp creates ModelContainerFactory.makeContainer(inMemory: true). All 4 test methods are REAL implementations that pass after CSVExportService is written.

    Test data: use ModelContext(container) to create Goal and CompletionEvent instances with insert(). Set goal.title, event.completedAt, event.tierRawValue, and event.goal relationship directly. For testCSVEscaping: goal.title = "My, Goal". For testIsFrozenColumn: create one event whose completedAt date is the same as a frozenDate entry; verify row ends with ",true"; create second event on a different day; verify ",false". For testSortOrder: create two events on day+2 and day+0; verify day+0 appears on line index 1 (after header).
  </action>
  <verify>
    <automated>xcodebuild test -scheme VitaminG -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:VitaminGTests/Phase26CSVExportServiceTests 2>&1 | grep -E "passed|failed|error:"</automated>
  </verify>
  <done>Phase26CSVExportServiceTests: 4 tests pass (GREEN). CSVExportService.swift compiles with no SwiftUI import. buildGlobalCSV on empty input returns exactly "goal_name,date,tier,is_frozen". buildGlobalCSV on a goal named "My, Goal" produces a row containing "\"My, Goal\"".</done>
</task>

<task type="auto">
  <name>Task 2: AppRoute.analytics case + RED AnalyticsViewModel test stubs</name>
  <files>VitaminG/VitaminG/VitaminG/Navigation/AppRoute.swift, VitaminG/VitaminG/VitaminGTests/Phase26AnalyticsViewModelTests.swift</files>
  <read_first>
    - VitaminG/VitaminG/VitaminG/Navigation/AppRoute.swift — read the full file to see current cases and Hashable/Codable conformance before adding the analytics case
    - VitaminG/VitaminG/VitaminGTests/NotificationSchedulerPhase25Tests.swift — test scaffold pattern; read lines 1–40 for class declaration, setUp, tearDown
    - VitaminG/VitaminG/VitaminG/ViewModels/StatsViewModel.swift — read to understand the AnalyticsViewModel shape expected by the stubs (weeklyBuckets, monthlyBuckets, heatmapDataByGoal, allGoals, refresh)
  </read_first>
  <action>
    AppRoute.swift: Add one case after the existing last case (communityGoals): "case analytics   // Phase 26 — ANLT-02/03/04". Do not change any other line. Keep the inline comment format matching existing cases. Confirm the enum is Hashable and the new case (no associated value) needs no special conformance.

    Phase26AnalyticsViewModelTests.swift: @MainActor final class Phase26AnalyticsViewModelTests: XCTestCase. import XCTest, SwiftData, @testable import VitaminG. private var container: ModelContainer!. private var viewModel: AnalyticsViewModel!. setUp: container = try ModelContainerFactory.makeContainer(inMemory: true); viewModel = AnalyticsViewModel(). tearDown: viewModel = nil; container = nil.

    Add 5 stub test methods that each call XCTFail("stub — implement in Plan 02") immediately (so they compile but fail RED):
    - func testWeeklyBuckets() throws — stub
    - func testMonthlyBuckets() throws — stub
    - func testCompletionRateFormula() throws — stub
    - func testHeatmapStartDateFallback() throws — stub
    - func testAllGoalsIncluded() throws — stub

    The file must compile. Since AnalyticsViewModel does not yet exist, declare a minimal placeholder forward-reference at the top of the test file inside a #if compiler guard OR accept that the file will have a compile error until Plan 02 delivers AnalyticsViewModel. If the file cannot compile without AnalyticsViewModel, comment out the private var viewModel line and the viewModel = AnalyticsViewModel() call, and add a comment "// AnalyticsViewModel added in Plan 02 — uncomment after 26-02 completes". The 5 test stubs must still call XCTFail and compile cleanly once the comment is resolved.

    Per the Nyquist requirement: stubs are intentionally RED. The test file must be added to the VitaminGTests target.
  </action>
  <verify>
    <automated>xcodebuild build -scheme VitaminG -destination 'platform=iOS Simulator,name=iPhone 16' 2>&1 | grep -E "error:|Build succeeded"</automated>
  </verify>
  <done>AppRoute.swift contains "case analytics" (grep confirms). Phase26AnalyticsViewModelTests.swift exists in VitaminGTests/ with 5 test methods whose names match exactly: testWeeklyBuckets, testMonthlyBuckets, testCompletionRateFormula, testHeatmapStartDateFallback, testAllGoalsIncluded. Project builds without errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user-entered data → CSV string | Goal titles (user-entered strings) flow through CSVExportService.buildGlobalCSV into an exported CSV; malicious content (formula injection) exits the app boundary when opened in a spreadsheet |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-01 | Tampering | CSVExportService.buildGlobalCSV — goal_name field | mitigate | RFC 4180 escaping via csvEscaped extension: wrap every field in double-quotes, double any embedded double-quotes. Spreadsheet apps do not execute formulas in double-quoted fields. Apply to goal_name and tier fields. |
| T-26-SC | Tampering | npm/pip/cargo installs | accept | No package manager installs in this phase — Apple SDK only. No supply chain risk. |
</threat_model>

<verification>
1. Phase26CSVExportServiceTests: 4 tests pass — run `xcodebuild test -scheme VitaminG -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:VitaminGTests/Phase26CSVExportServiceTests 2>&1 | grep -E "passed|failed"`
2. AppRoute.analytics exists — `grep -n "case analytics" VitaminG/VitaminG/VitaminG/Navigation/AppRoute.swift`
3. Phase26AnalyticsViewModelTests.swift has exactly 5 test method stubs — `grep -c "func test" VitaminG/VitaminG/VitaminGTests/Phase26AnalyticsViewModelTests.swift` returns 5
4. CSVExportService has no SwiftUI import — `grep -v "^//" VitaminG/VitaminG/VitaminG/Services/CSVExportService.swift | grep "import SwiftUI"` returns no output
5. Project builds — `xcodebuild build -scheme VitaminG -destination 'platform=iOS Simulator,name=iPhone 16' 2>&1 | tail -3`
</verification>

<success_criteria>
- CSVExportService.swift compiles with import Foundation only; buildGlobalCSV and buildGoalCSV are static methods
- Phase26CSVExportServiceTests: testCSVHeader, testCSVEscaping, testIsFrozenColumn, testSortOrder all GREEN
- Phase26AnalyticsViewModelTests: 5 stub methods named exactly per VALIDATION.md (testWeeklyBuckets, testMonthlyBuckets, testCompletionRateFormula, testHeatmapStartDateFallback, testAllGoalsIncluded) — RED/XCTFail stubs
- AppRoute.swift contains "case analytics   // Phase 26 — ANLT-02/03/04"
- Project builds without errors or warnings introduced by this plan
</success_criteria>

<output>
Create `.planning/phases/26-analytics-dashboard/26-01-SUMMARY.md` when done.
</output>
