---
phase: 26-analytics-dashboard
plan: 03
type: execute
wave: 3
depends_on:
  - 26-01
  - 26-02
files_modified:
  - VitaminG/VitaminG/VitaminG/Views/AllTimeHeatmapView.swift
  - VitaminG/VitaminG/VitaminG/Views/GoalAllTimeHeatmapView.swift
  - VitaminG/VitaminG/VitaminG/Views/AnalyticsView.swift
  - VitaminG/VitaminG/VitaminG/Views/StatsView.swift
  - VitaminG/VitaminG/VitaminG/Views/ContentView.swift
autonomous: false
requirements:
  - ANLT-02
  - ANLT-03
  - ANLT-04

must_haves:
  truths:
    - "User taps Analytics row at bottom of StatsView and AnalyticsView pushes onto the navigation stack"
    - "AnalyticsView shows a Picker segmented control (Weekly/Monthly) and a Swift Charts BarMark chart that reflects completionRate from AnalyticsViewModel.weeklyBuckets or monthlyBuckets"
    - "AnalyticsView shows a scrollable list of all goals (active + completed) — tapping any goal navigates to GoalAllTimeHeatmapView for that goal"
    - "GoalAllTimeHeatmapView shows AllTimeHeatmapView with LazyHStack that scrolls to the most recent week on appear"
    - "AnalyticsView has a global Export CSV ShareLink that calls CSVExportService.buildGlobalCSV"
    - "GoalAllTimeHeatmapView has a per-goal Export CSV ShareLink that calls CSVExportService.buildGoalCSV"
    - "Frozen days render as blue-tinted ❄️ cells in AllTimeHeatmapView (identical to HeatmapView)"
  artifacts:
    - path: "VitaminG/VitaminG/VitaminG/Views/AllTimeHeatmapView.swift"
      provides: "LazyHStack horizontally scrolling heatmap component"
      contains: "LazyHStack"
    - path: "VitaminG/VitaminG/VitaminG/Views/GoalAllTimeHeatmapView.swift"
      provides: "Per-goal all-time heatmap screen with per-goal CSV ShareLink"
      contains: "ShareLink"
    - path: "VitaminG/VitaminG/VitaminG/Views/AnalyticsView.swift"
      provides: "Analytics dashboard screen — chart + goal list + global CSV ShareLink"
      contains: "BarMark"
    - path: "VitaminG/VitaminG/VitaminG/Views/StatsView.swift"
      provides: "Analytics navigation row at bottom of existing StatsView VStack (D-01)"
      contains: "AppRoute.analytics"
    - path: "VitaminG/VitaminG/VitaminG/Views/ContentView.swift"
      provides: "NavigationDestination case for .analytics route"
      contains: "case .analytics"
  key_links:
    - from: "StatsView.swift"
      to: "AnalyticsView.swift"
      via: "NavigationLink(value: AppRoute.analytics)"
      pattern: "AppRoute\\.analytics"
    - from: "AnalyticsView.swift"
      to: "AnalyticsViewModel.swift"
      via: "@State private var viewModel = AnalyticsViewModel()"
      pattern: "AnalyticsViewModel"
    - from: "AnalyticsView.swift"
      to: "CSVExportService.swift"
      via: "ShareLink(item: CSVExportService.buildGlobalCSV(...))"
      pattern: "CSVExportService\\.buildGlobalCSV"
    - from: "GoalAllTimeHeatmapView.swift"
      to: "AllTimeHeatmapView.swift"
      via: "AllTimeHeatmapView(data: heatmapData, startDate: startDate)"
      pattern: "AllTimeHeatmapView"
---

<objective>
Views + navigation wiring — the integration plan that makes everything visible and interactive.

Purpose: Wire AnalyticsViewModel and CSVExportService (Plans 01 + 02) into SwiftUI views. This plan delivers the user-facing UI: the bar chart with granularity toggle, the all-time goal heatmap, and both CSV export surfaces. All heavy computation is pre-tested; this plan is purely display integration.

Output: AllTimeHeatmapView.swift, GoalAllTimeHeatmapView.swift, AnalyticsView.swift (all new), StatsView.swift (one navigation row added), ContentView.swift (one case added).
</objective>

<execution_context>
@/Users/kyleharrington/Desktop/AI/Vitamin G/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kyleharrington/Desktop/AI/Vitamin G/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/kyleharrington/Desktop/AI/Vitamin G/.planning/ROADMAP.md
@/Users/kyleharrington/Desktop/AI/Vitamin G/.planning/phases/26-analytics-dashboard/26-CONTEXT.md
@/Users/kyleharrington/Desktop/AI/Vitamin G/.planning/phases/26-analytics-dashboard/26-RESEARCH.md
@/Users/kyleharrington/Desktop/AI/Vitamin G/.planning/phases/26-analytics-dashboard/26-PATTERNS.md
@/Users/kyleharrington/Desktop/AI/Vitamin G/.planning/phases/26-analytics-dashboard/26-UI-SPEC.md
@/Users/kyleharrington/Desktop/AI/Vitamin G/.planning/phases/26-analytics-dashboard/26-01-SUMMARY.md
@/Users/kyleharrington/Desktop/AI/Vitamin G/.planning/phases/26-analytics-dashboard/26-02-SUMMARY.md

<interfaces>
From AnalyticsViewModel.swift (Plan 02 output — read before using):
  @MainActor @Observable final class AnalyticsViewModel {
      var weeklyBuckets: [BucketItem] = []
      var monthlyBuckets: [BucketItem] = []
      var heatmapDataByGoal: [PersistentIdentifier: [Date: Int]] = [:]
      var allGoals: [Goal] = []
      func refresh(events: [CompletionEvent], goals: [Goal], frozenDates: [Date] = [])
      func heatmapStartDate(for goal: Goal) -> Date
  }

  struct BucketItem: Identifiable { let id: UUID; let periodStart: Date; let completionRate: Double }
  enum ChartGranularity: String, Hashable { case weekly, monthly }

From CSVExportService.swift (Plan 01 output):
  struct CSVExportService {
      static func buildGlobalCSV(events: [CompletionEvent], goals: [Goal], frozenDates: [Date]) -> String
      static func buildGoalCSV(goal: Goal, events: [CompletionEvent], frozenDates: [Date]) -> String
  }

From HeatmapView.swift (cell color function to copy verbatim):
  private func cellColor(for count: Int) -> Color {
      switch count {
      case -1: return Color.blue.opacity(0.25)
      case 0:  return Color(.systemFill)
      case 1:  return .green.opacity(0.3)
      case 2:  return .green.opacity(0.6)
      default: return .green
      }
  }

From StatsView.swift (current VStack structure to append to):
  VStack(spacing: 20) {
      globalStreakCard
      ConsistencyScoreCard(...)
      tierStreakGrid
      heatmapSection
      // ADD: analyticsNavigationRow  (D-01)
  }

From ContentView.swift (Home tab destination switch to add to):
  switch route {
  case .stats:     StatsView()
  case .wins:      DailyWinsView()
  // ADD: case .analytics: AnalyticsView()
  default:         EmptyView()
  }

From GoalDetailView.swift (BarMark chart pattern, verified at line 453):
  Chart(chartItems) { item in
      BarMark(
          x: .value("Day", item.date, unit: .day),
          y: .value("Count", item.count)
      )
      .foregroundStyle(goal.tier.color)
  }
  .frame(height: 80)
  .chartYAxis(.hidden)

From VGTheme.swift:
  VGTheme.accentTerra  // use for bar chart foreground and Export button tint
  VGTheme.accentPurple // secondary accent (optional gradient)

From ProfileView.swift (ShareLink pattern, verified at line 472):
  ShareLink(item: url, subject: Text("..."), message: Text("...")) {
      Label("...", systemImage: "square.and.arrow.up")
          .font(.body.weight(.semibold)).fontDesign(.rounded)
  }
  .buttonStyle(.borderedProminent)
  .tint(VGTheme.terra)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: AllTimeHeatmapView + GoalAllTimeHeatmapView</name>
  <files>VitaminG/VitaminG/VitaminG/Views/AllTimeHeatmapView.swift, VitaminG/VitaminG/VitaminG/Views/GoalAllTimeHeatmapView.swift</files>
  <read_first>
    - VitaminG/VitaminG/VitaminG/Views/HeatmapView.swift — read the entire file; copy cellColor(for:) verbatim; copy the ZStack cell structure verbatim; understand the data: [Date: Int] parameter shape
    - VitaminG/VitaminG/VitaminG/Views/StatsView.swift — read the section card layout pattern (ZStack + RoundedRectangle wrapping) to apply to GoalAllTimeHeatmapView screen scaffold
    - VitaminG/VitaminG/VitaminG/ViewModels/AnalyticsViewModel.swift — read heatmapStartDate(for:) signature to confirm parameter type
    - VitaminG/VitaminG/VitaminG/Services/StreakFreezeService.swift — read frozenDates property to understand what GoalAllTimeHeatmapView must pass to CSVExportService.buildGoalCSV
  </read_first>
  <action>
    AllTimeHeatmapView.swift: pure display component. Import SwiftUI only. Struct parameters: let data: [Date: Int] (pre-built by AnalyticsViewModel), let startDate: Date (nil-fallback already applied by caller).

    Implement a private computed property weeks: [[Date]] that builds columns from startDate to today:
    - calendar = Calendar.current, today = calendar.startOfDay(for: Date())
    - weekStart = calendar.dateInterval(of: .weekOfYear, for: startDate)?.start ?? startDate
    - Loop: append (0..<7).compactMap { calendar.date(byAdding: .day, value: $0, to: weekStart) }, advance weekStart by 1 weekOfYear, stop when weekStart > today
    - Each inner array is one column (one week of up to 7 days)

    Body: ScrollViewReader wrapping a horizontal ScrollView (showsIndicators: false). Inside: LazyHStack(alignment: .top, spacing: 3). ForEach(Array(weeks.enumerated()), id: \.offset) { index, week in VStack(spacing: 3) { ForEach(week, id: \.self) { day in ... } }.id(index) }. Each day cell: if day > today, render Color.clear.frame(width:12, height:12) (do not show future cells). Otherwise render the ZStack cell: RoundedRectangle(cornerRadius: 2).fill(cellColor(for: data[day] ?? 0)).frame(width: 12, height: 12); if data[day] == -1, overlay Image(systemName: "snowflake").font(.system(size: 7)).foregroundStyle(Color.blue).accessibilityLabel("Streak freeze"). Add .padding(.horizontal, 8) to LazyHStack. On .onAppear: proxy.scrollTo(weeks.count - 1, anchor: .trailing).

    Copy cellColor(for count: Int) -> Color verbatim from HeatmapView.swift (switch on -1/0/1/2/default). Mark private.

    GoalAllTimeHeatmapView.swift: screen scaffold wrapping AllTimeHeatmapView. Import SwiftUI, SwiftData. Parameters: let goal: Goal, let heatmapData: [Date: Int], let frozenDates: [Date]. Also declare @Query private var events: [CompletionEvent] (for per-goal CSV — filter inside buildGoalCSV). Private var startDate: Date { goal.creationDate ?? goal.completionEvents?.compactMap(\.completedAt).min() ?? Calendar.current.date(byAdding: .day, value: -90, to: Date())! }.

    Body: NavigationStack-compatible screen. Use .navigationTitle(goal.title ?? "Goal").navigationBarTitleDisplayMode(.large). ScrollView with VStack(alignment: .leading, spacing: 20) { AllTimeHeatmapView(data: heatmapData, startDate: startDate); perGoalExportButton }. Use the card/section layout from StatsView (ZStack + RoundedRectangle(cornerRadius: 16) wrapping AllTimeHeatmapView).

    perGoalExportButton: a private computed var. Compute dateStamp = Date().formatted(.iso8601.year().month().day()). Return ShareLink(item: CSVExportService.buildGoalCSV(goal: goal, events: events.filter { $0.goal?.persistentModelID == goal.persistentModelID }, frozenDates: frozenDates), preview: SharePreview("vitamin-g-\(goal.title ?? "goal")-\(dateStamp).csv", image: Image(systemName: "doc.text"))) { Label("Export CSV", systemImage: "square.and.arrow.up").font(.body.weight(.semibold)).fontDesign(.rounded).frame(maxWidth: .infinity) }.buttonStyle(.borderedProminent).tint(VGTheme.accentTerra).

    Note on per-goal CSV and is_frozen: only real CompletionEvents are queried; pass frozenDates for the is_frozen column — consistent with open question resolution from RESEARCH.md (include is_frozen column using global frozenDates set, only mark true if event's day is in the frozen set).
  </action>
  <verify>
    <automated>xcodebuild build -scheme VitaminG -destination 'platform=iOS Simulator,name=iPhone 16' 2>&1 | grep -E "error:|Build succeeded"</automated>
  </verify>
  <done>AllTimeHeatmapView.swift and GoalAllTimeHeatmapView.swift compile without errors. AllTimeHeatmapView uses LazyHStack (grep confirms). GoalAllTimeHeatmapView contains ShareLink calling CSVExportService.buildGoalCSV (grep confirms). cellColor(for:) is private and verbatim-copied from HeatmapView.swift.</done>
</task>

<task type="auto">
  <name>Task 2: AnalyticsView + StatsView nav row + ContentView destination case</name>
  <files>VitaminG/VitaminG/VitaminG/Views/AnalyticsView.swift, VitaminG/VitaminG/VitaminG/Views/StatsView.swift, VitaminG/VitaminG/VitaminG/Views/ContentView.swift</files>
  <read_first>
    - VitaminG/VitaminG/VitaminG/Views/StatsView.swift — read the entire file; locate the ScrollView VStack closing brace to add analyticsNavigationRow; read the existing NavigationLink patterns and @Query declarations
    - VitaminG/VitaminG/VitaminG/Views/ContentView.swift — read the entire file; locate the Home tab navigationDestination(for: AppRoute.self) switch block (confirmed lines 14–21 area); add case .analytics: before the default case
    - VitaminG/VitaminG/VitaminG/Views/GoalDetailView.swift — read lines 449–475 for the BarMark Chart pattern with chartYAxis and chartXAxis modifiers to adapt for AnalyticsView
    - VitaminG/VitaminG/VitaminG/Views/ProfileView.swift — read lines 470–482 for the ShareLink button pattern with .borderedProminent style
    - VitaminG/VitaminG/VitaminG/ViewModels/AnalyticsViewModel.swift — confirm heatmapDataByGoal key type (PersistentIdentifier or Int hash) to correctly look up data[goal.persistentModelID]
  </read_first>
  <action>
    AnalyticsView.swift: import SwiftUI, SwiftData, Charts. Own @Query declarations (do NOT accept passed arrays per established convention): @Query private var events: [CompletionEvent]; @Query private var goals: [Goal]. @State private var viewModel = AnalyticsViewModel(); @State private var freezeService = StreakFreezeService(); @State private var granularity: ChartGranularity = .weekly.

    Navigation: .navigationTitle("Analytics").navigationBarTitleDisplayMode(.large) — per D-02.
    Refresh wiring: .onAppear { viewModel.refresh(events: events, goals: goals, frozenDates: freezeService.frozenDates) } plus .onChange(of: events.count) { viewModel.refresh(...) } plus .onChange(of: goals.count) { viewModel.refresh(...) }.

    Body: ScrollView { VStack(spacing: 20) { chartSection; goalListSection; globalExportButton }.padding(.horizontal, 16).padding(.vertical, 20) }.background(Color(UIColor.systemGroupedBackground)).

    chartSection (private computed var): ZStack + RoundedRectangle(cornerRadius: 16) card wrapping VStack(alignment: .leading, spacing: 12) {
      Text("Completion Rate").font(.title3.weight(.semibold)).padding(.horizontal, 4)
      Picker("Granularity", selection: $granularity) { Text("Weekly").tag(ChartGranularity.weekly); Text("Monthly").tag(ChartGranularity.monthly) }.pickerStyle(.segmented)
      Chart(granularity == .weekly ? viewModel.weeklyBuckets : viewModel.monthlyBuckets) { item in
        BarMark(x: .value("Period", item.periodStart, unit: granularity == .weekly ? .weekOfYear : .month), y: .value("Rate", item.completionRate))
        .foregroundStyle(VGTheme.accentTerra)
      }
      .chartYAxis { AxisMarks(format: .percent) }
      .frame(height: 180)
      .accessibilityLabel("Completion rate trends bar chart")
    }.padding(16)

    goalListSection (private computed var): ZStack + RoundedRectangle(cornerRadius: 16) card wrapping VStack(alignment: .leading, spacing: 12) {
      Text("Goals").font(.title3.weight(.semibold)).padding(.horizontal, 4)
      ForEach(viewModel.allGoals) { goal in
        NavigationLink(destination: GoalAllTimeHeatmapView(
          goal: goal,
          heatmapData: viewModel.heatmapDataByGoal[goal.persistentModelID] ?? [:],
          frozenDates: freezeService.frozenDates
        )) {
          HStack { Text(goal.title ?? "Untitled"); Spacer(); Image(systemName: "chevron.right").foregroundStyle(.secondary) }
          .padding(.vertical, 8)
        }
        .buttonStyle(.plain)
        if goal.persistentModelID != viewModel.allGoals.last?.persistentModelID { Divider() }
      }
    }.padding(16)

    globalExportButton (private computed var): compute dateStamp = Date().formatted(.iso8601.year().month().day()). ShareLink(item: CSVExportService.buildGlobalCSV(events: Array(events), goals: Array(goals), frozenDates: freezeService.frozenDates), preview: SharePreview("vitamin-g-history-\(dateStamp).csv", image: Image(systemName: "doc.text"))) { Label("Export CSV", systemImage: "square.and.arrow.up").font(.body.weight(.semibold)).fontDesign(.rounded).frame(maxWidth: .infinity) }.buttonStyle(.borderedProminent).tint(VGTheme.accentTerra).

    StatsView.swift: add private computed var analyticsNavigationRow: some View following the chevron-row pattern from PATTERNS.md. Add analyticsNavigationRow to the existing VStack(spacing: 20) after heatmapSection. Do NOT change any existing card or property. The NavigationLink uses value: AppRoute.analytics.

    ContentView.swift: in the Home tab's navigationDestination(for: AppRoute.self) switch block, add "case .analytics: AnalyticsView()" before the default case. Do not change any other routing logic.
  </action>
  <verify>
    <automated>xcodebuild build -scheme VitaminG -destination 'platform=iOS Simulator,name=iPhone 16' 2>&1 | grep -E "error:|Build succeeded"</automated>
  </verify>
  <done>Project builds without errors. grep confirms: AnalyticsView.swift contains "BarMark"; StatsView.swift contains "AppRoute.analytics"; ContentView.swift contains "case .analytics: AnalyticsView()"; AnalyticsView.swift contains "CSVExportService.buildGlobalCSV"; AnalyticsView.swift contains "LazyHStack" (via AllTimeHeatmapView in goal navigation destinations).</done>
</task>

<task type="checkpoint:human-verify">
  <what-built>
    AnalyticsView accessible from StatsView navigation row. Bar chart with Weekly/Monthly segmented control. Goal list showing all goals. Per-goal all-time heatmap via GoalAllTimeHeatmapView with LazyHStack horizontal scroll. Two CSV export buttons (global on AnalyticsView, per-goal on GoalAllTimeHeatmapView). Frozen days shown as blue ❄️ cells.
  </what-built>
  <how-to-verify>
    1. Build and run VitaminG on iPhone 16 Simulator.
    2. Navigate to Home tab → tap the Stats navigation link → scroll to the bottom of StatsView → tap "Analytics". Verify AnalyticsView loads with title "Analytics".
    3. Verify the bar chart renders with at least one bar (if you have any CompletionEvent data). Toggle Weekly/Monthly segmented control — verify chart updates.
    4. Verify a list of goals appears below the chart. Tap any goal. Verify GoalAllTimeHeatmapView pushes, shows the goal title as navigation title, and the heatmap loads.
    5. On GoalAllTimeHeatmapView: verify the heatmap scrolls horizontally without stutter. Verify the view scrolls to the most recent week on appear (most recent column visible without manual scroll).
    6. Verify the "Export CSV" button is visible on GoalAllTimeHeatmapView. Tap it — verify the iOS share sheet appears.
    7. Return to AnalyticsView. Verify the global "Export CSV" button is visible. Tap it — verify the iOS share sheet appears.
    8. If you have a goal with a frozen day: verify the frozen day shows a blue ❄️ cell in the heatmap.
  </how-to-verify>
  <resume-signal>Type "approved" if navigation, chart, heatmap, and both export buttons all work correctly. Or describe any issues observed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user-entered data → CSV → share sheet | Goal names flow through ShareLink into the system share sheet; same RFC 4180 escaping from Plan 01 applies at the service layer |
| SwiftData query → UI display | CompletionEvent and Goal data rendered in views; no external input crosses a trust boundary here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-03 | Tampering | ShareLink item — goal_name in CSV passed to system share sheet | mitigate | CSVExportService.buildGlobalCSV and buildGoalCSV apply RFC 4180 escaping (csvEscaped) to goal_name and tier fields. Escaping is tested in Phase26CSVExportServiceTests.testCSVEscaping (GREEN from Plan 01). No additional mitigation needed at the view layer. |
| T-26-04 | Information Disclosure | AnalyticsView @Query results visible in any view hierarchy | accept | Data is the user's own local SwiftData records. No external transmission in this plan. ShareLink only fires on explicit user tap. |
| T-26-SC | Tampering | npm/pip/cargo installs | accept | No package manager installs in this phase. Apple SDK only. |
</threat_model>

<verification>
1. Build passes: `xcodebuild build -scheme VitaminG -destination 'platform=iOS Simulator,name=iPhone 16' 2>&1 | tail -3`
2. BarMark in AnalyticsView: `grep -n "BarMark" VitaminG/VitaminG/VitaminG/Views/AnalyticsView.swift`
3. LazyHStack in AllTimeHeatmapView: `grep -n "LazyHStack" VitaminG/VitaminG/VitaminG/Views/AllTimeHeatmapView.swift`
4. Analytics route in StatsView: `grep -n "AppRoute.analytics" VitaminG/VitaminG/VitaminG/Views/StatsView.swift`
5. Analytics destination in ContentView: `grep -n "case .analytics" VitaminG/VitaminG/VitaminG/Views/ContentView.swift`
6. Global CSV export in AnalyticsView: `grep -n "buildGlobalCSV" VitaminG/VitaminG/VitaminG/Views/AnalyticsView.swift`
7. Per-goal CSV export in GoalAllTimeHeatmapView: `grep -n "buildGoalCSV" VitaminG/VitaminG/VitaminG/Views/GoalAllTimeHeatmapView.swift`
8. All prior tests still GREEN: `xcodebuild test -scheme VitaminG -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:VitaminGTests/Phase26CSVExportServiceTests -only-testing:VitaminGTests/Phase26AnalyticsViewModelTests 2>&1 | grep -E "passed|failed"`
9. Human verification: checkpoint confirmed by user
</verification>

<success_criteria>
- AnalyticsView renders a BarMark chart with ChartGranularity toggle per D-03 and D-04
- Chart Y-axis shows percent format (AxisMarks(format: .percent)) per D-05
- Navigation row at bottom of StatsView leads to AnalyticsView per D-01
- AnalyticsView.navigationTitle is "Analytics" with .large display mode per D-02
- GoalAllTimeHeatmapView shows AllTimeHeatmapView (LazyHStack) scrolled to most recent week on appear
- Goal list shows all goals (active + completed) per D-08
- Global CSV ShareLink on AnalyticsView exports all events (all goals) per D-09
- Per-goal CSV ShareLink on GoalAllTimeHeatmapView exports that goal's events per D-09
- CSV columns are goal_name, date, tier, is_frozen per D-10
- Date format in CSV is ISO 8601 YYYY-MM-DD per D-11
- All Phase26 tests GREEN (9 total: 4 CSV + 5 ViewModel)
- Human checkpoint: user confirms navigation, chart, heatmap, and export flows all work
</success_criteria>

<output>
Create `.planning/phases/26-analytics-dashboard/26-03-SUMMARY.md` when done.
</output>
