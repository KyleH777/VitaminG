---
phase: 22-public-profile-follow-discover
plan: 3
type: execute
wave: 3
depends_on:
  - 22-02
files_modified:
  - VitaminG/VitaminG/VitaminG/ViewModels/PublicProfileViewModel.swift
  - VitaminG/VitaminG/VitaminG/ViewModels/DiscoverViewModel.swift
  - VitaminG/VitaminG/VitaminG/Views/Components/PublicGoalCard.swift
  - VitaminG/VitaminG/VitaminG/Views/Components/FollowButton.swift
  - VitaminG/VitaminG/VitaminG/Views/Components/CheerButton.swift
  - VitaminG/VitaminG/VitaminG/Views/Explore/Discover/GoalSearchResultCard.swift
  - VitaminG/VitaminG/VitaminG/Views/Explore/Discover/PeopleSearchResultCard.swift
  - VitaminG/VitaminG/VitaminGTests/Phase22PublicProfileViewModelTests.swift
  - VitaminG/VitaminG/VitaminGTests/Phase22DiscoverViewModelTests.swift
autonomous: true
requirements:
  - PROF-01
  - PROF-02
  - PROF-03
  - PROF-04
  - DISC-01
  - DISC-02
  - DISC-04

must_haves:
  truths:
    - "PublicProfileViewModel ViewState.loaded carries a PublicProfileData associated value (D Claude's Discretion)"
    - "PublicProfileViewModel exposes followState (idle/loading/followed), canCheerToday(), onFollow(), onCheer() with ApplauseGate gating"
    - "PublicProfileViewModel.fetchOverride seam returns PublicProfileData (test seam type updated per Pitfall 6)"
    - "DiscoverViewModel debounces text input by 500ms using Task.sleep + cancellation (DISC-01)"
    - "DiscoverViewModel selectedSegment defaults to .goals; switching to .people swaps result set without re-firing the search"
    - "DiscoverViewModel.joinGoal creates a local SwiftData Goal with isPublic=false, marks the goal id as joined to prevent duplicates (Pitfall 5)"
    - "DiscoverViewModel.onFollowPerson(followerUsername:followeeUsername:) is the MVVM seam DiscoverOverlayView uses to follow a person from a PeopleSearchResultCard; backed by writeFollowOverride for testability"
    - "FollowButton renders three visual states (idle/loading/followed) with terra→sage color transition and minimum 44pt touch target"
    - "CheerButton renders available/used states with gold accent and disabled opacity 0.35 (UI-SPEC §3)"
    - "PublicGoalCard renders a 44pt Circle().trim() progress ring with terra arc + 15% terra track and a percentage label, no GoalTier dependency"
    - "GoalSearchResultCard renders ring + title + metadata + Join button (UI-SPEC §4)"
    - "PeopleSearchResultCard renders 40pt AvatarView + username + metadata + FollowButton + tappable row (UI-SPEC §5)"
  artifacts:
    - path: "VitaminG/VitaminG/VitaminG/ViewModels/PublicProfileViewModel.swift"
      provides: "Expanded ViewState + FollowState + cheer gate + follow action"
      contains: "case loaded(profile: PublicProfileData)"
    - path: "VitaminG/VitaminG/VitaminG/ViewModels/DiscoverViewModel.swift"
      provides: "Debounced search ViewModel with Goals/People segment + join action"
      contains: "@Observable final class DiscoverViewModel"
    - path: "VitaminG/VitaminG/VitaminG/Views/Components/PublicGoalCard.swift"
      provides: "44pt progress ring card for PublicProfileView goals list"
      contains: "struct PublicGoalCard"
    - path: "VitaminG/VitaminG/VitaminG/Views/Components/FollowButton.swift"
      provides: "Three-state pill button (idle/loading/followed)"
      contains: "struct FollowButton"
    - path: "VitaminG/VitaminG/VitaminG/Views/Components/CheerButton.swift"
      provides: "Gold-tinted cheer button with daily gate disable + floating animation hook"
      contains: "struct CheerButton"
    - path: "VitaminG/VitaminG/VitaminG/Views/Explore/Discover/GoalSearchResultCard.swift"
      provides: "Compact goal result card with Join action"
      contains: "struct GoalSearchResultCard"
    - path: "VitaminG/VitaminG/VitaminG/Views/Explore/Discover/PeopleSearchResultCard.swift"
      provides: "Person result card with FollowButton + tap-to-open profile"
      contains: "struct PeopleSearchResultCard"
  key_links:
    - from: "VitaminG/VitaminG/VitaminG/ViewModels/PublicProfileViewModel.swift"
      to: "VitaminG/VitaminG/VitaminG/Services/ProfileSharingService.swift"
      via: "fetchProfile, fetchFollowState, writeFollow calls"
      pattern: "ProfileSharingService\\.(fetchProfile|fetchFollowState|writeFollow)"
    - from: "VitaminG/VitaminG/VitaminG/ViewModels/PublicProfileViewModel.swift"
      to: "ApplauseGate enum (in CommunityHubViewModel.swift)"
      via: "canApplaud + markApplauseGiven calls"
      pattern: "ApplauseGate\\.(canApplaud|markApplauseGiven)"
    - from: "VitaminG/VitaminG/VitaminG/ViewModels/DiscoverViewModel.swift"
      to: "VitaminG/VitaminG/VitaminG/Services/PublicGoalService.swift"
      via: "searchGoals, searchPeople, incrementParticipantCount calls"
      pattern: "PublicGoalService\\.(searchGoals|searchPeople|incrementParticipantCount)"
    - from: "VitaminG/VitaminG/VitaminG/ViewModels/DiscoverViewModel.swift"
      to: "VitaminG/VitaminG/VitaminG/Services/ProfileSharingService.swift"
      via: "onFollowPerson MVVM seam calls writeFollow (CLAUDE.md MVVM rule: Views must not call services directly)"
      pattern: "ProfileSharingService\\.writeFollow"
---

<objective>
Build Phase 22's behavior layer (ViewModels) and presentation components in one plan: expand PublicProfileViewModel to carry PublicProfileData + follow state + cheer gate, create the new DiscoverViewModel with 500ms debounce + Goals/People segment + Join action, and ship five new SwiftUI components (PublicGoalCard, FollowButton, CheerButton, GoalSearchResultCard, PeopleSearchResultCard) consumed by Plan 05's screen integration.

Purpose: ViewModels and visual components form a coherent unit — components consume VM state and callbacks. Bundling them in one plan avoids cross-plan churn on FollowState placement. Plan 04 runs in parallel touching only app-entry/check-in/profile-edit files with zero overlap with this plan's files.

Output: Compiled VMs and components with Phase22PublicProfileViewModelTests and Phase22DiscoverViewModelTests fully green. UI components render in Xcode Previews without requiring Plan 05's integration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/22-public-profile-follow-discover/22-CONTEXT.md
@.planning/phases/22-public-profile-follow-discover/22-RESEARCH.md
@.planning/phases/22-public-profile-follow-discover/22-PATTERNS.md
@.planning/phases/22-public-profile-follow-discover/22-UI-SPEC.md
@.planning/phases/22-public-profile-follow-discover/22-02-PLAN.md

<interfaces>
<!-- Extracted from codebase. -->

From VitaminG/VitaminG/VitaminG/ViewModels/PublicProfileViewModel.swift (current public surface):
- enum ViewState: Equatable { case loading; case loaded(displayName: String?, avatarColorHex: String?); case error(message: String) }  [must change loaded case]
- @MainActor @Observable final class PublicProfileViewModel
- var state: ViewState = .loading
- var fetchOverride: ((String) async throws -> (String?, String?))? = nil  [must change closure return type to PublicProfileData]
- func fetchProfile(recordID: String)

From VitaminG/VitaminG/VitaminG/ViewModels/CommunityHubViewModel.swift (analog patterns):
- @MainActor @Observable final class declaration style
- enum ApplauseGate { static func canApplaud(recipientUsername:, defaults:); static func markApplauseGiven(to:, defaults:) }
- Test override closure pattern: `var xxxOverride: ((Input) async throws -> Output)? = nil`

From VitaminG/VitaminG/VitaminG/ViewModels/ExploreViewModel.swift (analog for DiscoverViewModel):
- @MainActor @Observable final class
- UserDefaults date gate pattern (vg_explore_gifterDate)
- Private async fetch funcs with try?/swallow pattern

From VitaminG/VitaminG/VitaminG/ViewModels/GoalViewModel.swift:
- func addGoal(input: GoalInput, context: ModelContext) — pattern for joinGoal local creation
- Existing fire-and-forget Task at lines 181–188

From VitaminG/VitaminG/VitaminG/Services/ProfileSharingService.swift (from Plan 02):
- static func fetchProfile(recordID:) async throws -> PublicProfileData
- static func fetchFollowState(followerUsername:followeeUsername:) async throws -> Bool
- static func writeFollow(followerUsername:followeeUsername:) async throws

From VitaminG/VitaminG/VitaminG/Services/PublicGoalService.swift (from Plan 02):
- static func searchGoals(keyword:) async throws -> [DiscoverGoalResult]
- static func searchPeople(usernamePrefix:) async throws -> [DiscoverPersonResult]
- static func incrementParticipantCount(recordName:) async
- static func fetchGoalsForUser(creatorUsername:) async throws -> [PublicGoalItem]

From VitaminG/VitaminG/VitaminG/Services/CommunityService.swift:
- static func writeApplause(giverUsername:recipientUsername:) async throws
- static func fetchApplauseGivenCount(username:) async throws -> Int

From VitaminG/VitaminG/VitaminG/VGTheme.swift (tokens to use):
- VGTheme.accentTerra, .accentSage, .accentGold, .surface, .surface2, .background, .separator, .textPrimary, .textSecondary, .textMuted, .textFaint
- VGTheme.serif(_ size: CGFloat, weight: Font.Weight = .regular) -> Font

From VitaminG/VitaminG/VitaminG/Views/AvatarView.swift:
- AvatarView(displayName: String?, avatarColorHex: String?, photoData: Data?, size: CGFloat)  — pass photoData: nil per D-06

From VitaminG/VitaminG/VitaminG/Views/Community/ApplauseButtonView.swift:
- Float animation mechanic — CheerButton must REUSE this animation by extracting a shared modifier OR by composing ApplauseButtonView with overridden styling. CheerButton uses VGTheme.accentGold not accentTerra.

From VitaminG/VitaminG/VitaminG/Models/CommunityHubModels.swift (from Plan 01):
- PublicProfileData (Equatable, Sendable)
- DiscoverGoalResult, DiscoverPersonResult, PublicGoalItem (Identifiable, Sendable)

Phase 22 new types DiscoverViewModel must declare:
- enum SearchSegment: Hashable { case goals, people }  — exposed for Picker binding

Phase 22 new types PublicProfileViewModel must declare:
- enum FollowState: Equatable { case idle; case loading; case followed }

Tests this plan must satisfy (remove remaining XCTSkip wrappers):
- Phase22PublicProfileViewModelTests.test_followState_initial_isIdle
- Phase22PublicProfileViewModelTests.test_onFollow_transitionsLoadingThenFollowed (use writeFollowOverride seam)
- Phase22PublicProfileViewModelTests.test_canCheerToday_trueOnFreshDate (isolated UserDefaults)
- Phase22PublicProfileViewModelTests.test_canCheerToday_falseAfterMarkApplauseGiven
- Phase22DiscoverViewModelTests.test_onSearchTextChanged_emptyText_clearsResults_immediately
- Phase22DiscoverViewModelTests.test_onSearchTextChanged_nonEmpty_debouncesBy500ms (uses Task.sleep + assertion that override is not called before 500ms)
- Phase22DiscoverViewModelTests.test_selectedSegment_defaultsToGoals
- Phase22DiscoverViewModelTests.test_isJoined_returnsFalseInitially
- Phase22DiscoverViewModelTests.test_joinGoal_insertsLocalGoalAndMarksJoined (in-memory ModelContext for Goal.self)
- Phase22DiscoverViewModelTests.test_joinGoal_isIdempotentOnRepeatTap (second call is a no-op; participant count override called only once)
- Phase22DiscoverViewModelTests.test_onFollowPerson_callsWriteFollowOverride (uses writeFollowOverride seam; verifies the override is invoked exactly once with the supplied follower/followee usernames; no CloudKit)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expand PublicProfileViewModel with PublicProfileData, FollowState, cheer gate</name>
  <files>VitaminG/VitaminG/VitaminG/ViewModels/PublicProfileViewModel.swift, VitaminG/VitaminG/VitaminGTests/Phase22PublicProfileViewModelTests.swift</files>
  <read_first>
    - VitaminG/VitaminG/VitaminG/ViewModels/PublicProfileViewModel.swift (entire current file — preserve fetchProfile call shape; ViewState enum case `loaded` is the only structural change)
    - VitaminG/VitaminG/VitaminG/ViewModels/CommunityHubViewModel.swift (ApplauseGate enum at lines 12–42; canApplaud + markApplauseGiven static methods)
    - VitaminG/VitaminG/VitaminG/Services/ProfileSharingService.swift (Plan 02 — new fetchProfile/fetchFollowState/writeFollow surface)
    - VitaminG/VitaminG/VitaminG/Services/CommunityService.swift (writeApplause + fetchApplauseGivenCount signatures)
    - .planning/phases/22-public-profile-follow-discover/22-PATTERNS.md (PublicProfileViewModel section — exact ViewState/FollowState shape and onCheer/canCheerToday signatures)
    - .planning/phases/22-public-profile-follow-discover/22-RESEARCH.md (Open Question 3 — accept username as init/method parameter, not from @AppStorage in VM)
    - VitaminG/VitaminG/VitaminGTests/Phase22PublicProfileViewModelTests.swift (remaining XCTSkip-wrapped tests to enable)
    - VitaminG/VitaminG/VitaminGTests/Phase21ApplauseDailyGateTests.swift (isolated UserDefaults pattern)
  </read_first>
  <behavior>
    - state: ViewState — old `.loaded(displayName: String?, avatarColorHex: String?)` is replaced by `.loaded(profile: PublicProfileData)`. fetchProfile call path constructs PublicProfileData via ProfileSharingService.fetchProfile + CommunityService.fetchApplauseGivenCount (parallel async let).
    - followState: FollowState defaults to .idle. fetchProfile completion path also calls ProfileSharingService.fetchFollowState (when myUsername and targetUsername known) and sets followState = .followed on true.
    - onFollow(followerUsername: targetUsername:): if followState != .idle, return (idempotent). Sets followState = .loading. Calls ProfileSharingService.writeFollow (or writeFollowOverride seam). On success: followState = .followed with spring animation. On failure: followState = .idle, followError = "Couldn't follow right now. Tap to retry." (auto-clears after 3s via Task.sleep).
    - canCheerToday(recipientUsername:, defaults: UserDefaults = .standard) -> Bool: delegates to ApplauseGate.canApplaud.
    - onCheer(recipientUsername:, giverUsername:, defaults: UserDefaults = .standard): guard canCheerToday else return. Calls ApplauseGate.markApplauseGiven(to:). Fires Task { try? await CommunityService.writeApplause(...) } — fire-and-forget.
    - fetchOverride closure type is `((String) async throws -> PublicProfileData)?` (Pitfall 6 — already changed by Plan 02 fetchProfile signature, but the VM property type must also update).
    - New seams for testability: var fetchFollowStateOverride: ((String, String) async throws -> Bool)? = nil; var writeFollowOverride: ((String, String) async throws -> Void)? = nil; var writeApplauseOverride: ((String, String) async throws -> Void)? = nil. Each defaults to nil (production uses the real services).
  </behavior>
  <action>
Edit the existing PublicProfileViewModel.swift. Change `ViewState.loaded(displayName:avatarColorHex:)` case to `case loaded(profile: PublicProfileData)`. Update every existing `case .loaded(...)` switch arm in the file (and in any other file that pattern-matches on this enum — grep the project to confirm only this VM and the Views/PublicProfileView.swift reference it). Update `var fetchOverride: ((String) async throws -> (String?, String?))? = nil` to `var fetchOverride: ((String) async throws -> PublicProfileData)? = nil`.

Modify `fetchProfile(recordID:)` to use the new ProfileSharingService.fetchProfile return type. Inside the async work item, run a parallel async let to also fetch cheersGivenCount via CommunityService.fetchApplauseGivenCount (when the fetched profile has a non-nil username). Assemble a fresh PublicProfileData that has the same fields as the fetched one EXCEPT cheersGivenCount which is the parallel-fetched value. Set state = .loaded(profile: enriched). On error: state = .error(message: "Couldn't load this profile. Check your connection and try again.") (UI-SPEC copy contract).

Add a new top-level enum `enum FollowState: Equatable { case idle; case loading; case followed }` ABOVE the class declaration (siblings to ViewState). Add `var followState: FollowState = .idle` and `var followError: String? = nil` instance properties on the class. Add three override closures for testability: `var fetchFollowStateOverride: ((String, String) async throws -> Bool)? = nil`, `var writeFollowOverride: ((String, String) async throws -> Void)? = nil`, `var writeApplauseOverride: ((String, String) async throws -> Void)? = nil`.

Add `func resolveFollowState(followerUsername:followeeUsername:)` — calls the override or ProfileSharingService.fetchFollowState; on success sets `followState = result ? .followed : .idle`. Add `func onFollow(followerUsername:followeeUsername:)` — guards on `followState != .idle`, sets `followState = .loading`, then fires a Task that calls writeFollowOverride or ProfileSharingService.writeFollow. On success: `followState = .followed` inside a `withAnimation(.spring(response: 0.35, dampingFraction: 0.75))` block. On failure: `followState = .idle`, `followError = "Couldn't follow right now. Tap to retry."`, then schedule Task.sleep(for: .seconds(3)) and clear followError on @MainActor.

Add `func canCheerToday(recipientUsername:, defaults: UserDefaults = .standard) -> Bool` returning `ApplauseGate.canApplaud(recipientUsername: recipientUsername, defaults: defaults)`. Add `func onCheer(recipientUsername:, giverUsername:, defaults: UserDefaults = .standard)` — guards on canCheerToday, calls `ApplauseGate.markApplauseGiven(to: recipientUsername, defaults: defaults)`, then fires `Task { try? await (self.writeApplauseOverride?(giverUsername, recipientUsername) ?? CommunityService.writeApplause(giverUsername: giverUsername, recipientUsername: recipientUsername)) }`.

In Phase22PublicProfileViewModelTests.swift, enable the four remaining XCTSkip-wrapped tests (test_followState_initial_isIdle, test_onFollow_transitionsLoadingThenFollowed, test_canCheerToday_trueOnFreshDate, test_canCheerToday_falseAfterMarkApplauseGiven). Tests use isolated UserDefaults (Phase21 pattern) and override closures (writeFollowOverride seam) to avoid CloudKit.
  </action>
  <verify>
    <automated>xcodebuild test -scheme VitaminG -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:VitaminGTests/Phase22PublicProfileViewModelTests 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - PublicProfileViewModel.swift contains `case loaded(profile: PublicProfileData)` (grep: `grep -F "case loaded(profile:" PublicProfileViewModel.swift` returns at least 1 hit)
    - File contains `enum FollowState` with `case idle`, `case loading`, `case followed` (grep returns 1 enum declaration)
    - File contains `var fetchOverride: ((String) async throws -> PublicProfileData)?` (grep matches)
    - File contains the three override seams `fetchFollowStateOverride`, `writeFollowOverride`, `writeApplauseOverride` (grep -E "fetchFollowStateOverride|writeFollowOverride|writeApplauseOverride" returns 3+ hits)
    - File contains `ApplauseGate.canApplaud` and `ApplauseGate.markApplauseGiven` calls (grep returns 2+ hits)
    - onCheer uses fire-and-forget Task wrapping CommunityService.writeApplause (no `await` at the call site outside a `Task { }` block)
    - PublicProfileView.swift if it pattern-matches on `.loaded(...)` is updated to the new associated value (Plan 05 owns the redesign but at minimum existing match arms must compile; if a Plan 05 dependency would cause a build break, add a transitional `case .loaded(let profile): _ = profile` no-op that Plan 05 will replace)
    - `xcodebuild test -only-testing:VitaminGTests/Phase22PublicProfileViewModelTests` exits 0 with zero XCTSkip calls in the file (grep -c "XCTSkip" returns 0)
    - Build for full scheme exits 0
  </acceptance_criteria>
  <done>PublicProfileViewModel exposes PublicProfileData state, FollowState, cheer gate, and seam-injectable async work. All Phase22PublicProfileViewModelTests are live and green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create DiscoverViewModel with debounce, segment, and join action</name>
  <files>VitaminG/VitaminG/VitaminG/ViewModels/DiscoverViewModel.swift, VitaminG/VitaminG/VitaminGTests/Phase22DiscoverViewModelTests.swift</files>
  <read_first>
    - VitaminG/VitaminG/VitaminG/ViewModels/ExploreViewModel.swift (analog — @MainActor @Observable + UserDefaults gate)
    - VitaminG/VitaminG/VitaminG/ViewModels/CommunityHubViewModel.swift (override closure seam pattern at lines 94–98)
    - VitaminG/VitaminG/VitaminG/ViewModels/GoalViewModel.swift (addGoal(input:context:) pattern for local Goal creation in joinGoal; fire-and-forget Task pattern at lines 181–188)
    - VitaminG/VitaminG/VitaminG/Services/PublicGoalService.swift (Plan 02 — searchGoals/searchPeople/incrementParticipantCount)
    - VitaminG/VitaminG/VitaminG/Models/SchemaV9.swift (Goal initializer signature — what fields are required vs optional with defaults)
    - .planning/phases/22-public-profile-follow-discover/22-PATTERNS.md (DiscoverViewModel section — full structure with override seams)
    - .planning/phases/22-public-profile-follow-discover/22-RESEARCH.md (Pattern 2 — Task.sleep + cancellation; Pitfall 5 — joinedGoalIDs Set dedup)
    - VitaminG/VitaminG/VitaminGTests/Phase22DiscoverViewModelTests.swift (all tests are XCTSkip from Plan 01 — must become live)
  </read_first>
  <behavior>
    - Class: `@MainActor @Observable final class DiscoverViewModel`
    - SearchSegment: nested or sibling `enum SearchSegment: Hashable { case goals, people }`
    - State: `var goalResults: [DiscoverGoalResult] = []`, `var peopleResults: [DiscoverPersonResult] = []`, `var isSearching: Bool = false`, `var searchError: String? = nil`, `var selectedSegment: SearchSegment = .goals`
    - Dedup: `private var joinedGoalIDs: Set<String> = []` (Set rather than [String:Date] because join is terminal in v2.0)
    - Override seams: `var searchGoalsOverride: ((String) async throws -> [DiscoverGoalResult])? = nil`, `var searchPeopleOverride: ((String) async throws -> [DiscoverPersonResult])? = nil`, `var incrementParticipantCountOverride: ((String) async -> Void)? = nil`, `var writeFollowOverride: ((String, String) async throws -> Void)? = nil`
    - Debounce: `private var searchTask: Task<Void, Never>?` — `func onSearchTextChanged(_ newText: String)` cancels prior task; if empty clears results immediately; otherwise sleeps 500ms then calls performSearch.
    - performSearch dispatches based on selectedSegment: calls searchGoalsOverride or PublicGoalService.searchGoals (goals segment), or searchPeopleOverride or PublicGoalService.searchPeople (people segment). On error sets searchError = "Search failed. Check your connection and try again." (UI-SPEC copy).
    - `func setSegment(_ segment: SearchSegment, searchText: String)` — sets selectedSegment, re-runs performSearch with current text (no debounce on segment swap — instant).
    - `func isJoined(goalID: String) -> Bool { joinedGoalIDs.contains(goalID) }`
    - `func joinGoal(_ result: DiscoverGoalResult, tier: GoalTier, context: ModelContext)`: guards `!isJoined(goalID: result.id)`, inserts result.id into joinedGoalIDs, creates a new Goal instance with title=result.title, category=result.category, tierRawValue=tier.rawValue, isPublic=false, creationDate=Date(), context.insert(goal), try? context.save(). Fires Task { await (incrementParticipantCountOverride?(result.id) ?? PublicGoalService.incrementParticipantCount(recordName: result.id)) }.
    - `func onFollowPerson(followerUsername: String, followeeUsername: String)`: MVVM seam called by DiscoverOverlayView when the user taps Follow on a PeopleSearchResultCard. Fires a fire-and-forget Task that calls writeFollowOverride if non-nil, otherwise ProfileSharingService.writeFollow. Test path uses writeFollowOverride to verify the call shape without CloudKit (no UI state on DiscoverViewModel for per-row follow state — that is a documented MVP shortcut; per-row optimistic state lives in PeopleSearchResultCard's followState parameter, which DiscoverOverlayView keeps as .idle in the MVP).
  </behavior>
  <action>
Create the new file VitaminG/VitaminG/VitaminG/ViewModels/DiscoverViewModel.swift. Imports: Foundation, Observation, SwiftUI, SwiftData. Declare `enum SearchSegment: Hashable, Sendable { case goals; case people }` at the top of the file (file-scope, importable elsewhere).

Declare the `@MainActor @Observable final class DiscoverViewModel`. Define every property and method described in <behavior>. The `onSearchTextChanged(_:)` method cancels the previous searchTask, returns early with `goalResults = []; peopleResults = []; isSearching = false; searchError = nil` when input is empty (D Claude's Discretion — no debounce when clearing). Otherwise: `isSearching = true`, then launch a Task that does `try? await Task.sleep(for: .milliseconds(500))`, guards `!Task.isCancelled`, and calls `await performSearch(newText)`.

Implement `private func performSearch(_ text: String) async`: switch on selectedSegment. For .goals: call `searchGoalsOverride?(text) ?? PublicGoalService.searchGoals(keyword: text)`; assign to goalResults on success; isSearching = false. For .people: same shape with searchPeopleOverride + searchPeople. Catch with `searchError = "Search failed. Check your connection and try again."`, isSearching = false. Note: catch only the async-throw error path; cancellation should NOT set error.

Implement `func joinGoal(_ result: DiscoverGoalResult, tier: GoalTier, context: ModelContext)`. Per Pitfall 5: guard `!isJoined(goalID: result.id)`, mark joined BEFORE creating local goal (so a re-tap during the brief window between insert and the count Task is no-op). Create Goal via `Goal()` then assign title=result.title, category=result.category, tierRawValue=tier.rawValue, isPublic=false, creationDate=Date() (verify exact field names by reading SchemaV9.Goal — they must match the SwiftData @Model class definition). Insert into context and try? context.save(). Fire-and-forget Task wraps the participant count call.

Implement `func onFollowPerson(followerUsername: String, followeeUsername: String)` as the MVVM seam DiscoverOverlayView calls when the user taps Follow on a PeopleSearchResultCard (CLAUDE.md MVVM rule: Views must not call services directly). Body: fire-and-forget Task wrapping `try? await (writeFollowOverride?(followerUsername, followeeUsername) ?? ProfileSharingService.writeFollow(followerUsername: followerUsername, followeeUsername: followeeUsername))`. This is non-throwing from the caller's perspective; failures are swallowed (MVP — per-row optimistic state on PeopleSearchResultCard remains the documented limitation; authoritative follow state is read via PublicProfileView tap).

In Phase22DiscoverViewModelTests.swift, enable every previously-skipped test. Add `test_onFollowPerson_callsWriteFollowOverride`: set `viewModel.writeFollowOverride = { follower, followee in callCount += 1; capturedFollower = follower; capturedFollowee = followee }`; call `viewModel.onFollowPerson(followerUsername: "alice", followeeUsername: "bob")`; await fire-and-forget completion (use a small `Task.yield()` or an expectation with 1s timeout); assert callCount == 1, capturedFollower == "alice", capturedFollowee == "bob". Use in-memory ModelContainer via `try ModelContainer(for: Goal.self, configurations: ModelConfiguration(isStoredInMemoryOnly: true))` for joinGoal tests. Use override seams for search, increment, and follow to avoid CloudKit. The debounce test asserts that the override is NOT called before 500ms have elapsed (use Date diff or a mock clock counter) and IS called after 500ms.
  </action>
  <verify>
    <automated>xcodebuild test -scheme VitaminG -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:VitaminGTests/Phase22DiscoverViewModelTests 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - File VitaminG/VitaminG/VitaminG/ViewModels/DiscoverViewModel.swift exists
    - File contains `@MainActor` and `@Observable` macros, `final class DiscoverViewModel`
    - File contains `enum SearchSegment: Hashable` declaration with `case goals` and `case people`
    - File contains `var selectedSegment: SearchSegment = .goals` (default segment)
    - File contains `private var searchTask: Task<Void, Never>?` and `try? await Task.sleep(for: .milliseconds(500))`
    - File contains `private var joinedGoalIDs: Set<String> = []` and `func isJoined(goalID: String) -> Bool`
    - File contains `func joinGoal(_ result: DiscoverGoalResult, tier: GoalTier, context: ModelContext)` — exact signature
    - joinGoal contains `goal.isPublic = false` (D-15)
    - File contains the four override seams (grep -E "searchGoalsOverride|searchPeopleOverride|incrementParticipantCountOverride|writeFollowOverride" returns 4+ hits)
    - File contains `func onFollowPerson(followerUsername: String, followeeUsername: String)` (grep matches the exact signature)
    - File contains `ProfileSharingService.writeFollow` fallback call inside onFollowPerson body (grep returns at least 1 hit)
    - `xcodebuild test -only-testing:VitaminGTests/Phase22DiscoverViewModelTests` exits 0; all 7 tests live including test_onFollowPerson_callsWriteFollowOverride (zero XCTSkip remaining in the file)
    - Full simulator suite still passes: `xcodebuild test -scheme VitaminG -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:VitaminGTests/Phase22*` exits 0
  </acceptance_criteria>
  <done>DiscoverViewModel is the single owner of Discover search state + debounce + segment + join dedup. All Phase22DiscoverViewModelTests are live and green.</done>
</task>

<task type="auto">
  <name>Task 3: Build five SwiftUI components — PublicGoalCard, FollowButton, CheerButton, GoalSearchResultCard, PeopleSearchResultCard</name>
  <files>VitaminG/VitaminG/VitaminG/Views/Components/PublicGoalCard.swift, VitaminG/VitaminG/VitaminG/Views/Components/FollowButton.swift, VitaminG/VitaminG/VitaminG/Views/Components/CheerButton.swift, VitaminG/VitaminG/VitaminG/Views/Explore/Discover/GoalSearchResultCard.swift, VitaminG/VitaminG/VitaminG/Views/Explore/Discover/PeopleSearchResultCard.swift</files>
  <read_first>
    - VitaminG/VitaminG/VitaminG/Views/Components/ProgressRingView.swift (Circle().trim() + rotationEffect + animation + accessibility pattern for PublicGoalCard)
    - VitaminG/VitaminG/VitaminG/Views/Community/ApplauseButtonView.swift (float animation mechanic — extract or reuse for CheerButton)
    - VitaminG/VitaminG/VitaminG/Views/Components/CommunityPostCard.swift (HStack card surface + shadow + corner pattern for GoalSearchResultCard and PeopleSearchResultCard)
    - VitaminG/VitaminG/VitaminG/Views/AvatarView.swift (size: 40 + photoData: nil usage for PeopleSearchResultCard)
    - VitaminG/VitaminG/VitaminG/VGTheme.swift (color tokens — accentTerra, accentSage, accentGold, surface, textPrimary, textMuted; serif font helper)
    - VitaminG/VitaminG/VitaminG/ViewModels/PublicProfileViewModel.swift (FollowState enum — Task 1 declared it; FollowButton consumes this enum)
    - .planning/phases/22-public-profile-follow-discover/22-PATTERNS.md (sections for each component — full Swift bodies provided)
    - .planning/phases/22-public-profile-follow-discover/22-UI-SPEC.md (component specs §1–5 — dimensions, fonts, colors, copy, accessibility)
  </read_first>
  <action>
Create five separate Swift files for the five components. Each is a standalone `struct ComponentName: View` with `let` inputs and closure callbacks (no @State on data — VMs own state). Use only VGTheme tokens for color, never hard-coded hex. Every interactive element gets `.frame(minWidth: 44, minHeight: 44)` for HIG touch targets and an explicit `.accessibilityLabel(...)` per UI-SPEC §Accessibility.

PublicGoalCard: 44pt ring + title (16pt rounded, textPrimary, lineLimit 2) + metadata "{category} · {N} days left" (13pt rounded, textMuted). 16pt corner radius, surface background, shadow(0.04, 6, y:2), padding H:16 V:12. Ring uses Circle().stroke(accentTerra.opacity(0.15), lineWidth: 4) for track, Circle().trim(from: 0, to: progress/100) with accentTerra arc, rotationEffect(-90°), reduced-motion-aware animation. Percentage label centered, size 44*0.22, semibold rounded, accentTerra.

FollowButton: input `state: FollowState`, callback `onTap: () -> Void`. Pill (cornerRadius 20pt), padding H:20 V:8, font 16pt semibold rounded. .idle → accentTerra background + "Follow" label + white foreground. .loading → accentTerra.opacity(0.7) + inline ProgressView(.white) + disabled. .followed → accentSage background + "Following" label + white foreground (terminal — no unfollow in v2.0). Spring animation `.spring(response: 0.35, dampingFraction: 0.75)` on state change for background color and label swap. Accessibility label: state == .followed ? "Following \(username)" : "Follow \(username)".

CheerButton: input `isAvailable: Bool` (derived from canCheerToday by caller), `recipientUsername: String`, callback `onCheer: () -> Void`. VStack with 👏 glyph (or hands.clap.fill SF Symbol tinted accentGold) above 16pt semibold rounded label "Cheer them on today" / "Cheered today". Label color: accentGold (available) / textMuted (used). Disabled state: `.opacity(0.35)` (D-color rule). Float animation: REUSE ApplauseButtonView's floating mechanism — either extract a shared `FloatingApplauseOverlay` view to a new file under Views/Components/ and use it in both ApplauseButtonView and CheerButton, OR compose ApplauseButtonView via parameter injection of the color tint. Prefer extraction. If extraction risks breaking ApplauseButtonView tests, document the alternative in CheerButton's source comment. `.frame(minWidth: 44, minHeight: 44)`. Reduced motion: skip animation, action still fires.

GoalSearchResultCard: input `result: DiscoverGoalResult`, `isJoined: Bool`, `onJoin: () -> Void`. HStack with 32pt ring (stroke 3pt, same color tokens as PublicGoalCard's ring), title 16pt rounded textPrimary lineLimit 1, metadata 13pt rounded textMuted "{@creator · {Category} · {N} people}", Spacer, Join button (13pt semibold rounded, accentTerra fg, accentTerra.opacity(0.12) bg, cornerRadius 8pt, padding H:12 V:8). When isJoined: label "Joined", accentSage tint, disabled. Card: surface background, cornerRadius 12pt, shadow(0.04, 6, y:2), padding H:16 V:12.

PeopleSearchResultCard: input `result: DiscoverPersonResult`, `followState: FollowState`, `onFollow: () -> Void`, `onTap: () -> Void`. HStack with AvatarView(displayName: result.username, avatarColorHex: result.avatarColorHex, photoData: nil, size: 40), username "@{username}" (16pt semibold rounded textPrimary), metadata "{N} goals · {N} cheers given" (13pt rounded textMuted), Spacer, embedded FollowButton(state: followState, onTap: onFollow). Card same surface/corner/shadow/padding as GoalSearchResultCard. Row uses .onTapGesture(perform: onTap) to open PublicProfileView — but the FollowButton must consume its tap and not propagate to the row. Use `.contentShape(Rectangle())` on the row and `.buttonStyle(.plain)` inside the FollowButton wrapper to control hit testing.

Every file includes a `#Preview` block with sample data so designers/devs can verify visuals in Xcode.

No business logic in any component. No async work. No CloudKit calls. Just pure SwiftUI render based on inputs + callbacks.
  </action>
  <verify>
    <automated>xcodebuild build -scheme VitaminG -destination 'platform=iOS Simulator,name=iPhone 16' 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - All 5 files exist at the declared paths
    - Each file contains exactly one `struct ComponentName: View`
    - Every interactive button has `.frame(minWidth: 44, minHeight: 44)` somewhere in its body chain (HIG compliance — grep for "minWidth: 44" returns at least 4 hits across the 5 files; PublicGoalCard is non-interactive, only the four buttons need touch target)
    - PublicGoalCard contains `Circle().trim(` and `rotationEffect(.degrees(-90))` (grep returns hits)
    - FollowButton consumes `FollowState` enum (grep for "FollowState" in FollowButton.swift returns at least 1 hit)
    - FollowButton uses `.spring(response: 0.35` (grep returns 1 hit)
    - CheerButton uses `VGTheme.accentGold` (grep returns hits; does NOT use `VGTheme.accentTerra` for its primary tint — grep -c "accentTerra" CheerButton.swift returns 0)
    - GoalSearchResultCard contains `Button(isJoined ? "Joined" : "Join"` or equivalent conditional label (grep matches "Joined" and "Join")
    - PeopleSearchResultCard uses `AvatarView(` with `photoData: nil` (grep matches "photoData: nil")
    - All five files contain `#Preview` (grep returns 5 hits across the files)
    - No file imports CloudKit, SwiftData services, or PublicGoalService (components are pure presentation — grep -L "import CloudKit\|PublicGoalService\|ProfileSharingService" returns all 5 files)
    - Build exits 0
  </acceptance_criteria>
  <done>Five pure-presentation components compile, render in Xcode Preview, and consume only enum/struct/closure inputs from VMs. Ready for Plan 05 to wire into screens.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| View → ViewModel | View passes raw user input (search text, tap callbacks) — VMs must guard idempotency and sanitize when forwarding to services |
| ViewModel → Service | DiscoverViewModel and PublicProfileViewModel forward strings to CloudKit; sanitization happens at the Service layer (Plan 02) but VMs must still defend against double-fire (Pitfall 5) |
| Component → onTap callbacks | Buttons must not bypass their disabled state (FollowButton disabled while .loading; CheerButton disabled when !canCheerToday) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-03-01 | Tampering | DiscoverViewModel.joinGoal duplicate via rapid taps | mitigate | joinedGoalIDs Set guard (Pitfall 5); mark BEFORE local goal insert; acceptance_criteria includes test_joinGoal_isIdempotentOnRepeatTap |
| T-22-03-02 | Tampering | PublicProfileViewModel.onFollow double-tap during loading | mitigate | `guard followState == .idle else { return }` at function head; followState transitions through .loading immediately before fire-and-forget Task |
| T-22-03-03 | Tampering | PublicProfileViewModel.onCheer bypass via direct ApplauseGate call | accept | T-21-03-01 carry-over (UserDefaults gate is non-cryptographic; v2.0 social trust model); acknowledged in 22-RESEARCH.md Security Domain |
| T-22-03-04 | Information Disclosure | FollowButton accessibilityLabel exposes username | accept | Public profiles are intentionally public; username is already shown in the visual button context; accessibility parity is required |
| T-22-03-05 | Denial of Service | DiscoverViewModel.onSearchTextChanged firing every keystroke | mitigate | 500ms Task.sleep debounce with prior-task cancellation (Pattern 2); acceptance_criteria includes test_onSearchTextChanged_nonEmpty_debouncesBy500ms |
| T-22-03-06 | Repudiation | onCheer fire-and-forget writeApplause failure | accept | Daily UserDefaults gate already marked; subsequent writeApplause failure is logged but not surfaced (matches Phase 21 behavior — cheers are best-effort) |
| T-22-03-07 | Tampering | DiscoverViewModel.onFollowPerson fire-and-forget writeFollow failure | accept | Follow record name deterministic (Plan 02); retry is idempotent server-side; per-row optimistic state is the documented MVP limitation — authoritative follow state surfaces when the user taps into PublicProfileView |
| T-22-03-SC | Tampering | npm/pip/cargo installs | n/a | No new package installs (Apple frameworks only) |
</threat_model>

<verification>
- `xcodebuild test -scheme VitaminG -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:VitaminGTests/Phase22PublicProfileViewModelTests` exits 0; all tests live (0 XCTSkip).
- `xcodebuild test -scheme VitaminG -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:VitaminGTests/Phase22DiscoverViewModelTests` exits 0; all 7 tests live (includes test_onFollowPerson_callsWriteFollowOverride).
- Full simulator suite: `xcodebuild test -scheme VitaminG -destination 'platform=iOS Simulator,name=iPhone 16'` exits 0.
- All five component files build standalone and render in Xcode previews.
</verification>

<success_criteria>
- PublicProfileViewModel's ViewState.loaded carries PublicProfileData; FollowState added; cheer gate wired via ApplauseGate.
- DiscoverViewModel exists with 500ms debounce, Goals/People segment, joinGoal with Set-based dedup.
- Five new SwiftUI components compile and meet UI-SPEC visual contract (colors, fonts, dimensions, accessibility).
- Phase22PublicProfileViewModelTests and Phase22DiscoverViewModelTests fully green with zero XCTSkip remaining.
- No business logic in components; all CloudKit interaction stays in services called from VMs.
</success_criteria>

<output>
Create `.planning/phases/22-public-profile-follow-discover/22-03-SUMMARY.md` when done.
</output>
