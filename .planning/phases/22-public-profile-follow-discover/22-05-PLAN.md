---
phase: 22-public-profile-follow-discover
plan: 5
type: execute
wave: 4
depends_on:
  - 22-03
  - 22-04
files_modified:
  - VitaminG/VitaminG/VitaminG/Views/PublicProfileView.swift
  - VitaminG/VitaminG/VitaminG/Views/Explore/ExploreView.swift
  - VitaminG/VitaminG/VitaminG/Views/ContentView.swift
  - VitaminG/VitaminG/VitaminG/Views/Explore/Discover/DiscoverOverlayView.swift
autonomous: false
requirements:
  - PROF-01
  - PROF-02
  - PROF-03
  - PROF-04
  - DISC-01
  - DISC-02
  - DISC-03
  - DISC-04

must_haves:
  truths:
    - "PublicProfileView renders hero (avatar 80pt + display name + @username + motto) + stats row (streak/goals/cheers) + action row (FollowButton + CheerButton) + 'MY PUBLIC GOALS' list of PublicGoalCard + Report/Block footer (UI-SPEC §PublicProfileView)"
    - "ExploreView.searchable on its NavigationStack ancestor in ContentView injects @Environment(\\.isSearching); ExploreView body branches on (isSearching, searchText.isEmpty) into three mutually exclusive states (D-02, D-03, D-04)"
    - "DiscoverOverlayView shows .segmented Picker (Goals/People) + LazyVStack of result cards with progress / empty / error states (UI-SPEC §DiscoverView)"
    - "When search is active + empty: TrendingChallengesSection renders (DISC-03); when search is active + non-empty: DiscoverOverlayView replaces the 6 ExploreView sections; when search inactive: ExploreView's existing 6 sections render unchanged"
    - "Tapping Join in a Discover result presents a tier-picker confirmationDialog or sheet with four GoalTier options; selection creates a local SwiftData Goal and increments participant count via fire-and-forget DiscoverViewModel.joinGoal"
    - "Tapping a PeopleSearchResultCard row opens PublicProfileView as a sheet; tapping the embedded FollowButton fires onFollow without opening the profile"
    - "Existing PublicProfileView shell (NavigationStack, .navigationTitle Profile, Done toolbar button, block/report alert + MailComposeView) is preserved verbatim — only the body content between Done toolbar and the alerts changes"
  artifacts:
    - path: "VitaminG/VitaminG/VitaminG/Views/PublicProfileView.swift"
      provides: "Redesigned full-card public profile per UI-SPEC §PublicProfileView"
      contains: "PublicGoalCard"
    - path: "VitaminG/VitaminG/VitaminG/Views/Explore/ExploreView.swift"
      provides: "Three-branch body driven by @Environment(\\.isSearching) and searchText state"
      contains: "@Environment(\\.isSearching)"
    - path: "VitaminG/VitaminG/VitaminG/Views/ContentView.swift"
      provides: "Explore tab's NavigationStack receives .searchable modifier"
      contains: ".searchable("
    - path: "VitaminG/VitaminG/VitaminG/Views/Explore/Discover/DiscoverOverlayView.swift"
      provides: "Discover results overlay with segment picker + result lists"
      contains: "Picker(\"Search type\""
  key_links:
    - from: "VitaminG/VitaminG/VitaminG/Views/PublicProfileView.swift"
      to: "VitaminG/VitaminG/VitaminG/ViewModels/PublicProfileViewModel.swift"
      via: "@State viewModel + switch on state ViewState + bind followState + onFollow + onCheer"
      pattern: "viewModel\\.(state|followState|onFollow|onCheer|canCheerToday)"
    - from: "VitaminG/VitaminG/VitaminG/Views/Explore/Discover/DiscoverOverlayView.swift"
      to: "VitaminG/VitaminG/VitaminG/ViewModels/DiscoverViewModel.swift"
      via: "@State viewModel + Picker selection bound to selectedSegment + onChange of searchText"
      pattern: "viewModel\\.(selectedSegment|goalResults|peopleResults|onSearchTextChanged|joinGoal)"
    - from: "VitaminG/VitaminG/VitaminG/Views/ContentView.swift"
      to: "VitaminG/VitaminG/VitaminG/Views/Explore/ExploreView.swift"
      via: ".searchable on the NavigationStack that wraps ExploreView (Pitfall 1 — must be on NavigationStack, not ScrollView)"
      pattern: "\\.searchable\\("
---

<objective>
Integrate Plan 03's components and ViewModels into actual screens: redesign PublicProfileView from Phase 17's minimal stub into the full UI-SPEC card, add .searchable to the Explore tab's NavigationStack in ContentView, and create DiscoverOverlayView so ExploreView branches between normal mode, trending-only mode, and Discover-results mode per D-01–D-04. Final task is a human verification of the live UI on simulator since animation timing and search responsiveness cannot be automated.

Purpose: This is the only plan that wires user-visible behavior end-to-end. After this lands, all eight Phase 22 requirements (PROF-01–04, DISC-01–04) are reachable from the running app.

Output: Three integrated screens (PublicProfileView, ExploreView, DiscoverOverlayView), ContentView wired with .searchable, and a human-confirmed UI checkpoint that the redesigned profile + Discover search + Join flow look and behave per UI-SPEC.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/22-public-profile-follow-discover/22-CONTEXT.md
@.planning/phases/22-public-profile-follow-discover/22-RESEARCH.md
@.planning/phases/22-public-profile-follow-discover/22-PATTERNS.md
@.planning/phases/22-public-profile-follow-discover/22-UI-SPEC.md
@.planning/phases/22-public-profile-follow-discover/22-03-PLAN.md
@.planning/phases/22-public-profile-follow-discover/22-04-PLAN.md

<interfaces>
<!-- Extracted from codebase + Plans 02/03/04. -->

From VitaminG/VitaminG/VitaminG/Views/PublicProfileView.swift (existing shell to preserve verbatim):
- struct PublicProfileView: View
- let recordID: String
- @State private var viewModel = PublicProfileViewModel()
- @Environment(\.dismiss) private var dismiss
- @AppStorage("vg_appleUserID") for report mail context
- showBlockConfirm + showMailCompose @State props
- NavigationStack { content ... .navigationTitle("Profile") ... .navigationBarTitleDisplayMode(.inline) ... .toolbar { Done } }
- .onAppear { viewModel.fetchProfile(recordID: recordID) }
- Existing .alert("Block this user?") + .sheet for MFMailComposeView — both must remain unchanged
- The CURRENT loaded case renders only avatar + display name; Plan 22-05 replaces THIS body content with the full UI-SPEC layout

From VitaminG/VitaminG/VitaminG/Views/Explore/ExploreView.swift:
- struct ExploreView: View
- Existing body: ScrollView { VStack(alignment: .leading, spacing: 24) { 6 sections } } + .background(VGTheme.background.ignoresSafeArea()) + .navigationTitle + .toolbar
- Existing sectionLabel(_:) helper
- Plan 22-05 wraps the existing body content into a `private var existingScrollContent: some View` and branches the new `body` on (isSearching, searchText.isEmpty)

From VitaminG/VitaminG/VitaminG/Views/ContentView.swift (lines 27–33 per 22-PATTERNS.md):
- NavigationStack { ExploreView() .navigationDestination(for: GoalCategory.self) { ... } }  → must add .searchable on this NavigationStack with @State searchText that is forwarded into ExploreView via .environment or a passed-in @Binding

From VitaminG/VitaminG/VitaminG/ViewModels/PublicProfileViewModel.swift (Plan 03):
- enum ViewState: Equatable { case loading; case loaded(profile: PublicProfileData); case error(message:) }
- enum FollowState: Equatable { case idle; case loading; case followed }
- var state: ViewState; var followState: FollowState; var followError: String?
- func fetchProfile(recordID:); func resolveFollowState(followerUsername:followeeUsername:); func onFollow(...); func canCheerToday(...) ; func onCheer(...)
- The current implementation lacks publicGoals — Plan 22-05 adds a `var publicGoals: [PublicGoalItem] = []` property and a fetch call to PublicGoalService.fetchGoalsForUser(creatorUsername:) inside fetchProfile's success path. This is an additive ViewModel change — minor.

From VitaminG/VitaminG/VitaminG/ViewModels/DiscoverViewModel.swift (Plan 03):
- enum SearchSegment: Hashable, Sendable { case goals; case people }
- @MainActor @Observable final class DiscoverViewModel
- var goalResults, peopleResults, isSearching, searchError, selectedSegment, isJoined
- func onSearchTextChanged(_:); func setSegment(_:searchText:); func joinGoal(_:tier:context:)

From VitaminG/VitaminG/VitaminG/Views/Components/PublicGoalCard.swift (Plan 03)
From VitaminG/VitaminG/VitaminG/Views/Components/FollowButton.swift (Plan 03)
From VitaminG/VitaminG/VitaminG/Views/Components/CheerButton.swift (Plan 03)
From VitaminG/VitaminG/VitaminG/Views/Explore/Discover/GoalSearchResultCard.swift (Plan 03)
From VitaminG/VitaminG/VitaminG/Views/Explore/Discover/PeopleSearchResultCard.swift (Plan 03)

From VitaminG/VitaminG/VitaminG/Views/Challenges/ChallengeDiscoveryView.swift:
- struct ChallengeDiscoveryView: View — reused as the TRENDING CHALLENGES section content (DISC-03)
- Constructor pattern per 22-UI-SPEC §6 — likely `ChallengeDiscoveryView(navigationPath: .constant(NavigationPath()))` — verify by reading the file

@AppStorage keys established earlier:
- "vg_appleUserID" — current user's Apple ID (existingRecordID arg for ProfileSharingService)
- "vg_username" — current user's display username (giverUsername for onFollow / onCheer)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redesign PublicProfileView per UI-SPEC §PublicProfileView</title>
  <files>VitaminG/VitaminG/VitaminG/Views/PublicProfileView.swift, VitaminG/VitaminG/VitaminG/ViewModels/PublicProfileViewModel.swift</files>
  <read_first>
    - VitaminG/VitaminG/VitaminG/Views/PublicProfileView.swift (entire current file — must preserve NavigationStack shell, navigationTitle, Done toolbar, alert + MailComposeView)
    - VitaminG/VitaminG/VitaminG/ViewModels/PublicProfileViewModel.swift (Plan 03 — current ViewState + FollowState + cheer gate surface; this task adds a publicGoals property and fetchGoalsForUser call)
    - VitaminG/VitaminG/VitaminG/Views/Components/PublicGoalCard.swift (Plan 03)
    - VitaminG/VitaminG/VitaminG/Views/Components/FollowButton.swift (Plan 03)
    - VitaminG/VitaminG/VitaminG/Views/Components/CheerButton.swift (Plan 03)
    - VitaminG/VitaminG/VitaminG/Views/AvatarView.swift (size: 80 usage)
    - VitaminG/VitaminG/VitaminG/Services/PublicGoalService.swift (Plan 02 — fetchGoalsForUser(creatorUsername:))
    - .planning/phases/22-public-profile-follow-discover/22-UI-SPEC.md §PublicProfileView (full layout, padding, typography for hero/stats/action/goals/footer sections; empty state copy "No public goals yet" / "This user hasn't shared any goals.")
    - .planning/phases/22-public-profile-follow-discover/22-PATTERNS.md (PublicProfileView section — shell to preserve verbatim, sectionLabel helper extraction)
  </read_first>
  <action>
First, expand PublicProfileViewModel: add `var publicGoals: [PublicGoalItem] = []` and `var publicGoalsError: String? = nil` instance properties. Add `var fetchGoalsForUserOverride: ((String) async throws -> [PublicGoalItem])? = nil` test seam. Inside fetchProfile's success path, after the parallel async-let fetch for profile + cheersGivenCount, kick off a third async-let for publicGoals (call fetchGoalsForUserOverride if non-nil, else PublicGoalService.fetchGoalsForUser(creatorUsername: profile.username ?? "")). Assign to viewModel.publicGoals. On error: publicGoalsError = "Couldn't load this user's goals."  (silent UI: the section just shows the empty state instead of an error toast).

Then redesign PublicProfileView.swift. Keep the NavigationStack wrapper, .navigationTitle("Profile"), .navigationBarTitleDisplayMode(.inline), the Done toolbar button (VGTheme.accentTerra confirmationAction), the .onAppear hook, the .alert("Block this user?") block, the .sheet for MFMailComposeView, and all @AppStorage reads. Add `@AppStorage("vg_username") private var myUsername: String = ""` if not already present.

Replace the body content that renders inside the `.loaded(profile:)` switch case with the UI-SPEC §PublicProfileView ScrollView layout:

1. Hero header (padding top: 24pt): AvatarView(displayName: profile.displayName, avatarColorHex: profile.avatarColorHex, photoData: nil, size: 80). Display name in VGTheme.serif(28). "@\(profile.username ?? "user")" in 13pt regular rounded textMuted. Motto (if profile.motto != nil) in 16pt regular rounded textSecondary, lineLimit 3.

2. Stats row (padding H: 24, V: 16): three equal-width cells separated by VGTheme.separator dividers. Each cell stacks a value (VGTheme.serif(20, weight: .semibold)) above a label (13pt regular rounded textMuted). Streak cell: profile.streakLength + "day streak". Goals cell: profile.goalCount + "goals". Cheers cell: profile.cheersGivenCount + "cheers given".

3. Action row (padding H: 24, top: 16, bottom: 24): HStack(spacing: 16) of FollowButton(state: viewModel.followState, onTap: { viewModel.onFollow(followerUsername: myUsername, followeeUsername: profile.username ?? "") }) and CheerButton(isAvailable: viewModel.canCheerToday(recipientUsername: profile.username ?? ""), recipientUsername: profile.username ?? "", onCheer: { viewModel.onCheer(recipientUsername: profile.username ?? "", giverUsername: myUsername) }).

4. If viewModel.followError != nil: inline `Text(viewModel.followError!).font(.system(size: 13)).foregroundStyle(VGTheme.accentTerra).padding(.horizontal, 24)` below the action row.

5. VGTheme.separator divider, padding H: 16.

6. Section "MY PUBLIC GOALS" (sectionLabel helper, padding H: 16, top: 20). Below: LazyVStack(spacing: 12) of PublicGoalCard(goal:) for each item in viewModel.publicGoals. If publicGoals is empty: render UI-SPEC empty state — VStack with "No public goals yet" (16pt semibold rounded) above "This user hasn't shared any goals." (16pt regular rounded textSecondary), padding 24pt.

7. VGTheme.separator divider, padding H: 16.

8. Footer (padding H: 16, top: 16, bottom: 24): "Report or Block" button (13pt regular, VGTheme.accentTerra, opens existing Block/Report contextMenu or alert). "Shared via Vitamin G" (13pt VGTheme.textFaint).

Use the same sectionLabel(_:) helper pattern from ExploreView (text.uppercased(), 13pt semibold, kerning 0.4, textMuted, padding H: 16). Inline-define it as a private func on the view or extract a shared SectionLabelView component — prefer inline for now.

In .onAppear: also call viewModel.resolveFollowState(followerUsername: myUsername, followeeUsername: lookup-fired-by-the-VM) — actual implementation: the VM should kick this off inside fetchProfile after the profile load resolves the username. Since myUsername is a @AppStorage on the View, pass it in via a new method `viewModel.beginLoad(recordID: recordID, myUsername: myUsername)` that combines fetchProfile + resolveFollowState — simpler than two separate calls.

The .loading and .error switch arms remain as they were (ProgressView centered with accentTerra tint; error VStack with exclamationmark.icloud.fill + message text + retry option). Verify these match UI-SPEC §State handling.
  </action>
  <verify>
    <automated>xcodebuild build -scheme VitaminG -destination 'platform=iOS Simulator,name=iPhone 16' 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - PublicProfileView.swift contains `PublicGoalCard(` reference (grep returns at least 1 hit)
    - PublicProfileView.swift contains `FollowButton(state:` reference (grep returns 1 hit)
    - PublicProfileView.swift contains `CheerButton(` reference (grep returns 1 hit)
    - PublicProfileView.swift contains `AvatarView(` with `size: 80` (grep -E "AvatarView.*size:\s*80" returns 1 hit; if not on the same line, verify via inspection)
    - PublicProfileView.swift contains `VGTheme.serif(28)` for display name (grep returns 1 hit)
    - PublicProfileView.swift contains the empty-state copy "No public goals yet" (grep returns 1 hit)
    - PublicProfileView.swift contains "MY PUBLIC GOALS" string (grep returns 1 hit)
    - PublicProfileView.swift PRESERVES the existing block/report code paths: grep for "BlockListService" or "MailComposeView" or "showBlockConfirm" — at least 1 hit (carry-forward from Phase 17 unchanged)
    - PublicProfileView.swift NavigationStack + .navigationTitle("Profile") + Done toolbar button still present (grep matches all three)
    - PublicProfileViewModel.swift now has `var publicGoals: [PublicGoalItem]` property (grep returns 1 hit)
    - PublicProfileViewModel.swift calls PublicGoalService.fetchGoalsForUser somewhere in the fetchProfile path (grep returns 1 hit; or via the fetchGoalsForUserOverride seam if more direct)
    - Build exits 0
    - Full Phase 22 test suite continues to pass: `xcodebuild test -scheme VitaminG -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:VitaminGTests/Phase22*` exits 0
  </acceptance_criteria>
  <done>PublicProfileView renders the full UI-SPEC §PublicProfileView card with avatar/stats/actions/goals/footer. Existing block/report and Done toolbar shells are intact.</done>
</task>

<task type="auto">
  <name>Task 2: Add .searchable to Explore tab's NavigationStack and create DiscoverOverlayView</title>
  <files>VitaminG/VitaminG/VitaminG/Views/ContentView.swift, VitaminG/VitaminG/VitaminG/Views/Explore/ExploreView.swift, VitaminG/VitaminG/VitaminG/Views/Explore/Discover/DiscoverOverlayView.swift</files>
  <read_first>
    - VitaminG/VitaminG/VitaminG/Views/ContentView.swift (entire file — lines 27–33 wrap ExploreView in a NavigationStack; .searchable goes here per Pitfall 1)
    - VitaminG/VitaminG/VitaminG/Views/Explore/ExploreView.swift (entire current body — must be preserved as a private computed var; only the top-level body becomes a 3-branch conditional)
    - VitaminG/VitaminG/VitaminG/Views/Explore/Discover/GoalSearchResultCard.swift (Plan 03)
    - VitaminG/VitaminG/VitaminG/Views/Explore/Discover/PeopleSearchResultCard.swift (Plan 03)
    - VitaminG/VitaminG/VitaminG/ViewModels/DiscoverViewModel.swift (Plan 03)
    - VitaminG/VitaminG/VitaminG/Views/Challenges/ChallengeDiscoveryView.swift (constructor + nav path expectation for DISC-03 reuse)
    - .planning/phases/22-public-profile-follow-discover/22-PATTERNS.md (ExploreView and DiscoverOverlayView sections — exact body branching and segment Picker setup)
    - .planning/phases/22-public-profile-follow-discover/22-RESEARCH.md (Pattern 5 + Pitfall 1 — .searchable MUST be on NavigationStack; Anti-pattern about Binding propagation)
    - .planning/phases/22-public-profile-follow-discover/22-UI-SPEC.md §DiscoverView (segment Picker + result list layout)
  </read_first>
  <action>
ContentView.swift — locate the NavigationStack that wraps `ExploreView()` (around lines 27–33). Add `@State private var exploreSearchText: String = ""` as a property of ContentView. Add `.searchable(text: $exploreSearchText, placement: .navigationBarDrawer(displayMode: .always), prompt: "Search goals or people…")` modifier directly on that NavigationStack (immediately after the `.navigationDestination(for: GoalCategory.self)` block so the modifier chain stays attached to NavigationStack, not the inner view). Pass exploreSearchText into ExploreView via initializer or environment — prefer `ExploreView(searchText: $exploreSearchText)`. If the existing ExploreView constructor cannot take parameters cleanly, use `.environment(\.exploreSearchText, exploreSearchText)` with a custom EnvironmentKey defined alongside ExploreView.

ExploreView.swift — restructure the body without losing content:
1. Move the entire existing ScrollView { VStack { 6 sections } } into a `private var existingScrollContent: some View { ... }` (zero logic changes, just relocation).
2. Add `@Environment(\.isSearching) private var isSearching` property (only works because .searchable is on a NavigationStack ancestor — Pitfall 1).
3. Add `let searchText: String` if passed in via init, or `@Environment(\.exploreSearchText) private var searchText: String` if the env key approach was used. Either way ExploreView must be able to read the current search text without owning the binding.
4. Add `@State private var discoverViewModel = DiscoverViewModel()` so the same VM instance survives across body re-renders while in search mode.
5. Replace the body with:
   ```
   if isSearching && searchText.isEmpty {
       // D-02: trending only
       ScrollView { VStack(alignment: .leading, spacing: 16) {
           sectionLabel("TRENDING CHALLENGES")
           ChallengeDiscoveryView(navigationPath: .constant(NavigationPath()))
       }.padding(.top, 8).padding(.bottom, 24) }
       .background(VGTheme.background.ignoresSafeArea())
   } else if isSearching {
       // D-03: search results overlay
       DiscoverOverlayView(searchText: searchText, viewModel: discoverViewModel)
   } else {
       // D-04: normal Explore
       existingScrollContent
   }
   ```
6. Add `.onChange(of: searchText) { _, newText in discoverViewModel.onSearchTextChanged(newText) }` somewhere on the outer body so debounce kicks off when text changes.
7. Preserve the existing sectionLabel helper, navigationDestination, toolbar.

DiscoverOverlayView.swift (new file at the declared path):
- imports: SwiftUI, SwiftData
- `struct DiscoverOverlayView: View { let searchText: String; @Bindable var viewModel: DiscoverViewModel; @Environment(\.modelContext) private var modelContext; @Environment(\.accessibilityReduceMotion) private var reduceMotion; @State private var presentingTierPickerFor: DiscoverGoalResult? = nil }`
- Body: VStack(spacing: 0) {
    Picker("Search type", selection: $viewModel.selectedSegment) { Text("Goals").tag(SearchSegment.goals); Text("People").tag(SearchSegment.people) }
      .pickerStyle(.segmented)
      .padding(.horizontal, 16).padding(.vertical, 8)
      .onChange(of: viewModel.selectedSegment) { _, _ in viewModel.setSegment(viewModel.selectedSegment, searchText: searchText) }
    ScrollView {
      LazyVStack(spacing: 8) {
        if viewModel.isSearching { ProgressView().tint(VGTheme.accentTerra).padding(.top, 24) }
        else if let err = viewModel.searchError {
          // UI-SPEC error state copy
          VStack(spacing: 8) { Text(err).font(.system(size: 16)).foregroundStyle(VGTheme.textSecondary).multilineTextAlignment(.center).padding(24) }
        } else if viewModel.selectedSegment == .goals {
          if viewModel.goalResults.isEmpty {
            // UI-SPEC empty state: "No goals found" / "Try a different keyword."
            emptyStateView(heading: "No goals found", body: "Try a different keyword.")
          } else {
            ForEach(viewModel.goalResults) { result in
              GoalSearchResultCard(result: result, isJoined: viewModel.isJoined(goalID: result.id), onJoin: { presentingTierPickerFor = result })
            }
          }
        } else {
          if viewModel.peopleResults.isEmpty {
            emptyStateView(heading: "No people found", body: "Try searching by username.")
          } else {
            ForEach(viewModel.peopleResults) { result in
              PeopleSearchResultCard(result: result, followState: .idle /* MVP: per-result follow state requires per-row VM; defer per-row lookup to v3 */, onFollow: { /* fire-and-forget writeFollow via ProfileSharingService */ }, onTap: { /* present PublicProfileView sheet with recordID = result.id */ })
            }
          }
        }
      }
      .animation(reduceMotion ? nil : .easeInOut(duration: 0.2), value: viewModel.selectedSegment)
      .padding(.horizontal, 16).padding(.top, 8)
    }
  }
  .background(VGTheme.background.ignoresSafeArea())
  .confirmationDialog("Which type of goal is this for you?", isPresented: .init(get: { presentingTierPickerFor != nil }, set: { if !$0 { presentingTierPickerFor = nil } }), titleVisibility: .visible) {
    ForEach(GoalTier.allCases, id: \.self) { tier in
      Button(tier.displayName) {
        if let result = presentingTierPickerFor {
          viewModel.joinGoal(result, tier: tier, context: modelContext)
        }
        presentingTierPickerFor = nil
      }
    }
    Button("Cancel", role: .cancel) { presentingTierPickerFor = nil }
  }
  .sheet(item: ... // for PeopleSearchResultCard onTap → PublicProfileView)

For onTap of PeopleSearchResultCard: present `PublicProfileView(recordID: result.id)` as a .sheet bound to an @State item. Use `@State private var presentingProfileFor: DiscoverPersonResult? = nil` and `.sheet(item: $presentingProfileFor) { person in PublicProfileView(recordID: person.id) }`.

For the per-row follow inside PeopleSearchResultCard MVP: pass `followState: .idle` and `onFollow: { Task { try? await ProfileSharingService.writeFollow(followerUsername: myUsername, followeeUsername: result.username) } }` reading myUsername from a new @AppStorage("vg_username"). Per-row optimistic state update is acceptable to defer to a later phase — note this as an explicit MVP shortcut in a `// MVP NOTE:` comment. If per-row follow state freshness is critical for D-02 acceptance, escalate via the human verify checkpoint.

emptyStateView(heading:body:) is a private @ViewBuilder helper that renders VStack { Text(heading).font(.system(size:16, weight:.semibold).fontDesign(.rounded)); Text(body).font(.system(size:16).fontDesign(.rounded)).foregroundStyle(VGTheme.textSecondary) }.padding(24).
  </action>
  <verify>
    <automated>xcodebuild build -scheme VitaminG -destination 'platform=iOS Simulator,name=iPhone 16' 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - ContentView.swift contains `.searchable(text:` modifier (grep returns at least 1 hit)
    - ContentView.swift contains `placement: .navigationBarDrawer` (grep matches)
    - ContentView.swift contains `prompt: "Search goals or people…"` exact string (grep matches; the … is a real horizontal ellipsis or three dots — match UI-SPEC literal)
    - ExploreView.swift contains `@Environment(\\.isSearching)` (grep matches)
    - ExploreView.swift contains `existingScrollContent` private property (grep matches)
    - ExploreView.swift body contains a three-branch conditional referencing `isSearching` and `searchText.isEmpty` (manual inspection if grep is awkward; pattern: `if isSearching && searchText.isEmpty` or similar)
    - ExploreView.swift contains `DiscoverOverlayView(` (grep matches)
    - ExploreView.swift contains `ChallengeDiscoveryView(` (grep matches — DISC-03 trending reuse)
    - DiscoverOverlayView.swift exists at the declared path
    - DiscoverOverlayView.swift contains `.pickerStyle(.segmented)` (grep matches)
    - DiscoverOverlayView.swift contains `GoalSearchResultCard(` and `PeopleSearchResultCard(` (grep returns 2 hits)
    - DiscoverOverlayView.swift contains `viewModel.joinGoal(` call (grep matches; tier-picker dispatch)
    - DiscoverOverlayView.swift contains `.confirmationDialog(` (grep matches — tier picker)
    - DiscoverOverlayView.swift contains `PublicProfileView(recordID:` inside a .sheet block (grep matches — person card tap navigates)
    - DiscoverOverlayView.swift contains empty-state copy "No goals found" and "No people found"
    - Build exits 0
    - Full simulator suite: `xcodebuild test -scheme VitaminG -destination 'platform=iOS Simulator,name=iPhone 16'` exits 0
  </acceptance_criteria>
  <done>Explore tab supports search-as-overlay: empty search shows trending only, typed search shows segmented Goals/People results, blank state shows normal 6-section Explore. PublicProfileView opens from People card tap. Join opens a tier picker that creates a local Goal.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verifies PublicProfileView redesign + Discover search + Join flow on simulator</name>
  <what-built>
    PublicProfileView redesigned with hero card (avatar 80pt, display name in serif, @username, motto), three-cell stats row, Follow + Cheer action row, public goals list, Report/Block footer. Explore tab now has a search bar that branches: blank/inactive shows the original Explore sections; active + empty shows TRENDING CHALLENGES only; active + typed shows Goals/People segmented results. Join opens a tier-picker dialog and adds the public goal to the local list with an incremented participant count. People rows tap to open PublicProfileView.
  </what-built>
  <how-to-verify>
    1. Build and run on iPhone 16 simulator (`xcodebuild` or Xcode play button).
    2. PublicProfileView: open any Phase 21 Community tab user card or seed a profile via the Today's Glimpses carousel. Verify:
       - Avatar (80pt) appears at the top using initials + the user's avatarColorHex (no photo — D-06).
       - Display name renders in serif 28pt; @username below in 13pt textMuted; motto (if set) in 16pt textSecondary maxLines 3.
       - Stats row shows three cells: streak count + "day streak", goals count + "goals", cheers count + "cheers given".
       - FollowButton appears with terra background and "Follow" label; tapping it shows a spring transition to sage background with "Following" label. Tapping again does nothing (terminal state).
       - CheerButton appears with gold tint and "Cheer them on today" label; tapping it fires the floating 👏 animation and disables to "Cheered today" at opacity 0.35.
       - MY PUBLIC GOALS section lists the user's public goals as PublicGoalCard with 44pt terra progress rings + percentage label + title + "{category} · {N} days left". If the user has none, the empty state "No public goals yet" / "This user hasn't shared any goals." appears.
       - "Report or Block" footer button still opens the existing alert / mail compose (Phase 17 carry-forward).
       - Done button in the top-right closes the sheet.
    3. Explore tab search:
       - Tap the Explore tab. The 6 original sections render unchanged.
       - Pull down to reveal the search bar with placeholder "Search goals or people…".
       - Tap into the search bar without typing. The 6 sections collapse; only "TRENDING CHALLENGES" remains.
       - Type a keyword (e.g. "run"). After 500ms, results appear in the Goals segment. Each card shows ring + title + "@creator · category · N people" + Join button.
       - Switch to the "People" segment. Results show 40pt avatar + "@username" + "N goals · N cheers given" + Follow button.
       - Tap a People row (NOT the Follow button). PublicProfileView opens as a sheet.
       - Tap the Follow button on a People row. Confirm it transitions to "Following" without opening the profile.
       - Tap Join on a Goals result. The confirmationDialog "Which type of goal is this for you?" appears with four tier buttons. Pick one. The Join button updates to "Joined" + disabled. Open the Goals tab — the new goal appears in the user's goal list.
       - Clear the search field. The 6 Explore sections return.
    4. Accessibility: enable Reduce Motion in Settings → Accessibility. Verify the CheerButton applause animation is suppressed but the action still fires. The PublicGoalCard ring is filled without animation. The segment swap is instant.
    5. Edge: enter a search query with a special character (e.g. quote mark or emoji). Verify no crash; results either match-or-not without raising an error toast that says "Search failed".
  </how-to-verify>
  <resume-signal>Type "approved" if all 5 sections match the UI-SPEC contract. Otherwise describe specifically which step failed and which UI-SPEC §X expectation was violated.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input (search text) → ViewModel | Untrusted search text passed to DiscoverViewModel.onSearchTextChanged then to CKQuery NSPredicate |
| User input (motto display) → View | Other users' motto strings read from CloudKit may contain malicious content; Plan 02 sanitization protects writes; read-side render must use Text() not WebView (XSS) |
| View → DiscoverViewModel.joinGoal | Tier selection from confirmationDialog must default to a valid GoalTier value |
| PeopleSearchResultCard tap → PublicProfileView | recordID passed to PublicProfileView must be the CloudKit recordID, not an arbitrary string |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-05-01 | Tampering | DiscoverOverlayView search text → NSPredicate | mitigate | NSPredicate(format: "title CONTAINS[cd] %@", keyword) uses parameterized format string (the %@ placeholder); CloudKit's CKQuery escapes the value; no string interpolation in predicates |
| T-22-05-02 | Information Disclosure | Other user's motto rendered with Text() | accept | Plan 02 sanitizes motto on write (InputSanitizer strips <>"' + control chars); reading user's render via Text() is XSS-safe (SwiftUI Text does not interpret HTML); accepted risk |
| T-22-05-03 | Tampering | DiscoverOverlayView Join confirmationDialog tier injection | mitigate | Tier buttons are constructed from GoalTier.allCases enum — a closed set; user cannot inject an arbitrary tier value |
| T-22-05-04 | Spoofing | PeopleSearchResultCard onFollow uses myUsername | accept | myUsername comes from @AppStorage("vg_username") set during onboarding; spoofing requires already-compromised device storage |
| T-22-05-05 | Tampering | ExploreView .searchable on wrong NavigationStack | mitigate | Pitfall 1 — explicitly placed on the NavigationStack in ContentView (not inside ScrollView); human verify Task 3 step 3 confirms isSearching toggle works |
| T-22-05-06 | Denial of Service | DiscoverOverlayView per-row follow writes spam | accept | Follow record name deterministic (Plan 02); duplicate write is idempotent server-side; no rate-limit needed |
| T-22-05-07 | Information Disclosure | Stale follow state shown on PeopleSearchResultCard | accept | MVP shortcut documented in DiscoverOverlayView; per-row follow state freshness deferred to a future phase; tap-to-open profile shows authoritative follow state |
| T-22-05-08 | Repudiation | Block/Report flow regression risk | mitigate | Task 1 acceptance_criteria explicitly require the existing block/report code to remain in PublicProfileView (grep for BlockListService or MailComposeView) — the redesign preserves the Phase 17 moderation surface verbatim |
| T-22-05-SC | Tampering | npm/pip/cargo installs | n/a | No new package installs |
</threat_model>

<verification>
- `xcodebuild build -scheme VitaminG -destination 'platform=iOS Simulator,name=iPhone 16'` exits 0.
- `xcodebuild test -scheme VitaminG -destination 'platform=iOS Simulator,name=iPhone 16'` exits 0 — no regressions.
- Human verify checkpoint Task 3 returns "approved" or specific deviations are addressed in a follow-up plan.
- PROF-05 (Phase 17 block/report) still works on the redesigned PublicProfileView.
</verification>

<success_criteria>
- PublicProfileView renders the UI-SPEC §PublicProfileView card with all five sections (hero, stats, action, goals list, footer).
- Explore tab's NavigationStack hosts .searchable and ExploreView body branches on (isSearching, searchText.isEmpty).
- DiscoverOverlayView shows Goals/People segment with proper empty/error/loading states.
- Join action presents tier-picker and creates local Goal + increments participant count.
- PeopleSearchResultCard row tap opens PublicProfileView; Follow button consumes tap without opening profile.
- Phase 17 block/report still functional on PublicProfileView (regression-free).
- Full simulator test suite green.
- Human verification checkpoint produces explicit "approved" signal.
</success_criteria>

<output>
Create `.planning/phases/22-public-profile-follow-discover/22-05-SUMMARY.md` when done.
</output>
