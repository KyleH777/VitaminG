---
phase: 22-public-profile-follow-discover
plan: 2
type: execute
wave: 2
depends_on:
  - 22-01
files_modified:
  - VitaminG/VitaminG/VitaminG/Services/ProfileSharingService.swift
  - VitaminG/VitaminG/VitaminG/Services/PublicGoalService.swift
autonomous: true
requirements:
  - PROF-01
  - PROF-02
  - PROF-04
  - DISC-01
  - DISC-02
  - DISC-04

must_haves:
  truths:
    - "ProfileSharingService.publishProfile writes streakLength, goalCount, and motto when provided (D-05, D-07)"
    - "ProfileSharingService.fetchProfile returns PublicProfileData struct populated from PublicProfile CKRecord fields"
    - "ProfileSharingService.fetchFollowState returns true for an existing Follow record and false on .unknownItem"
    - "ProfileSharingService.writeFollow is idempotent: writing the same (follower, followee) twice does not create a duplicate"
    - "PublicGoalService.writePublicGoal saves a PublicGoal CKRecord using goal.id.uuidString as recordName and InputSanitizer on every String field (D-09)"
    - "PublicGoalService.deletePublicGoal swallows .unknownItem (D-10)"
    - "PublicGoalService.searchGoals issues a CKQuery with NSPredicate(format: \"title CONTAINS[cd] %@\", keyword) and returns [DiscoverGoalResult]"
    - "PublicGoalService.searchPeople issues a CKQuery on PublicProfile with NSPredicate(format: \"username BEGINSWITH[cd] %@\", prefix) and returns [DiscoverPersonResult]"
    - "PublicGoalService.incrementParticipantCount uses fetch+increment+save with one retry on .serverRecordChanged and swallows all errors (D-16 fire-and-forget)"
    - "PublicGoalService.backfillPublicGoals iterates local goals where isPublic && cloudKitPublicGoalRecordID == nil and writes PublicGoal records (D-11)"
  artifacts:
    - path: "VitaminG/VitaminG/VitaminG/Services/ProfileSharingService.swift"
      provides: "Expanded publishProfile/fetchProfile + new fetchFollowState/writeFollow methods"
      contains: "fetchFollowState"
    - path: "VitaminG/VitaminG/VitaminG/Services/PublicGoalService.swift"
      provides: "New service for PublicGoal CKRecord CRUD + Discover search + participant count"
      contains: "enum PublicGoalService"
  key_links:
    - from: "VitaminG/VitaminG/VitaminG/Services/ProfileSharingService.swift"
      to: "VitaminG/VitaminG/VitaminG/Models/CommunityHubModels.swift"
      via: "fetchProfile returns PublicProfileData struct"
      pattern: "->\\s*PublicProfileData"
    - from: "VitaminG/VitaminG/VitaminG/Services/PublicGoalService.swift"
      to: "VitaminG/VitaminG/VitaminG/Models/CommunityHubModels.swift"
      via: "searchGoals returns [DiscoverGoalResult]; searchPeople returns [DiscoverPersonResult]; fetchGoalsForUser returns [PublicGoalItem]"
      pattern: "DiscoverGoalResult|DiscoverPersonResult|PublicGoalItem"
    - from: "VitaminG/VitaminG/VitaminG/Services/PublicGoalService.swift"
      to: "CloudKit Public DB"
      via: "CKContainer(identifier: containerID).publicCloudDatabase"
      pattern: "CKContainer\\(identifier:\\s*containerID\\)"
---

<objective>
Build the Phase 22 service layer: expand ProfileSharingService with the new publishProfile fields (streakLength, goalCount, motto), the PublicProfileData return type on fetchProfile, and add fetchFollowState + writeFollow methods. Create a new PublicGoalService enum that owns PublicGoal CKRecord writes, deletes, Discover search (goals + people), the fetch-for-user lookup PublicProfileView needs, the atomic participant count increment, the backfill walk for existing local public goals, and the syncOwnedPublicGoals refresh after check-in.

Purpose: Plans 03 (ViewModels) and 05 (UI) both call into these services. Wave 2 isolates the network/CK boundary so Wave 3 plans can run in parallel against a stable service API.

Output: Two service files compile and pass Phase22PublicGoalServiceTests + Phase22FollowServiceTests + the relevant Phase22PublicProfileViewModelTests fetch-path stubs (unblocking the XCTSkip wrappers from Plan 01).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/22-public-profile-follow-discover/22-CONTEXT.md
@.planning/phases/22-public-profile-follow-discover/22-RESEARCH.md
@.planning/phases/22-public-profile-follow-discover/22-PATTERNS.md
@.planning/phases/22-public-profile-follow-discover/22-VALIDATION.md
@.planning/phases/22-public-profile-follow-discover/22-01-PLAN.md

<interfaces>
<!-- Extracted from codebase. -->

From VitaminG/VitaminG/VitaminG/Services/ProfileSharingService.swift (current public surface to extend, do not break):
- private static let containerID = "iCloud.com.kyleharrington.VitaminG"
- static func publishProfile(displayName: String?, avatarColorHex: String?, username: String? = nil, existingRecordID: String?) async throws -> String
- static func fetchProfile(recordID: String) async throws -> (displayName: String?, avatarColorHex: String?)
- static func unpublishProfile(recordID: String) async throws  (lines ~71–81 — delete pattern to mirror)

From VitaminG/VitaminG/VitaminG/Services/CommunityService.swift (patterns to copy verbatim):
- CKContainer write: `let db = CKContainer(identifier: containerID).publicCloudDatabase; _ = try await db.save(record)`
- toggleReaction fetch+mutate+save+retry (lines ~73–96) — copy structure for incrementParticipantCount
- fetchPosts NSPredicate + records(matching:) (lines ~30–38) — copy structure for searchGoals/searchPeople
- writeApplause sanitize+CKRecordValue assignment pattern (lines ~326–340)

From VitaminG/VitaminG/VitaminG/Services/UsernameLookupService.swift (writeUsername fetch-or-create with unknownItem catch, lines ~53–70) — exact pattern for writeFollow.

From VitaminG/VitaminG/VitaminG/Utils/InputSanitizer.swift:
- static func sanitizeForPublic(_ raw: String) -> String  (must wrap every String CKRecordValue write)

From VitaminG/VitaminG/VitaminG/Models/CommunityHubModels.swift (new in 22-01):
- struct PublicProfileData (Equatable, Sendable) — fields listed in 22-01 plan
- struct PublicGoalItem (Identifiable, Sendable)
- struct DiscoverGoalResult (Identifiable, Sendable)
- struct DiscoverPersonResult (Identifiable, Sendable)

From VitaminG/VitaminG/VitaminG/Models/SchemaV9.swift (new in 22-01):
- Goal.cloudKitPublicGoalRecordID: String? — set after successful writePublicGoal so backfill is one-shot per goal
- UserProfile.motto: String? — read by publishProfile callers

CloudKit record types (deployed in 22-01 Task 4):
- "PublicGoal" fields: title String, category String, creatorUsername String, participantCount Int64, progressPercent Int64, durationDays Int64, creationEpoch Int64
- "Follow" fields: followerUsername String, followeeUsername String, createdAt Int64
- "PublicProfile" added fields: streakLength Int64, goalCount Int64, motto String (queryable index on username already present from Phase 17)

Phase22PublicGoalServiceTests symbols this plan must satisfy (from 22-01 Task 3 stubs to unblock):
- PublicGoalService.writePublicGoal(goal:)
- PublicGoalService.deletePublicGoal(recordName:)
- PublicGoalService.backfillPublicGoals(goals: [Goal], context: ModelContext)
- PublicGoalService.searchGoals(keyword:) -> [DiscoverGoalResult]
- PublicGoalService.searchPeople(usernamePrefix:) -> [DiscoverPersonResult]
- PublicGoalService.incrementParticipantCount(recordName:) async  (non-throwing, fire-and-forget)
- PublicGoalService.fetchGoalsForUser(creatorUsername:) async throws -> [PublicGoalItem]
- PublicGoalService.syncOwnedPublicGoals(goals: [Goal]) async (refreshes progressPercent on owned public goals)

Phase22FollowServiceTests symbols this plan must satisfy:
- ProfileSharingService.fetchFollowState(followerUsername:followeeUsername:) async throws -> Bool
- ProfileSharingService.writeFollow(followerUsername:followeeUsername:) async throws
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expand ProfileSharingService with new fields, struct return type, and Follow methods</name>
  <files>VitaminG/VitaminG/VitaminG/Services/ProfileSharingService.swift, VitaminG/VitaminG/VitaminGTests/Phase22PublicProfileViewModelTests.swift, VitaminG/VitaminG/VitaminGTests/Phase22FollowServiceTests.swift</files>
  <read_first>
    - VitaminG/VitaminG/VitaminG/Services/ProfileSharingService.swift (entire file — must preserve existing publish/unpublish call sites)
    - VitaminG/VitaminG/VitaminG/Services/UsernameLookupService.swift (lines 1–92 — writeUsername fetch-or-create with unknownItem catch is the writeFollow blueprint)
    - VitaminG/VitaminG/VitaminG/Utils/InputSanitizer.swift (sanitizeForPublic signature)
    - VitaminG/VitaminG/VitaminG/Models/CommunityHubModels.swift (PublicProfileData struct shape from 22-01)
    - .planning/phases/22-public-profile-follow-discover/22-PATTERNS.md (ProfileSharingService section — exact publishProfile signature with additive parameters, writeFollow + fetchFollowState implementations)
    - .planning/phases/22-public-profile-follow-discover/22-RESEARCH.md (Pitfall 3 — only write new fields when caller explicitly provided them; Pitfall 6 — fetchOverride return type change must update tests in same commit)
    - VitaminG/VitaminG/VitaminGTests/Phase22PublicProfileViewModelTests.swift (Plan 01 RED stubs — uncomment #if false blocks that reference fetchFollowState / PublicProfileData fetchOverride)
    - VitaminG/VitaminG/VitaminGTests/Phase22FollowServiceTests.swift (Plan 01 RED stubs to enable)
  </read_first>
  <behavior>
    - publishProfile(displayName:avatarColorHex:username:existingRecordID:streakLength:goalCount:motto:) writes Int64(streakLength), Int64(goalCount), and (when motto != nil) the sanitized motto string. When streakLength == 0 and goalCount == 0 and motto == nil (defaults), existing CKRecord values are NOT clobbered — only write fields the caller explicitly opted into via non-default arguments. Test: stub a record with motto="old", call publishProfile with motto: nil, fetch back → motto still "old".
    - fetchProfile(recordID:) returns PublicProfileData with displayName, avatarColorHex, username, motto, streakLength, goalCount mapped from CKRecord fields. cheersGivenCount is set to 0 here (CommunityHubViewModel/PublicProfileViewModel adds it separately via CommunityService.fetchApplauseGivenCount).
    - fetchFollowState(followerUsername:followeeUsername:) returns true when CK record with recordName == "follower_followee" exists; returns false when CKError.unknownItem; rethrows other errors.
    - writeFollow(followerUsername:followeeUsername:) is idempotent — if record exists, returns without raising. If unknownItem, creates a CKRecord(recordType:"Follow") with sanitized usernames and createdAt epoch, then saves. Test: call writeFollow twice with same args → only one save attempted on second call (use a count override).
  </behavior>
  <action>
Modify the existing `publishProfile` declaration to add three new parameters at the end of the signature: `streakLength: Int = 0, goalCount: Int = 0, motto: String? = nil`. Keep all existing parameters and default values intact so every existing call site at Plan 17/19 code paths continues to compile. Inside the function, after the existing field writes, add `record["streakLength"] = Int64(streakLength) as CKRecordValue` and `record["goalCount"] = Int64(goalCount) as CKRecordValue` ONLY when the caller passed non-zero values (avoid clobbering — per Pitfall 3 in 22-RESEARCH.md, check `if streakLength > 0` and `if goalCount > 0` before writing; alternatively gate behind a separate signature variant if defaulting collides with existing callers). For motto, write `record["motto"] = InputSanitizer.sanitizeForPublic(motto) as CKRecordValue` ONLY when motto is non-nil.

Change the return type of `fetchProfile(recordID:)` from `(displayName: String?, avatarColorHex: String?)` to `PublicProfileData` (defined in CommunityHubModels.swift from 22-01). Map the seven PublicProfileData fields from the fetched CKRecord, using `(record["streakLength"] as? Int64) ?? 0` then Int conversion for the count fields, and `0` for cheersGivenCount (Plans 03/05 inject the real count via CommunityService.fetchApplauseGivenCount).

Add two new static methods to the enum body, copying the UsernameLookupService.writeUsername pattern verbatim. First, `static func fetchFollowState(followerUsername: String, followeeUsername: String) async throws -> Bool` — fetch by deterministic recordName "follower_followee", return true on hit, return false on CKError.unknownItem, rethrow other errors. Second, `static func writeFollow(followerUsername: String, followeeUsername: String) async throws` — fetch by same recordName; if hit return immediately (idempotent); if unknownItem, create CKRecord(recordType: "Follow", recordID:), set followerUsername and followeeUsername with InputSanitizer.sanitizeForPublic, set createdAt = Int64(Date().timeIntervalSince1970), then save. Both methods use the existing `containerID` and CKContainer(identifier:).publicCloudDatabase pattern.

In Phase22PublicProfileViewModelTests.swift and Phase22FollowServiceTests.swift, enable the #if false / XCTSkip-guarded test bodies from Plan 01 that referenced these symbols. The bodies become live tests against the new methods.
  </action>
  <verify>
    <automated>xcodebuild test -scheme VitaminG -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:VitaminGTests/Phase22FollowServiceTests -only-testing:VitaminGTests/Phase22PublicProfileViewModelTests 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - ProfileSharingService.swift contains a publishProfile signature with `streakLength: Int = 0` and `motto: String? = nil` parameters (grep: `grep -E "streakLength:\s*Int" VitaminG/VitaminG/VitaminG/Services/ProfileSharingService.swift` returns at least 1 hit)
    - ProfileSharingService.fetchProfile return type is `PublicProfileData` (grep: `grep -E "fetchProfile.*->.*PublicProfileData" VitaminG/VitaminG/VitaminG/Services/ProfileSharingService.swift` returns at least 1 hit)
    - File contains `static func fetchFollowState(followerUsername:` and `static func writeFollow(followerUsername:` declarations
    - File contains the deterministic record name string interpolation `"\(followerUsername)_\(followeeUsername)"` (grep finds at least 2 hits — one in each new method)
    - Every CloudKit String write goes through InputSanitizer.sanitizeForPublic (grep: `grep -c "InputSanitizer.sanitizeForPublic" VitaminG/VitaminG/VitaminG/Services/ProfileSharingService.swift` >= 3 — existing displayName/username sanitizer hits plus new motto/follow ones)
    - All existing publishProfile call sites in the project still compile (build verification — no caller is forced to supply the new args because they have defaults)
    - `xcodebuild test -only-testing:VitaminGTests/Phase22FollowServiceTests` passes with at least 5 active tests (test_followRecordName_isDeterministic, test_followRecordName_isOrderSensitive, test_writeFollow_skipsIfRecordExists, test_fetchFollowState_returnsTrueForKnownRecord, test_fetchFollowState_returnsFalseForUnknownItem) — no remaining XCTSkip in this file
    - `xcodebuild test -only-testing:VitaminGTests/Phase22PublicProfileViewModelTests` passes the fetch-path tests (test_fetchProfile_success_transitionsToLoaded_withPublicProfileData and test_fetchProfile_failure_transitionsToError become live; follow/cheer tests may still XCTSkip until Plan 03)
  </acceptance_criteria>
  <done>ProfileSharingService publishes the three new fields without clobbering, returns PublicProfileData, and adds Follow read+write. Phase22FollowServiceTests is fully green; Phase22PublicProfileViewModelTests fetch-path is green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create PublicGoalService with CRUD, search, increment, backfill, and sync</name>
  <files>VitaminG/VitaminG/VitaminG/Services/PublicGoalService.swift, VitaminG/VitaminG/VitaminGTests/Phase22PublicGoalServiceTests.swift</files>
  <read_first>
    - VitaminG/VitaminG/VitaminG/Services/CommunityService.swift (entire file — same structure to mirror: enum, containerID, static async methods, InputSanitizer everywhere, fetchPosts NSPredicate pattern, toggleReaction fetch+mutate+save+retry, writeApplause record build)
    - VitaminG/VitaminG/VitaminG/Services/ProfileSharingService.swift (unpublishProfile delete pattern with .unknownItem catch — same for deletePublicGoal)
    - VitaminG/VitaminG/VitaminG/Utils/InputSanitizer.swift (sanitizeForPublic on every String CKRecordValue write)
    - VitaminG/VitaminG/VitaminG/Models/CommunityHubModels.swift (DiscoverGoalResult, DiscoverPersonResult, PublicGoalItem struct shapes)
    - VitaminG/VitaminG/VitaminG/Models/SchemaV9.swift (Goal.isPublic, Goal.cloudKitPublicGoalRecordID, Goal.title, Goal.category, Goal.durationDays — fields used by backfill/sync)
    - .planning/phases/22-public-profile-follow-discover/22-PATTERNS.md (PublicGoalService section — full structure of every method including atomic increment retry)
    - .planning/phases/22-public-profile-follow-discover/22-RESEARCH.md (Pattern 4 atomic increment, Pattern 1 NSPredicate substring/prefix, Pitfall 5 join dedup context — set tracking is in ViewModel not service)
    - VitaminG/VitaminG/VitaminGTests/Phase22PublicGoalServiceTests.swift (Plan 01 RED stubs to enable)
  </read_first>
  <behavior>
    - writePublicGoal(goal: Goal) async throws: builds CKRecord(recordType:"PublicGoal", recordID: CKRecord.ID(recordName: goal.id?.uuidString ?? "")) (skip if id nil), sanitizes title/category/creatorUsername (creator derived from goal.userProfile.username or caller-provided argument — confirm by reading SchemaV9.Goal), writes participantCount as Int64(1) on creation (the creator counts as one participant), progressPercent as Int64(0) on creation (or current calculated value if Goal model has it), durationDays from Goal field, creationEpoch from Goal.creationDate. Saves with .serverRecordChanged one-retry. After successful save, returns the recordName so the caller can stamp Goal.cloudKitPublicGoalRecordID.
    - deletePublicGoal(recordName:) async throws: db.deleteRecord(withID:); swallows CKError.unknownItem (already deleted is not an error per D-10).
    - searchGoals(keyword:) async throws -> [DiscoverGoalResult]: NSPredicate(format: "title CONTAINS[cd] %@", keyword); CKQuery; records(matching: query, resultsLimit: 25); map records to DiscoverGoalResult via private mapper that reads each CKRecord field with safe casts.
    - searchPeople(usernamePrefix:) async throws -> [DiscoverPersonResult]: NSPredicate(format: "username BEGINSWITH[cd] %@", prefix); CKQuery on "PublicProfile" record type; map to DiscoverPersonResult; reads username, avatarColorHex, goalCount Int64, cheersGivenCount Int64 (cheersGivenCount may not yet be populated server-side — fall back to 0).
    - fetchGoalsForUser(creatorUsername:) async throws -> [PublicGoalItem]: NSPredicate(format: "creatorUsername == %@", creatorUsername); CKQuery on PublicGoal; resultsLimit 50; map to PublicGoalItem.
    - incrementParticipantCount(recordName:) async (NON-THROWING — fire-and-forget): fetch record; current = (record["participantCount"] as? Int64) ?? 0; record["participantCount"] = current + 1 as CKRecordValue; save. On .serverRecordChanged: one retry with same logic. On any other error: silently swallow (D-16 — count failure must not affect caller).
    - backfillPublicGoals(goals: [Goal], context: ModelContext) async: iterate goals; skip those where isPublic != true OR cloudKitPublicGoalRecordID != nil; for each candidate, call writePublicGoal and on success set goal.cloudKitPublicGoalRecordID = returned recordName and try? context.save(). Per-goal failures are silently logged; loop continues (D-11 — best-effort backfill).
    - syncOwnedPublicGoals(goals: [Goal]) async: iterate goals; for each goal where isPublic == true AND cloudKitPublicGoalRecordID != nil, fetch the CKRecord, update progressPercent (recomputed from goal completion state — accept as Int parameter from caller in MVP, or pass through GoalViewModel to compute) and participantCount if needed (keep current value, don't overwrite). On any per-goal error: silently log and continue (D-12 — fire-and-forget).
  </behavior>
  <action>
Create the new file at the declared path with `enum PublicGoalService` (no instances, static methods only — matches CommunityService and ProfileSharingService idiom). Add `private static let containerID = "iCloud.com.kyleharrington.VitaminG"`. Every database access uses `CKContainer(identifier: containerID).publicCloudDatabase` per the established pattern (NEVER CKContainer.default() — confirmed in 22-RESEARCH.md State of the Art).

Implement the eight public static methods listed in <behavior>, copying the exact analog patterns from CommunityService and ProfileSharingService listed in 22-PATTERNS.md (writePublicGoal mirrors writeApplause structure; deletePublicGoal mirrors unpublishProfile; searchGoals/searchPeople mirror fetchPosts NSPredicate+records(matching:) call; incrementParticipantCount mirrors toggleReaction fetch+mutate+save+one-retry; backfillPublicGoals/syncOwnedPublicGoals mirror writeGlimpse upsert loop).

Two private helpers: `private static func mapRecordToDiscoverGoalResult(_ record: CKRecord) -> DiscoverGoalResult` and `private static func mapRecordToDiscoverPersonResult(_ record: CKRecord) -> DiscoverPersonResult` extract fields with `as? String ?? ""` and `(... as? Int64) ?? 0` then Int conversion. Same idea for fetchGoalsForUser's mapper to PublicGoalItem.

Every String field written to CloudKit must pass through InputSanitizer.sanitizeForPublic (search predicate inputs do NOT — they are filters not stored data, but sanitize keyword/prefix at the call sites in ViewModels if needed; CKQuery itself does not need sanitized inputs).

Enable the previously-skipped tests in Phase22PublicGoalServiceTests.swift so test_writePublicGoal_sanitizesTitle, test_deletePublicGoal_swallowsUnknownItem, test_backfillPublicGoals_skipsGoalsWithExistingRecordID, test_searchGoals_buildsContainsCdPredicate, test_searchPeople_buildsBeginsWithCdPredicate, and test_fetchGoalsForUser_mapsRecordToPublicGoalItem all run live. Predicate inspection tests work without CloudKit — assert on `NSPredicate(format:).predicateFormat` equality. CKRecord-dependent tests use mock seams: introduce a `static var dbOverride: CKDatabase?` test seam if needed, or pass mock-friendly closures into the methods. Prefer the dbOverride approach to keep production signatures clean.
  </action>
  <verify>
    <automated>xcodebuild test -scheme VitaminG -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:VitaminGTests/Phase22PublicGoalServiceTests 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - File VitaminG/VitaminG/VitaminG/Services/PublicGoalService.swift exists
    - File contains `enum PublicGoalService` (single declaration)
    - File contains exactly 8 public/internal static methods: writePublicGoal, deletePublicGoal, searchGoals, searchPeople, fetchGoalsForUser, incrementParticipantCount, backfillPublicGoals, syncOwnedPublicGoals (`grep -E "static func (writePublicGoal|deletePublicGoal|searchGoals|searchPeople|fetchGoalsForUser|incrementParticipantCount|backfillPublicGoals|syncOwnedPublicGoals)" VitaminG/VitaminG/VitaminG/Services/PublicGoalService.swift | wc -l` returns 8)
    - File uses `CKContainer(identifier: containerID)` and does NOT use `CKContainer.default()` (`grep -c "CKContainer.default()" PublicGoalService.swift` returns 0)
    - File contains the title CONTAINS[cd] predicate (`grep -E "CONTAINS\\[cd\\]" PublicGoalService.swift` >= 1) and the username BEGINSWITH[cd] predicate (`grep -E "BEGINSWITH\\[cd\\]" PublicGoalService.swift` >= 1)
    - File contains the .serverRecordChanged catch in incrementParticipantCount (`grep "serverRecordChanged" PublicGoalService.swift` >= 1)
    - File contains the .unknownItem swallow in deletePublicGoal (`grep "unknownItem" PublicGoalService.swift` >= 1)
    - Every String write to a CKRecord uses InputSanitizer.sanitizeForPublic (`grep -c "sanitizeForPublic" PublicGoalService.swift` >= 3)
    - incrementParticipantCount signature is non-throwing: `static func incrementParticipantCount(recordName: String) async` (no `throws`)
    - `xcodebuild test -only-testing:VitaminGTests/Phase22PublicGoalServiceTests` exits 0 with at least 6 active live tests (no XCTSkip remaining in the file)
  </acceptance_criteria>
  <done>PublicGoalService is the single owner of PublicGoal record interactions and Discover search queries. All Phase22PublicGoalServiceTests tests are live and green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ViewModel → ProfileSharingService | Untrusted keyword/prefix/motto strings cross into CloudKit write paths |
| ViewModel → PublicGoalService | Goal title, category, creatorUsername, search keywords, prefix strings cross into CloudKit write/query |
| CloudKit public DB → ViewModel | CKRecord field values are untrusted on read — must safe-cast with defaults |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-02-01 | Tampering | ProfileSharingService.publishProfile motto field | mitigate | `InputSanitizer.sanitizeForPublic(motto)` strips <>"' and control chars; enforced by acceptance_criteria grep count >= 3 |
| T-22-02-02 | Tampering | ProfileSharingService.publishProfile overwrite hazard | mitigate | New fields are only written when caller passes non-default values (`if streakLength > 0`, `if motto != nil`), preventing the Pitfall 3 silent overwrite scenario |
| T-22-02-03 | Spoofing | ProfileSharingService.writeFollow follower identity | accept-with-control | followerUsername is supplied by caller (PublicProfileViewModel reads it from @AppStorage("vg_username")); CloudKit identity isolation prevents writing on another user's behalf at the CK auth layer; v2.0 social trust model |
| T-22-02-04 | Information Disclosure | PublicGoalService.searchGoals exposes private goal titles | accept | Only goals with isPublic == true ever reach the CK PublicGoal record type (D-10 — written only when goal.isPublic, deleted when toggled private); private goals never enter the search index |
| T-22-02-05 | Tampering | PublicGoalService.incrementParticipantCount drift | accept | Per 22-RESEARCH.md Security Domain — accepted risk; no server-side rate limit available; cosmetic-only impact; participants must actually create a local Goal first to trigger the increment via DISC-04 flow |
| T-22-02-06 | Denial of Service | PublicGoalService.searchGoals resultsLimit overrun | mitigate | resultsLimit: 25 hard cap on every search query; matches CommunityService.fetchPosts cap |
| T-22-02-07 | Tampering | InputSanitizer skipped on a CK String write | mitigate | Acceptance criteria require `grep -c "sanitizeForPublic" PublicGoalService.swift >= 3`; reviewer should also confirm `grep -E "record\\[.*\\]\\s*=\\s*[a-z]" PublicGoalService.swift` shows no raw String assigned to a String field without sanitizing |
| T-22-02-SC | Tampering | npm/pip/cargo installs | n/a | No new package installs (Apple frameworks only) |
</threat_model>

<verification>
- `xcodebuild build -scheme VitaminG -destination 'platform=iOS Simulator,name=iPhone 16'` exits 0.
- `xcodebuild test -scheme VitaminG -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:VitaminGTests/Phase22FollowServiceTests` exits 0, all tests live (no XCTSkip).
- `xcodebuild test -scheme VitaminG -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:VitaminGTests/Phase22PublicGoalServiceTests` exits 0, all 6+ tests live.
- `xcodebuild test -scheme VitaminG -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:VitaminGTests/Phase22PublicProfileViewModelTests` exits 0 — fetch-path tests are live; follow/cheer tests may still skip pending Plan 03.
- Existing Phase 17/19 publishProfile call sites continue to compile (no breakage from additive args).
</verification>

<success_criteria>
- ProfileSharingService.publishProfile signature additively accepts streakLength, goalCount, motto with safe defaults.
- ProfileSharingService.fetchProfile returns PublicProfileData struct.
- ProfileSharingService exposes fetchFollowState and writeFollow with deterministic record naming.
- PublicGoalService exists with 8 static methods covering write/delete/search/fetch/increment/backfill/sync.
- Every CK String write passes through InputSanitizer.sanitizeForPublic.
- Phase22FollowServiceTests and Phase22PublicGoalServiceTests are fully green with no remaining XCTSkip.
- Phase22PublicProfileViewModelTests fetch-path tests are green.
</success_criteria>

<output>
Create `.planning/phases/22-public-profile-follow-discover/22-02-SUMMARY.md` when done.
</output>
