import XCTest
@testable import VitaminG

// MARK: - NudgeTimePickerTests
// Wave 0 stub for Plan 06. Will verify:
//   - NotificationPreferences.save(hour:minute:) round-trips a selected hour
//   - (i.e., persisted hour is read back from UserDefaults correctly)

final class NudgeTimePickerTests: XCTestCase {

    func test_stub_implementedInPlan06() throws {
        throw XCTSkip("Wave 0 stub — implemented in Plan 06")
    }
}
