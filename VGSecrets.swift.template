// VGSecrets.swift.template — committed placeholder.
// Copy to VitaminG/VitaminG/VitaminG/Services/VGSecrets.swift (gitignored)
// and fill in the real token. The Services folder is an Xcode synchronized
// group, so the file joins the app target automatically.

enum VGSecrets {
    /// Shared token for the vg-ai-proxy Cloudflare Worker.
    /// Must match the SHARED_TOKEN secret: npx wrangler secret put SHARED_TOKEN
    static let workerToken = "REPLACE_WITH_UUID"
}
